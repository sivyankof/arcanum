# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# Arcanum — приложение для изучения таро (Expo SDK 54)

Офлайн-приложение: карта дня, каталог 78 карт, курс, расклады, флеш-карты (SRS). Весь контент в бандле, сервера нет.

## Команды

- `npm start` — dev-сервер Expo (`npm run android` / `ios` / `web` — сразу на платформу).
- `npx tsc --noEmit` — проверка типов (strict). Линтера нет.
- `npm run kill:dev` — останавливает node-процессы ЭТОГО проекта: dev-сервер Expo и осиротевшие
  воркеры jest (`scripts/kill-dev.ps1`, Windows). Фильтрует по пути репозитория, поэтому дев-серверы
  других проектов на машине не трогает. ⚠️ Зачем нужен: у машины много ядер, jest поднимает воркер
  на ядро, и при нормальном завершении родитель гасит их сам — но **в Windows нет групп процессов**,
  и если родителя снять принудительно (таймаут, закрытая сессия, `Stop-Process` без `-T`), дети
  остаются жить вечно. 21.08 так накопилось 14 воркеров больше чем на гигабайт памяти.
  ⚠️ Ограничивать воркеры ради этого НЕ надо — замерено 21.08: 2.97 с по умолчанию против 3.53 с
  при `--maxWorkers=4`, то есть ~20% платы на каждом прогоне за проблему, возникающую изредка.
  ⚠️ Ловушка при правке этого скрипта: **`.ps1` с кириллицей обязан лежать с UTF-8 BOM.** Windows
  PowerShell 5.1 читает файл без BOM как ANSI, русские комментарии превращаются в мусор и ломают
  разбор строк — скрипт падает с `TerminatorExpectedAtEndOfString`. Родня правила про
  `Get-Content`/`Set-Content` ниже: оболочка портит UTF-8 в обе стороны, и при чтении тоже.
- `npm test` — юнит-тесты (jest-expo). На 23.08 (задача 57): 1683 теста в 46 сьютах, все зелёные. Правило: новая формула или алгоритм — тест в том же коммите (`docs/testing-strategy.md`). ⚠️ Node несёт полный ICU, а Hermes на телефоне — урезанный, поэтому всё, что считает сам движок (формы числительных, даты, форматы чисел), обычным тестом не проверяется: механизм сносится руками до загрузки модуля — образец `src/lib/__tests__/i18nPlurals.test.ts` (hf-02).
- Веб-проверка (пункт 6а/6б процесса): `npx expo start --web` поднимает приложение на
  http://localhost:8081. Playwright в проекте НЕ установлен и ставить его не нужно — он есть
  в кэше npx, скрипт запускается так:
  `NODE_PATH="C:/Users/Artem/AppData/Local/npm-cache/_npx/705bc6b22212b352/node_modules" node <скрипт>`
  (внутри `require('playwright')`, вьюпорт 390×844, headless). Состояние приложения на вебе —
  `localStorage` под ключом `arcanum-app` в формате zustand/persist (`{state, version}`):
  AsyncStorage на вебе это localStorage напрямую, поэтому сид ставится одним `evaluate`.
  ⚠️ Сид ставить НЕ через `addInitScript` (он срабатывает на каждой навигации и затирает
  состояние после следующего `goto`): `goto` → `evaluate` → `reload`, дальше ходить кликами.
  ⚠️ Карта дня качается ±6 px бесконечно, поэтому обычный `.click()` ждёт неподвижности
  и падает по таймауту 30 с — нужен `click({ force: true })`.
  ⚠️ Правило проекта: сценарий проверки сначала прогоняется на СЛОМАННОМ коде и обязан
  покраснеть; зелёный с первого раза — повод искать ошибку в самой проверке.
- Контент-конвейер (Python, запускать из корня):
  - `python scripts/build_cards.py` — пересобирает `content/cards.json` из исходника tarot-api (ожидает `/tmp/tarot-api/static/card_data.json`). Сливает, а не затирает: `keywords`, `search` и тексты блоков переносятся из существующего файла, из источника обновляются только `name`/`image`/`source`. (До 11.08 скрипт стирал весь написанный контент.)
  - `python scripts/merge_quiz.py` — сливает ВСЕ черновики викторин `content/quiz-*.json` (по файлу на модуль: `quiz-m1-m2`, `quiz-m3`, `quiz-m4`, `quiz-m5`, `quiz-m6` — 160 вопросов на 15.08) в `content/course.json` (поля `quiz`/`quizStatus`, остальное не трогает). Файл на модуль потому, что статус у файла ОДИН на все его уроки: живи М1–М2 и М3 вместе, прогон откатывал бы reviewed в draft. Идемпотентен, валидирует схему до записи: ровно 5 вопросов и 3 варианта, `correct` в 0..2, двуязычность, `cardId` существует в колоде.
  - `python scripts/gen_card_images.py` — регенерирует `src/lib/cardImages.ts` после изменения списка карт.
  - `python scripts/gen_notification_icon.py` — рисует `assets/images/notification-icon.png` (иконка пуша Android, спека 55): белый силуэт эмблемы 96×96 на прозрачном, геометрия из `src/components/Emblem.tsx`, рендер в 4× и LANCZOS. Идемпотентен, запуск из корня без аргументов. ⚠️ Файл руками не править: «all-white on transparent» (требование Google) проверяет САМ скрипт после записи — перечитывает PNG и падает с кодом 1, если непрозрачный пиксель не белый или доля непрозрачных вне 8–40 % (факт 25.7 %). Контракт-тест `notificationIcon.test.ts` держит только формат (IHDR: 96×96, depth 8, colorType 6) и связку «app.json → файл → канал `daily`»: декодировать PNG в jest без зависимостей не окупается. ⚠️ Увидеть иконку можно ТОЛЬКО на сборке — в Expo Go уведомления показывает сам Expo Go со своей иконкой.
  - `python scripts/content_stats.py` — отчёт о готовности контента ПО ЯЗЫКАМ (спека 28а): значения карт, слова карт, теория, викторины + статусы staging-файлов + карты с недописанным каноном. До 19.08 считал только `cards.json` и не различал языки. ⚠️ В доках раньше значился как `npm run content:stats` — такого скрипта в `package.json` нет и не было.
  - `python scripts/set_status.py <статус>` — двигает статусы по workflow `todo → draft → reviewed → final` сразу в трёх файлах (значения карт, слова карт, теория, викторины). Есть `--only` (`cards|words|theory|quiz`), `--lang` и `--dry-run`. ⚠️ Статус живёт ПО ЯЗЫКАМ (спека 28а): без `--lang` двигается канон `ru,en`, вычитанный испанский — `--lang es`. Двигаются только те языки, чей статус совпал с исходным, поэтому «ru reviewed, es draft» не схлопывается. ⚠️ Статус НЕ выдаётся языку, у которого нет ни знака текста, — иначе `--lang es` на непереведённом корпусе проставил бы испанский статус всем 1100 единицам, а отчёт готовности отрапортовал бы «готово 100%» при нуле переводов (находка финального ревью 19.08); пропущенные единицы скрипт называет числом. Движение НАЗАД по workflow требует явного `--allow-back` (редактор отозвал вычитку). ⚠️ Викторинам статус правится в ДВУХ местах (черновик редактора и собранный course.json), иначе следующий прогон `merge_quiz.py` откатит его назад.
  - `python scripts/migrate_status_lang.py [--dry-run]` — ОДНОРАЗОВАЯ идемпотентная миграция статусов на пер-языковую схему (спека 28а, отработана 19.08). Оставлена в репо как исполняемая документация формы: пробегает все четыре хранилища статуса разом. ⚠️ Ключ `wordsStatus` кладётся ровно туда, куда его ставит `build_cards.py` (сразу после `search`) — иначе следующая пересборка колоды даёт дифф на 312 строк чистой перестановки.
  - `python scripts/check_canon.py [--scope cards|course] [--only N]` — корпусная проверка контента (11 проверок: длины, рамка предсказания, жаргон, медицина в health, род читателя, глоссарий, однообразие зачинов по каждому языку, обороты-тики, рассинхрон ru↔en, наборы слов, длины вариантов викторины). Ничего не правит, печатает отчёт. Ловит то, что не видит вычитка отдельного текста: дефект живёт в статистике корпуса. **С 21.08 (сессия L-5) проверяет ВСЕ ЧЕТЫРЕ языка**: словари испанского и португальского вынесены в `scripts/dict_es.py` и `scripts/dict_pt.py` — отдельные модули, потому что калибруются на живом корпусе и в них записана история калибровки (сколько ложных срабатываний дала каждая отброшенная регулярка: наивное `sol[oa]` — 134 при 2 настоящих, `\w+r[áa]s` — 265 почти сплошь «palabras»/«detrás»). ⚠️ Романские языки ломаются НЕ там, где русский: род читателя вылезает не в глаголе прошедшего времени, а в согласовании прилагательного со связкой, поэтому якорь ставится на подлежащее. Перенос проверен приёмкой мутациями — подсаженные в es/pt обещание, медицинская лексика и родовые формы ловятся поимённо. ⚠️ Прежде чем считать находкой расхождение перевода с правилом, проверь, соблюдает ли это правило сам оригинал: первая мутация приёмки («recuperación llegará») не поймалась, но этих слов нет и в русском словаре — он про диагнозы и врачей, а не про «восстановление».
  - `scripts/dict_es.py` / `scripts/dict_pt.py` — словари корпусных проверок для испанского и португальского (заведены L-5, 21.08): PREDICTION, JARGON, MEDICINE, GENDER, GLOSSARY. Не запускаются сами — их импортирует `check_canon.py`. Правя их, держи формат: рядом с каждой отброшенной попыткой записано число ложных срабатываний на корпусе, чтобы следующий не проверял заново уже проверенные ловушки.
  - `python scripts/check_edits.py <файл-списка-правок>` — проверка САМИХ правок «было → стало» (спека 35), дополняет предыдущий: тот смотрит на корпус сейчас, этот — не сломала ли правка то, что было целым (тавтология, повтор с соседним блоком, сменившаяся мысль, длина, одноязычная правка без парной). Формат входа — `docs/specs/31-changed-addresses.md`. **С 21.08 работает на четырёх языках**: до этого падал `KeyError` на испанских стоп-словах и дважды `ValueError` там, где стояла распаковка ровно одного языка — проверки писались, когда «не все языки» означало «ровно один», и волна, тронувшая es и pt, их роняла. ⚠️ Пара считается внутри СЕМЬИ (канон `ru`/`en` против переводов `es`/`pt`): правка обоих переводов при нетронутом каноне — норма волны локализации, а не находка, иначе проверка 8 выдаёт по строке на каждую правку (216 «находок» на волне без дефектов). **С 23.08 читает и адреса ВИКТОРИНЫ** (`m1l1-q2[1].es` — урок, вопрос с единицы, вариант с нуля, язык): до этого парсер требовал ровно три части через точку и двухчастные адреса МОЛЧА пропускал — прогон печатал «Записей 0 / находок 0» и выглядел успехом, из-за чего 121 правка задачи 28е не проверялась вовсе. Теперь непонятый адрес называется числом и текстом; «0 записей» при непустом файле — сама по себе находка. Проверка 1 сверяет корпус с ПОСЛЕДНЕЙ правкой адреса (списки с историей: волна + хвост приёмки), связность истории стережёт проверка 11. Под дистракторы добавлены проверки 9 (добавленная фраза повторяет другой вариант того же вопроса) и 10 (дистрактор сблизился с верным ответом — у вопроса становится два правильных). ⚠️ Калибровка записана числами ложных срабатываний в докстроках (приём `dict_es.py`): без вычитания текста самого вопроса проверка 9 даёт 8 ложных, без снятия служебных слов проверка 10 — 2, без семейной логики проверка 5 — 85. Правя эти проверки, гоняй мутацию НА СЛЕПОТУ: фраза для инъекции обязана отсутствовать в тексте вопроса, иначе калибровка стирает и настоящие находки. ⚠️ Проверка 6 «сменилась мысль» на переводческих волнах неинформативна by design (перевод пишется заново) — это материал для глаз, а не находки.
  - `python scripts/check_quiz_guess.py [--against REV] [--only mN] [--mutate …]` — механический слой замера «вопрос викторины решается БЕЗ знания таро» (задачи 28е/28р). Три функции: (1) **категоричность** — абсолютные слова стоят в дистракторах и отсутствуют в верном, тогда работает эвристика «выбирай осторожный вариант»; (2) **длина** — верный ≥1.3× второго по длине, и «верный единственный самый длинный» больше чем в 2 вопросах урока (`card`-вопросы вне правила длин, кроме М6); (3) **`--against REV` — страж правки**: текст верного варианта, `correct`, вопрос и пояснение обязаны совпадать с ревизией git. Волна правок дистракторов заканчивается этим прогоном — за переставленные варианты при нетронутом `correct` проект платил дважды (29 и 31). ⚠️ Калибровка записана числами в докстроке: квантификаторы («все», «только», «ничего») выведены из находок в [ОТЧЁТ] — 86 строк при выборке 4 ложных из 4, точного правила для них нет. ⚠️ Периметр УЖЕ, чем у `check_canon` проверки 11: «самый длинный по уроку» считается только по ru и без `card`. Прогонять оба. ⚠️ Не-слепота проверяется `--mutate absolute|longest|correct` (порча копии в памяти, файлы не трогаются) — повторять при каждой правке скрипта.
  - `python scripts/apply_quiz_edits.py <файл-списка> [--apply]` — применение правок дистракторов викторины по списку «было → стало» (формат `docs/specs/28r-changed-addresses.md`). Родня `apply_canon_fixes.py`: **вариант ищется по точному тексту «было», а не по индексу**, совпадение обязано быть ровно одно и ровно на месте, указанном адресом. Отказывает и называет причину: правка целит в верный вариант (`correct`), язык es/pt (переводы льёт Cowork), текст не найден или найден в другом варианте, текст уже равен «стало». Непонятый заголовок — ошибка входа с номером строки, а не молчаливый пропуск. По умолчанию сухой прогон; `--apply` пишет файлы (`indent=1`, `ensure_ascii=False` — сверено побайтово с форматом репозитория). После записи ОБЯЗАТЕЛЬНЫ `merge_quiz.py`, `check_quiz_guess.py --against`, `check_edits.py`.
  - `python scripts/apply_canon_fixes.py [--apply] [--out <файл-списка>] [--fixes …] [--cards …]` — применение правок канона, утверждённых редактором в `docs/canon-fixes.md` (задача 28б). Утверждением считается `[x]` ИЛИ вписанный текст под «Свой вариант:» (свой всегда побеждает предложенный). Блок ищется **по точному тексту «было»**, а не по названию карты: совпадение обязано быть ровно одно, иначе правка не применяется — так закрыт класс «правка уехала не в тот блок». Русский и английский правятся синхронно (расхождение ru↔en — отдельный класс дефекта, задача 31); правки бывают законно одноязычными в обе стороны. По умолчанию сухой прогон; `--out` пишет список адресов в формате `check_edits.py`, которым прогон обязан заканчиваться. Отдельно печатает: места, применимые только вручную (правка описана словами — «заменить X на Y», а не готовым текстом), и адреса, где уже есть es/pt — их перевод остался со старой формулировкой и его обязана перезалить следующая L-сессия. ⚠️ Пишет JSON с отступом 1, как он лежит в репозитории.
  - `python scripts/check_translation.py --wave <majors|wands-cups|swords-pents|minors|all> --lang es,pt` — приёмка волны перевода (задача 28, роль CC-L2 плана локализации): третий в семье и отвечает на свой вопрос — «поставка залита правильно и ничего не сломала». Проверяет атомарность единицы перевода (name+keywords+search+wordsStatus+блоки одним куском), полноту блоков волны (у младших 12, у старших ещё `birth_path`), статус нового языка `draft`, пустые строки и кириллицу, порядок языков ru→en→es→pt, «перевод ≠ английский дословно», чужие английские имена карт в переводе, повторы зачинов ПО КАЖДОМУ ЯЗЫКУ отдельно и **неприкосновенность канона ru/en** (сверка с `git HEAD`; `--no-git` отключает, `--file` проверяет черновик до слияния). Отдельно печатает [ОТЧЁТ] — материал для человека, не находки: единообразие зачинов-указателей сферных блоков (решение 20.08; указатель определяется как самая частая формула сферы), длины относительно английского, карты вне волны с правками нового языка (законно только как корректирующий проход). ⚠️ Все 12 классов проверок подтверждены мутациями — при правке скрипта прогон мутаций повторить. **`--scope course|spreads`** принимает перевод курса и раскладов (периметр сессии L-4): полнота заголовков модулей и уроков, теории, вопросов, вариантов и пояснений; статусы `theory.status`/`quizStatus` = draft; пустые строки и кириллица; неприкосновенность канона. ⚠️ Главная проверка курса — не полнота, а **`correct` и порядок вариантов**: переставили варианты, не тронув индекс, — и верным ответом становится дистрактор при валидной схеме и молчащем контракт-тесте (проект платил за это в задачах 29 и 31). Совпадение вариантов ПО СМЫСЛУ механически не проверяется и остаётся человеку. Оба режима подтверждены мутациями (correct, перестановка, потерянный вариант, статус, пустое, кириллица, тронутый канон; для раскладов — пропуск позиции, пустое описание, расхождение числа позиций с числом карт).
  - `python scripts/fetch_card_scans.py [--only id,…] [--dry-run]` — заменяет исходники 78 карт на сканы Wikimedia Commons (спека 42): качает оригиналы в `.cache/rws-commons/` (в .gitignore), находит чёрную рамку рисунка по яркости, кадрирует с полем 1.2 %, уменьшает до 900 px, пишет JPEG q78 в `assets/cards/` на прежние имена (код картинок не меняется); печатает отчёт и контакт-лист `docs/screenshots/42/contact-sheet.jpg`; карта с ненайденной рамкой — FAIL, файл не пишется, код 1. Страж — контракт-тест `cardAssets.test.ts` (ширина ровно 900, пропорция 1.68–1.76).

## Архитектура

**Роутинг** — expo-router v6, file-based: дерево `app/` и есть маршруты.

**Контент** — статические JSON в `content/` (`cards.json`, `spreads.json`, `course.json`), доступ ТОЛЬКО через `src/lib/content.ts` (типизированные экспорты `cards`, `spreads`, `course`, `cardById`, `cardOfDay`). Каждая карта: 12 контент-блоков (general, reversed, love, career, finances, health, day_card, symbolism + четыре `*_reversed` спеки 25; у старших ещё `birth_path`), каждый блок `{ru, en, status}`, где `status` — словарь ПО ЯЗЫКАМ (`{"ru": "reviewed", "en": "draft"}`, спека 28а) со статусным workflow `todo → draft → reviewed → final`; языка нет в словаре = `todo`. ⚠️ Текст берётся `inLang` (фолбэк на английский), а готовность спрашивается у языка, который РЕАЛЬНО показан: `statusIn`/`blockText` из `content.ts` — прямое чтение `block.status` в экранах запрещено, иначе испанец получит «Текст готовится» поверх готового английского. Отсутствие ключа языка и пустая строка значат разное: `"es": ""` — это «испанский есть и он пустой», конвейеру такие заглушки создавать запрещено (контракт-тест `cardsContent.test.ts`). Слов у карты два набора: `keywords` — 4 слова витрины (чипы под названием), `search` — 8–12 скрытых поисковых синонимов, которые нигде не отображаются (правила подбора — `docs/content-guide.md`). Расклады и модули курса имеют флаг `free` (freemium).

**Изображения карт** — Metro требует статических `require`, поэтому `src/lib/cardImages.ts` — автогенерируемая карта id → require. Руками не править, только `python scripts/gen_card_images.py`.

**Состояние** — zustand + persist в AsyncStorage (`src/store/useApp.ts`): тема, язык, streak, история карт дня, повторение флеш-карт (`srs`/`reviewDay`, спека 45). Ключ хранилища `arcanum-app`, схема на persist version 10 — менять осторожно, данные уже персистятся у пользователей.

**Тема** — дизайн-токены в `src/theme/theme.ts` (направление «Небесное золото», dark/light + spacing/radius/fonts) — единственный источник правды по цветам. Экраны берут тему через `useTheme()` (режим читается из zustand-стора). Отдельных Themed-компонентов нет.

**i18n** — react-i18next, ресурсы inline в `src/lib/i18n.ts`. Языков четыре: ru/en/es/pt, тип `Lang` объявлен ровно один раз — в `src/lib/lang.ts` (там же эндонимы, локали, `detectLang`, `Localized<T>` и доступ к многоязычному тексту `inLang(запись, язык)` с фолбэком на английский; контентные блоки читаются ТОЛЬКО через `inLang`, прямая индексация по языку из кода запрещена). Единственный способ узнать текущий язык в компоненте — хук `useLang()`; самодельный каст языка контракт-тест `src/lib/__tests__/langSources.test.ts` не пропускает. `AVAILABLE_LANGS` — языки, у которых есть UI-строки (в dev-сборке — все четыре); язык, которого там нет, не появляется ни в пикере, ни в определении языка устройства. Язык лежит в zustand; корневой layout синхронизирует i18n на layout-эффекте, чтобы первый кадр не мигал прежним языком. `src/lib/deviceLang.ts` — единственный файл, знающий про `expo-localization` (язык устройства читается один раз, при первой установке). Новую UI-строку добавлять сразу в оба языка `i18n.ts`.

**Луна** — `src/lib/moon.ts`: точные моменты новолуния и полнолуния по Meeus (гл. 49), без зависимостей, ~1–2 мин точности (`phaseInstant`/`moonEvents`/`lunationAround`), фаза и лунный день считаются от них (окно ±0.92 сут вокруг ТОЧНОГО момента, hf-01). Сетка месяца и события по ЛОКАЛЬНОМУ календарю — `src/lib/moonCalendar.ts`; экран — `app/moon.tsx` (под гардом онбординга, вход — нажимаемая строка луны `MoonRow` на «Сегодня»). ⚠️ Допуск сверки с эфемеридами в тестах — 2 мин, НЕ шире: при 10 сьют оставался зелёным на перепутанном знаке ΔT. Тесты сверены с полным списком US Naval Observatory за 2025–2027 (74 момента). Пуши новолуния/полнолуния — замена утреннего в день события (`pushPlan.ts`, вид `moon`, спека 47б).

**SRS** — `src/lib/srs.ts`, чистые функции SM-2 по локальным дням (оценки 0–3) для флеш-карт (спека 45); сессия и сводка — `src/lib/review.ts`. Экран тренажёра — `app/review.tsx` (под гардом онбординга, вход — карточка «Повторение» над первым модулем таба «Курс»); общие компоненты, выросшие из задачи 45б — `FlipCard` (3D-переворот), `ResultPanel` (панель итога), `MeaningPanel` (панель значения), `KeywordChips` (чипы ключевых слов).

**Прогресс изучения** — `src/lib/collection.ts` (чистые функции: секции колоды по группам, прогресс «N из 78», `filterProgress` под активным чипом справочника; спека 46/46б) + фильтр `'learned'` в `src/lib/cardSearch.ts`; показывается на табе «Карты» (панель + приглушение неизученных + чип «Изучено»). Отдельного экрана альбома нет (первая редакция 46 снята как дубль справочника). Общие компоненты из задачи 46 — `CardCell`/`CardGridRow` (ячейка и ряд сетки карт), `StarBack` (рубашка со звездой, расклады).

Alias путей: `@/*` → корень репозитория. Комментарии в коде — на русском.
