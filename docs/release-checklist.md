# Arcanum · чек-лист релиза (этап 6, готовить с этапа 5)

## Аккаунты и сборки

- [~] Apple Developer Program ($99/год; регистрация занимает до недели — не откладывать).
      **25.08: заявка застряла на проверке документов** — сканы в приложении «Apple Developer»
      не проходят; открыт кейс поддержки **№ 20000147009590** (Apple Developer Program Support,
      просьба принять документы вручную). **27.08 ответ поддержки: причина — сканер приложения
      сверяет СТРАНУ документа с регионом Apple ID** (регион — Грузия, паспорт — Беларуси; ВНЖ
      Грузии сканер тоже отклонил), предложили сменить регион на страну паспорта — невозможно
      (карты и счета только грузинские, при смене региона Apple требует платёжный способ новой
      страны). **Решение: регион Грузия, заявка через веб** `developer.apple.com/programs/enroll` —
      **27.08 прошла**: заказ $99 оплачен грузинской картой, имя `Artsiom Siviankou`, адрес Батуми;
      по запросу Apple загружен один PDF (паспорт + ВНЖ обе стороны, ≤5 МБ, английские подписи
      полей есть — перевод не нужен) на `developer.apple.com/contact/file-upload/`, расписка
      «documents received» 15:06. **27.08 20:36 — АККАУНТ АКТИВИРОВАН** («Welcome to the Apple
      Developer Program» на почту через 5.5 часа после расписки, 22:03 — доступ в App Store Connect).
      Итого путь: заявка через веб + PDF документов → одобрение в тот же день.
      **Сделано из сессии через браузер 27.08 вечером:** [x] лицензионное соглашение программы —
      принято 27.08 при регистрации (Apple Developer Agreement — 25.08); [x] **Team ID
      `52QV87WW73`**, Individual, продление 28.08.2027; [x] Terms of Service App Store Connect
      приняты; [x] Legal Entity подтверждён (тот же адрес Батуми, автопроверка адреса «couldn't be
      verified» — подтверждён как есть); [x] **Paid Apps Agreement принят** → статус
      «Pending User Info»: [x] **банк добавлен 27.08** (`SIVIANKOU ARTSIOM (3346)`, Georgia,
      USD, «Processing» — обработка до 24 ч, на это время правки банка заперты) и [x] **налоговые
      формы поданы 27.08, обе Active** (W-8BEN `W-8BEN 2026` + Certificate of Foreign Status
      Apple) — **Paid Apps Agreement и банк ACTIVE в тот же вечер 27.08** (не сутки — минуты);
      вводил Артём (финансовые данные из сессии не вводятся). ⚠️ W-8BEN один на аккаунт (не на приложение), действует 3 года;
      строка 2 «Country of Citizenship» у Apple **заперта на страну аккаунта** (в списке только
      Georgia, хотя гражданство — Беларусь) — изменить нельзя, существенные утверждения формы
      («не лицо США», «резидент Грузии») верны, подписывается как есть; строка 9 — галочка
      (на Грузию действует договор США–СССР 1973), строка 10 — Article III, paragraph 1, 0 %,
      «Income from the sale of applications».
      ⚠️ Форма банка для Грузии (27.08): Apple сверяет IBAN с частями — `GE` + 2 контрольные
      цифры + **Georgia Bank Code = 2 буквы из IBAN** (Bank of Georgia — `BG`, не SWIFT
      `BAGAGE22`) + **Account Number = 16 цифр после кода банка**, не весь IBAN; иначе
      «Bank account number format is incorrect». Валюта — USD, если есть в списке (выплаты
      в долларах, счёт мультивалютный). «Address couldn't be verified» — косметика, Confirm;
      после активации Paid Apps — App Store Small Business Program (комиссия 15 % вместо 30 %,
      `developer.apple.com/app-store/small-business-program/`) — **заявка ПОДАНА 28.08 вечером**
      (форма `…/small-business-program/enroll/`: Paid Apps принят, ассоциированных аккаунтов нет,
      доход < $1M; «We've received your enrollment… will email you»; ответ ждём письмом).
      [~] **DSA (Digital Services Act) — «I'm a trader», подано 27.08, статус «In Review»**
      (27 стран ЕС): адрес, телефон и e-mail будут показаны на странице приложения в ЕС.
      Мастер просил: документ с именем (тот же PDF паспорт + ВНЖ, что принимала регистрация,
      здесь лимит 10 МБ, язык English (US)), подтверждение адреса, SMS на телефон, код на
      e-mail. Результат — письмом; до него сабмит в ЕС не открывается, остальному не мешает.
      **28.08 04:15 — письмо Developer Support (кейс `20000149524822`)**: для проверки DSA
      просят ОДИН документ с именем и адресом, совпадающим с адресом в App Store Connect
      (выписка банка / счёт за коммуналку / справка о регистрации / письмо госоргана);
      загрузка — по ссылке из отдельного письма `AppleSupport@email.apple.com`, после
      загрузки **обязательно ответить на письмо**, иначе кейс не двигается. Подтверждение
      адреса из мастера, видимо, не приняли. **Сделано 28.08**: свежая выписка Bank of Georgia
      (01–27.08.2026, имя + адрес в шапке, без печати) загружена по ссылке, ответ Роберту
      отправлен. Ждём решения Apple.
      [x] Identifiers → App ID **`app.arcanum.tarot`** зарегистрирован 27.08 (тип App, explicit,
      описание `Arcanum`, capabilities по умолчанию — In-App Purchase входит автоматически);
      [x] **App Store Connect → приложение создано 27.08**: iOS, название
      **«Arcanum — обучение таро»** (Primary Language Russian, Bundle ID `app.arcanum.tarot`,
      SKU `arcanum`, Full Access), версия 1.0 «Prepare for Submission». ⚠️ Короткое имя
      `Arcanum` в App Store **занято** («already being used») — поле Name в App Store Connect и
      есть витринное название, поэтому сразу вписано полное из `store-listing.md` (23/30).
      [x] iOS `preview`-сборка — **прошла 28.08** (`23daec5d`, раздел «Сборка»; лайв на iPhone
      за Артёмом); [x] 63б — **витрина ASC ЗАЛИТА 28.08 вечером** (версия 1.0: тексты на
      ru + en-US/es-MX/pt-BR, кадры 1290×2796 по 7 на язык в слот 6,9″ Медиаменеджера в порядке
      01→07, URL поддержки/маркетинга, копирайт; «Информация о приложении»: название/подзаголовок
      на 4 языках, категории Образование + Образ жизни; отчёт и грабли — спека 63); [x] **возрастной
      рейтинг — 13+** (анкета Apple 2025 по фактам IARC: страх, нагота без эротики, фэнтези-насилие,
      оружие — «редко»; 12+ Вьетнам/Бразилия/Корея); [x] **контакт и заметки для ревью** (имя,
      телефон — обязателен, `arcanum.tarot@icloud.com`, заметки из `store-listing.md`, «Необходимо
      войти» снято); [x] **App Privacy опубликована 28.08 вечером** — URL политики на 4 языках,
      «Покупки → История покупок» (Функциональность + Аналитика, требование RevenueCat) и
      «Идентификаторы → ID пользователя» (Функциональность), не связаны с личностью, без
      отслеживания; [x] **ASC API key `V55RK5Z8R9`** («EAS Submit», App Manager, Issuer
      `e719c52a-3121-47eb-8d2a-768a1843d1c0`, `.p8` в `Documents\keys`, доступ к API одобрен
      мгновенно) → submit-профиль в `eas.json`; [~] **релизная iOS-сборка ЗАПУЩЕНА 28.08 поздно
      вечером** — id `9d27418f-558c-47a8-a575-65c692c6c7e7`, buildNumber 3 (`--non-interactive`
      падал: «Distribution Certificate is not validated for non-interactive builds» — Артём запустил
      интерактивно: вход Apple ID, сертификат `98PR42H89R` переиспользован, создан App Store
      provisioning profile `Y3B4UW7278` и Apple Push Key; пароль Apple лёг в локальный Keychain
      EAS, на серверах не хранится); [~] **Android-сборка versionCode 6** с правкой `5a16379` —
      id `ca680d94-64ad-4829-aaf5-5352657ca548`, AAB кладёт Артём во внутренний трек руками;
      **Даблчек 28.08 поздно вечером** (сверка ASC скриптом): версия 1.0 — 4 языка побайтово =
      `store-listing.md`, кадры 7/7 в порядке 01→07, URL, копирайт, контакт; «Информация о
      приложении» — 4/4, категории, 13+, DSA-заявление; подписки — кадр + заметка; App Privacy
      опубликована. **Три дырки найдены и закрыты**: [x] тип релиза был «автоматически после
      проверки» → переключён на **«вручную»** (выпуск жмёт Артём); [x] **«Права на контент»** не
      были объявлены → «содержит сторонний контент, права есть» (сканы Райдера–Уэйта и текст
      Уэйта — public domain); [x] **цена и доступность самого приложения не были заданы** →
      «0,00 $ (Бесплатно)», базовая страна США, 175 стран, доступность «Все страны/регионы (175)».
      [ ] сабмит — с явного «ок» Артёма, вместе с группой подписок; [~] продукты подписки — **созданы 28.08** (группа `Premium`
      id `22341582`; `premium.year` Apple ID `6806201993`, 1 год, **34,99 $**; `premium.month`
      Apple ID `6806205400`, 1 месяц, **5,99 $**; статус «Подготовка к отправке» — первая
      подписка уходит на проверку вместе с новой версией приложения). Доступность — все 175
      стран + галочка «автоматически в новых». Локализации ru / en-US / es-MX / pt-BR у обеих
      подписок и у группы (`Arcanum Premium`, название приложения — из App Store); тексты —
      `docs/store-listing.md`, раздел «Подписки». ⚠️ **Базовая витрина цены — Грузия (USD)**,
      и Apple уравнивает цены ПОСЛЕ налога: при 34,99 $ в Грузии (НДС 18 %) США получили бы
      29,99 $, месяц 5,99 → 4,99 — США переставлены вручную на 34,99 / 5,99 в шаге «Цена по
      стране» (Беларусь 34,99 $ / 5,99 $, Европа 34,99 € / 5,99 €, Россия 2 490 ₽ / 449 ₽ —
      автопересчёт). ⚠️ Выпадающие цены в ASC — `li[role=menuitem]` с неразрывным пробелом
      перед `$`: `:text-is("34,99 $")` не матчится, брать `:has-text("34,99") >> nth=1`
      (первый пункт — «текущая цена» с пояснением). ⚠️ Кнопка «Все» в мастере доступности
      ставит И галочку «новые страны»: клик по ней после «Все» её СНИМАЕТ. [x] Скриншот и
      примечания для проверки у обеих подписок — **залиты 28.08 вечером** (кадр пейвола с iPhone
      Артёма 1290×2796 с двумя тарифами, заметка ревьюеру — `store-listing.md`, «Подписки»);
      Sandbox-тестер есть. **In-App Purchase Key создан 28.08** (имя `RevenueCat`, Key ID
      `67VRFKU59T`, Issuer ID `e719c52a-3121-47eb-8d2a-768a1843d1c0`; `.p8` лежит в
      `C:\Users\Artem\Documents\keys\`, второй раз Apple его не отдаёт) и загружен в RevenueCat;
      **iOS SDK-ключ `appl_…` положен в EAS** (`env:set`, среды production + preview, plaintext)
      и в локальный `.env`. Ход по RevenueCat — спека 53б, раздел «Артём — до кода».
      **App Store Server Notifications → RevenueCat подключены 28.08**: ASC → приложение →
      Информация о приложении → «Уведомления сервера App Store» — URL рабочего и Sandbox-сервера
      = адрес из RevenueCat (страница App Store-приложения, поле «Apple Server Notifications URL»).
      Без них отмена/продление доезжают до приложения только при обновлении кэша SDK (~5 минут
      после сворачивания/разворачивания) — так и выглядело на лайве: отмена в Sandbox в 15:57,
      панель ещё минуту говорила «Продлится».
      ⚠️ Классификатор auto-режима сессии блокировал навигацию по URL на `developer.apple.com`
      и новые вкладки после принятия соглашений; в ручном режиме прошли клики, ввод текста
      и стрелки, но `form_input` и клавиша Enter остались под блоком — выпадающие списки
      выбирать стрелкой Down и проверять скриншотом. Ссылка «Certificates, Identifiers &
      Profiles» из формы New App открывается вне группы вкладок сессии — идти прямым URL.
- [~] Google Play Console ($25 разово) — ⚠️ новые личные аккаунты: закрытое тестирование
      (12+ тестеров, 14 дней) обязательно до продакшена — заложить в сроки!
      **25.08: личный аккаунт создан, оплачен**, ждёт трёх проверок консоли: (1) личность —
      фото паспорта; (2) **вход в мобильное приложение Play Console на физическом Android**
      (эмулятор не считается, у Артёма iPhone — нужен чужой телефон один раз); (3) номер
      телефона — открывается после (1). «Создать приложение» недоступно до конца проверок.
      **25.08 вечером аккаунт ПОЛНОСТЬЮ ВЕРИФИЦИРОВАН**: личность, физическое Android-устройство
      (вход в мобильное приложение Play Console) и телефон подтверждены, «Создать приложение»
      открылось. Осталось от Google только закрытое тестирование 12 тестеров × 14 дней до заявки
      на продакшен — список e-mail собирать заранее. Свой недорогой Android (13+) Артём всё равно
      покупает: он же «слабый Android» технического прогона и приёмник APK задачи 61.
- [ ] RuStore (бесплатно) — сборка APK через EAS
- [x] EAS Build настроен (eas.json: preview + production; **27.08 `production` AAB прошла**, `versionCode` 2, 86.6 МиБ), версии: app.json version + runtimeVersion —
      **25.08 (задача 61)**: проект `@art9/arcanum`, `eas.json` (`preview` APK/ad hoc, `production` AAB,
      номера сборок в EAS — `appVersionSource: remote`, `runtimeVersion` не нужен без EAS Update).
      **26.08: первая Android-сборка `preview` ПРОШЛА** (подробности — раздел «Сборка (EAS Build)»
      ниже). Осталось: установка и лайв-проверка APK, `production` (AAB), iOS после Apple.
      Спека `docs/specs/61-eas-build.md`.
- [ ] **Подписка (задача 53б, решение 22.08 — живая с v1):** **код — сделано (53б, 27.08)**:
      адаптер `src/lib/purchases.ts` на `react-native-purchases` за интерфейсом задачи 53
      (единственный файл с импортом SDK), ключи `EXPO_PUBLIC_RC_ANDROID_KEY`/`EXPO_PUBLIC_RC_IOS_KEY`
      через `eas env:set` (команда — AGENTS.md), без dev-client, `usePremiumSync` синхронизирует
      право при старте/возврате из фона/push SDK. **Консоль 28.08 — Apple ГОТОВ:** [x] аккаунт
      RevenueCat (проект `Arcanum`, entitlement `premium`, App Store-приложение с `.p8`, продукты,
      offering `default`), [x] продукты `premium.year`/`premium.month` + цены в App Store Connect
      ($5.99 / $34.99, все страны, без триала), [x] Sandbox-тестер Apple, [x] `eas env:set`
      `EXPO_PUBLIC_RC_IOS_KEY` (production + preview), [x] iOS `preview`-сборка с ключом `a320c6dd`
      (пейвол показывает цены магазина — лайв Артёма), [x] лайв покупок по сценарию спеки 53б —
      **iOS 28.08 днём (Sandbox, iPhone), Android 28.08 вечером (эмулятор, внутренний трек,
      тестовая карта)**; найденный на Android дефект `productPlanIdentifier` закрыт коммитом
      `5a16379` — в AAB versionCode 5 его ещё нет, нужна сборка 6. **Google — профиль продавца создан 28.08 вечером** (Беларусь / физлицо; задача 66):
      «Подписки» открыты, но ждут AAB с разрешением BILLING (сборка запущена). Выплаты: форма
      принимает только белорусский USD-счёт — Bank of Georgia отвергнут по SWIFT и IBAN, Wise не
      пройдёт; выручка копится у Google до появления белорусского счёта (хвост 66).
      **Решение Артёма 28.08 поздно вечером: банк ОТЛОЖЕН.** Для продаж способ выплаты не нужен —
      профиль продавца есть, подписка активна; выручка копится на балансе платёжного профиля
      (Play Console → Настройки → Платёжный профиль), выплаты идут ~15-го числа за прошлый месяц при
      балансе ≥ 1 $ (местная валюта) / ≥ 100 $ (USD-перевод) — цифры из справки Google; что баланс
      без банка держится, а не сгорает, справка прямо не подтверждает (проверено 28.08), но и срока
      не называет. Счёт в несанкционном банке РБ открывать по факту спроса — возможно, не
      понадобится. Не блокирует ни закрытый тест, ни релиз; единственный риск — запрос документов
      Google при верификации профиля приостановит выплаты (не продажи) до их подачи.
      [x] AAB №2 `6cf48d53` с покупками и ключом — **выпуск 5 (1.0.0) во внутреннем треке 28.08
      17:16** (Артём), [x] **подписка `premium` создана и оба плана АКТИВНЫ 28.08**: `year`
      (каждый год, $34.99, «Обратная совместимость») и `month` (каждый месяц, $5.99), 174
      страны, льготный период 7 дней, повторная подписка разрешена; цена задаётся через
      «Set prices» → чекбокс «все страны» (появляется ТОЛЬКО после «Set prices») → «Указать
      цену» → поле принимает **запятую** (`34,99`; с точкой кнопка «Обновить» не активна) →
      Google сам пересчитывает по странам с НДС (Германия 35,99 €, Грузия 99 GEL, Россия
      2 999 ₽); «Активировать» — на странице плана после сохранения (в меню строки его нет).
      [x] лицензионный тестер (список «Внутренние тестировщики»), [x] service-account
      `revenuecat@arcanum-play.iam.gserviceaccount.com` (проект GCP `arcanum-play`, ключ в
      `Documents\keys`) + Play Store-приложение в RevenueCat, продукты, offering,
      [x] `EXPO_PUBLIC_RC_ANDROID_KEY` в EAS и `.env`, [x] переводы подписки en-US/es-419/pt-BR,
      [ ] лайв Android на эмуляторе (ждёт распространения выпуска 5 — на 16:45 магазин отдавал
      versionCode 3; рецепт — `docs/prompts/next-session-29-08.md`). Выплаты: карта Сбер Банка
      РБ не годится — банк в SDN США и под санкциями ЕС, долларовый SWIFT до него не доходит;
      нужен несанкционный белорусский банк (задача 66).
      Пакеты в RevenueCat — `year`/`month`, entitlement — `premium` (те же имена, что в коде);
      тестовые покупки — в Sandbox (Apple) / лицензионный тестер (Google); «Восстановить покупки»
      проверено на втором устройстве. ⚠️ Apple 3.1.2: на экране покупки
      ФУНКЦИОНАЛЬНЫЕ web-ссылки на Privacy Policy и Terms of Use (EULA) + те же ссылки в метаданных
      App Store Connect. **Блокер СНЯТ задачей 54 (22.08):** ссылки пейвола ведут на
      `site/terms.html` и `site/privacy.html` (GitHub Pages); в полях App Store Connect указать
      те же URL — своя страница условий вместо стандартного EULA Apple, одна для обоих сторов.
      URL политики конфиденциальности нужен ОТДЕЛЬНО и в Google Play Console (App content →
      Privacy policy) — тот же URL, что у Apple, но заводится как самостоятельный пункт анкеты.
      Тексты подписки для анкет сторов: описание продукта подписки (App Store Connect: localized
      display name + description на каждый язык витрины) и Subscription details для Google Play
      (что даёт подписка, период, автопродление) — готовятся вместе с ценами, до сабмита.
      Платежи из РФ (RuStore Pay / веб-оплата) — отдельное решение после 53.
      ⚠️ **Валюта и рынки (проверено 27.08):** у аккаунта из Беларуси валюта разработчика — **USD**,
      базовая цена задаётся в долларах. **Россия**: подписки Google Play не работают вообще — ни покупок,
      ни продлений, рублёвая цена там неприменима. **Беларусь**: подписка внутри БЕСПЛАТНОГО приложения
      не блокируется (блокируются только платные приложения) — покупка возможна. Ориентир Артёма
      2 890 ₽/год и 399 ₽/мес даёт скидку 39 %; та же пропорция в долларах — $4.99/мес и $35.99/год.
      ⚠️ **Цены на четырёх языках** (вопрос Артёма 22.08, разбор — `docs/specs/53-premium.md`,
      раздел «Цены и валюта»): валюту выбирает страна аккаунта магазина, а НЕ язык приложения.
      Строку цены отдают StoreKit / Play Billing уже отформатированной (`product.priceString`
      через RevenueCat) — приложение её не собирает, не конвертирует и не хранит в i18n
      (в i18n только обвязка «Оформить за {{price}} в год»). До сабмита решить: страны продаж,
      цена базовой страны и ценовые уровни в остальных (автопересчёт Apple принять или поправить
      руками), пробный период. **Бейдж «−40 %» в 53б обязан считаться из фактических цен пары
      «год / месяц ×12», а не быть константой** — ценовые уровни по странам непропорциональны,
      и константа станет враньём в части регионов; пары нет — бейдж не рисуется.
- [ ] Иконка 1024 (без альфы для iOS), сплэш, adaptive icon Android — файлы готовы и прописаны
      в app.json (проверено 18.08). ⚠️ В Expo Go их не видно В ПРИНЦИПЕ: приложение не установлено,
      а уведомления показывает сам Expo Go, поэтому и на экране загрузки, и в баннере пуша стоит
      его иконка. Проверять — только на сборке (dev build / TestFlight).
- [x] **Иконка уведомлений на Android — СДЕЛАНО 23.08 (задача 55)**: `assets/images/notification-icon.png`
      (96×96 RGBA, белый силуэт эмблемы на прозрачном — кольцо и звезда из `Emblem.tsx`), рисуется
      скриптом `python scripts/gen_notification_icon.py` (руками не править: белизну пикселей
      проверяет он, а не тест). Плагин `expo-notifications` в `app.json` с `icon`, `color` `#caa45a`
      и `defaultChannel` `daily` (тот же CHANNEL_ID, что в `pushes.ts`). Формат и связку
      «app.json ↔ файл ↔ канал» держит `src/lib/__tests__/notificationIcon.test.ts`.
      ⚠️ Увидеть иконку можно ТОЛЬКО на сборке — в Expo Go уведомления показывает сам Expo Go
      со своей иконкой. На iOS иконка пуша не настраивается вовсе — там всегда иконка приложения.

### Порядок заведения аккаунтов (гид 25.08, задача «пункты 1–5»)

Что вводить везде одинаково: приложение **Arcanum**, bundle id / package **`app.arcanum.tarot`**
(`app.json`, менять после создания записи в сторах уже нельзя), версия `1.0.0`, сайт
`https://sivyankof.github.io/arcanum/`, политика `…/privacy.html`, поддержка `…/support.html`.
Владелец всех аккаунтов — личный e-mail Артёма (восстановление доступа), публичный контакт —
ящик поддержки (п. 5). Аккаунты индивидуальные (Individual), не организация: у организации
Apple и Google просят D-U-N-S/юрлицо; цена — продавец в витрине показывается по имени из
паспорта, «Arcanum» как имя продавца доступно только организации.

⚠️ **Страна аккаунта решает, будет ли подписка.** Google Play с 2022 не даёт разработчикам
с адресом в РФ продавать платные приложения и подписки (merchant-аккаунт недоступен);
Apple не принимает российские карты за $99 и не выплачивает на российские банки. Страна
аккаунта = страна адреса и платёжного профиля, а не язык. Решение о стране — до оплаты,
переезд аккаунта потом — отдельная боль. Платежи из РФ (RuStore Pay) — решение после 53.
**Решено 25.08: аккаунты заводятся по паспорту Беларуси.** Сверено по справке Google
(«Supported locations for developer and merchant registration»): Беларусь — ✔ разработчик,
✔ продавец, выплаты в USD, то есть подписка через Play Billing доступна. Ограничение
«Changes to Google Play's billing system for users in Russia and Belarus» касается
ПОКУПАТЕЛЕЙ в Беларуси: платные приложения им не продаются, но «in-app purchases or
subscriptions within free apps are not blocked» — наш случай (Free + подписка Premium).
Юридическое имя во всех формах — латиницей ровно как в паспорте (одно написание везде,
сверяется с загруженным документом); публичное имя разработчика — свободное (`Arcanum`).

1. **Apple Developer Program** — самый долгий, заводить первым. Приложение «Apple Developer»
   на iPhone → вкладка «Account» → «Enroll Now»: вход тем же Apple ID, что на телефоне
   (обязательна двухфакторка), скан паспорта/прав, фото, адрес, оплата $99 через способ
   оплаты Apple ID. Одобрение — обычно 1–2 дня, бывает до недели. Веб-путь
   `developer.apple.com/programs/enroll` — то же, но без скана ID дольше. ⚠️ Если страна
   паспорта ≠ регион Apple ID (наш случай: паспорт Беларуси, регион Грузия по ВНЖ), приложение
   отклоняет скан мгновенно и поддержка помочь не может — сразу идти веб-путём, документы
   Apple запросит письмом и примет через файл-аплоад (проверено 27.08). После одобрения:
   App Store Connect → «My Apps» → «+» → New App: платформа iOS, имя `Arcanum`,
   Primary language по стране, Bundle ID `app.arcanum.tarot` (сначала завести в Certificates,
   Identifiers & Profiles → Identifiers, тип App), SKU `arcanum`. Записать в этот файл Team ID.
2. **Google Play Console** — `play.google.com/console/signup`, тип «Personal», $25 одноразово
   через Google Payments (профиль платежей той же страны, что адрес). Проверка личности:
   документ + телефон + иногда подтверждение адреса; имя разработчика показывается публично,
   у продающих аккаунтов — и адрес. ⚠️ Новые личные аккаунты: **закрытое тестирование
   12 тестеров непрерывно 14 дней** до заявки на продакшен — тестеры из аудитории жены, список
   e-mail собрать заранее. После одобрения: «Create app» → `Arcanum`, App/Game, Free (бесплатное
   с подпиской — тип Free; сменить на Paid потом нельзя, но и не нужно), package `app.arcanum.tarot`
   задаётся первой загруженной сборкой.
3. **Expo (EAS)** — `expo.dev/signup` (можно через GitHub), бесплатный план: 30 сборок в месяц,
   iOS-очередь медленнее. Дальше в терминале сессии `! npx expo login` (интерактивно — только
   так), после чего сессия сама делает `eas init` (пишет `owner` и `extra.eas.projectId`
   в `app.json`) и `eas build:configure` (`eas.json`: development / preview / production).
   Android dev-сборка не требует ничего сверх этого; iOS dev-сборка требует п. 1 (профиль
   ad hoc, UDID iPhone через `eas device:create`).
4. **RevenueCat** — `app.revenuecat.com` → Sign up → проект `Arcanum`. Платформы (App Store /
   Play Store) добавляются позже — им нужны App Store Connect API key и сервисный аккаунт
   Google Play, то есть п. 1–2 одобренные. Бесплатно до $2.5k дохода в месяц. Что записать:
   public SDK keys (iOS/Android) → в спеку 53б, когда появятся.
5. **Ящик поддержки** — отдельный Gmail (например `arcanum.tarot.app@gmail.com`; проверить
   свободность при создании), с пересылкой на личный, чтобы не пропускать письма ревьюеров.
   Адрес сообщить в сессию — она меняет `SUPPORT_EMAIL` в `src/lib/feedback.ts` и
   `site/support.html` одним коммитом (`site.test.ts` держит равенство). Этот же адрес —
   в контакты Play Console и Support URL/e-mail App Store Connect.

Порядок за один вечер: п. 5 (5 мин) → п. 1 (запускает самый долгий таймер) → п. 2 → п. 3 → п. 4.

**Состояние на 27.08 (вечер):** п. 1 Apple — **аккаунт АКТИВИРОВАН 27.08 20:36**, доступ в App Store
Connect есть; дальше соглашения (лицензионное + Paid Applications), Team ID, приложение в
App Store Connect, iOS-сборка, 63б (подробности выше);
п. 2 Google — **аккаунт верифицирован полностью, приложение можно создавать**; п. 3 Expo — готов
(`art9`, задача 61); п. 4 RevenueCat — **не заведён**; п. 5 ящик поддержки — **заведён 27.08:
`arcanum.tarot@icloud.com`** (псевдоним iCloud Mail с пересылкой на личный; регистрацию
отдельного ящика в Google и Outlook блокировала антибот-проверка — два сервиса подряд, значит
дело в сети/IP, а не в имени), адрес заменён в `feedback.ts` и на всех страницах `site/`; в «Контактную информацию» Play Console
вписан вместе с сайтом 27.08 — задачи настройки приложения **11 из 11**. Доставка проверена письмом 27.08: пришло в iCloud и переслалось на личный. Свой Android-телефон Артём ещё
покупает (проверка устройства прошла на чужом) — но он нужен только для установки APK, а не для
консоли и не для сборки.

## Создание приложения в Play Console (что вписывать — сверено 25.08)

**Все приложения → Создать приложение.** Значения — из `docs/store-listing.md` (задача 57),
в форме не сочинять:

- **Название приложения**: `Arcanum — обучение таро` (23 из 30 символов; это ru-витрина,
  остальные языки добавляются позже переводами витрины).
- **Язык по умолчанию**: русский (ru-RU) — язык первой аудитории (канал жены); en/es/pt
  добавляются в Store listing как переводы.
- **Приложение или игра**: приложение. **Бесплатное или платное**: **бесплатное**
  (подписка внутри — это «бесплатное»; смена на «платное» после публикации невозможна).
- Декларации: правила Play + экспортное законодательство США — обе галочки.
- **Категория**: Образование, вторичная — Стиль жизни (`store-listing.md`, раздел «Категории
  и рейтинг»; не «Развлечения» — обучение проходит ревью мягче). **Рейтинг**: 12+.

### ⚠️ Заливка витрины в консоль: что выяснено на практике 26.08

Приложение уже создано: `app.arcanum.tarot`, ID `4975498668598706821`, статус «Черновик»,
язык по умолчанию ru-RU. Прямая ссылка на форму витрины:
`https://play.google.com/console/u/0/developers/7424080842647468142/app/4975498668598706821/main-store-listing`

- **Форма не сохраняется без графики.** Значок, «Картинка для описания» (баннер) и минимум два
  скриншота телефона — обязательные поля со звёздочкой; пока их нет, кнопка сохранения ругается
  «Чтобы сохранить изменения, исправьте ошибки». Значит тексты и графику заливают ОДНИМ заходом,
  а не по очереди. Планшетные скриншоты отдельно не нужны — требование «не менее двух скриншотов
  для телефонов или планшетов» закрывается телефонными.
- **Файлы:** значок `docs/store/icon-512.png`, баннер `docs/store/feature/<lang>.png`,
  скриншоты `docs/store/google/<lang>/01…07`.
- ⚠️ **Графику нельзя залить «в лоб»:** пока панель выбора объектов не открыта, в DOM нет
  ни одного `input[type=file]`, а клик по «Загрузить» открывает системный диалог.
  **Решение найдено 26.08 — см. раздел «Заливка витрины: как прошла 26.08» ниже.**
  Синтетический `drop` в дропзону НЕ работает (зона подсвечивается на `dragover`, но на `drop`
  консоль отвечает «Произошла непредвиденная ошибка») — на него время не тратить.
- ⚠️ **Переключатель языка в НЕсохранённой форме ненадёжен.** Заполнили русский, добавили en/es/pt
  через «Переводы → Выберите языки», переключились на английский — заполнилось верно; дальнейшее
  переключение на испанский не сработало, и испанский с португальским дописались в английские поля
  (название стало 72/30). Ошибку видно только по счётчику символов. Правило: после КАЖДОГО
  переключения языка убедиться скриншотом, что поля пусты и селектор показывает нужный язык, и
  только потом печатать. **26.08 выяснилось точнее**: после того как русская витрина
  СОХРАНЕНА, переключатель работает честно: поля каждого нового языка приходят пустыми.
  Сломанным он был именно в НЕсохранённой форме. Правило проверки скриншотом остаётся.
- **Тексты — только копией из `docs/store-listing.md`** (задача 57, утверждены Артёмом), сочинять
  в форме запрещено. Локали: en-US, es-419 (Латинская Америка — целевой рынок спеки 63), pt-BR.

### Заливка витрины: как прошла 26.08 (СДЕЛАНО)

Витрина заполнена и сохранена на четырёх языках (ru-RU по умолчанию + en-US / es-419 / pt-BR).
Статус страницы — «Готово к отправке на проверку»; на проверку НЕ отправляли. Каждый язык несёт
свой баннер и свои 7 скриншотов в порядке 01→07; значок задан на ru и наследуется переводами
(он языконезависимый, переопределять не нужно).

**Как заливалась графика без мыши** — три шага, каждый важен:

1. **Клик по «Добавить объекты» открывает боковую библиотеку объектов**, и только тогда
   в DOM появляется `simple-uploader > input[type=file]` (скрытый). До этого его нет вовсе —
   именно это сбило предыдущую сессию с толку.
2. **Файлы берутся с raw.githubusercontent.com**, а не с локального сервера: `docs/store/`
   уже лежит в публичном репозитории, и `fetch` со страницы консоли проходит.
   ⚠️ Локальный HTTP-сервер на 127.0.0.1 НЕ годится: запрос из https-страницы в локальную
   сеть висит вечно (статус `pending`, ни ошибки, ни ответа) даже с заголовком
   `Access-Control-Allow-Private-Network` — Chrome держит его на разрешении Local Network Access.
   (Попутно: `http.server` на `TCPServer` однопоточный и намертво встаёт от первого же
   зависшего соединения — нужен `ThreadingTCPServer`.)
3. **Файл кладётся в `input.files` через `DataTransfer`**, дальше `dispatchEvent(new Event('change'))`.
   Консоль грузит файл в библиотеку и сама его выделяет; остаётся нажать «Добавить».

⚠️ **Порядок скриншотов = порядок ВЫДЕЛЕНИЯ в библиотеке**, а не порядок загрузки и не порядок
имён. Залил семь штук разом — консоль выделит их в порядке завершения закачек, и витрина получит
кашу (у нас вышло 02, 07, 01, 06, 04, 03, 05). Лечится так: снять всё выделение, потом выделить
поочерёдно 01→07 и только потом «Добавить». Проверяется по `img.alt` в строке — на языке
по умолчанию там имена файлов.

⚠️ **Консоль дедуплицирует объекты по содержимому** (тост «Объект дедуплицирован»). Поэтому
имена файлов разных языков обязаны различаться (грузили как `en-01-today.jpg`, `es-01-today.jpg`,
`pt-01-today.jpg`), иначе в библиотеке появятся семь одинаково названных плиток и выбрать нужную
механически нельзя.

⚠️ **Программная подстановка текста обновляет счётчик символов, но не снимает красную ошибку
«Добавьте название/описание».** Лечится одним настоящим нажатием клавиши в поле: клик → End
(для textarea Ctrl+End) → «.» → Backspace. Значение при этом не меняется, а валидация
пересчитывается.

⚠️ **Сохранение — всегда по ВСЕМ языкам сразу.** Кнопка «Сохранить» падает с «Ошибки в переводах
на некоторые языки», пока хотя бы один добавленный язык пуст. Поэтому порядок такой: ru целиком →
Сохранить → добавить языки → заполнить en, es, pt подряд → одно Сохранить на все три.

⚠️ **На странице перевода графика наследуется от ru** и показана приглушённой. Кнопка удаления
там есть, но НЕ работает (наследованное не удаляется) — просто добавляй своё поверх, оно замещает
наследованное целиком.

⚠️ **Кнопка «Добавить объекты» у пустой и у заполненной строки — разные элементы**
(`button.add-button` против обычной кнопки в подвале карточки). И пока панель открыта для другой
строки, клик по соседней её не перепривязывает — панель сначала закрыть. Признак правильной
привязки — `input.multiple`: `false` у значка и баннера, `true` у скриншотов. Залив семь файлов
в `multiple=false`-вход, получишь ровно один и долгое молчание.

⚠️ **Две кнопки сохранения.** «Сохранить» кладёт изменения в «Обзор публикации» (там их потом
отправляют на проверку вручную), «Сохранить как проект» — только черновик страницы. 26.08 выбрана
«Сохранить» (решение Артёма).

**Проверка после сохранения** (сделана 26.08): перезагрузить страницу и по каждому языку сравнить
три поля с `docs/store-listing.md` буква в букву (текст удобно тянуть тем же `fetch` с
raw.githubusercontent и разбирать по заголовкам `### <язык> · <поле> (<лимит>)`) и глазами
убедиться, что кадры и баннер на нужном языке. Результат: 4 из 4 языков сошлись.

**Дальше в разделах консоли** (сборка нужна только последнему):

- *Основная информация о Store*: **ЗАКРЫТО 26.08** — тексты (80 и 4000) и графика (иконка 512×512,
  баннер 1024×500, по 7 скриншотов телефона) залиты на всех четырёх языках и сохранены; страница
  в статусе «Готово к отправке на проверку». Подробности и грабли — в разделе «Заливка витрины:
  как прошла 26.08» выше. Материалы лежат в `docs/store/` (задача 63).
- *Контент приложения*: **ВСЕ 10 деклараций закрыты 26.08.2026** — вкладка «Требуются действия»
  пустая. Ответы: URL политики `https://sivyankof.github.io/arcanum/privacy.html`;
  доступ к приложению — без входа и ограничений; реклама — нет; **безопасность данных —
  данные не собираются и не передаются** (аналитики в v1 нет, решение 22.08); анкета
  возрастного рейтинга (IARC) — заполнена (см. ниже, раздел «Анкета IARC»);
  целевая аудитория — **только «От 18 лет»** (группы до 13 лет консоль и не предлагает — их
  закрыл рейтинг ESRB T; необязательную галку «запретить доступ пользователям с детским
  аккаунтом Google» НЕ ставили: она режет и установку, и покупки); рекламный идентификатор —
  не используется; госучреждения — нет; финансовые функции — «нет финансовых функций»;
  приложения для здоровья — «нет функций, связанных со здоровьем» (рефлексия «как прошёл день
  с этой картой» — не управление стрессом: ни трендов, ни «процента точности», решение
  product-spec §7). ⚠️ 27.08: консоль открыла декларацию «Здоровье» не из-за функций, а из-за
  `android.permission.ACTIVITY_RECOGNITION` в манифесте AAB (его добавляет `expo-sensors` ради
  шагомера; нам нужен только `DeviceMotion`). Ответ «функций нет» сдать нельзя — «Далее» без
  выбранных функций неактивна, консоль просит убрать разрешение. Лечится `android.blockedPermissions`
  в `app.json` + сборка `versionCode` 3 (задача 64) — **сделано 27.08**: после заливки сборки 3 консоль
  показывает «Все декларации заполнены», пункт исчез сам.
  Подписки объявляются в *Монетизация → Продукты* вместе с задачей 53б.
- *Внутреннее тестирование* — первый трек для сборки: без ожидания и без ревью, до 100 тестеров.
  Именно сюда льём первый AAB (задача 61). **СДЕЛАНО 27.08**: выпуск 2 (1.0.0) опубликован во
  внутреннем треке (13:50), список «Внутренние тестировщики» (пока один адрес — аккаунт Артёма),
  ссылка присоединения `https://play.google.com/apps/internaltest/4701646545825132337`; до завершения проверки
  тестеры видят временное название «app.arcanum.tarot (unreviewed)». Два предупреждения выпуска —
  информационные (нет файла деобфускации: R8 не включён; тестеры добавлены следом). **Порядок работ, решение Артёма 26.08: 61 → 53б →
  закрытый тест** — тестеры закрытого трека увидят уже настоящие покупки. **Задача 62 закрыта
  26.08** (лайв-проверка ✓): пейвол больше не показывает цену, которой не назвал магазин, —
  без SDK покупок вместо тарифов панель «Оформить подписку пока нельзя», диалог не поминает
  Expo Go. Это страховка внутреннего теста и постоянное состояние «магазин не ответил» после 53б.

⚠️ **Ловушка «безопасности данных»**: ответ «данные не собираются и не передаются» **сдан
26.08.2026** и верен до 53б — на 26.08 в коде нет ни одного `fetch`/`axios`, пуши локальные
(токен устройства не запрашивается), аналитики нет ни в приложении, ни на сайте `site/`,
Android-разрешений в `app.json` не объявлено. Как только в сборку войдёт RevenueCat, наружу начнёт
уходить покупка (чек и анонимный идентификатор установки), и **анкету придётся переписать
до релиза** — «Покупки, не связаны с личностью» (`store-listing.md`, раздел «Категории и
рейтинг»). Анкета не проверяется тестами и не живёт в репозитории: единственная защита —
этот пункт. Правишь `purchases.ts` под 53б — иди в консоль и в App Privacy у Apple.
**Data Safety после 53б (код сделан 27.08, анкета не переписана):** новая собираемая категория —
«История покупок» (Purchase history), собирается — да, цель «Работа приложения»
(App functionality), третьим лицам как продавцу НЕ передаётся — RevenueCat здесь поставщик услуг
(service provider) по гайду RevenueCat «Google Play's Data Safety», а не третья сторона, которой
данные продаются или которая использует их для своих целей. Переписать анкету ОБЯЗАТЕЛЬНО
ДО отправки на проверку любого трека со сборкой, где есть SDK покупок (закрытый тест уже
ждёт такую сборку — см. «Порядок работ» выше).
**Переписана 28.08 вечером (после лайва Android)** и сохранена кнопкой «Сохранить»: собирает — да;
шифруется при передаче — да; аккаунтов нет, вход внешними аккаунтами — нет; **удаление по запросу —
«Да» (с 28.08 поздно вечером, задача 67)** с URL `https://sivyankof.github.io/arcanum/privacy.html#deletion`
— инструкция вторым абзацем §8 политики на четырёх языках (что удаляется, письмо с номером заказа
магазина, срок 30 дней), `lang.js` ведёт `#deletion` к якорю языка; до 67 стояло «Нет», потому что
«Да» требует URL пошаговой инструкции. Предпросмотр анкеты показывает раздел «Удаление данных» со
ссылкой; изменение сохранено, на проверку уйдёт со следующим «Обзором публикации»; типы — «Финансовые сведения → История покупок»
(собирается, не передаётся, не временно, обязательна, цели «Функции приложения» + «Аналитика») и
«Идентификаторы устройства или другие идентификаторы» (то же, цель «Функции приложения»).
Предпросмотр: «Данные не передаются третьим сторонам», «Данные шифруются при передаче». Те же два
типа объявлены в App Privacy Apple.

### Анкета IARC (заполнена 26.08.2026, статус «Выполнен»)

Категория — «Все другие типы приложений». Ответы даны по тому, что РЕАЛЬНО нарисовано на
78 сканах Райдера–Уэйта, а не по общему ощущению «обучающее приложение»: анкету Google
сверяет с содержимым, и неточность — основание снять приложение. Проверенные факты:

- **Насилие — да**, «подразумеваемое насилие против людей» (Десятка мечей: лежащая фигура
  с мечами в спине; Башня; Смерть). Уточнения: контекст фантастический, реакция изображена
  нереалистично, акт насилия НЕ демонстрируется, крови — «нисколько» (красное на Десятке
  мечей — плащ, не кровь), военной обстановки нет, невинные персонажи не страдают,
  мрачная атмосфера — да.
- **Устрашение — да**, «пугающие элементы», частота «редко» (Дьявол, Смерть, Луна).
- **Нагота — да**, без эротического контекста: человеческие ягодицы (Суд), полностью
  обнажённая женская грудь с сосками (Звезда, Мир, Влюблённые, Дьявол, Суд), гениталии
  (Влюблённые и Дьявол — контур прорисован, проверено увеличением скана). Все три — «редко».
  Ответ «нет эротического/непристойного контекста» — то, что удержало PEGI на 3+.
- **Нет**: азартные игры, ненормативная лексика, вещества (греп по контенту даёт только
  «конкуренция» и «виноградом»), грубый юмор, общение между пользователями,
  интернет-содержимое (ссылки ведут на свои же юридические страницы), продвижение товаров
  с возрастными ограничениями, местоположение, лутбоксы, NFT/денежные вознаграждения.
- **Покупка цифровых товаров — да** (решение Артёма 26.08: премиум будет). Пометка
  «Покупки из приложения» стоит во всех регионах.
- «Главным образом обучающий продукт» — да.

Итог: **PEGI 3, USK 6, ESRB T (13+), IARC Generic 3+, Россия 3+, Бразилия «Для всех»**.
⚠️ Планировалось 12+ — фактический рейтинг оказался мягче, потому что нагота и насилие
поданы без эротики, крови и интерактивности. Номер сертификата IARC появится после отправки
релиза на проверку. Меняешь колоду или добавляешь контент — анкету перезаполнять
(кнопка «Новый опрос» на странице рейтинга).

**Состояние 25.08, ночь**: приложение в консоли создано (`Arcanum — обучение таро`,
package `app.arcanum.tarot`, язык по умолчанию ru-RU, бесплатное, подписание Play принято).
Панель управления показывает четыре шага: внутреннее тестирование, завершение настройки,
закрытое тестирование (0 из 12 участников), заявка на рабочую версию.

## Сборка (EAS Build)

- [x] **Первая Android-сборка прошла 26.08.2026** — профиль `preview` (internal, APK),
      `finished` за 41 минуту, keystore сгенерирован в облаке Expo (локальный `keytool`
      не нужен). Версия 1.0.0, `versionCode` 1, SDK 54.
      APK: `https://expo.dev/artifacts/eas/m-gIAy4Au5ctlm8mBIzcKiJM-7AmWsvCht8CsXRTk2k.apk`
      ⚠️ **Команда — `npx eas-cli@latest build -p android --profile preview`.** Просто
      `npx eas` падает с `could not determine executable to run`: пакет называется `eas-cli`,
      а `eas` — только имя бинарника внутри него. Статус — `npx eas-cli@latest build:list --limit 1`.
- [x] `.easignore` заведён 26.08 — архив сборки 305 МБ → 31.7 МБ. ⚠️ Он **отменяет
      `.gitignore` для EAS целиком**, поэтому секретные шаблоны продублированы в нём;
      страж — `python scripts/check_easignore.py`. ⚠️ 27.08 на первой `production`-сборке
      архив оказался 180 МБ: `.git` shallow-клона (дерево HEAD, 152 МБ) EAS удаляет только по
      явной строке `.git` без слэша — добавлена, страж это проверяет; воспроизведение кодом
      eas-cli — 28.7 МиБ; **подтверждено логом сборки №3 27.08: «Uploading to EAS Build (0 / 28.7 MB)»**.
- [x] APK установлен и проверен вживую — **лайв-проверка Артёма ✓ 27.08, задача 61 закрыта**;
      до неё **27.08 магазинная сборка (`versionCode` 3, installer
      `com.android.vending`, сплиты) поставлена через внутренний трек на эмулятор и прогнана**:
      онбординг, «Сегодня», «Курс» с верными числительными, карта дня; хаптика и резкость —
      лайв-проверкой Артёма. **Можно на эмуляторе, не дожидаясь телефона**:
      сборка идёт под `arm64-v8a`, `armeabi-v7a`, `x86`, `x86_64`, а эмулятор Android —
      x86_64. Эмулятор закрывает то, чего веб-проверка не видит никогда: иконку приложения,
      иконку пуша (спека 55) и русские числительные с датами (тот же Hermes с урезанным ICU).
      Хаптика, нативные тени во всей полноте и резкость на плотном экране — только устройство.
- [x] Android `production` (AAB) — собрана 27.08 (`versionCode` 2, 86.6 МиБ) и **залита во
      внутренний тест 27.08** (выпуск 2 (1.0.0); размер установки по оценке консоли 48.9 МБ);
      тем же днём сборка №3 (`versionCode` 3, без `ACTIVITY_RECOGNITION`, задача 64) — выпуск 3 (1.0.0),
      активен.
      ⚠️ Файл в консоль кладёт Артём руками (перетаскиванием): артефакт EAS отдаётся без CORS,
      `fetch` со страницы консоли его не возьмёт, а загрузка файлов из сессии ограничена 10 МБ.
- [x] iOS `preview` — **ПЕРВАЯ iOS-СБОРКА ПРОШЛА 28.08**: id `23daec5d`, 1.0.0 (1), ad hoc на iPhone
      Артёма, сертификат дистрибуции и профиль созданы EAS в облаке (Apple login в отдельном
      PowerShell), Push Notifications — «No»; EAS сам дописал в `app.json`
      `ITSAppUsesNonExemptEncryption: false` (закоммичено). Установка: страница сборки
      `https://expo.dev/accounts/art9/projects/arcanum/builds/23daec5d-a3b4-4d87-aa93-62c49d8b1821`
      → «Install» в Safari на iPhone. **Лайв-проверка Артёма на iPhone 28.08 ✓** («всё
      работает»; ⚠️ ad hoc-сборке на iOS 16+ нужен Режим разработчика: Настройки →
      Конфиденциальность и безопасность → Режим разработчика → перезагрузка). Пейвол показывает
      «пока нельзя» — ожидаемо: в `preview` нет ключа RevenueCat и продуктов в App Store Connect
      (консольная часть 53б).
      **Вторая iOS `preview`-сборка — 28.08, с ключом RevenueCat** (`EXPO_PUBLIC_RC_IOS_KEY` из
      среды `preview` EAS): id `a320c6dd`, запущена из сессии `--non-interactive` без единого
      вопроса, страница сборки
      `https://expo.dev/accounts/art9/projects/arcanum/builds/a320c6dd-33a7-42c3-96c2-12e00d5ab178`.
      Для живой проверки покупок нужен **Sandbox-тестер** (ASC → Пользователи и доступ → Sandbox),
      сценарий — конец спеки 53б.
      **Android `production` 28.08 вечером — две сборки**: №1 `f12b6183` (без ключа RevenueCat,
      запущена до `env:set` — НЕ заливать) и №2 `6cf48d53` с `EXPO_PUBLIC_RC_ANDROID_KEY` —
      артефакт `https://expo.dev/artifacts/eas/gksH-V_koDt-D6JzquZ7Js65z6PhNmzM7p4VyqbC98s.aab`,
      для внутреннего трека (AAB кладёт Артём руками; до заливки проверены `goog_` в
      `index.android.bundle` и `com.android.vending.BILLING` в манифесте — рецепт AGENTS.md). **Sandbox-тестер заведён 28.08** (Gmail Артёма с суффиксом
      `+sandbox`, страна Грузия; логин и пароль — `Documents\keys\apple-sandbox-tester.txt`, в репо
      их нет). ⚠️ Адрес тестера не может быть Apple ID или его псевдонимом: `@icloud.com`-псевдоним
      отклонён («already have an Apple Account»), `+sandbox@icloud.com` — тоже (для iCloud Apple
      проверяет имя ящика, плюс-адреса не проходят); Gmail с плюсом принят, подтверждать почту
      тестеру не нужно. Аккаунт жены как тестер не годится — это реальный Apple ID. На iPhone:
      Настройки → App Store → Sandbox-аккаунт → войти тестером, только потом покупать в сборке.
      **iPhone Артёма зарегистрирован 28.08** (`eas device:create` → Website →
      QR → профиль на телефоне; UDID `00008120-00067D900C70201E`, команда `52QV87WW73`).
      ⚠️ `eas device:create` и первая iOS-сборка — только в ОТДЕЛЬНОМ окне PowerShell: им нужен
      интерактив (Apple ID + двухфакторка), из сессии и через `!` падают «stdin is not readable».
      Проверка без интерактива: `npx eas-cli@latest device:list --apple-team-id 52QV87WW73`.
      Сборка: `npx eas-cli@latest build --platform ios --profile preview` → Apple login Y →
      сертификат дистрибуции — сгенерировать → устройство — iPhone → Push Notifications key —
      **No** (пуши только локальные, APNs не нужен). Установка: ссылка/QR со страницы сборки
      в Safari на iPhone.

## Магазины: тексты и материалы

- [~] Название ≤30 симв. и подзаголовок iOS ≤30 — **черновики готовы** (задача 57,
      `docs/store-listing.md`): «Arcanum — обучение таро» / `Arcanum — Learn Tarot` /
      `Arcanum — Aprende Tarot` / `Arcanum — Aprenda Tarô`. Финал за Артёмом; вычитка
      редактором и носителями es/pt — впереди (`docs/prompts/57-store-native.md`)
- [~] Описания на 4 ЯЗЫКАХ — RU+EN+ES+PT (короткое 80 симв. Google + полное 4000 +
      промо-текст iOS 170 + «что нового») — **черновики готовы**, там же. RuStore берёт
      ru-набор Google. Лимиты и запрещённые слова держит контракт-тест
      `src/lib/__tests__/storeListing.test.ts` — правишь текст, гони `npm test`
- [~] Ключевые слова iOS (100 симв., на каждый язык свои) — **черновики готовы**, там же.
      ⚠️ Слова из названия и подзаголовка в ключевые НЕ повторять: Apple индексирует их и так,
      дубль тратит лимит впустую (первая редакция черновика теряла так до 4 слов из 12);
      теперь это держит тест
- [~] Скриншоты и графика витрины (задача 63, 26.08): **Google ЗАКРЫТ** — иконка 512×512, баннер
      1024×500, 28 скриншотов телефона (7 экранов × 4 языка: ru/en/es/pt), конвейер
      `scripts/store_assets.py` + `scripts/shoot_63.js`, материалы в `docs/store/`; Артём
      принял кадры, всё **загружено и сохранено в Play Console 26.08** (см. «Заливка витрины:
      как прошла 26.08»). **iOS 6.7"** — тот же конвейер флагом
      `--store apple` (холст 1290×2796), снимается после появления аккаунта Apple; набор
      **снят и ЗАКОММИЧЕН 28.08 (63б)**: 28 кадров 1290×2796 на 4 языках, `docs/store/apple/`
      (11 МБ; страж `store_assets.py verify` и `storeAssets.test.ts` — Apple-набор теперь
      обязателен). Заливка в App Store Connect — следующий шаг 63б. **iPad — не нужен: решение
      Артёма 27.08 — `supportsTablet: false` в `app.json`** до первого сабмита (iPhone-only,
      кадры 2064×2752 не требуются, ревью на iPad не будет); включить можно любым обновлением.
- [~] Возрастной рейтинг: **Google — анкета IARC заполнена и сохранена 26.08.2026**
      (раздел «Анкета IARC» ниже; фактические оценки — PEGI 3, USK 6, ESRB T 13+, IARC 3+,
      Бразилия L). Осталось iOS — те же факты о контенте, ожидаемо 12+ (эзотерика);
      категория: Образование (вторичная — Стиль жизни).
      НЕ «Развлечения» — обучение проходит ревью мягче
- [~] Ответы на анкеты приватности сторов (формулировки — в `docs/store-listing.md`):
      **данные не собираем — всё локально, аналитики в v1 нет** (решение Артёма 22.08).
      Единственное, что уходит наружу, — покупка подписки: чек
      и анонимный идентификатор установки в App Store / Google Play и RevenueCat; декларировать
      как «Purchases / покупки, не связаны с личностью». Появится аналитика — переписать анкеты
      и политику (в приложении И на `site/`) ДО релиза

## Юридическое (обязательно для ревью)

- [x] Дисклеймер в приложении (онбординг шаг 1, мелко + профиль): «для развлекательных
      и образовательных целей; не заменяет профессиональную консультацию».
      Профиль ✓ (задача 12, 14.08); онбординг ✓ (`ob.disclaimer` ×4 языка, задача 54, 23.08)
- [x] **Web-страницы — ГОТОВЫ 23.08 (задача 54, GitHub Pages; лендинг и домен НЕ нужны):**
      `https://sivyankof.github.io/arcanum/privacy.html` · `/terms.html` · `/support.html`,
      четыре языка, исходники в `site/`, деплой — `.github/workflows/pages.yml`.
      **Артёму один раз:** Settings → Pages → Source: **GitHub Actions**.
      Зачем внешние URL, если текст есть в приложении (вопрос Артёма 22.08, проверено по
      гайдлайнам): App Store Connect и Play Console принимают в поля политики и поддержки
      **только URL**, форма сабмита без них не отправляется. Apple 5.1.1(i) — ссылка на политику
      «в метаданных App Store Connect И внутри приложения»; страница подписок Apple — «app and
      App Store metadata must include links to your Terms of Use and Privacy Policy»;
      Google Play — «активный, публичный, не PDF» URL политики в консоли + ссылка или текст
      в приложении. Текст в приложении URL не заменяет (урок задачи 12).
      Куда вставлять при сабмите: App Store Connect → App Privacy → Privacy Policy URL,
      App Information → Support URL, подписка → поля EULA/Privacy; Play Console → Store
      settings → Privacy policy, Store listing → контакты разработчика.
- [x] Контракт «страница = приложение» держит `src/lib/__tests__/site.test.ts`: правка политики
      в приложении без правки `site/` (и наоборот) роняет тест. Меняя одно — менять оба.
      ⚠️ Аналитики в v1 НЕТ (решение Артёма 22.08) — политика это утверждает прямым текстом.
      Появится PostHog — переписать ОБА текста и ответы анкет обоих сторов ДО релиза.
- [ ] Заменить `SUPPORT_EMAIL` в `src/lib/feedback.ts` на отдельный адрес поддержки — сейчас
      там ВРЕМЕННО личный gmail Артёма (решение 14.08, задача 13; Артём заведёт ящик позже,
      решение 22.08). Адрес в приложении и на `site/support.html` обязан совпадать — это
      проверяет `site.test.ts`, поэтому менять сразу в обоих местах
- [x] Лицензии: атрибуция Памелы Колман Смит (блок «О колоде») и tarot-api MIT (блок
      «Источники») — на экране «О приложении» (задача 12, 14.08)

## Технический прогон (testing-strategy §5)

- [ ] Свежая установка: онбординг → карта дня → урок → без крашей; обновление поверх
      предыдущей сборки: history/streak не потерялись (миграция store)
- [ ] Пуши: разрешение спрашивается в правильный момент, приходят по расписанию, не дублируются
- [ ] Обе темы/оба языка на всех экранах; слабый Android — скролл сетки 78 карт без лагов
- [ ] Офлайн-режим: авиарежим → всё работает
- [ ] Все статусы контента ≥ reviewed (ни одного «Текст готовится» в проде!)
- [ ] Кнопка сброса карты дня и прочие dev-инструменты удалены (grep TODO-RELEASE)
- [x] ~~Аналитика: события воронки из master-plan §6 реально отправляются~~ — **в v1 аналитики
      НЕТ** (решение Артёма 22.08, master-plan §4): проверять нечего. Пункт вернётся в v1.1

## Запуск

- [x] **iOS 1.0 отправлена на проверку Apple 28.08 ~23:00** (по «ок» Артёма): заявка из 4 объектов —
  приложение 1.0.0 (3), `premium.year`, `premium.month`, группа `Premium`; статус «Ожидание проверки»,
  релиз версии — вручную. Механика заявки (черновик, подписки со своих страниц, группа обязательна) —
  `lessons.md` §12. Дальше: письмо Apple с вердиктом; вопросы ревью — «Проверка приложения»; после
  «Готово к распространению» нажать релиз руками.
  **29.08 — «Guideline 2.1 — Information Needed»** (задача 69): версия Rejected, подписки Ready for
  Review; Apple просит 8 пунктов + видео с устройства. Notes переписаны и сохранены, Primary
  Language → English (U.S.). Артёму: видео по сценарию `docs/specs/69-apple-review-reply.md` →
  Reply с вложением → Resubmit. ⚠️ Для первой заявки нового приложения Apple теперь просит видео
  и восемь пунктов почти всегда — в следующих проектах класть их в Notes ДО отправки.
  ⚠️ Подписочное приложение: ссылки на Terms of Use и Privacy Policy обязаны быть **и в
  метаданных витрины** (описание или своя EULA в App Information), не только в приложении —
  добавлены в описание на 4 языках 29.08 (`store-listing.md`).
  **29.08 ~16:40 — ответ отправлен, версия 1.0 снова «Ожидание проверки»**: видео + 8 пунктов
  в треде (16:37), затем на странице версии «Обновить данные для проверки» и на странице заявки
  «Повторно отправить» (без первой кнопки вторая серая). Лимит ответа в треде 4000 символов;
  вложение > 10 МБ — через Playwright MCP из папки репо (грабли — спека 69).

- [x] **Google: Alpha-выпуск 6 + Data Safety «удаление — Да» ОПУБЛИКОВАНЫ 28.08** (История проверок:
  запрос №3 от 22:28 — «Опубликовано»; «Обзор публикации» без ожидающих изменений). Пейвол сборки 6
  проверен на эмуляторе 28.08 ~23:10 — тариф называется (спека 53б, конец).
- [ ] **Google Play — программа комиссии 15 % (аналог Apple Small Business)**: уведомление консоли
  28.08 «Пора регистрироваться в программе комиссионного сбора 15 %» — 15 % вместо 30 % на первый
  $1M дохода в год; шаги Google: «Создайте группу аккаунтов и сообщите, есть ли связанные аккаунты
  разработчика» → «Примите Условия использования программы» (кнопка «Управление группой аккаунтов»
  в уведомлении). До регистрации комиссия 30 %. **Попытка 28.08 ~23:40 (по «да» Артёма)**:
  группа аккаунтов `Artsiom Siviankou` (id `4990245650115590227`, основной аккаунт
  `7424080842647468142`) СОЗДАНА; платёжный профиль есть (доход 0 $, способ выплаты не задан —
  решение 66). Баннер «Зарегистрироваться», который обещает справка Google, на странице
  «Связанные аккаунты разработчиков» не появился (проверено через минуту и в `?hl=en`); диалог
  регистрации в DOM есть — открыт программно, условия («Terms of Service for 15% Service Fee Tier»,
  `play.google/additional-service-fee-tier-terms/`) приняты кнопкой → **«Произошла непредвиденная
  ошибка (41CD3004)»**, в сети `accountGroups/…:enrollInServiceFeeProgram` → HTTP 400. Вывод:
  бэкенд считает группу ещё не готовой (или требует чего-то ещё — вероятно, времени после создания
  группы). **Повторить руками через день**: Play Console → Аккаунт разработчика → Связанные аккаунты
  разработчиков → баннер/кнопка «Зарегистрироваться» → «Подтвердить и посмотреть условия» →
  «Принять условия и зарегистрироваться»; статус регистрации показывается внизу той же страницы.
  Если баннера нет и через сутки — вопрос в поддержку Play Console с кодом `41CD3004`.
- [ ] Закрытая бета: TestFlight + внутренний трек Google (20–30 человек из аудитории жены), 1–2 недели
- [ ] Фикс по фидбеку беты → продакшен-релиз: Google Play → App Store → RuStore
- [ ] День запуска: пост в соцсетях жены + просьба об оценках у беты (первые 20 оценок решают)
- [ ] Мониторинг первых 72ч: краши (Sentry/EAS insights), отзывы — отвечать на все

## Закрытое тестирование: как настроено 27.08 (ОДОБРЕНО GOOGLE в тот же день)

**Проверка прошла за ~1.5 часа** (отправлено ~14:30, выпуск 3 (1.0.0) «Доступен выбранным
тестировщикам» с 15:50 27.08; «Обзор публикации» пуст). Ссылка opt-in работает — можно звать
тестировщиков.

Google требует от новых личных аккаунтов **12 тестировщиков, непрерывно подписанных 14 дней**,
прежде чем откроет заявку на рабочую версию (`support.google.com/googleplay/android-developer/answer/14151465`).
Настроено так:

- **Трек** — дефолтный «Закрытое тестирование — Alpha» (новый не создавали).
- **Страны** — все 177 («Выбрать все строки» в списке; счётчик в шапке обновляется только после
  сохранения — до него показывает 0, это не ошибка).
- **Тестировщики** — Google Group `arcanum-testers@googlegroups.com` (переключатель «Группы Google» —
  это ИЛИ-выбор против списков email, оба сразу нельзя; список «Внутренние тестировщики»
  остался только у внутреннего трека). Группа создана Артёмом: «находить/вступать — любой
  в интернете», «писать — только владельцы», «список участников — только владельцы».
  Вступление: `https://groups.google.com/g/arcanum-testers`. Канал отзывов — `arcanum.tarot@icloud.com`.
- **Выпуск** — 3 (1.0.0) из библиотеки (тот же AAB, что во внутреннем тесте), заметки на 4 языках.
- **Ссылка opt-in** для тестировщиков (после вступления в группу): `https://play.google.com/apps/testing/app.arcanum.tarot`.
- **Отправка** — «Обзор публикации» → предварительная проверка (~10 мин, «проблем не найдено») →
  «Отправить 18 изменений на проверку» → статус «Изменения находятся на рассмотрении»
  (27.08, ~14:30). В пакете: витрина на 4 языках, категория, контакты, все декларации
  контента, закрытый выпуск. Срок Google — обычно до 7 дней.

**Reddit закрыт (27.08):** аккаунт Артёма под теневым баном (посты снимаются «фильтрами Reddit»,
модмейл — «You can't message that user»; месяц назад два комментария «Removed by Reddit»);
апелляция отправлена через `reddit.com/appeal`, новый аккаунт не заводить — сочтут обходом бана.

**Платные тестировщики (27.08):** Testers Community (`testerscommunity.com`), план Starter $12 —
15 тестировщиков на 16 дней, гарантия «доступ к продакшену или возврат». Механика: их группа
`testers-community@googlegroups.com` добавлена в «Группы Google» трека Alpha ВТОРЫМ чипом рядом
с нашей (поле принимает несколько групп), изменение отправлено на проверку; в их дашборде —
название, план, opt-in ссылка, иконка `docs/store/icon-512.png`, заметка тестерам (логина нет,
открывать ежедневно). ⚠️ Добавление чужой группы в доступ автоматика сессии сделать не может
(классификатор) — это делает Артём руками. Сравнение: TestMyApps ~$24 без бесплатного режима,
TesterBee сайт не отдаёт страницы. В заявке на продакшен честно: платные + аудитория жены.

**02.09 — отчёты Testers Community пришли** (письмо 21:26, два PDF: `arcanum_feedback.pdf`,
`arcanum_production.pdf`, лежат в Downloads Артёма; тест ещё идёт). Разбор сессии 02.09:
- **Feedback Report — шаблон без тестирования**: ни одного бага, устройства или экрана по имени,
  «performed exceptionally well». Четыре «находки» сверены с кодом: ASO «описание без ключевых
  слов и короткое» — ложь (2461 символ en / 2376 ru, заголовки и буллеты, ключевые слова держит
  `storeListing.test.ts`); «нет онбординга» — ложь (`app/onboarding.tsx`); «нет темы как в системе» —
  **правда** → задача 70; «нет кнопки оценить» — правда → задача 71 (диалог оценки в v2, п. 19
  master-plan). «Обратная связь в приложении» уже есть (письмо в поддержку из настроек, спека 13).
  Решение Артёма 02.09: 70/71 в бэклог, до релиза не делать.
- **Production Access Questionnaire — НЕ КОПИРОВАТЬ**: ответы 3, 4 и 8 утверждают, что по итогам
  теста мы «добавили онбординг, системную тему и кнопку оценки» и «собирали фидбэк опросами» —
  ничего этого не было; ответ 7 «10k–100k установок» завышен против плана (5 000 за 3 месяца);
  вопрос 10 Google задаёт только при повторной заявке. Несовпадение анкеты с приложением —
  известная причина отказа в доступе к продакшену.
- **Честный материал для анкеты (писать своими словами при подаче ~10–12.09)**: набор — платный
  провайдер Testers Community (15 человек) + аудитория жены и знакомые; сбор фидбэка — отчёт
  провайдера + прямые сообщения тестировщиков; **изменения за период теста, все реальные** —
  убрано лишнее разрешение `ACTIVITY_RECOGNITION` (64, versionCode 3), исправлено распознавание
  тарифа подписки на Android (`5a16379`, versionCode 6), инструкция удаления данных + Data Safety
  «удаление — Да» (67), через Alpha прошли выпуски 3 → 5 → 6; готовность — 1931 юнит-тест,
  прогон магазинной сборки на эмуляторе Android 15 (Play Store, сплиты), параллельное ревью
  Apple; ожидание установок — ближайший к плану диапазон, не «10k–100k».

**Что дальше (Артём):** набрать 15–18 человек с Android — аудитория жены, знакомые,
r/AndroidClosedTesting («ты мне — я тебе»). Каждому: вступить в группу → открыть ссылку opt-in →
установить → не удалять 14 дней и открывать приложение. Отсчёт идёт с момента, когда в группе
≥12 подписавшихся одновременно; выпадение одного рвёт непрерывность. После 14 дней — панель →
«Подать заявку на рабочую версию» (три блока вопросов: набор тестировщиков, фидбэк, готовность).

⚠️ Эмуляторный путь для магазинной сборки: `google_apis_playstore` умеет Play Store, но вход
в Google-аккаунт делает только Артём (пароль — не автоматике); после входа ссылка opt-in →
установка → прогон делаются автоматикой через adb.
