# Промт следующей сессии (после 28.08.2026, вечер) — Apple: витрина, ревью, релиз

> Артёму: открой новую сессию Claude Code в папке проекта, переключи режим разрешений в ручной
> (Shift+Tab) ДО работы с консолями и вставь текст ниже целиком (от «Контекст:» и до конца файла).

---

Контекст: сессия 28.08 закрыла консольную часть 53б по Apple (подписки в App Store Connect,
RevenueCat, Sandbox-тестер, лайв покупки на iPhone ✓) и довела Google до заливки AAB (задача 66:
продавец «Беларусь», выплаты ждут несанкционный белорусский счёт). Хроника — `docs/changelog.md`
(запись «53б · Консольная часть»), состояние — CLAUDE.md «Статус», уроки — `docs/lessons.md`
раздел 12 (семь ⚠️ от 28.08 — прочитать перед работой с консолями). Повторно не пересказывать.

## Что сначала спросить у Артёма

1. Залит ли AAB №2 (с ключом `goog_…`) во внутренний трек и создана ли подписка `premium` в Play
   Console (если предыдущая сессия не успела) — и открыта ли карта в несанкционном банке для
   выплат Google (задача 66; BIC сверять по `ohmyswift.io/sanctions/sdn`).
2. Пришёл ли ответ Apple по DSA (кейс `20000149524822`, документ адреса отправлен 28.08).
3. Хвост лайва iOS: истёк ли Sandbox-«год» (пейвол вернулся к тарифам?) и проверена ли ветка
   «Действует до …» месячной покупкой + отменой (Настройки → Разработчик → Sandbox Apple Account).

## Главная задача — Apple до сабмита (порядок)

1. **Витрина в App Store Connect** (хвост 63б): версия 1.0 «Подготовка к отправке» — тексты из
   `docs/store-listing.md` на 4 языках (ru основной + en-US, es-MX, pt-BR; подзаголовок 30, промо
   170, описание 4000, ключевые 100), кадры `docs/store/apple/<lang>/01–07.jpg` (порядок 01→07;
   приём заливки — release-checklist «Заливка витрины: как прошла 26.08»: `input[type=file]`
   появляется после клика «Добавить», файлы берутся `fetch`-ем с raw.githubusercontent), URL
   поддержки/маркетинга/политики из `src/lib/appInfo.ts`, возрастной рейтинг (те же факты, что
   IARC — ожидаемо 12+), категории Образование + Стиль жизни. На проверку НЕ отправлять.
2. **App Privacy** в ASC — переписать: с 53б собираются «История покупок» и «Идентификаторы»
   (RevenueCat как поставщик услуг, не третья сторона); чек-лист — release-checklist, раздел
   про Data Safety/App Privacy; те же изменения — в анкету «Безопасность данных» Play Console.
3. **Подписки → «Информация для проверки»**: у `premium.year` и `premium.month` — скриншот экрана
   покупки (≥ 640×920; кадр пейвола с тарифами с iPhone Артёма или снятый скриптом с
   `mockSetup` из `scripts/shoot_56.js`) и заметка ревьюеру (Sandbox-тестер не указывать — у
   ревью свой).
4. **App Store Connect API key** для `eas submit` (с Windows иначе не залить): Пользователи и
   доступ → Интеграция → API App Store Connect → «Запросить доступ» → ключ (роль App Manager),
   `.p8` — через `download.saveAs` в `Documents\keys` (урок 28.08), затем `eas submit` /
   `eas.json` submit-профиль.
5. **Релизная iOS-сборка** `production` → TestFlight (по желанию) → сабмит версии 1.0 ВМЕСТЕ с
   группой подписок (первая подписка уходит только с новой версией приложения).
6. **Small Business Program** (15 % вместо 30 %) — ASC → Agreements → заявка, до первых продаж.

## Google — если ещё не закрыто

- AAB №2 (с ключом) во внутренний трек кладёт Артём руками (артефакт EAS без CORS, ≤10 МБ из сессии).
- Play Console → Монетизация → Подписки: `premium`, базовые планы `year` (P1Y, $34.99) и `month`
  (P1M, $5.99), без вводных предложений, активировать; тексты подписки — `store-listing.md`,
  раздел «Подписки».
- Лайв Android (сценарий 6в в спеке 53б; лицензионный тестер уже настроен) → анкета
  «Безопасность данных» → слияние ветки/закрытие 53б.
- RTDN (Pub/Sub) для RevenueCat — рекомендация, не блокирует.

## ⚠️ Грабли 28.08 (кратко; полностью — lessons.md, раздел 12)

- Кнопки и списки Google/RevenueCat/Apple — через `browser_run_code_unsafe` (JS `click()` по
  видимому элементу, `[role=menuitem][data-value]` с `force`); форма Google Payments — в
  кросс-доменном iframe, `browser_evaluate` её не видит.
- Скачивания браузера падают в `.playwright-mcp/` репо — ловить `page.waitForEvent('download')`
  и `saveAs` в `Documents\keys`; `browser_file_upload` — только из корня репо.
- В auto-режиме классификатор блокирует часть кликов на сайтах Apple — ручной режим до старта.
