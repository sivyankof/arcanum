# Промт следующей сессии (после 28.08.2026, вечер) — Apple до сабмита + хвост Google

> Артёму: открой новую сессию Claude Code в папке проекта, переключи режим разрешений в ручной
> (Shift+Tab) ДО работы с консолями и вставь текст ниже целиком (от «Контекст:» и до конца файла).
> Сессия Google 28.08 закрыта, браузер Playwright свободен.

---

Контекст: сессия 28.08 закрыла консольную часть 53б по обеим платформам: Apple — подписки в
App Store Connect, RevenueCat, Sandbox-тестер, App Store Server Notifications, лайв покупки на
iPhone ✓; Google — профиль продавца «Беларусь», подписка `premium` (год $34.99 / месяц $5.99,
оба плана активны, переводы en/es/pt), сервисный аккаунт + RevenueCat Play Store, ключ `goog_…`,
AAB №2 (`6cf48d53`, versionCode 5) во внутреннем треке. Не закрыто: живая покупка на Android
(выпуск 5 ещё не разошёлся по внутреннему треку на 28.08 16:45) и белорусский счёт для выплат
Google (задача 66; карта Сбер Банка РБ не годится — банк под санкциями). Хроника —
`docs/changelog.md` («53б · Консольная часть»), состояние — CLAUDE.md «Статус», уроки —
`docs/lessons.md` раздел 12 (семь ⚠️ от 28.08 — прочитать перед консолями). Не пересказывать.

Правила сессии: стейджить файлы поимённо (`git add <путь>`), общие доки править точечно,
перед коммитом `git pull --rebase`; `.env` и `C:\Users\Artem\Documents\keys\*` не трогать —
ключи Apple (`.p8`) сохранять только через `download.saveAs` в `Documents\keys`; кнопки и
списки консолей — через `browser_run_code_unsafe` (урок 28.08).

## Что сначала спросить у Артёма

1. Открыл ли карту в **несанкционном** белорусском банке (BIC сверить по
   `ohmyswift.io/sanctions/sdn`) — тогда вписать её в Play Console → Настройки → Платёжный
   профиль → «Добавить способ оплаты» → банковский перевод (владелец, банк, SWIFT, IBAN `BY…`).
2. Пришёл ли ответ Apple по DSA (кейс `20000149524822`, документ адреса отправлен 28.08).
3. Хвост лайва iOS: истёк ли Sandbox-«год» (пейвол вернулся к тарифам?) и проверена ли ветка
   «Действует до …» месячной покупкой + отменой (Настройки → Разработчик → Sandbox Apple Account).

## Хвост Google — живая покупка на эмуляторе (без телефона)

Эмулятор `arcanum_pixel` с Play Store, аккаунт Артёма = лицензионный тестер (рецепт запуска —
AGENTS.md). Порядок: `adb shell pm uninstall app.arcanum.tarot` → открыть ссылку внутреннего
теста `https://play.google.com/apps/internaltest/4701646545825132337` → «Download test app» →
«Установить» → `dumpsys package app.arcanum.tarot | grep versionCode` обязан дать **5** (на 28.08
магазин ещё отдавал 3 — ждать распространения выпуска). Дальше сценарий 6в спеки 53б: пейвол с
двумя тарифами ценами магазина, покупка месячного тестовой картой (лицензионный тестер: «Test
card, always approves»), панель «Месячная подписка · Продлится …», чип «✦ АКТИВНА», модуль 3,
«Управлять подпиской» → подписки Play → отмена → «Действует до …» (тестовый месяц продлевается
каждые 5 минут, гаснет после 6), удалить → поставить → «Восстановить покупки», авиарежим.
Покупатель и события — RevenueCat → Customers («Show sandbox data»). После лайва — анкета
«Безопасность данных» Play Console (покупки: история покупок, идентификаторы; RevenueCat —
поставщик услуг), чек-лист в release-checklist; RTDN (Pub/Sub) — по желанию.

## Главная задача — Apple до сабмита (порядок)

1. **Витрина в App Store Connect** (хвост 63б): версия 1.0 «Подготовка к отправке» — тексты из
   `docs/store-listing.md` на 4 языках (ru основной + en-US, es-MX, pt-BR; подзаголовок 30, промо
   170, описание 4000, ключевые 100), кадры `docs/store/apple/<lang>/01–07.jpg` (порядок 01→07;
   приём заливки — release-checklist «Заливка витрины: как прошла 26.08»: `input[type=file]`
   появляется после клика «Добавить», файлы берутся `fetch`-ем с raw.githubusercontent), URL
   поддержки/маркетинга/политики из `src/lib/appInfo.ts`, возрастной рейтинг (те же факты, что
   IARC — ожидаемо 12+), категории Образование + Стиль жизни. На проверку НЕ отправлять.
2. **App Privacy** в ASC — переписать: с 53б собираются «История покупок» и «Идентификаторы»
   (RevenueCat как поставщик услуг, не третья сторона); те же изменения — в анкету
   «Безопасность данных» Play Console.
3. **Подписки → «Информация для проверки»**: у `premium.year` и `premium.month` — скриншот экрана
   покупки (≥ 640×920; кадр пейвола с тарифами с iPhone Артёма или снятый скриптом с
   `mockSetup` из `scripts/shoot_56.js`) и заметка ревьюеру (Sandbox-тестер не указывать — у
   ревью свой).
4. **App Store Connect API key** для `eas submit` (с Windows иначе не залить): Пользователи и
   доступ → Интеграция → API App Store Connect → «Запросить доступ» → ключ (роль App Manager),
   `.p8` — через `download.saveAs` в `Documents\keys`, затем submit-профиль в `eas.json`.
5. **Релизная iOS-сборка** `production` → TestFlight (по желанию) → сабмит версии 1.0 ВМЕСТЕ с
   группой подписок (первая подписка уходит только с новой версией приложения).
6. **Small Business Program** (15 % вместо 30 %) — ASC → Agreements → заявка, до первых продаж.

## ⚠️ Грабли 28.08 (кратко; полностью — lessons.md, раздел 12)

- Кнопки и списки Google/RevenueCat/Apple — через `browser_run_code_unsafe` (JS `click()` по
  видимому элементу, `[role=menuitem][data-value]` с `force`); форма Google Payments — в
  кросс-доменном iframe, `browser_evaluate` её не видит; поля цен Play Console принимают запятую.
- Скачивания браузера падают в `.playwright-mcp/` репо — ловить `page.waitForEvent('download')`
  и `saveAs` в `Documents\keys`; `browser_file_upload` — только из корня репо.
- В auto-режиме классификатор блокирует часть кликов на сайтах Apple — ручной режим до старта.
- Apple уравнивает цены после налога от витрины Грузии (США переставлять руками); Sandbox-тестер —
  не Apple ID и не iCloud; на iOS 18 Sandbox-аккаунт — Настройки → Разработчик.
