# Промт следующей сессии (после 26.08.2026, вечер)

> Артёму: открой новую сессию Claude Code в папке проекта и вставь текст ниже целиком
> (от «Контекст:» и до конца файла).

---

> **Обновление 27.08, день:** пункты 1 и 2 ниже СДЕЛАНЫ — ящик `arcanum.tarot@icloud.com`
> (псевдоним iCloud; Google/Outlook регистрацию блокировали), контакты в консоли 11 из 11,
> `production` AAB `versionCode` 2 собран и опубликован во внутреннем треке; `.easignore` разобран
> (архив был 180 МБ из-за `.git` shallow-клона, строка добавлена). Актуальный статус — CLAUDE.md,
> разделы «Статус» и «ЧТО ДАЛЬШЕ»; раздел про лунный баннер 28.08 остаётся в силе.
> Вечером 27.08: задача 64 закрыта (сборка 3 без `ACTIVITY_RECOGNITION`, все декларации заполнены),
> закрытый тест (трек Alpha, группа `arcanum-testers@googlegroups.com`) настроен, 18 изменений отправлены на
> проверку Google — **одобрены в тот же день (15:50)**, закрытый тест активен; ждём набора
> 12+ тестировщиков в группу (Артём публикует пост на Reddit и зовёт аудиторию жены).

Контекст: предыдущая сессия закрыла задачу 63 (витрина в Google Play) и провела первую
Android-сборку EAS. Ниже — что уже сделано, что осталось и в каком порядке идти.
Подробности по каждой строке — в спеках и `docs/changelog.md`, повторно их пересказывать
не нужно, но прочитай перед работой по своей теме.

## ⏰ Первым делом, если сегодня 28.08.2026

Спроси Артёма, что показал утренний баннер лунного пуша. **Там обязано быть «Полнолуние ✦».**
Это отложенное подтверждение задачи 47б, назначенное ещё 22.08; другого случая не будет
до следующего лунного цикла. Ответ запиши в `docs/specs/47b-*.md` и в changelog.
Если день другой — просто пропусти этот раздел.

## Что сделано 26.08 (не переделывать)

- **Задача 63 ЗАКРЫТА.** Витрина залита в Play Console на четырёх языках (ru-RU + en-US /
  es-419 / pt-BR): тексты копией из `docs/store-listing.md`, значок 512×512, свой баннер
  1024×500 и свои 7 скриншотов на каждый язык в порядке 01→07. Сохранено кнопкой «Сохранить»,
  статус страницы «Готово к отправке на проверку» — **на проверку НЕ отправляли**.
  Проверено после перезагрузки: тексты всех четырёх языков побайтово равны `store-listing.md`.
  Грабли заливки — `docs/release-checklist.md`, раздел «Заливка витрины: как прошла 26.08».
- **Категория приложения** в Play Console проставлена: Приложение / Образование.
- **Первая Android-сборка EAS прошла** (задача 61): профиль `preview`, APK 118 МБ,
  версия 1.0.0 / `versionCode` 1, keystore сгенерирован в облаке Expo.
  ⚠️ Команда — **`npx eas-cli@latest build …`**; `npx eas` падает, пакет называется `eas-cli`.
- **`.easignore` заведён**: архив сборки 229.7 МБ → 31.7 МБ. Страж —
  `python scripts/check_easignore.py`. ⚠️ Файл **отменяет `.gitignore` для EAS целиком**,
  поэтому секретные шаблоны продублированы в нём.
- **Эмулятор Android поднят на машине Артёма, APK установлен и проверен.**
  Рецепт запуска — в `AGENTS.md`, раздел про эмулятор. Проверено: приложение ставится
  и запускается, в логах ни одной ошибки, онбординг проходится, дисклеймер на месте,
  переключение языка работает, **русские числительные на Hermes правильные**
  (6 МОДУЛЕЙ / 32 УРОКА / 4 УРОКА / 6 УРОКОВ / 8 КАРТ) — тот класс дефекта, который
  веб-проверка не видит в принципе (hf-02).

Цифры на конец 26.08: тестов **1860 в 48 сьютах**, `tsc` чист, **persist version 11**.

## Что делать, по порядку

### 1. Ящик поддержки (ждёт Артёма, 5 минут — но блокирует два пункта)

Артём заводит ОТДЕЛЬНЫЙ Gmail (не алиас `+`: личный адрес виден в нём прямым текстом,
а Google показывает контакт публично) с пересылкой на личный. Как только он назовёт адрес:

- поменять `SUPPORT_EMAIL` в `src/lib/feedback.ts` **и** в `site/support.html` одним
  коммитом — равенство держит контракт-тест `site.test.ts`;
- вписать его в Play Console → Настройки для Google Play → **Контактная информация**,
  туда же сайт `https://sivyankof.github.io/arcanum/`. Это закроет **11-й пункт из 11**
  в консоли (сейчас закрыты 10). ⚠️ Email там обязателен, и без него не сохраняется
  даже адрес сайта — заполнять только вместе.

### 2. Задача 61 — довести до закрытия

Осталось по её чек-листу:

- лайв-проверка Артёма на настоящем устройстве (хаптика, резкость, жесты — эмулятор
  их не показывает; всё остальное уже проверено на эмуляторе);
- сборка `production` (AAB) для внутреннего теста:
  `npx eas-cli@latest build -p android --profile production`;
- ⚠️ **на этой сборке подтвердить `.easignore`**: в логе должно стоять ~30 МБ вместо
  305 МБ. Пока правило проверено только расчётом — так и записано в спеке 61.
- после этого: release-checklist пункт «EAS Build настроен» → `[x]`.

### 3. Дальше по плану (решение Артёма 26.08: 61 → 53б → закрытый тест)

- **53б** — RevenueCat: аккаунт не заведён. Продукты в Play Console, entitlement `premium`,
  цены. Это архитектурная задача: спеку писать на Fable, реализацию — на Opus.
  ⚠️ Как только RevenueCat войдёт в сборку, **анкету «Безопасность данных» в Play Console
  придётся переписать** — наружу начнёт уходить покупка. Сейчас там «данные не собираются».
- **внутреннее тестирование** — первый трек, без ожидания и ревью, до 100 тестеров.
  Закрытый тест (12 тестеров × 14 дней) обязателен до продакшена — список email собирать заранее.

### 4. Ждёт не нас

- **Apple** — **АКТИВИРОВАН 27.08 20:36** (веб-заявка + PDF документов, одобрение в тот же
  день; 22:03 — доступ в App Store Connect). Теперь порядок — release-checklist п. 1:
  лицензионное соглашение → Team ID → Paid Applications Agreement (налоги + грузинский счёт,
  самый долгий таймер, без него нет подписки 53б на iOS) → приложение в App Store Connect →
  iOS `preview` (`eas device:create`) → **63б** (решение по `supportsTablet`, Apple-кадры) →
  продукты подписки + API key для RevenueCat.
- **Android-телефон** Артём покупает.

## Открытые хвосты, которые легко потерять

- Вычитка носителем строк задачи 54 и строк пейвола 62 (`paywall.soonTitle`/`soonSub`/
  `unavailableText` на es/pt) — следующая волна Cowork.
- Фразы `push.freeze_saved` / `freeze.saved` / `push.moon_*` ждут вычитки.
- Задача 58 (картинка выдаёт ответ в 25 карточных вопросах) — решено НЕ чинить (23.08),
  вернуться только по сигналу телеметрии v1.1.
- Задача 41 — в v2.

## Правила, которые в этой сессии стоили времени

- ⚠️ **Пути Windows в bash пишутся через ПРЯМЫЕ слэши.** `--sdk_root=C:\Android\sdk`
  в MINGW теряет обратные слэши, превращается в `C:Androidsdk` (относительный путь)
  и создаёт каталог в текущей папке — в тот раз 5.4 ГБ легли внутрь репозитория.
- ⚠️ **«Автоматизацией нельзя» проверяй сам.** Вывод прошлой сессии «графику в Play Console
  нельзя залить, в DOM нет `input[type=file]`» был снимком одного состояния: скрытый input
  рождается только после клика «Добавить объекты».
- ⚠️ **Инструмент, отчитавшийся кодом 0, мог не сделать ничего.** `sdkmanager` печатал
  «100% Computing updates» и выходил успешно, не приняв лицензии и не скачав ни байта.
- Полный список — `docs/lessons.md`, разделы 12 и 9.
