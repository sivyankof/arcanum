# Промт следующей сессии (после 28.08.2026, поздно вечером, сессия 67) — сабмит iOS 1.0

> Артёму: открой новую сессию Claude Code в папке проекта, переключи режим разрешений в ручной
> (Shift+Tab) ДО работы с консолями и вставь текст ниже целиком (от «Контекст:» и до конца файла).
> Перед сессией — сам выполни в терминале заливку IPA (см. «Что сначала»): из сессии команда
> заблокирована классификатором разрешений.

---

Контекст: сессия 28.08 поздно вечером (промт `next-session-29-08-submit.md`) закрыла всё, кроме
самого сабмита. Обе `production`-сборки `finished`: iOS **`9d27418f`** (1.0.0, buildNumber 3),
Android **`ca680d94`** (versionCode 6) — обе с коммита `8e9274b`, в них правка `planOf` (`5a16379`)
и `ITSAppUsesNonExemptEncryption=false` (`c115b6a`, вопрос про шифрование ASC задавать не должен).
**Задача 67 закрыта** (`docs/specs/67-data-deletion.md`, коммит `39c11e5`): инструкция удаления
данных RevenueCat — четвёртый абзац `about.dataText` и §8 `privacy.html` на 4 языках, якорь
`https://sivyankof.github.io/arcanum/privacy.html#deletion`, контракт `site.test.ts` по абзацам;
тестов 1931 в 52 сьютах. **Play Console**: Data Safety «удаление — Да» + URL; AAB 6 залит
автоматизацией (`setInputFiles`, урок в `lessons.md` §12) — внутренний трек «Активно 6 (1.0.0)»,
закрытый Alpha-выпуск 6 + Data Safety **«на проверке»** (Google, обычно часы — 27.08 было ~1.5 ч).
Хроника — `docs/changelog.md` («28.08 поздно вечером · 67 …»), состояние — CLAUDE.md «Статус».
Не пересказывать.

Правила сессии: стейджить файлы поимённо (`git add <путь>`), общие доки править точечно,
перед коммитом `git pull --rebase`; `.env` и `C:\Users\Artem\Documents\keys\*` не трогать;
кнопки консолей — через `browser_run_code_unsafe`; в ASC «Сохранить» становится «Сохранено» и
отключается КЛАССОМ `tb-btn--disabled`, `getByText` ловит скрытые `<option>`; Play Console — id
приложения `4975498668598706821`, developer `7424080842647468142`, внутренний трек
`4701646545825132337`, Alpha `4700554711110756815`, страница закрытого тестирования —
`/closed-testing` (без `tracks/`).

## Что сначала спросить у Артёма

1. Выполнил ли он заливку IPA: `! npx eas-cli@latest submit --platform ios --profile production
   --latest` (ascAppId и ключ `V55RK5Z8R9` — в `eas.json`; `--latest` берёт `9d27418f`). Если нет —
   попросить выполнить прямо в сессии через `!` и дождаться «Submitted» + обработки в ASC
   (10–30 мин, письмо «has completed processing»).
2. Пришли ли письма Apple: **Small Business Program** (одобрение) и **DSA** (кейс `20000149524822`).
3. **«Ок» на отправку версии 1.0 на проверку** — без него шаг 2 ниже не делать.
4. Срок «30 дней» в инструкции удаления данных (решение сессии 67) — устраивает? Менять — в четырёх
   `dataText` `i18n.ts` и в §8 `privacy.html` (контракт `site.test.ts` держит равенство).
5. Банк РБ для выплат Google — **не спрашивать**, отложено решением Артёма 28.08.

## Главная задача — сабмит iOS 1.0

1. В ASC (app id `6805994267`) → версия 1.0 → «Сборка» → выбрать **1.0.0 (3)**; если ASC всё же
   спросит про экспортное шифрование — «только стандартное (HTTPS), исключений нет».
2. **Только по «ок»**: «Добавить для проверки» ВМЕСТЕ с группой подписок `Premium`
   (`premium.year`/`premium.month` — первая подписка уходит только с новой версией) → отправить.
   Даблчек 28.08 поздно вечером уже закрыл тип релиза «вручную», «Права на контент», цену
   «Бесплатно» + 175 стран — повторно не сверять. Релиз версии — вручную, не автоматически.
3. Проверить статус «Ожидает проверки» и записать время отправки в changelog/CLAUDE.md.

## Android — хвосты

1. Пейвол versionCode 6 на эмуляторе через внутренний трек (рецепт — AGENTS.md; выпуск 6
   опубликован 28.08 ~22:20, магазин раздаёт ~час): ожидается «Месячная/Годовая подписка» вместо
   «Подписка активна» (`productPlanIdentifier`, `5a16379`). Если стоит APK — сначала
   `adb shell pm uninstall app.arcanum.tarot`.
2. Статус проверки Google (Alpha 6 + Data Safety) — Play Console → «Обзор публикации».
3. Задача 65 (предупреждение о файле деобфускации) — не блокирует, остаётся в бэклоге.

## ⚠️ Грабли сессии 28.08 поздно вечером (кратко; полностью — lessons.md §8 и §12)

- Скрипт мутаций, упавший на «●» в консоли cp1251 между записью мутации и восстановлением,
  оставляет рабочее дерево сломанным — `PYTHONIOENCODING=utf-8`, восстановление в `try/finally`,
  `md5` до/после.
- Play Console: AAB 89 МиБ — `input[type=file]` есть сразу, `setInputFiles` работает; кнопка
  подтверждения отправки на проверку называется «Отправка изменений на проверку»; `eas submit` из
  сессии заблокирован классификатором.
