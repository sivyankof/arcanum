# Промт следующей сессии (после 28.08.2026, поздний вечер) — релизная iOS-сборка и сабмит

> Артёму: открой новую сессию Claude Code в папке проекта, переключи режим разрешений в ручной
> (Shift+Tab) ДО работы с консолями и вставь текст ниже целиком (от «Контекст:» и до конца файла).
> Браузер Playwright свободен; эмулятор `arcanum_pixel` можно не поднимать.

---

Контекст: сессия 28.08 (вечер) закрыла всё «до сабмита» по Apple и лайв Android. **Apple
(ASC, app id `6805994267`)**: витрина версии 1.0 на 4 языках (тексты `store-listing.md`, кадры
1290×2796 в слот 6,9″ в порядке 01→07, URL поддержки/маркетинга, копирайт `2026 Artsiom
Siviankou`), «Информация о приложении» (название/подзаголовок ru/en-US/es-MX/pt-BR, категории
Образование + Образ жизни, рейтинг **13+** по анкете 2025), контакт ревью (+телефон, заметки),
подписки `premium.year`/`premium.month` — кадр пейвола + заметка ревьюеру, **App Privacy
опубликована** (Покупки + Идентификаторы, не связаны с личностью, без трекинга; URL политики на
4 языках), **ASC API key `V55RK5Z8R9`** (Issuer `e719c52a-3121-47eb-8d2a-768a1843d1c0`, `.p8` в
`Documents\keys`, submit-профиль в `eas.json`), **Small Business Program — заявка отправлена**
(ждём письмо). **Google**: лайв покупки на эмуляторе пройден целиком (покупка → «✦ АКТИВНА» →
модуль 3 → отмена → «Действует до …» → истечение → тарифы → переустановка → право вернулось →
авиарежим), анкета Data Safety переписана (История покупок + Идентификаторы, не передаются,
шифруются, удаление — «нет»). Найден и закрыт дефект: на Android план подписки не
распознавался (`productPlanIdentifier`) — коммит `5a16379`, в сборках ≤ versionCode 5 панель
пейвола пишет «Подписка активна» без тарифа. Хроника — `docs/changelog.md`
(«28.08 вечер · Apple до сабмита + лайв Android»), состояние — CLAUDE.md «Статус», уроки —
`docs/lessons.md` раздел 12 (⚠️ от 28.08 вечера). Не пересказывать.

Правила сессии: стейджить файлы поимённо (`git add <путь>`), общие доки править точечно,
перед коммитом `git pull --rebase`; `.env` и `C:\Users\Artem\Documents\keys\*` не трогать;
кнопки консолей — через `browser_run_code_unsafe`; в ASC «Сохранить» становится «Сохранено» и
отключается КЛАССОМ `tb-btn--disabled` (не атрибутом), `getByText` ловит скрытые `<option>`.

## Что сначала спросить у Артёма

1. Пришло ли письмо Apple по **Small Business Program** (одобрение) и по **DSA** (кейс
   `20000149524822`).
2. Открыл ли карту в несанкционном белорусском банке для выплат Google (задача 66; BIC сверить по
   `ohmyswift.io/sanctions/sdn`).
3. Готов ли **сам** запустить интерактивную сборку (нужен ввод в терминале, см. ниже).

## Главная задача — релизная iOS-сборка и сабмит

1. **Сборка `production` для iOS — только интерактивно** (28.08 `--non-interactive` упал:
   «Distribution Certificate is not validated for non-interactive builds»). Артём в строке сессии:
   `! npx eas-cli@latest build --platform ios --profile production` — EAS создаст distribution
   certificate и App Store provisioning profile через Apple ID (вопросы «Generate a new
   certificate?» → Yes). buildNumber уже инкрементирован до 2 неудачной попыткой — станет 3, это
   нормально (`appVersionSource: remote`). Сборка идёт ~15–30 мин; `--no-wait` и ждать по ссылке.
2. **Android — новая сборка тоже нужна**: исправление `planOf` (`5a16379`) не попало в AAB №2
   (versionCode 5). `npx eas-cli@latest build --platform android --profile production
   --non-interactive --no-wait` → AAB versionCode 6 → Артём кладёт руками во внутренний трек
   (и в закрытый Alpha) → на эмуляторе проверить, что панель пейвола называет «Месячная/Годовая
   подписка». Порядок ставим/удаляем — рецепт в AGENTS.md.
3. **Сабмит iOS** — ТОЛЬКО с явного «ок» Артёма: `npx eas-cli@latest submit --platform ios
   --profile production --latest` (ключ из `eas.json`), затем в ASC → версия 1.0 → выбрать сборку
   → «Добавить для проверки» ВМЕСТЕ с группой подписок `Premium` (первая подписка уходит только с
   новой версией) → отправить. До сабмита: «Релиз версии» — вручную (не автоматически),
   проверить, что App Privacy и рейтинг на месте (уже да), TestFlight — по желанию.
4. **Хвост политики** (мелкая задача, 4 языка + контракт `site.test.ts`): добавить в
   `site/privacy.html` раздел «Как запросить удаление данных RevenueCat» (письмо на
   `arcanum.tarot@icloud.com`, что удаляется — история покупок и анонимный идентификатор, срок),
   после чего в Play Console → Data Safety → «Удаление данных: Да» + URL `privacy.html#deletion`.
   Тот же текст — в `dataText` четырёх языков `i18n.ts`.
5. Google: после письма о банке — Play Console → Настройки → Платёжный профиль → банковский
   перевод (владелец, банк, SWIFT, IBAN `BY…`).

## ⚠️ Грабли 28.08 вечера (кратко; полностью — lessons.md, раздел 12)

- ASC-кадры: слот по умолчанию 6,5″ (1242×2688/1284×2778) молча отвергает 1290×2796 — заливать в
  Медиаменеджер → «iPhone 6,9-дюймовый дисплей»; порядок = порядок завершения закачек, лить по
  одному; новая локализация наследует русские кадры («Используется русский») — «Редактировать»
  отвязывает; тексты новой локализации ASC копирует из основного языка — перезаписывать.
- Из страницы ASC `fetch` на raw.githubusercontent запрещён CSP — исходники брать второй вкладкой;
  в Node-контексте `run_code` нет `require` и `fetch`; `setInputFiles` принимает любой путь.
- Телефон в контакте ревью обязателен и держит `aria-invalid` до сохранения; `beforeunload` на
  перезагрузке = несохранённые правки.
- Play: выпуск внутреннего трека расходится ~1 ч (признак — «Есть платный контент» и новый размер
  на странице магазина); тестовая месячная подписка = 5 мин, отмена доезжает до RevenueCat за минуту
  без RTDN; Data Safety «удаление — Да» требует URL с инструкцией.
