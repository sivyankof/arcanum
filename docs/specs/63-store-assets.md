# 63 · Материалы витрины Google Play: иконка, баннер, скриншоты — без телефона

Статус: **спека готова (26.08), ждёт «ок» Артёма**. Заведена 25.08 при разборе «что дальше»
после верификации аккаунта Google: витрина Store listing не сохраняется без баннера, а в репозитории
нет ни одного материала магазина. Порядок: после 62, до закрытого теста. Детальный план —
`docs/plans/63-store-assets.md`.

## Цель

Все графические материалы витрины Google Play делаются из репозитория одной командой, без телефона
и без дизайнера, на четырёх языках, и не могут протухнуть молча: формат и размер каждого файла
держит контракт-тест, а генератор сам перечитывает и проверяет то, что записал (приём спеки 55).

## Требования магазинов (сверено 26.08 с справкой Play Console и App Store Connect)

| Материал | Google Play | App Store (на будущее, 63б) |
|---|---|---|
| Иконка витрины | 512×512, 32-bit PNG, sRGB, ≤ 1024 KB, **полный квадрат без скругления и без тени** — Play сам режет углы и кладёт тень; прозрачность не рекомендована (прозрачные пиксели покажут фон Play) | берётся из сборки (1024×1024 из `app.json`), отдельно не грузится |
| Баннер (feature graphic) | **1024×500**, JPEG или **24-bit PNG без альфы**; обязателен — без него витрина не сохраняется; локализуется по языкам | нет такого |
| Скриншоты телефона | от **2 до 8** на тип устройства; JPEG или 24-bit PNG без альфы; каждая сторона 320–3840 px; **длинная сторона ≤ 2× короткой**; для попадания в подборки — ≥ 4 кадра ≥ 1080 px в 9:16 (**1080×1920**) | 1–10 на размер; **без альфы**; 6.9″ 1320×2868 или 6.7″ **1290×2796**; ⚠️ при `supportsTablet: true` обязательны iPad 13″ 2064×2752 |

⚠️ Наш рабочий вьюпорт 390×844 даёт 2.16:1 — для Google **не годится**. Решение — не резать экран,
а **компоновать кадр**: приложение снимается как есть (390×844 при `deviceScaleFactor: 3` →
1170×2532), а затем кладётся с подписью на холст нужного магазина (Google 1080×1920 = 9:16 ровно;
Apple 1290×2796 — тот же конвейер, другой холст). Один снимок приложения → два магазина.

## Что делаем

### 1. Иконка 512 — `docs/store/icon-512.png`

Из `assets/images/icon.png` (1024×1024 RGB, тёмная синева + золотая звезда-солнце, полный квадрат —
пригоден как есть): уменьшить LANCZOS до 512, сохранить 32-bit PNG (RGBA, все пиксели непрозрачны —
формат по букве требования, прозрачности нет по духу). Скрипт после записи перечитывает файл и падает,
если: размер ≠ 512×512, есть пиксель с альфой < 255, файл > 1024 KB, исходник не квадратный.
Проверено глазами 26.08: лучи иконки не доходят до углов — маска Play (радиус 30 %) их не срежет.

### 2. Баннер 1024×500 — `docs/store/feature/<lang>.png` ×4

Рисуется в HTML (`docs/store/feature.html`) по «Небесному золоту» и снимается Playwright.
Композиция: фон — радиальный градиент `bgTop → bg` тёмной темы (#1b2140 → #0a0c1d, как в приложении),
слева знак (арт иконки, `icon.png` без фона не нужен — квадрат иконки со скруглением 20 % и рамкой
`frame`), справа название **Arcanum** (Cormorant Garamond 600, ~120 px, цвет `head` #f5eacb) и
строка-слоган (Manrope 500, ~34 px, `muted` #848cb2, letterSpacing 1). Слоган — **подзаголовок iOS
из `docs/store-listing.md`** того же языка («Курс, значения карт, расклады» / «Course, card meanings,
spreads» / «Curso, significados y tiradas» / «Curso, significados e tiragens»): текст уже утверждён
задачей 57 и держится тестом лимитов, второй источник правды не заводим. Безопасная зона: всё
значимое — внутри центральных 80 % ширины (Play показывает баннер в нескольких раскладках и
подрезает края). Запрещено: цены, рейтинги, «бесплатно», «новинка», кадры устройства.
Шрифты — из `node_modules/@expo-google-fonts/*` (те же файлы, что грузит приложение), встраиваются
data-URI: file://-страница в Chromium не имеет права грузить file://-шрифт без флага.
Сохраняется PNG; альфа снимается python-скриптом (24-bit), он же проверяет 1024×500 и режим RGB.

### 3. Скриншоты — `docs/store/google/<lang>/<NN>-<screen>.jpg`

**Тема:** тёмная (фирменная: на белой странице Play тёмные кадры контрастны и читаются как «дорого»;
светлые слились бы с фоном выдачи). Светлая тема — **один кадр-витрина в конце** («Светлая тема»),
это функция, её показываем, а не прячем.

**Языки:** четыре набора (ru/en/es/pt). Play показывает графику по локали пользователя, а локали без
своей графики получает набор языка по умолчанию — русский; англоязычный (США — деньги) и
латиноамериканский рынок с русскими кадрами конверсии не дадут. Стоимость четырёх наборов при
автоматизации — только подписи (×4), приложение переключается сидом `lang`.

**Подписи над кадром: да.** Два уровня: заголовок (Cormorant 600, ~72 px, `head`) и подстрока
(Manrope 500, ~34 px, `muted`). Кадр приложения с золотой рамкой `frame` 2 px и скруглением 54 px
(18 × 3) лежит по центру под подписью, целиком, без обрезки и без рисованного корпуса телефона.
Тексты — `docs/store/captions.json` (единственный источник; ru/en пишет сессия по фактам
`store-listing.md`, es/pt — черновик той же сессии, вычитка носителем в волне 57н). Подписи
не повторяют запрещённые слова `storeListing.test.ts` (предсказания, гарантии, лечение).

**Экраны и порядок (7 кадров; первые два — решают):**

| № | id | Экран | Заголовок (ru) / подстрока (ru) | Почему |
|---|---|---|---|---|
| 1 | `today` | «Сегодня», карта дня ОТКРЫТА (Маг), значение, серия, луна | Карта дня / Каждое утро — новая карта и её значение | Лицо приложения и ежедневная петля; самый сильный визуал |
| 2 | `course` | Путь курса: М1–М2 пройдены, М3 текущий | Курс из 32 уроков / 6 модулей, викторины, прогресс | Название витрины — «обучение таро», второй кадр обязан это показать |
| 3 | `detail` | Страница карты «Луна»: скан, чипы, сферы, значение | 78 карт со значениями / Общее, любовь, работа, финансы, здоровье | Причина №1 открыть таро-приложение (справочник) |
| 4 | `spread` | Сохранённый расклад «Три карты», все карты открыты, толкования | Расклады / Три карты, Кельтский крест и лунные | Практика; сохранённый расклад — детерминирован, без тапов по карте |
| 5 | `trainer` | Тренажёр, карточка перевёрнута: значение + три оценки | Тренажёр памяти / Интервальные повторения | Отличие от справочников-гадалок |
| 6 | `moon` | Лунный календарь сентября 2026, события, панель расклада | Лунный календарь / Новолуния и полнолуния | Уникальная функция, красивый экран |
| 7 | `today-light` | Кадр 1 в светлой теме | Светлая тема / И тёмная — на выбор | Показывает вторую тему; дёшево (тот же сид) |

Семь кадров ≥ 4 → квалификация на подборки Play. Сетка «Карты» и профиль с дневником — вне набора
(8-й слот свободен; профиль показывает имя и дату рождения — личные данные в витрине не нужны).

**Карты на кадрах — только без наготы.** Правила Play к графике витрины запрещают сексуальный
контент и наготу; в колоде Райдера–Уэйта нагота есть у Звезды, Солнца, Влюблённых, Дьявола, Суда,
Мира (факты — анкета IARC в `release-checklist.md`). Эти шесть карт **не попадают ни в один кадр**:
карта дня — `magician`, страница — `moon`, расклад — `empress`/`chariot`/`strength`, кадр
тренажёра — любая из `fool`, `magician`, `high-priestess`, `empress`, `emperor`, `hierophant`,
`chariot` (порядок случаен). ⚠️ Это НЕ значит, что колода тренажёра — семь карт: она строится
во время исполнения из карт пройденных уроков (`learnedCardIds`, `src/lib/courseProgress.ts`) и
на сиде задачи реально включает восемь — `lovers` тоже изучена (урок «Повторение» м2л5 тащит все
карты модуля). Из НЕЁ `lovers` исключена не списком, а датой повторения в будущем (сид `srs`,
находка финального ревью 63/3) — карта остаётся в колоде, но не может выпасть в сессию. Список
выше — только то, что физически может лечь на КАДР витрины, а не состав колоды.

**Дата пришпилена:** часы браузера `2026-09-18 10:00` (пятница) — заголовок «ПЯТНИЦА · 18 СЕНТЯБРЯ»,
серия 5 дней с записями дневника 14–18.09, в календаре новолуние 11.09 (прошло) и полнолуние 26.09
(впереди), панель расклада полнолуния с датой. Пересъёмка через месяц даёт те же кадры.

**Сид** — форма `scripts/shoot_56.js` (не сочинять): пройдены М1–М2, `history` с сегодняшней
записью (`magician`, не перевёрнута) и четырьмя прошлыми, `srs` по колоде тренажёра с `due` в
прошлом, `spreadsHistory` с одним сохранённым «Три карты», `streak: 5`, `xp: 400`, профиль с датой
рождения и `birthArcanaId`. ⚠️ `todayDraw()` ищет запись по `localDateISO()` — дата записи считается
от пришпиленных часов в ЛОКАЛЬНОМ поясе, не `toISOString()` (в аудите 56 UTC-срез оставил карту
дня закрытой — на кадре рубашка вместо лица). У каждого кадра — маркер, обязанный быть на экране
(имя карты из `cards.json` на языке кадра, заголовок из `i18n.ts`, видимый `<img>` карты) и
анти-маркер («нажми, чтобы открыть» на кадре 1); нет маркера — FAIL с именем кадра, пустой кадр
в набор не попадает (урок 56: «экран снят» ≠ «экран проверен»).

### 4. Конвейер и защита от протухания

- `scripts/shoot_63.js` (Playwright, рецепт `AGENTS.md`): снимает приложение для каждого
  (язык × экран), компонует через `docs/store/frame.html` (data-URI кадра и шрифтов, подписи из
  `captions.json`), пишет JPEG q92 (альфы у JPEG нет по определению); рендерит `feature.html` ×4.
  Флаги: `--store google|apple` (холст 1080×1920 / 1290×2796), `--lang`, `--only`.
- `scripts/store_assets.py`: `icon` (генерация + самопроверка), `feature` (снять альфу + проверка),
  `verify` (всё: иконка, баннеры, скриншоты по каталогам, `captions.json` полон по языкам).
  Запуск из корня, Python 3.10-совместимо, потоки в UTF-8 (урок 55 про «×» в консоли).
- `src/lib/__tests__/storeAssets.test.ts`: иконка — PNG 512×512 RGBA; баннер — PNG 1024×500
  **colorType 2** (RGB, без альфы) для каждого языка из `LANGS`; скриншоты — для каждого языка
  каталог существует, файлов 2–8, каждый JPEG ровно 1080×1920; `captions.json` — каждый экран
  несёт все языки, заголовок ≤ 25 символов, подстрока ≤ 46 (бюджет строки при выбранных кеглях,
  калибруется замером при реализации); каталог `apple/` проверяется, только если существует.
  Парсеры заголовков JPEG/PNG уже есть в `cardAssets.test.ts` и `notificationIcon.test.ts` —
  выносятся в общий `src/lib/imageHeaders.ts` (правило DRY). ⚠️ Именно в `src/lib/`, а не в
  `__tests__/`: preset `jest-expo` держит дефолтный `testMatch` Jest и считает сьютом ЛЮБОЙ файл
  папки `__tests__/` независимо от суффикса — проверено 26.08, файл ронял `npm test`.
- Кадры и баннеры **коммитятся** (~28 JPEG ≈ 8–10 МБ + 4 PNG + иконка): тест на свежем клоне
  проверяет файлы, а не команду; Apple-набор генерируется тем же скриптом, но не коммитится до 63б.

## Что НЕ делаем

- Не грузим в App Store Connect и не снимаем iPad-кадры — аккаунта Apple нет; конвейер держит
  `--store apple`, вопрос про `supportsTablet` решается до сабмита в Apple (вопрос Артёму).
- Не рисуем корпус телефона, не делаем промо-видео, не делаем баннеры «что нового».
- Не вычитываем подписи es/pt носителем внутри задачи — это волна 57н (Cowork).
- Не трогаем `assets/images/*` и `app.json` (иконка сборки уже верна, спека 42/55).
- RuStore отдельных материалов не получает — берёт ru-набор Google (release-checklist).
- Не заливаем материалы в консоль скриптом — загрузка руками Артёма (~20 минут), список полей
  в плане.

## Критерии приёмки (готово, когда…)

- [x] `python scripts/store_assets.py icon` пишет `docs/store/icon-512.png`, отчёт `[OK] 512×512 RGBA,
      непрозрачных 100 %`; мутация «сохранить с альфой 0 в углу» роняет самопроверку.
- [x] `node scripts/shoot_63.js` создаёт 4 баннера и 28 кадров (7 × 4 языка), каждый кадр прошёл
      маркер; на кадре 1 — лицо карты «Маг» и её значение, не рубашка; на кадре 5 — оборот
      карточки с оценками; мутация «сид без сегодняшней записи» роняет кадр 1 с именем кадра.
- [x] `python scripts/store_assets.py feature` снимает альфу; `verify` зелёный; мутация «баннер
      1024×512» краснеет и в `verify`, и в `storeAssets.test.ts`.
- [x] `npm test` зелёный, `npx tsc --noEmit` чист; `cardAssets`/`notificationIcon` работают через
      общий `imageHeaders.ts`.
- [x] Ни на одном кадре нет карт из списка наготы. Отмечено выполненным 26.08 на ложной посылке —
      проверка списком видела только константы кадров, а не РЕАЛЬНУЮ колоду тренажёра (она строится
      из прогресса курса и на сиде включает `lovers`); закрыто финальным ревью (находка 63/3):
      `lovers` исключена из сессии датой повторения в будущем, а гвард сверяет не константы,
      а то, что реально может попасть в сессию (`REAL_DECK` + `entersSession`).
- [x] Артём посмотрел 7 кадров ru + баннер ru (глазами, на ноутбуке — телефон не нужен) и сказал «ок»
      по композиции и подписям (**26.08: «кадры нравятся»**, замечаний нет); en/es/pt — та же
      композиция, проверяются маркерами.
- [x] Материалы загружены в Play Console (Store listing → графика) и витрина сохранилась без
      ошибок формата; release-checklist: пункт «Скриншоты» → `[~]` с оговоркой про iOS/iPad.
      **26.08: сделано.** Залито на четырёх языках (ru-RU + en-US / es-419 / pt-BR): тексты
      копией из `store-listing.md`, значок, свой баннер и свои 7 скриншотов на язык в порядке
      01→07. Сохранено кнопкой «Сохранить» (решение Артёма), статус страницы «Готово к отправке
      на проверку» — на проверку НЕ отправляли. Проверено после перезагрузки: тексты всех
      четырёх языков побайтово равны `store-listing.md`. Грабли заливки — в
      `docs/release-checklist.md`, раздел «Заливка витрины: как прошла 26.08».

## Отчёт (26.08.2026)

Пять критериев из семи закрыты кодом; два оставшихся — человеческие (просмотр Артёмом и загрузка
в консоль) и в эту задачу не входят. Число коммитов задачи не выписываем числом (дважды
разошлось с реальностью) — актуальное даёт `git log --oneline 3180e54..HEAD`.

**Числа.** `npm test`: **1860 тестов в 48 сьютах** (было 1849/47 до задачи), `npx tsc --noEmit`
чист. Полный прогон конвейера на финальном коде: `node scripts/shoot_63.js` — 28 кадров снято,
0 не снято, 4 баннера; `python scripts/store_assets.py feature` — альфа снята со всех четырёх;
`python scripts/store_assets.py verify` — `[OK]`. Иконка — 512×512 RGBA, непрозрачных 100 %,
126 KB.

**Мутации (все подтверждены прогоном, не предположены).**
- Иконка: пиксель с альфой 0 в углу → `[FAIL] есть прозрачные пиксели (минимальная альфа 0)`,
  код 1; убрано → `[OK]`.
- Кадр 1 без сегодняшней записи в сиде (`.slice(1)` на истории) → `--only today` даёт
  `кадров снято: 0, не снято: 1` с текстом «нет лица карты дня (рубашка?); нет имени «Маг»»;
  то же для `today-light`. Снята → зелёно.
- Карта из списка наготы в сиде (`DETAIL_CARD = 'star'`) → скрипт падает ДО запуска браузера,
  код 1, `Error: карта star из списка наготы — в витрину нельзя`.
- Баннер сохранён в RGBA → красный и в `verify` («режим RGBA, ожидается RGB»), и в
  `storeAssets.test.ts` (`colorType: 6` вместо `2`). Баннер 1024×512 → тот же красный дубль
  по размеру в обеих проверках.
- `check()`/`prepare()` кадра «Курс» бросает исключение (по очереди) → `failed` с текстом
  `ИСКЛЮЧЕНИЕ в prepare()/check(): …` и именем кадра, код выхода 1, а не падение всего скрипта;
  прогон по остальным языкам не прерывается.
- `captions.json` без пары для языка/экрана или без `tagline` языка → падение ДО браузера
  с именем экрана/языка в сообщении, независимо от того, какой `--lang` запрошен.

**Отступления от плана, зафиксированные по ходу (все обоснованы прогоном, не догадкой).**
Общий парсер `jpegSize`/`pngHeader` лёг в `src/lib/imageHeaders.ts`, а не в
`src/lib/__tests__/imageHeaders.ts` — дефолтный `testMatch` Jest матчит ЛЮБОЙ файл внутри
`__tests__/`, не только с суффиксом `test`, и файл ронял `npm test`. `TITLE_MAX`/`SUB_MAX`
заменены с плейсхолдера 26/48 на измеренные 25/46 (рендер `frame.html`, худшие строки —
«Карта дня»/ru и «Интервальные повторения»/ru). Ожидание «баннеры Playwright несут альфу»
не подтвердилось (использованная версия Chromium уже пишет RGB для непрозрачной страницы) —
подкоманда `feature` сохранена как гарантия формата, красный прогон получен мутацией. Кадр
«Курс» пересмотрен: план предписывал снимать на текущем модуле (М3 при сиде «пройдены М1–М2»),
но так кадр показывал пять замков и «0%» вместо обещанного подписью прогресса — переснят со
сбросом скролла к началу пути (модуль 1, 100%, четыре пройденных урока). Подробности и уроки —
`docs/changelog.md` (запись «63») и `docs/lessons.md` (десять находок по темам).

**Что осталось (на момент отчёта).** Показать Артёму 7 кадров ru + баннер ru на ноутбуке;
после правок в `frame.html`/`feature.html`/`captions.json` — обязателен полный повторный прогон
конвейера (все 28 кадров пересоздаются, иначе наборы разъедутся). Загрузить материалы
в Play Console. **Оба пункта закрыты 26.08 — задача 63 закрыта; заливка описана ниже.**

## Финальное ревью (26.08.2026, вторая волна)

Семь находок независимой состязательной проверки, все подтверждены и закрыты кодом.

1. **Подписи таб-бара срезаны (Important).** ⚠️ Первый диагноз был НЕВЕРЕН и стоил лишней
   пересъёмки: решили, что режет кромка вьюпорта (на вебе нижний safe-area inset — 0), раздвинули
   контейнер бара на 34 px — и подписи остались срезанными. Настоящая причина найдена замером DOM:
   элемент подписи вкладки на вебе — блок высотой **ровно 10 px с `overflow: hidden`** при шрифте
   10 px и `line-height: normal`, а реальная строка Manrope занимает ~13.5 px, поэтому низ глифов
   («д», «у», «ф», «g», «j») режет САМ контейнер подписи; до кромки вьюпорта подпись не достаёт
   (кончается на 839 из 844). На устройстве нативный текст так не режется — дефект чисто веб-рендера
   навигации, приложение чинить не надо.
   `fixTabBarSafeArea()` в `shoot_63.js` делает теперь два прохода: находит контейнер бара по
   геометрии (прижат к низу, во всю ширину, высота 20–150 px) и раздвигает его на 34 px
   (`!important`, иначе перетирается атомарным классом при ререндере), а затем снимает ограничение
   с самих подписей (`height: auto`, `overflow: visible`, `line-height: 1.35`), находя их по текстам
   `tabs.*` из `i18n.ts` на языке кадра. Функция самопроверяется: подписей на экране с таб-баром
   обязано быть ровно пять, у каждой `overflow` не `hidden` и высота больше кегля — иначе кадр
   уходит в `failed` с именем, а не снимается обрезанным. На экранах без таб-бара оба прохода
   молча ничего не делают.
2. **Маркер тренажёра слеп (Important).** Первое ключевое слово нарисовано на ОБЕИХ гранях
   FlipCard в направлении toMeaning, поэтому маркер был зелёным без переворота примерно в
   половине прогонов. Маркер заменён на три кнопки оценки — они рисуются только после ответа
   независимо от направления. Заголовок панели «ЗНАЧЕНИЕ» в маркер сознательно НЕ включён:
   подсказка «ВСПОМНИТЕ ЗНАЧЕНИЕ · НАЖМИТЕ» до ответа уже содержит слово «ЗНАЧЕНИЕ»/«MEANING»
   как подстроку — с ним маркер страдал бы той же болезнью.
3. **`lovers` в реальной колоде тренажёра (Important).** Колода строится из карт пройденных
   уроков (`learnedCardIds`), и на сиде задачи она — восемь карт, включая `lovers` (список
   наготы), а не семь из `DECK`. `lovers` исключена из сессии сидом `srs` с датой повторения
   в 2099 году (не новая — запись есть; не просроченная — due не наступил); гвард наготы
   пересчитывает то же множество из `content/course.json` и падает на старте, если карта из
   списка наготы реально может попасть в сессию. Спека и критерий приёмки поправлены (см. выше).
4. **Баннеры en/ru за безопасной зоной (Minor).** Слоган (не заголовок «Arcanum», как
   предполагалось) был самой широкой строкой и выходил за x 102…922. `#tagline` — 34 → 30 px,
   letter-spacing 1 → 0.6, `#text` получил `max-width: 528px` защитой на будущее; `store_assets.py`
   меряет крайние небоновые пиксели каждого баннера и падает при выходе за зону.
5. **Кадр «Светлая тема» не проверял тему (Minor).** Карта дня видна в обеих темах — добавлена
   сверка вычисленного фона ScreenBg с `lightTheme.bg` из `theme.ts` (не хардкод).
6. **`--only` не сверялся со списком экранов (Minor).** Неизвестный id теперь роняет прогон
   с его именем и списком допустимых; заодно код научен списку id через запятую, как обещал
   `AGENTS.md`.
7. **Неверное число коммитов (Minor).** Было «девять» при фактических двенадцати. ⚠️ Правка
   разошлась с реальностью ВТОРОЙ раз (написали 14, следующий же коммит сделал 15) — точное число
   в тексте живёт ровно до следующего коммита. Формулировка заменена на невраньё: актуальное число
   всегда даёт `git log --oneline 3180e54..HEAD`.

Полный повторный прогон на финальном коде (одна связная сессия): `node scripts/shoot_63.js` —
28/28 кадров, 4/4 баннера; `store_assets.py feature` и `verify` — `[OK]`; `npx tsc --noEmit`
чист; `npm test` — 1860/1860 (те же числа, что 26.08 — правки не потребовали новых тестов, кроме
пиксельной проверки внутри `store_assets.py`, у которого юнит-тестов нет по замыслу задачи).
Мутации по каждой находке — красный/зелёный прогон приведены в
`.superpowers/sdd/63-store-assets/final-fixes-report.md`.

## Открытые вопросы Артёму (решить до реализации)

1. Набор и порядок кадров (таблица выше) — подтвердить или переставить; в частности, что первым:
   карта дня (рекомендую) или путь курса.
2. Подписи над кадрами — оставить (рекомендую) или чистые кадры без текста.
3. Тема — тёмная + один светлый кадр (рекомендую) или все тёмные.
4. Языковых наборов четыре (рекомендую) или для закрытого теста хватит ru+en.
5. Слоган баннера = подзаголовок iOS (30) из `store-listing.md` — да/нет; другой текст — какой.
6. Карты на кадрах: Маг (день), Луна (страница), Императрица/Колесница/Сила (расклад) — жена
   может предложить свои из списка «без наготы».
7. Apple, не в 63: `supportsTablet: true` в `app.json` обязывает к iPad-скриншотам 2064×2752 и
   к проверке приложения на iPad ревьюером. Оставить планшет (тогда 63б снимает iPad-кадры
   в широком вьюпорте) или выключить до первого сабмита? **Решено 27.08: выключить** —
   `supportsTablet: false`, 63б снимает только iPhone 6.7" (1290×2796).

## Заливка в Play Console (26.08.2026) — задача закрыта

Витрина заполнена и сохранена на четырёх языках: ru-RU (по умолчанию) + переводы en-US, es-419,
pt-BR. В каждом — название, краткое (80) и полное (4000) описания копией из `docs/store-listing.md`,
значок 512×512, свой баннер 1024×500 и свои 7 скриншотов телефона в порядке 01→07. Значок задан
на ru и наследуется переводами сознательно: картинка одна на все языки. Статус страницы —
«Готово к отправке на проверку»; на проверку НЕ отправляли, настройки аккаунта и цены не трогали.

**Проверка.** После сохранения страница перезагружена; по каждому языку три текстовых поля
сверены с `store-listing.md` побайтово (исходник тянулся `fetch`-ем с raw.githubusercontent
и разбирался по заголовкам `### <язык> · <поле> (<лимит>)`) — 4 из 4 сошлись. Кадры и баннеры
каждого языка проверены глазами: подписи на нужном языке, порядок 01→07.

**Механика и грабли** (полностью — `docs/release-checklist.md`, раздел «Заливка витрины: как
прошла 26.08»; уроки — `docs/lessons.md`, раздел 12): скрытый `input[type=file]` появляется
только после клика «Добавить объекты»; файлы берутся `fetch`-ем с raw.githubusercontent
(локальный сервер на 127.0.0.1 из https-страницы висит вечно, синтетический `drop` не работает);
порядок скриншотов = порядок ВЫДЕЛЕНИЯ в библиотеке, а не закачки; консоль дедуплицирует объекты
по содержимому, поэтому файлы разных языков грузились под разными именами; программная
подстановка текста не снимает красную ошибку валидации — нужно одно настоящее нажатие клавиши;
«Сохранить» падает, пока хотя бы один добавленный язык пуст.

## Заливка в App Store Connect (28.08.2026, вечер) — 63б закрыта

Версия 1.0 (app id `6805994267`): на четырёх языках (русский основной + английский (США),
испанский (Мексика), португальский (Бразилия)) — промо-текст, описание, ключевые слова, URL
поддержки и маркетинга копией из `docs/store-listing.md`; по 7 кадров 1290×2796 на язык в порядке
01→07. Название и подзаголовок — в «Информации о приложении» (тот же источник).

⚠️ **Слот на странице версии — 6,5″** (1242×2688 / 1284×2778): наши 1290×2796 он отвергает
(«Неверные размеры…»). Слот для 6,7/6,9″ (1260×2736, 1320×2868, **1290×2796**) — только в
«Медиаменеджере» (`…/version/inflight/media-manager/iphone`), секция «iPhone 6,9-дюймовый дисплей»;
после заливки страница версии показывает «Используется 6,9-дюймовый дисплей».
⚠️ **Порядок кадров = порядок завершения закачек**, как у Google: семь файлов разом дали
01, 06, 04, 03, 05, 07, 02. Лечится «Удалить все» + заливка по одному с ожиданием «N из 10».
⚠️ **Новая локализация наследует медиа основного языка** («Используется русский 6,9-дюймовый
дисплей») и приходит с русскими текстами — тексты перезаписать, кадры отвязать кнопкой
«Редактировать» в Медиаменеджере, затем лить свои. Файлы берутся `setInputFiles` с любого локального
пути (CSP страницы ASC режет `fetch` на raw.githubusercontent — тексты тянутся второй вкладкой).
Проверка после перезагрузки: порядок по `img.src` (в нём имя файла) — 4 из 4 языков сошлись.
