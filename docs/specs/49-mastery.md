# Спека 49 — уровни мастерства карты (чип на странице карты + полоски в справочнике)

Статус: спека на согласовании. Этап 4 (master-plan §3.1 п. 13), хвост (а) задачи 46.
Ветка `feat/49-mastery`. Детальный план — `docs/plans/49-mastery.md` (задача идёт в ветке).
Реализация — на Opus (решение Артёма 21.08; спека и план — Fable).

## Решения

Артём, 20.08 (вопросы 14–16 по дорисовкам) и 21.08 (сегменты):

1. **Ступени по интервалу SM-2**, пороги как в макете: НОВАЯ (нет записи) · ЗНАКОМАЯ (1–5 дней) ·
   УВЕРЕННАЯ (6–20) · МАСТЕР (21+).
2. **Показ в двух местах**: чип на странице карты + полоски в ячейке справочника.
   Полоски можно снять на лайве, если сетка окажется перегруженной.
3. **В тренажёре после оценки ступень НЕ показывать** (кандидат на потом: только при росте ступени).
4. **Сегменты 1/2/3/4**: НОВАЯ подсвечивает 1 сегмент из 4, ЗНАКОМАЯ — 2, УВЕРЕННАЯ — 3,
   МАСТЕР — 4. Демо макета (0/1/3/4 у чипа, произвольные значения у полосок) приводится
   к этому правилу — расхождение макета со спекой решено по 6а-0, макет правится.

## Цель

У изученной карты видно, насколько прочно она сидит в памяти. Данные уже есть — `srs[cardId]`
из тренажёра (задача 45); задача только ВЫВОДИТ их: чистая функция + два места показа.
Ничего не хранится, persist version 10 НЕ меняется.

## Что делаем

### А. Чистый модуль `src/lib/mastery.ts` (+ тесты)

```ts
export type MasteryLevel = 1 | 2 | 3 | 4;
/** i18n-суффиксы ступеней по уровню: mastery.new / familiar / confident / master */
export const MASTERY_KEYS: Record<MasteryLevel, string>;
/** Ступень ИЗУЧЕННОЙ карты (изученность проверяет вызывающий — learnedCardIds). */
export function masteryLevel(s: SrsState | undefined): MasteryLevel;
```

Правило: записи нет → **1** (НОВАЯ — изучена уроком, ещё не повторялась);
`intervalDays` 0–5 → **2** (ЗНАКОМАЯ); 6–20 → **3** (УВЕРЕННАЯ); 21+ → **4** (МАСТЕР).

Два осознанных решения:

- **Порог «1–5» расширен нулём.** «Не помню» сбрасывает `intervalDays` в 0 (srs.ts) — без нуля
  карта после провала выпадала бы из шкалы вовсе.
- **Ступень падает при провале** — МАСТЕР после «не помню» становится ЗНАКОМАЯ, не НОВАЯ
  (запись есть, карта в работе) и не остаётся МАСТЕРОМ (SM-2 честно говорит: знание потеряно).

Изученность функция НЕ проверяет: у обоих экранов множество `learnedCardIds` уже есть,
а тащить в чистый модуль курс и прогресс — лишняя связность. Неизученная карта не имеет
ступени по определению — вызывающий не зовёт функцию для неё.

Юнит-тесты на все границы: `undefined` → 1; `intervalDays` 0, 1, 5 → 2; 6, 20 → 3;
21, 365 → 4 (кейс на каждую границу, а не «по одному на ступень»).

### Б. Чип `src/components/MasteryChip.tsx` + страница карты

Компонент по `.mastery` макета: 4 точки 4×4 (radius 2, зазор 2) + ярлык ступени.
Геометрия: inline-flex, gap 5, границa 1px, radius 12, паддинг 3/9; ярлык 8.5/ls 1.5/700 капсом.
Точки цветом текста ступени: включённая — opacity 1, выключенная — 0.3; включено ровно
`level` точек (решение 4).

Цвета по ступеням (токены из `theme.ts`, хардкод запрещён):

| Ступень | текст | граница | фон |
|---|---|---|---|
| 1 НОВАЯ | `muted` | `line` | `panel` |
| 2–3 ЗНАКОМАЯ/УВЕРЕННАЯ | `accent` | `frame` | `chipBg` |
| 4 МАСТЕР | `success` | `success` | `successBg` (новый токен) |

Новый токен `successBg = 'rgba(90,160,126,0.12)'` в ОБЕИХ темах (`rgba(90,160,126,.12)`
из макета `.mastery.m4`); контраст ярлыка на светлой теме замерить программно (правило
задач 25/48), при провале нормы — решение с Артёмом по прецеденту 48.

Страница карты (`app/card/[id].tsx`): чип отдельной строкой МЕЖДУ overline «0 · СТАРШИЙ АРКАН»
и строкой названия, отступ сверху 6 (позиция макета, строка 872). Показывается ТОЛЬКО
изученной карте: `learnedCardIds(course, lessonsProgress)` (мемоизировать, как в справочнике) +
`srs` из стора → `masteryLevel(srs[id])`. У неизученной строки нет совсем.

### В. Полоски в ячейке справочника (`CardCell`)

Новый проп `mastery?: MasteryLevel`. Полоски `.mbar` макета: ряд из 4 полосок 8×2
(radius 1, зазор 2), по центру, **между миниатюрой и подписью** (отступ сверху 4).
Включённая полоска — `accent`, opacity 1; выключенная — `muted`, opacity 0.28;
включено `mastery` штук. Проп не задан — полосок нет (высота ячейки без них не меняется).

⚠️ Расхождение с демо макета — осознанное (6а-0): CSS-комментарий макета говорит «полоска
под миниатюрой», а демо-скрипт кладёт её `appendChild`-ом В КОНЕЦ ячейки, под подпись, —
из-за переменной высоты подписей (1–2 строки) полоски в ряду прыгали бы по высоте.
Реализуем по комментарию (под миниатюрой — ровная линия по ряду), демо макета правим так же.

Справочник (`app/(tabs)/cards.tsx`): в рендере ячейки
`mastery={learned.has(c.id) ? masteryLevel(srs[c.id]) : undefined}` — множество `learned`
уже вычисляется для бейджа и приглушения, добавляется только подписка на `srs`.

### Г. i18n — 4 ярлыка × 4 языка

Ключи `mastery.new` / `mastery.familiar` / `mastery.confident` / `mastery.master`, строки
капсом (как `cards.learned` = «ИЗУЧЕНО ✓»):

| | ru | en | es | pt |
|---|---|---|---|---|
| new | НОВАЯ | NEW | NUEVA | NOVA |
| familiar | ЗНАКОМАЯ | FAMILIAR | CONOCIDA | CONHECIDA |
| confident | УВЕРЕННАЯ | CONFIDENT | SEGURA | SEGURA |
| master | МАСТЕР | MASTER | MAESTRA | MESTRA |

Род — женский, согласован со словом «карта» (carta/carta — ж.р. в es/pt; правило безличного
рода читателя не затронуто: ярлык описывает карту, не пользователя). Es/pt пишутся здесь же
(4 коротких UI-слова), помечаются на вычитку Cowork при следующей сессии — прецедент фраз
пушей (`push.freeze_saved`); хвост в бэклог.

### Д. DEV-строка «Мастерство: рассадить ступени» (настройки, `__DEV__`)

Без неё ступени 3–4 недостижимы на лайве раньше чем через 6/21 день. Экшен раскладывает
изученным картам `srs`-записи по кругу: 1-я карта — без записи (НОВАЯ), 2-я — `intervalDays 1`,
3-я — `6`, 4-я — `21`, дальше повтор. `due` — завтра и позже, чтобы очередь тренажёра
не наводнялась; `reps`/`ease` — согласованные правдоподобные значения. Прецеденты:
`devSkipYesterday` (задача 10), DEV-строки повторения (45а).

### Е. Макет и документы

- `docs/design-reference.html`: демо-массив `MASTERY` → dots 1/2/3/4; чип-пример на странице
  карты — точки по тому же правилу; демо полосок — значения 1–4 и вставка ПЕРЕД подписью.
- `docs/design-system.md` §5: блок «Уровни мастерства» (чип, полоски, токен `successBg`,
  правило сегментов).
- `docs/backlog.md`: запись задачи 49 (сейчас это хвост (а) в записи 46), хвост «вычитка
  ярлыков es/pt Cowork»; `CLAUDE.md` — статус по закрытии.

## Что НЕ делаем

- Ступень в тренажёре после оценки (решение 16; кандидат на потом — только при росте).
- Анимация смены ступени, история ступеней, персист чего-либо (persist остаётся 10).
- Ступени у неизученных карт (нет ни чипа, ни полосок — даже «НОВАЯ»).
- Фильтр/сортировка справочника по ступени.
- Изменения `srs.ts`/`review.ts` — только чтение.

## Критерии приёмки («готово, когда…»)

- [ ] `masteryLevel` покрыта тестами по всем границам (0/1/5/6/20/21, нет записи) — все зелёные.
- [ ] Страница ИЗУЧЕННОЙ карты показывает чип своей ступени между overline и названием;
      у НЕизученной чипа нет (обе темы, скриншоты 390×844 в `docs/screenshots/49/`).
- [ ] В справочнике у изученных карт полоски 1–4 по ступени, у неизученных полосок нет;
      полоски в ряду на одной высоте.
- [ ] Число подсвеченных сегментов = номер ступени в ОБОИХ местах (одна функция, один источник).
- [ ] Провал в тренажёре опускает ступень МАСТЕР → ЗНАКОМАЯ (юнит + ручная проверка
      через DEV-строки).
- [ ] Ярлыки локализованы на 4 языка, чип не переполняется самым длинным («CONHECIDA»,
      «УВЕРЕННАЯ») — проверка на es/pt скриншотом.
- [ ] Контраст ярлыка чипа всех четырёх ступеней замерен программно в обеих темах
      (формула WCAG, правило 25/48); ниже 4.5:1 — флаг Артёму, не молчаливый фикс.
- [ ] Консоль веб-прогона чистая; `npx tsc --noEmit` чистый; `npm test` зелёный.
- [ ] Веб-проверка сценарием, который сначала ПОКРАСНЕЛ на сломанном коде (правило AGENTS.md).
- [ ] Лайв-проверка Артёма на iPhone (6в) — финал; там же решение «полоски остаются/снимаются».

## План по файлам (кратко; детальный — `docs/plans/49-mastery.md`)

1. `src/lib/mastery.ts` + `src/lib/__tests__/mastery.test.ts` — модуль и тесты.
2. `src/theme/theme.ts` — токен `successBg` (обе темы); `docs/design-system.md` §5.
3. `src/components/MasteryChip.tsx` — чип.
4. `app/card/[id].tsx` — чип на странице карты (learned + srs из стора).
5. `src/components/CardCell.tsx` — проп `mastery` + полоски; `app/(tabs)/cards.tsx` — передача.
6. `src/lib/i18n.ts` — 16 строк (4 ключа × 4 языка).
7. `src/store/useApp.ts` + `app/settings.tsx` — DEV-экшен и строка «рассадить ступени».
8. `docs/design-reference.html` — правка демо под правило 1/2/3/4.
9. Веб-проверка 6а/6б → фиксы → синхронизация доков → лайв-проверка.

`npm install` не нужен: ни одного нового пакета.

## Риски и ловушки для имплементера

- **Подписка на `srs` в справочнике** — новая зависимость рендера сетки: каждая оценка
  в тренажёре меняет объект `srs`. Сетка и так перерисовывается по `lessonsProgress`
  (бейджи), но проверить, что `FlatList` не дёргается на каждый тик тренажёра
  (селектор — сам объект, мемоизация рядов уже есть через `toRows`).
- **Чип в строку между overline и названием** — на странице карты overline и название лежат
  в `hx` рядом с героем; чип не должен растягиваться на всю ширину колонки
  (`alignSelf: 'flex-start'` или обёртка-строка — как ляжет по вёрстке).
- **`success` в светлой теме**: токен подбирался под тёмную; на `successBg` в светлой контраст
  ярлыка может не пройти норму — мерить, не оценивать на глаз (урок 25).
- **Полоски и `expo-image`**: между миниатюрой и подписью уже живут скелетон и бейдж
  (абсолютные, внутри `imWrap`) — полоски кладутся ВНЕ `imWrap`, следующим элементом,
  их эти слои не задевают.
- **DEV-экшен пишет в `srs`** — не забыть `due` в будущее, иначе после «рассадки» очередь
  тренажёра покажет 78 карт к повторению и сломает ручную проверку соседней фичи.
- **Веб-прогон**: сид стора через `localStorage` (`goto → evaluate → reload`, НЕ `addInitScript`);
  ярлыки вкладок и маршруты — из исходников, не из памяти (урок 28а).

## Отчёт веб-проверки 6а/6б (21.08)

Прогон четырьмя независимыми срезами + изолированный красный прогон. Dev-сервер поднимался
ЗАНОВО с `--clear`: на порту висел экземпляр, запущенный 19.08, а у работающего сервера своя
закешированная карта модулей — новых файлов `mastery.ts`/`MasteryChip.tsx` он бы не увидел.

**Результат: 32 проверки пройдены, 1 провалена** (контраст — вынесен Артёму, см. ниже).
`npx tsc --noEmit` чистый, `npm test` — **1475 тестов в 38 сьютах** (было 1469 в 37; +6 —
новый сьют `mastery`). Консоль браузера чистая на всех экранах: единственное предупреждение —
`props.pointerEvents is deprecated` из `@react-navigation/elements`, оно не наше (спека 39).

**Функционал (16/16).** Справочник: `fool` 1 подсвеченная полоска из 4, `magician` 2,
`high-priestess` 3, `empress` 4; у неизученного `emperor` полосок 0 при НАЙДЕННОЙ ячейке.
Страницы карт: ярлыки НОВАЯ/ЗНАКОМАЯ/УВЕРЕННАЯ/МАСТЕР с 1/2/3/4 точками, строки сверены
с `i18n.ts` грепом, а не по памяти. Позиция чипа замером координат: overline.bottom 77.5 →
chip 83.5–103.5 → title.top 107.5, то есть строго между ними на всех четырёх картах.

**Прокликивание (6/6).** DEV-рассадка раскладывает ступени по кругу 1→2→3→4 (до клика все
были 1 — то есть кнопка меняет состояние, а не совпадает с сидом); очередь тренажёра после
рассадки «Ждут сегодня: 0» — `due` действительно в будущем. Главное: **падение ступени
подтверждено вживую** — `empress` с `intervalDays 21` и просроченным `due` попала в сессию,
оценка «Не помню» → чип стал ЗНАКОМАЯ (2 точки), полоски в справочнике тоже 2/4 (чип и сетка
согласованы одной функцией). «Сбросить повторение» → у всех изученных ровно 1 полоска.

**Скриншоты и сверка с макетом (6/7).** 8 снимков 390×844 в `docs/screenshots/49/`
(`innerWidth` проверялся перед каждым — иначе снимок недостоверен, урок задачи 16).
Геометрия совпала с макетом ЧИСЛАМИ: чип border 1, radius 12, паддинг 3/9, кегль 8.5,
ls 1.5; точки 4×4 r2 зазор 2; полоски 8×2 r1 зазор 2, отступ 4, по центру.
`font-weight` в вебе показывает 400, а не 700 — это НЕ дефект: `Txt` транслирует вес
в отдельное семейство шрифта (Manrope Bold), а не в CSS-свойство (задокументировано в 45б).
Переполнения нет: самый длинный ярлык корпуса — pt `CONHECIDA` (110.3px) против ширины
колонки 198px, высота чипа 20px во всех пяти замерах (перенос дал бы 32–34).
Полоски на одной высоте подтверждены на ряду, где у одной карты подпись в две строки
(`p08` «Восьмёрка Пентаклей» — единственное двухстрочное название колоды при этой ширине):
`mbarTop` = 263px у всех трёх ячеек ряда. Это и есть выигрыш от решения «полоски ВЫШЕ подписи».

**Красный прогон — сценарий честный, не декорация.** База 16/16 зелёных.
Мутация A (`intervalDays <= 5` → `<= 20`) уронила ровно 2 проверки, обе про `high-priestess`
(единственная карта с интервалом в диапазоне 6–20): полоски 2 вместо 3, ярлык ЗНАКОМАЯ
вместо УВЕРЕННАЯ. Мутация B (`i <= mastery` → `i < mastery`) уронила ровно 4 проверки
справочника со сдвигом на единицу вниз, включая обнуление у ступени 1 (`fool` 0 из 4);
проверки страницы карты НЕ задеты — мутация локальна в `CardCell`, а чип рисует другой
компонент, и это верно отражает их раздельность. Обе мутации откачены, `git diff` по обоим
файлам пустой.

### Провал приёмки: контраст ярлыка чипа (решение за Артёмом)

Спека требует мерить программно и НЕ чинить молча. Замерено; числа ниже перепроверены мной
лично **по пикселям скриншота** (а не только формулой по токенам), потому что два агента
разошлись: один композитил полупрозрачный фон чипа на градиент `ScreenBg` в фактической
позиции чипа, другой — на плоский `theme.bg`, и получил завышенные 5.14 вместо 4.44.
Прав первый: чип стоит на y≈93 из 844, то есть в верхней, СВЕТЛОЙ части градиента.
Правило общее: **полупрозрачный фон раскладывают на то, что под ним в этой точке экрана,
а не на «фон экрана вообще»** — иначе замер systematically завышает контраст в тёмной теме.

| Ступень | Пара токенов | Тёмная | Светлая |
|---|---|---|---|
| 1 НОВАЯ | `muted` / `panel` | 4.55 ✓ | 5.33 ✓ |
| 2–3 ЗНАКОМАЯ, УВЕРЕННАЯ | `accent` / `chipBg` | 8.26 ✓ | **3.17 ✗** |
| 4 МАСТЕР | `success` / `successBg` | **4.44 ✗** | **3.00 ✗** |

Норма 4.5:1 (кегль 8.5 — исключение для крупного текста не применяется).

Важно для решения: **пары разного возраста.** `success`/`successBg` заведена ЭТОЙ задачей —
её провал наш. `accent`/`chipBg` существует с задачи 09: ровно ею красится чип
«✦ ВАШ АРКАН РОЖДЕНИЯ», стоящий на той же странице строкой ниже (замер того чипа дал те же
2.97–2.99). Это тот самый открытый вопрос, что записан после задачи 48 про мелкие золотые
подписи на светлой теме, — он просто впервые замерен точно.

Кандидаты, подобранные подбором светлоты до пересечения нормы:

- тёмная МАСТЕР: `success` #5aa07e → **#5ba280** (4.55:1) — светлее на 0.6 % L, глазом неразличимо;
- светлая МАСТЕР: `success` #4d9370 → **#3c7358** (4.53:1) — темнее на 9.6 % L, заметно;
- светлая золотая пара (вне периметра 49): `accent` #a8802e → **#896825** (4.51:1).
  ⚠️ Уточнение к записи в CLAUDE.md: `accent2` светлой темы (#8a6a24) даёт **4.40**, а не 4.35
  и норму НЕ берёт — предложенный тогда вариант «на светлой красить accent2» не работает.

Рекомендация сессии была: тёмную МАСТЕР поправить сразу (пара новая, правка неразличима),
светлую решать одной задачей на все мелкие подписи.

**РЕШЕНИЕ АРТЁМА 21.08 (после лайв-проверки на iPhone): оставить как есть, «вроде норм
смотрится».** Цвета не менялись ни в одной теме. Это осознанное решение, а не недосмотр:
замеры выше сделаны, показаны и отклонены — на устройстве ярлык читается. Вся тройка
провалов (Overline+`muted` 1.84, `accent`/`chipBg` 3.17, `success`/`successBg` 3.00/4.44)
вынесена в отдельную **задачу 50** и чинится одним заходом во всех местах сразу (урок 48:
точечная правка одного места делает интерфейс разнобойным). ⚠️ Не заводить это снова как
новую находку: числа известны, решение принято, адрес — задача 50.

### Флаг 6а-0: расхождение внутри самого макета (не дефект приложения)

В `design-reference.html` переменная `--done` тёмной темы = #3d7a5c, тогда как
`design-system.md` и код объявляют `success` = #5aa07e. Правило `.mastery.m4` красит текст
`var(--done)`, а фон задаёт литералом `rgba(90,160,126,.12)` — то есть макет на этом узле
противоречит сам себе. Приложение самосогласовано и следует спеке (токен `success`), поэтому
НЕ правилось. Расхождение старше задачи 49 и затрагивает ещё четыре места макета (узел
пройденного урока, кнопки оценки, карточка повторения, шаг викторины), а в одном месте
(строка 996) кто-то уже перекрыл его вручную на #5aa07e — записано хвостом в бэклог.

### Чего веб-проверка не видит (перечислено, а не умолчано)

Хаптику, системные тени, реальные шрифты и резкость — судья только пункт 6в. Направление
флеш-карты в прогоне падения ступени выпало `toMeaning`; ветка `toCard` не пройдена, хотя
`masteryLevel` от направления не зависит. Ступени намеренно не показываются в тренажёре
(решение 16), поэтому там не проверялись.
