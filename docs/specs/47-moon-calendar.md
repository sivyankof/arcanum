# Спека 47 — лунный календарь: фазы месяца, новолуние и полнолуние как события (этап 4 плана)

Статус: спека написана 19.08 (брейншторм: вопросы 8–10 бэклога и вопрос точности решены Артёмом
по рекомендациям Claude 19.08, остальные развилки — ниже в таблице «Решения»), ждёт «ок» Артёма
по тексту. **ЗАКРЫТА 20.08**: веб-проверка 6а/6б (35/35) и лайв-проверка Артёма на iPhone пройдены ✓,
отчёт — в конце файла. Реализация — сессия на Opus 5 по детальному плану `docs/plans/47-moon-calendar.md`
(код по шагам отдаётся sonnet-сабагентам), ветка `feat/47-moon-calendar`, веб-проверка 6а/6б
и лайв-проверка Артёма на iPhone — как у любой UI-задачи.

Контекст: п. 11 master-plan §3.1-Р («Лунный календарь — новолуние/полнолуние как события:
специальные расклады лунного цикла, тематические пуши. 2–4 „праздника“ в месяц»), этап 4 плана,
третья задача этапа после флеш-карт (45) и коллекции (46). Макет — `v-moon` в
`design-reference.html` (Cowork, 15.08), нарисован «простейшим вариантом»: шапка месяца, строка
луны, сетка месяца с глифами событий, две строки событий. Вход со строки луны на «Сегодня»
в макете НЕ нарисован (к экрану ведёт демобар; кнопка «назад» подписана «СЕГОДНЯ») — вход
определяется здесь и дорисовывается в макет (правило 6а-0).

Пуши новолуния/полнолуния — ОТДЕЛЬНАЯ задача **47б** (решение 19.08, прецедент 06а→06б: пуши
в вебе не проверяются, смешивать циклы проверки значило бы держать половину задачи непроверенной).

## Решения

| Вопрос | Решение |
|---|---|
| 8 · Вход | **Только со строки луны на «Сегодня»**: строка становится нажимаемой, тап → `/moon`; «назад» подписан «Сегодня». Справа от текста строки — шеврон `chevron-forward-outline` 12 px `muted` (в макете его нет — дорисовать: без него не догадаться, что строка ведёт куда-то). Отдельного пункта в профиле/настройках нет. |
| 9 · Контент | **Минимум**: сетка фаз по дням + события новолуния/полнолуния с короткой подписью («Время задумывать новое» / «Время подводить итоги») — две UI-строки в `i18n.ts`, ru+en. Текстов на лунные дни и фазы нет. |
| 10 · Пуши | **Отдельная задача 47б** после экрана. Здесь — ничего про пуши; `pushPlan.ts` не трогаем. |
| Точность моментов | **Точные моменты новолуния/полнолуния по алгоритму Меюса, без зависимостей.** Нынешний `moon.ts` считает «среднюю луну» от эпохи 2000 (±14 ч к реальному моменту): для строки «Растущая луна» хватало, для календаря с ДАТАМИ — нет (формула приложения даёт новолуние 13.08.2026 07:46 UTC, реальное — 12.08.2026 17:37 UTC; примерно каждое третье событие оказалось бы на соседнем дне относительно любого другого лунного календаря). Ядро `moon.ts` переписывается; строка «Сегодня», календарь и будущие пуши 47б считают от одной правды. |
| День события | **Один день — локальный календарный день точного момента, с местным временем** («НОВОЛУНИЕ · 12 АВГУСТА · 20:37»), а не диапазон «12–13» из макета: макет рисовал окно фазы (±0.92 сут), точный момент — это момент. Строка «Новолуние» на «Сегодня» при этом живёт ~2 суток (окно фазы) — «период» против «момента», не баг; записано в logic-spec §6. |
| Прошедшие события месяца | **Показываем, приглушаем** (`opacity .45` на всю строку) — так же, как прошедшие дни в сетке (`.dim`). В макете «прошлых» строк просто не было (даты макета позже «сегодня»). |
| Начало недели | **По языку**: ru — понедельник; en/es/pt — воскресенье (CLDR для выбранных локалей en-US/es-MX/pt-BR). Таблица `WEEK_START: Record<Lang, 0 \| 1>` в `lang.ts` (0 = воскресенье, 1 = понедельник — нумерация `Date.getDay()`). |
| Подписи дней недели | `toLocaleDateString(LOCALES[lang], { weekday: 'short' })` → UPPERCASE, хвостовая точка срезается (pt-BR отдаёт «seg.»). Тот же механизм, что `formatEntryDate` дневника — на устройстве проверен лайв-проверками. |
| Глифы ● / ○ | **Рисуем `View`-кружками, а не символами шрифта**: 6×6 в ячейке (● — заливка `text`, ○ — кольцо `accent` border 1), 14×14 в строке события (border 1.5). Символ зависел бы от системного шрифта и платформы, кружок — нет. Правило 16 («если символ уже нарисован иконкой — побеждает иконка») не нарушено: таких глифов в приложении ещё нет. |
| ☾ в разделителе | У `Rule` появляется проп `glyph?: string` (по умолчанию ✦); на экране календаря — ☾, как в макете (`.rule span`). ☾ нет в Manrope — `Rule` и так рисует звёздочку системным `Text` без `fontFamily`. |
| Листание месяцев | **Не делаем** — только текущий месяц, как в макете. Если попросят на лайве — отдельной задачей (v1.1). |
| Обновление «сегодня» | Экран вычисляет `now` при монтировании и пересчитывает на возврате из фона (`useAppActive`) — правило 06а: всё, что зависит от текущего времени, слушает `AppState`. Переход через полночь при открытом экране — сетка обновится при следующем возврате из фона, таймера нет. |
| Хранение | **Ничего нового не храним**: всё выводится из времени. Persist version остаётся **10**, бэкап не меняется. |

## Цель

- Пользователь видит месяц луны одним экраном: какой сегодня лунный день и фаза, в какие дни
  новолуние и полнолуние (с местным временем), что прошло и что впереди. Это 2–4 «праздника»
  в месяц — крючок возврата (master-plan п. 11) и фундамент для пушей 47б.
- Одна правда о луне: строка на «Сегодня», календарь и будущие пуши читают один `moon.ts`
  с точными моментами; средней луны в коде не остаётся.
- Логика чистая и под тестами (против опубликованных эфемерид); экран — тонкий слой из общих
  компонентов.

## Что делаем

### А. Ядро `src/lib/moon.ts` — переписать на точные моменты (под юнит-тестами)

Публичный API (сигнатуры прежних функций не меняются — `app/(tabs)/index.tsx` продолжает звать
`moonInfo(now)`):

```ts
export type MoonPhase = 'new' | 'waxing' | 'full' | 'waning';
export type MoonEventKind = 'new' | 'full';
export interface MoonEvent { kind: MoonEventKind; at: Date }   // at — точный момент (UTC-инстант)

export const HALF_WINDOW = 0.92;                               // полуширина окна new/full, сутки (hf-01)
export function phaseInstant(k: number, kind: MoonEventKind): Date;   // Меюс: k целое → новолуние лунации k, kind 'full' → k + 0.5
export function moonEvents(from: Date, to: Date): MoonEvent[];         // все new/full в [from, to), по времени
export function lunationAround(date: Date): { prevNew: Date; full: Date; nextNew: Date };
export function moonAge(date: Date): number;      // сутки от prevNew, [0, ~29.83)
export function moonPhase(date: Date): MoonPhase; // 'new' если |date − ближайшее новолуние| < HALF_WINDOW; 'full' — то же для полнолуния; иначе waxing (до full) / waning (после)
export function lunarDay(date: Date): number;     // floor(moonAge) + 1, 1…30
export function moonInfo(date: Date): { age: number; phase: MoonPhase; day: number };
```

Убираются экспорты `MOON_EPOCH`, `SYNODIC_MONTH`, `PHASE_BOUNDS` (использовались только тестами;
средняя длина месяца остаётся внутренней константой для оценки номера лунации `k`).

**Алгоритм** — Meeus, *Astronomical Algorithms* (2-е изд.), гл. 49 «Phases of the Moon». Номер
лунации `k` (0 = новолуние 6 января 2000): оценка `k ≈ (год_дробный − 2000) × 12.3685`, для
новолуния `k` целое, для полнолуния `k + 0.5`. Далее `T = k / 1236.85` (юлианские столетия от J2000):

```
JDE = 2451550.09766 + 29.530588861·k + 0.00015437·T² − 0.000000150·T³ + 0.00000000073·T⁴
E   = 1 − 0.002516·T − 0.0000074·T²
M   = 2.5534   + 29.10535670·k − 0.0000014·T² − 0.00000011·T³                      (средняя аномалия Солнца)
M'  = 201.5643 + 385.81693528·k + 0.0107582·T² + 0.00001238·T³ − 0.000000058·T⁴      (средняя аномалия Луны)
F   = 160.7108 + 390.67050284·k − 0.0016118·T² − 0.00000227·T³ + 0.000000011·T⁴      (аргумент широты Луны)
Ω   = 124.7746 − 1.56375588·k + 0.0020672·T² + 0.00000215·T³                        (долгота восходящего узла)
```
(все углы в градусах, привести к [0, 360) перед синусами). Поправка к JDE (сутки) для **новолуния**:
```
−0.40720 sin M'      +0.17241 E sin M      +0.01608 sin 2M'     +0.01039 sin 2F
+0.00739 E sin(M'−M) −0.00514 E sin(M'+M)  +0.00208 E² sin 2M   −0.00111 sin(M'−2F)
−0.00057 sin(M'+2F)  +0.00056 E sin(2M'+M) −0.00042 sin 3M'     +0.00042 E sin(M+2F)
+0.00038 E sin(M−2F) −0.00024 E sin(2M'−M) −0.00017 sin Ω       −0.00007 sin(M'+2M)
+0.00004 sin(2M'−2F) +0.00004 sin 3M       +0.00003 sin(M'+M−2F) +0.00003 sin(2M'+2F)
−0.00003 sin(M'+M+2F) +0.00003 sin(M'−M+2F) −0.00002 sin(M'−M−2F) −0.00002 sin(3M'+M)
+0.00002 sin 4M'
```
Для **полнолуния** — тот же ряд с первыми семью коэффициентами `−0.40614, +0.17302, +0.01614,
+0.01043, +0.00734, −0.00515, +0.00209`, остальные совпадают. Плюс **планетные поправки** (обе фазы):
```
A1 = 299.77 + 0.107408·k − 0.009173·T²   A2 = 251.88 + 0.016321·k    A3 = 251.83 + 26.651886·k
A4 = 349.42 + 36.412478·k                A5 = 84.66 + 18.206239·k    A6 = 141.74 + 53.303771·k
A7 = 207.14 + 2.453732·k                 A8 = 154.84 + 7.306860·k    A9 = 34.52 + 27.261239·k
A10 = 207.19 + 0.121824·k                A11 = 291.34 + 1.844379·k   A12 = 161.72 + 24.198154·k
A13 = 239.56 + 25.513099·k               A14 = 331.55 + 3.592518·k
ΔJDE = 0.000325 sin A1 + 0.000165 sin A2 + 0.000164 sin A3 + 0.000126 sin A4 + 0.000110 sin A5
     + 0.000062 sin A6 + 0.000060 sin A7 + 0.000056 sin A8 + 0.000047 sin A9 + 0.000042 sin A10
     + 0.000040 sin A11 + 0.000037 sin A12 + 0.000035 sin A13 + 0.000023 sin A14
```
JDE — динамическое время (TD); в UTC переводим вычитанием ΔT ≈ 69 с (константа, для 2020–2030
меняется в пределах ±3 с — ниже нашей точности). `Date`: `ms = (JD − 2440587.5) × 86 400 000`.
Точность ряда — около 1–2 минут; допуск тестов — **±2 минуты**. Не шире: при ±10 сьют оставался
ПОЛНОСТЬЮ зелёным на сломанном коде — перепутанный знак ΔT сдвигает моменты на 2×69 с ≈ 2.3 мин
и умещался в такой допуск целиком (замерено мутацией 20.08: 32/32 passed; после сужения краснеют
17 из 32). Реальные отклонения верной реализации — не больше 0.74 мин на 74 моментах USNO.

`lunationAround(date)`: `k0 = floor((date − эпоха) / 29.530588861 сут)` по средней формуле, затем
новолуния для `k0 − 1 … k0 + 1` точным расчётом, из них — последнее ≤ date (`prevNew`) и первое
> date (`nextNew`); `full = phaseInstant(k_prev, 'full')`. `moonEvents(from, to)` — перебор `k`
от оценки для `from − 1 месяц` до `to + 1 месяц`, фильтр по диапазону, сортировка.

**Опорные моменты для тестов (UTC).** Формулы выше и таблица ниже сверены ДРУГ О ДРУГА пробным
скриптом 19.08 (транскрипция ряда — из книги по памяти, даты — из памяти об опубликованных
эфемеридах; два независимых источника ошибок сошлись на всех 18 моментах с расхождением ≤ 0.5 мин).
**Внешняя сверка выполнена 20.08 и закрыта:** таблица целиком подтверждена US Naval Observatory
(`aa.usno.navy.mil/calculated/moon/phases`) — все 18 моментов совпали МИНУТА В МИНУТУ. Оттуда же
взят полный список 2025–2027 (74 момента), и реализация проверена по нему: максимальное отклонение
**0.74 мин**, за допуск не выходит ни один — этот список лежит отдельным тестом в `moon.test.ts`,
а таблица ниже осталась как курированная (каждая строка объясняет, ЧЕМ момент примечателен):

| Событие | Момент (UTC) | Почему опорный |
|---|---|---|
| Новолуние | 2025-08-23 06:06 | тест-кейс logic-spec §6 (есть в нынешнем `moon.test.ts`) |
| Полнолуние | 2025-08-09 07:55 | тест-кейс logic-spec §6 (там записано «08:00») |
| Полнолуние | 2025-01-13 22:27 | первое полнолуние 2025 |
| Новолуние | 2025-03-29 10:58 | частное солнечное затмение |
| Полнолуние | 2025-03-14 06:55 | полное лунное затмение |
| Новолуние | 2025-09-21 19:54 | частное солнечное затмение |
| Полнолуние | 2025-09-07 18:09 | полное лунное затмение |
| Полнолуние | 2026-01-03 10:03 | первое полнолуние 2026 |
| Новолуние | 2026-01-18 19:52 | первое новолуние 2026 |
| Новолуние | 2026-02-17 12:01 | кольцеобразное солнечное затмение |
| Полнолуние | 2026-03-03 11:38 | полное лунное затмение |
| Новолуние | 2026-08-12 17:37 | полное солнечное затмение; месяц макета |
| Полнолуние | 2026-08-28 04:18 | частное лунное затмение; месяц макета |
| Новолуние | 2026-12-09 00:52 | последнее новолуние 2026 |
| Полнолуние | 2026-12-24 01:28 | последнее полнолуние 2026 |
| Новолуние | 2027-02-06 15:56 | кольцеобразное солнечное затмение |
| Новолуние | 2027-08-02 10:05 | полное солнечное затмение |
| Новолуние | 2027-12-27 20:12 | последнее новолуние 2027 |

Семантика фаз с точными моментами (logic-spec §6 переписать): окно `new`/`full` — ±0.92 сут
вокруг точного момента (гарантия hf-01 сохраняется: в реальное новолуние строка говорит
«Новолуние»); между окнами — `waxing` (от новолуния к полнолунию) и `waning`. Лунный день —
`floor(возраст) + 1`; длина лунации 29.27–29.83 сут, поэтому день 30 бывает не в каждом месяце
(29 → 1) — это не дефект, так в любом лунном календаре.

### Б. Сетка и события месяца — `src/lib/moonCalendar.ts` (чистый, под тестами) + `dates.ts`/`lang.ts`

```ts
export interface MonthEvent { kind: MoonEventKind; at: Date; day: number }  // day — число месяца по ЛОКАЛЬНОМУ времени
export function monthEvents(year: number, month0: number, source = moonEvents): MonthEvent[];
// границы — ЛОКАЛЬНЫЕ полночь 1-го числа и полночь 1-го числа следующего месяца (new Date(y, m, 1));
// source подменяется в тестах синтетическими событиями, собранными локальным конструктором Date —
// иначе тест зависел бы от часового пояса машины (момент 17:37 UTC — это 12-е число в Москве и 13-е в Окленде)
export function monthGrid(year: number, month0: number, weekStart: 0 | 1): { leading: number; daysInMonth: number };
// leading = (getDay(1-е число) − weekStart + 7) % 7 — пустых клеток перед 1-м; хвост не нужен, сетка сама переносит
```
В `src/lib/lang.ts`: `WEEK_START: Record<Lang, 0 | 1> = { ru: 1, en: 0, es: 0, pt: 0 }`.
В `src/lib/dates.ts`: `weekdayLabels(lang): string[]` — 7 подписей начиная с `WEEK_START[lang]`,
`toLocaleDateString(locale, { weekday: 'short' })`, UPPERCASE, без хвостовой точки;
`formatTime(date: Date, lang): string` — `toLocaleTimeString(locale, { hour: '2-digit', minute: '2-digit' })`
(ru «20:37», en «08:37 PM»). Хелперы форматирования живут в `dates.ts`, а не в `moonCalendar.ts`:
тот остаётся числовым и про язык не знает.

Месяц всегда содержит 1–3 события: в феврале может не быть полнолуния, в длинном месяце бывает
два новолуния (1-го и 30/31-го). Ноль событий невозможен (интервал между соседними событиями
≈ 14.8 сут < 28). Порядок — по времени; кинды могут повторяться («новолуние, полнолуние,
новолуние»).

### В. Выносы и общие компоненты (ДО нового кода, правило «2+ раза»)

- **`MoonRow`** (`src/components/MoonRow.tsx`) — строка луны «☽ **Растущая луна** · 8-й лунный
  день» выносится из `app/(tabs)/index.tsx` (второе место — экран календаря). Пропсы:
  `phase: MoonPhase`, `day: number`, `onPress?: () => void`, `style?`. Без `onPress` — как сейчас
  (обычный `Text`, ☽ системным шрифтом, фаза `head` 600, остаток `muted`, 13 px, по центру,
  `marginTop 12`). С `onPress` — обёртка `PressableScale` (единая замена самодельным нажатиям),
  после текста шеврон `Ionicons chevron-forward-outline` 12 px `muted` с отступом 4.
- **`Rule`** — проп `glyph?: string`, дефолт `'✦'`; экран календаря передаёт `'☾'`.

### Г. Экран `app/moon.tsx` — композиция по `v-moon`

Маршрут под гардом онбординга в `app/_layout.tsx` (`<Stack.Screen name="moon" options={transparentHeader(t)} />`),
в самом экране `<Stack.Screen options={{ headerBackTitle: tr('tabs.today') }} />` — как у тренажёра.
`ScreenBg` + `ScrollView` с `paddingTop: insets.top + 64 + spacing.l`, `paddingHorizontal: spacing.xl`,
`paddingBottom: 120` (как `review.tsx`). Сверху вниз:

1. `FadeUp index=0`: overline `.date` — `formatMonthTitle(YYYY-MM, lang).toUpperCase()` («АВГУСТ 2026»);
   заголовок `.h2` — `tr('moon.title')` («Лунный календарь»); `Rule glyph="☾"`.
2. `FadeUp index=1`: `MoonRow` (без `onPress`) с `moonInfo(now)`; под ней сетка `.mooncal`:
   `marginTop 12`, 7 колонок, `gap 4` (RN `gap` и по строкам, и по колонкам), ряд подписей
   дней недели (`weekdayLabels(lang)`: 8.5 px, `letterSpacing 1`, `muted`, `paddingVertical 4`,
   по центру), затем `leading` пустых клеток и `daysInMonth` ячеек. Ячейка: `aspectRatio 1`,
   `borderRadius 10`, `borderWidth 1` прозрачная, колонка по центру; число 11 px `text`;
   под числом глиф события (`marginTop 1`, 6×6). Состояния: **прошедший** день
   (`day < сегодня`) — цвет `muted` + `opacity .45`; **сегодня** — `borderColor accent`,
   `backgroundColor chipBg`; будущий — как есть. Ячейки не нажимаются.
3. `FadeUp index=2`: строки событий, по одной на `MonthEvent`: панель `backgroundColor panel`,
   `borderWidth 1 frame`, `borderRadius 14`, `padding 12/14`, `marginTop 9`, ряд `gap 12`,
   по центру вертикали; слева глиф 14×14 (● `text` / ○ `accent`); справа колонка: overline
   `tr('moon.new'|'moon.full').toUpperCase() · formatDayMonth(…).toUpperCase() · formatTime(at)`
   (9 px, `letterSpacing 2`, `accent`), под ним `tr('moon.newHint'|'moon.fullHint')` (serif
   `fonts.display`, 14.5 px, `head`). Прошедшее событие (локальный день < сегодня) — `opacity .45`
   на всю строку. `backdrop-filter: blur(8px)` макета не воспроизводим (панели `Block`
   в приложении блюра не несут; `panel` полупрозрачен сам).

Сегодняшний день и `now` — `useState(() => new Date())` + `useAppActive(() => setNow(new Date()))`.
Пустых/ошибочных состояний у экрана нет.

### Д. Вход с «Сегодня»

В `app/(tabs)/index.tsx` строка луны заменяется на `<MoonRow phase day onPress={() => router.push('/moon')} />`
внутри того же `FadeUp index=1`. Ничего больше на «Сегодня» не меняется.

### Е. Строки i18n (`src/lib/i18n.ts`, ru + en)

`moon.title` «Лунный календарь» / «Moon calendar»; `moon.newHint` «Время задумывать новое» /
«A time to begin»; `moon.fullHint` «Время подводить итоги» / «A time to take stock». Уже есть:
`moon.new/waxing/full/waning`, `moon.day`, `tabs.today`. Числительных и плюрализации задача
не добавляет.

### Ж. Документы и макет

- `docs/logic-spec.md` §6 — переписать: точные моменты по Меюсу, окно ±0.92 вокруг точного
  момента, лунный день, «день события = локальный день момента, показывается с местным временем»,
  тест-кейсы — таблица опорных моментов.
- `docs/product-spec.md` — в разделе «1. Сегодня»: строка луны нажимаема → экран; новый подраздел
  «1а. Лунный календарь» (элементы, состояния ячеек, события, что не делаем).
- `docs/design-system.md` §5 — компонент «Лунный календарь»: сетка, ячейка и её состояния,
  строка события, кружки 6/14, шеврон строки луны.
- `docs/design-reference.html` — `v-today`: шеврон после текста `.moonrow` и `onclick → show('v-moon')`;
  `v-moon`: события одним днём с временем («НОВОЛУНИЕ · 12 АВГУСТА · 20:37», «ПОЛНОЛУНИЕ ·
  28 АВГУСТА · 07:18» — московское время реальных моментов), в сетке глифы только на 12 и 28.
- `docs/backlog.md` — решения 8–10 отмечены, задача 47 заведена, хвосты: 47б (пуши), «расклады
  лунного цикла» (макета нет), листание месяцев (по запросу).

## Что НЕ делаем

- Пуши новолуния/полнолуния (47б), тексты на лунные дни и фазы, листание месяцев, расклады
  лунного цикла, виджет.
- Не трогаем стор, persist, бэкап, `pushPlan.ts`.
- Не рисуем фазу каждого дня картинкой-серпом: в макете её нет — только глифы событий.

## Критерии приёмки («готово, когда…»)

- [ ] `moon.test.ts` переписан: все опорные моменты таблицы А (сверенные с источником) совпадают
      с `phaseInstant`/`moonEvents` с допуском ±2 мин; полный список USNO 2025–2027 (74 момента)
      в том же допуске; окна фаз: новолуние ± 0.91 сут → `new`,
      ± 0.93 сут → `waxing`/`waning`; `lunarDay` = 1 через час после новолуния, ≤ 30 всегда, не
      убывает внутри лунации; `moonEvents` отдаёт чередующиеся кинды по возрастанию времени.
- [ ] `moonCalendar.test.ts`: август 2026 — `leading` 5 при `weekStart 1` и 6 при `weekStart 0`,
      `daysInMonth` 31; `monthEvents` на синтетическом источнике: событие 1-го числа 00:30 попадает
      в месяц, событие 1-го числа следующего месяца 00:30 — нет; месяц с одним событием и с тремя.
- [ ] `dates.test.ts`: `weekdayLabels('ru')` начинается с «ПН», `('en')` — с «SUN»; без точек
      в хвосте; `formatTime` ru «20:37».
- [ ] На «Сегодня» строка луны нажимаема, показывает шеврон, ведёт на `/moon`; текст и положение
      строки не изменились (замер до/после).
- [ ] `/moon`: шапка месяца, заголовок, ☾ в разделителе, та же строка луны, сетка 7 колонок
      с подписями дней по языку, сегодня обведён, прошедшие приглушены, глифы событий стоят на
      днях из `monthEvents`; строки событий с датой и временем, подписи по виду события,
      прошедшие приглушены; обе темы; en — неделя с воскресенья, «SUN … SAT», 12-часовое время.
- [ ] Прямой `/moon` до онбординга редиректит на онбординг (гард).
- [ ] `npx tsc --noEmit` чистый, `npm test` зелёный; веб-проверка 6а/6б (скриншоты
      `docs/screenshots/47/`, обе темы, 390×844), лайв-проверка Артёма на iPhone.
- [ ] Документы из раздела Ж обновлены, макет дорисован.

## План по файлам (кратко; детальный — `docs/plans/47-moon-calendar.md`)

1. `src/lib/moon.ts` + `src/lib/__tests__/moon.test.ts` — ядро Меюса, тесты против эфемерид
   (сначала таблица и красный прогон, потом формулы).
2. `src/lib/lang.ts` (`WEEK_START`), `src/lib/dates.ts` (`weekdayLabels`, `formatTime`) + тесты.
3. `src/lib/moonCalendar.ts` + тест.
4. Выносы: `src/components/MoonRow.tsx` (из `index.tsx`, поведение без `onPress` не меняется),
   `Rule.glyph`.
5. `src/lib/i18n.ts` — три ключа ru/en.
6. `app/moon.tsx` + объявление в `app/_layout.tsx`.
7. `app/(tabs)/index.tsx` — `MoonRow` с `onPress`.
8. Макет и документы (раздел Ж), бэклог, CLAUDE.md «Статус».
`npm install` не нужен: зависимостей не добавляется.

## Проверка

- **6а** (Playwright-скрипт, 390×844, обе темы): «Сегодня» со строкой луны и шевроном; `/moon`
  целиком; сравнение с `v-moon` макета по чек-листу `docs/ui-verification.md`. ⚠️ Сравнивать
  композицию, а не даты: макет хардкодит август 2026 и «сегодня = 9-е».
- **6б**: тап по строке луны → `/moon` → «назад» → «Сегодня»; прямой `/moon` без онбординга →
  онбординг; смена языка ru→en на открытом приложении → подписи недели и время меняются, сетка
  сдвигается на один столбец; смена темы; консоль без ошибок. Сид «сегодня» не нужен — экран
  берёт реальную дату; для прошедших/будущих строк достаточно любого дня месяца, кроме 1-го
  и последнего.
- **6в (Артём, iPhone)**: время событий совпадает с любым лунным календарём на телефоне
  (с точностью до минуты); подписи дней недели и время — по-русски и по-английски (Hermes-формат
  дат — судья только устройство, правило hf-02); шеврон и нажатие строки на «Сегодня»; обе темы.

## Риски и ловушки для имплементера

- Ряд Меюса легко ошибиться знаком или коэффициентом — поэтому тесты против эфемерид пишутся
  ПЕРВЫМИ и обязаны быть красными на пустой реализации; одной опорной даты мало, нужны ≥12 на
  разные годы и оба кинда (ошибка в коэффициенте полнолуния при верном новолунии иначе пройдёт).
- Углы в градусах → перед `Math.sin` переводить в радианы; приведение к [0, 360) не обязательно
  для синуса, но полезно для отладки.
- `k` для полнолуния — `целое + 0.5`; `T` считать от ЭТОГО `k`, не от целого.
- Локальные границы месяца — `new Date(y, m, 1)`, не ISO-строки (ловушка H2 из `dates.ts`).
- Тесты `monthEvents` — только на синтетическом источнике (зависимость от часового пояса машины).
- `toLocaleTimeString` с `hour: '2-digit'` в en-US даёт «08:37 PM» — это ожидаемо, не «чинить».
- `Rule` с новым пропом не должен менять ни один из трёх существующих экранов (дефолт ✦).

---

## Отчёт веб-проверки 6а/6б и закрытие задачи (20.08)

**Веб-проверка: 35 PASS / 0 FAIL**, консольных ошибок 0, `window.innerWidth = 390` (вьюпорт честный —
урок задачи 16 про раздувшееся окно Chrome не повторился). Скриншоты — `docs/screenshots/47/`
(8 файлов, все ровно 390×844). **Лайв-проверка Артёма на iPhone пройдена ✓ («все ок»).**

Покрыто: вход со строки луны и переход на `/moon`; шапка месяца, заголовок, разделитель ☾;
подписи недели `ПН…ВС` (ru) и `SUN…SAT` (en); 31 ячейка, обводка сегодняшнего дня
(`rgb(226,189,114)` dark / `rgb(168,128,46)` light), приглушение прошедших (`opacity 0.45`);
1–3 строки событий с временем — «НОВОЛУНИЕ · 12 АВГУСТА · 21:36» / «ПОЛНОЛУНИЕ · 28 АВГУСТА · 08:18»
(en: «NEW MOON · AUGUST 12 · 09:36 PM»); кружки на днях событий; гард `/moon` без онбординга →
`/onboarding`; «назад» → «Карта дня». Три темо-языковых прогона: dark-ru, light-ru, dark-en.

**Два красных прогона, оба честные.** (1) `monthEvents → []`: покраснели ровно 6 проверок
(«строк событий 1–3» и «кружки на днях событий» × 3 тега). (2) отключена подсветка сегодняшней
ячейки: покраснели ровно 3 («сегодня обведён», `todayBorder` стал `rgba(0,0,0,0)`). После обоих —
`git checkout --`, зелёный прогон 35/35.

### Чему научила сама проверка (три ловушки, найденные в скрипте, а не в приложении)

⚠️ **`Array.prototype.every` на пустом массиве истинен всегда.** Проверка «кружки стоят на днях
событий» была построена как `eventDays.every(...)` и на мутации `monthEvents → []` осталась ЗЕЛЁНОЙ:
событий ноль, дней ноль, условие выполнено пусто. Это ровно тот класс «тест-декорация», который
проект ловил в 06б (ветка обрезки пушей) и hf-02. Лечится добавленным `eventDays.length > 0`.
Общее правило: любая проверка вида «все найденные X удовлетворяют условию» обязана рядом
утверждать, что X вообще найдены.

⚠️ **Сид с `installSeed: 0` — это «свежая установка», и стор перезапишет язык.** `onRehydrateStorage`
в `useApp.ts` считает нулевой/отсутствующий `installSeed` признаком первой установки и вызывает
`detectLang(deviceLocaleTags())`, затирая явно посеянный `lang: 'en'` локалью машины. Все английские
проверки при этом смотрели на русский экран. Сид для веб-проверки обязан нести НЕнулевой
`installSeed` — иначе проверяешь не то состояние, которое посеял.

⚠️ **Кнопка «назад» нативной шапки в вебе — это `role="link"`, а не `role="button"`.** Локатор
`getByRole('button', …)` её не находил, и проверка тихо уходила в фолбэк `page.goBack()` — то есть
проверяла историю браузера вместо тапа по кнопке приложения. Первый набор скриншотов снят именно
в таком состоянии и был переснят.

### Известные ограничения проверки (перечислены, а не умолчаны)

- Мутациями подтверждены 2 разновидности проверок из 9; остальные 7 (overline месяца, подписи
  недели, число ячеек, формат времени, число строк событий, приглушение прошедшего дня) прочитаны
  глазами и не тавтологичны — ожидаемое значение считается независимо от кода экрана, — но
  экспериментально красными не проверялись.
- Снимки макета `mockup-moon-*.png` обрезаны по высоте страницы и не показывают строки событий;
  сверка блока событий сделана по тексту раздела Ж, а не попиксельно. Светлого снимка макета
  `v-today` нет.
- Время событий на скриншотах (21:36 / 08:18) — часовой пояс машины UTC+4; в спеке выше примеры
  для MSK. Сверять надо композицию, а не даты.

### Что веб не проверил и подтвердил только телефон (пункт 6в)

Формат времени и подписи дней недели считает САМ движок JS: `toLocaleTimeString` появился в проекте
этой задачей впервые, а у Hermes ICU урезанный против браузерного. Это тот же класс, что hf-02
(русские числительные молча уходили в английский фолбэк). Артём подтвердил на устройстве: русские
`ПН…ВС`, «12 августа · 21:36», английские `SUN…SAT` и 12-часовой формат — все верные.

### Расхождения с макетом

Новых нет. Два предвиденных подтверждены как ожидаемые: кнопка «назад» рисует стрелку без видимого
текста (текст живёт в accessibility-label — тот же паттерн на всех экранах приложения); блюра под
строками событий нет (решение спеки, макет его тоже фактически не рисует).

**Контраст светлой темы — не дефект этой задачи, а кросс-проектный паттерн.** Замер по пикселям
скриншота (формула WCAG): подписи дней недели `muted` 8.5px дают **1.84:1** при норме 4.5:1. Тот же
замер по макету — 2.18:1, то есть приложение не хуже эталона и нового дефекта не вносит; числа
самих дней читаются отлично (7.3:1 светлая, 13:1 тёмная). Это ровно то свойство пары
Overline + `muted` на светлой теме, которое задача 25 уже разобрала и признала свойством темы
целиком. Кандидат в отдельную задачу — вместе с хвостом задачи 46 про бейдж «ИЗУЧЕНО ✓»: причина
у них одна, и чинить их надо одним токеном во всех местах сразу, а не точечно в календаре.
