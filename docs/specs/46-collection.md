# Спека 46 — коллекция-альбом: изученные карты открываются в альбоме (этап 4 плана)

> ⚠️ **Первая редакция (отдельный экран-альбом `/collection`, вход-строка на «Картах», строки
> мастей) СНЯТА решением Артёма 19.08 после лайв-проверки** — читалась как дубль справочника.
> Актуальна редакция **46б** — раздел «# 46б — альбом внутри справочника» ниже; всё до него —
> история (решения, отчёты первой редакции) и сохранено как след разбора. Макет `v-collection`
> из `design-reference.html` удалён.

Статус: спека написана 19.08 (брейншторм: вопросы 5–7 бэклога решены по рекомендациям Claude
решением Артёма 19.08, остальные развилки — ниже в таблице «Решения»), ждёт «ок» Артёма.
Реализация — сессия на Opus 5 по детальному плану `docs/plans/46-collection.md` (код по шагам
отдаётся sonnet-сабагентам), ветка `feat/46-collection`, веб-проверка 6а/6б и лайв-проверка
Артёма на iPhone — как у любой UI-задачи.

Контекст: п. 13 master-plan §3.1-Р («Коллекция-альбом — изученные карты открываются в альбоме,
прогресс 23/78, уровни мастерства каждой карты. Крючок „закрыть коллекцию“»), этап 4 плана,
вторая задача этапа после флеш-карт (45). Макет — `v-collection` в `design-reference.html`
(Cowork, 15.08), нарисован «простейшим вариантом»: экран с прогрессом, сеткой старших арканов
и четырьмя строками мастей. Вход с экрана «Карты» в макете НЕ нарисован (к экрану ведёт только
демобар; кнопка «назад» подписана «КАРТЫ») — вход определяется здесь и дорисовывается в макет
(правило 6а-0).

## Решения

| Вопрос | Решение |
|---|---|
| 5 · Вход | **Отдельный экран `app/collection.tsx`, вход со вкладки «Карты»** — строка-панель «Коллекция · N ИЗ 78 ›» под заголовком «78 карт», над поиском. Режим-переключатель внутри справочника отклонён: справочник — инструмент поиска (фильтры, скролл-панель), альбом — витрина прогресса, у них разные задачи и разные состояния. Нарисованная в макете кнопка «назад → КАРТЫ» совпадает с решением. |
| 6 · Что открывает карту | **Пройденный урок** — `learnedCardIds(course, lessonsProgress)`, то же множество, что бейдж «ИЗУЧЕНО ✓» справочника и колода тренажёра (45): «открыто в альбоме» = «изучено» = «в колоде повторения», три места читают одно правило. «Первое повторение в тренажёре» отклонено: тренажёр повторяет уже изученное, встреча с картой в нём не первая. |
| 7 · Масти до старта | **Компакт-строками «0 ИЗ 14»**, как нарисовано. Общее правило секции (см. ниже): секция без единой открытой карты — строка; с хотя бы одной — сетка с заголовком «НАЗВАНИЕ · N ИЗ M». Применяется и к старшим арканам: у новичка без уроков альбом = прогресс «0 из 78» + подсказка + пять строк. Альбом «растёт на месте» по мере курса: после М2 — ровно состояние макета (8 из 78, старшие сеткой, масти строками), после m4l1 — «ЖЕЗЛЫ · 5 ИЗ 14» сеткой, остальные масти строками. |
| Глифы мастей 🜂🜄🜁🜃 из макета | **Не используем** — вместо них слева в строке масти мини-рубашка 22×36 (`StarBack`: градиент рубашки + ✶, тот же плейсхолдер, что у закрытой ячейки сетки, уменьшенный). Причина: алхимические символы (блок U+1F700) — глифы системного шрифта, на iOS их наличие не гарантировано (на Windows-браузере макета они есть из Segoe UI Symbol); правило задачи 16 — «символ в макете указывает на смысл, а не на способ рисования». Мини-рубашка заодно говорит ровно то, что нужно: «14 закрытых карт». ⚠️ Если Артём на ревью спеки проверит, что 🜂🜄🜁🜃 видны на его iPhone (в Заметках), решение можно откатить к глифам — тогда строка получает текстовый глиф 17 `accent` вместо мини-рубашки; в остальном спека не меняется. |
| Уровни мастерства карты (master-plan п. 13) | **Не в этой задаче.** Макет их не рисует (правило бэклога: без эталона UI не делаем), в коде понятия нет. Данные для них уже есть (`srs[cardId].intervalDays`), вывод — чистая функция; заводится хвостом 46 в бэклоге: сначала дорисовка в макет, потом спека. |
| Ячейка сетки | **Вынос** локальной `Cell` из `app/(tabs)/cards.tsx` в общий `CardCell` (второе место + новый режим «закрыто») — по правилу «2+ раза». |
| Рубашка со звездой ✶ | **Вынос** связки `CardBackSurface + ✶` в общий `StarBack`: она уже живёт копиями в `SpreadCard` и `SpreadRow`, коллекция стала бы третьей (правило «сначала выносы, потом новый код», прецедент 45б). |
| Строка «иконка + название + счётчик» | Новый общий `CountRow` (геометрия `.suitrow` макета): четыре строки мастей на экране альбома + строка-вход на «Картах» = пять мест одной разметки. `SettingsRow` не подходит: другая типографика (14/13 против 12.5/10 капс с трекингом) и радиус. |
| `filter: grayscale(.4)` закрытой ячейки | **Не воспроизводим**, только `opacity .42`. CSS-фильтры в RN на iOS поддержаны частично, а на двухцветной плашке (синий градиент + золотая ✶) при .42 прозрачности разница неразличима. Записывается в design-system как осознанное упрощение. |
| Хранение | **Ничего нового не храним**: альбом целиком выводится из `lessonsProgress`. Persist version остаётся 10, бэкап не меняется. |

## Цель

- У пользователя появляется «альбом», который заполняется по мере курса: открытые карты лежат
  картинками, закрытые — рубашками, масти до старта — строками «0 ИЗ 14». Крючок — «закрыть
  коллекцию» (78 из 78), видимый числом на «Картах» и полосой в альбоме.
- Один источник правды «изучено»: бейдж справочника, колода тренажёра и альбом читают
  `learnedCardIds`; никаких дубликатов множества.
- Логика чистая и под тестами; экран — тонкий слой, из общих компонентов.

## Что делаем

### А. Чистый модуль `src/lib/collection.ts` (под юнит-тестами)

- `type CollectionGroup = 'major' | 'wands' | 'cups' | 'swords' | 'pentacles'`;
  `COLLECTION_GROUPS` — порядок экрана: старшие, Жезлы, Кубки, Мечи, Пентакли (порядок чипов
  справочника и курса М4).
- `groupOf(card: TarotCard): CollectionGroup` — `arcana === 'major'` → `'major'`, иначе `suit`.
- `interface CollectionSection { group; cards: TarotCard[]; open: number; total: number }` —
  `cards` в порядке `number` по возрастанию (старшие 0–21, младшие 1–14: Туз … Король).
- `collectionSections(deck: readonly TarotCard[], learned: ReadonlySet<string>): CollectionSection[]`
  — пять секций в порядке `COLLECTION_GROUPS`, каждая карта колоды ровно в одной; `open` =
  число карт секции в `learned`, `total` = размер секции (22/14/14/14/14).
- `collectionProgress(sections): { open: number; total: number }` — сумма по секциям (8/78).
- `type SectionMode = 'grid' | 'row'`; `sectionMode(s) = s.open > 0 ? 'grid' : 'row'` — правило
  из таблицы решений, записано один раз.
- Тесты `src/lib/__tests__/collection.test.ts`: на реальной колоде — пять секций, тоталы 22+14×4 =
  78, каждая карта ровно в одной секции, порядок внутри секции по `number`; `open` считается
  по множеству (карта, которой нет в колоде, не считается); `collectionProgress` — сумма;
  `sectionMode` — 0 → row, 1 → grid; `groupOf` на карте каждой масти и на старшем аркане.

### Б. Общие компоненты (выносы — ДО нового кода)

- **`StarBack`** (`src/components/StarBack.tsx`) — `CardBackSurface` + ✶ (`Text` без `fontFamily`,
  правило Txt.tsx: ✶ нет в Manrope), звезда центрируется самим компонентом (absoluteFill-обёртка
  с центрированием), проп `starSize` (20 у `SpreadCard`, 13 у `SpreadRow`, 17 у закрытой ячейки
  сетки — `.gc .ph font-size:17px`, 9 у мини-рубашки строки). `SpreadCard` и `SpreadRow`
  переводятся на него; визуально — ноль изменений (проверяется скриншотом расклада до/после).
- **`CardCell`** (`src/components/CardCell.tsx`) — ячейка сетки карт из `cards.tsx` как есть
  (замер позиции на нажатии для перелёта, `Skeleton` до загрузки, подпись 9.5/600), плюс:
  `from: 'cards' | 'collection'` (параметр маршрута страницы карты → подпись «назад»),
  `badge?: string` (текст углового бейджа — справочник передаёт «ИЗУЧЕНО ✓» у изученных,
  альбом не передаёт ничего: в нём всё открытое изучено по определению), `closed?: boolean` —
  закрытая ячейка: вместо картинки `StarBack` при opacity .42 обёртки, подпись «· · ·»
  (литерал, типографика, не i18n) цветом `muted` при opacity .5, БЕЗ `PressableScale` и без
  onPress (карта скрыта — вести некуда; пружина обещала бы переход, прецедент `ReviewPanel`).
  Константы сетки `GRID_COLS = 3`, `GRID_GAP = 11`, `CELL_W` переезжают сюда и импортируются
  обоими экранами.
- **`CountRow`** (`src/components/CountRow.tsx`) — строка `.suitrow` макета: панель `panel`,
  бордер `line`, radius 13, паддинг 11/14, зазор 12; слева `icon` (Ionicons 17 `accent`) ЛИБО,
  если иконки нет, мини-`StarBack` 22×36 (radius 4, бордер `line`); название 12.5/600 `text`
  (`flex: 1`); справа счётчик «N ИЗ M» 10/700 ls1 `muted`; опционально `chevron`
  (`chevron-forward` 14 `muted`) и `onPress` → `PressableScale`, без onPress — обычный `View`.
  Пропсы: `{ icon?, title, count, total, chevron?, onPress? }`; текст счётчика — ключ
  `collection.ofTotal` («{{open}} ИЗ {{total}}»).

### В. Экраны

**Вход на «Картах»** (`app/(tabs)/cards.tsx`): внутри `FadeUp index={0}` шапки, под заголовком
«78 карт», с отступом 14 сверху — `CountRow icon="albums-outline" title=«Коллекция» count=open
total=78 chevron onPress → router.push('/collection')`. Показывается всегда, включая «0 ИЗ 78» —
ноль и есть крючок. `open` считается `collectionProgress(collectionSections(cards, learned))`,
`learned` у экрана уже есть (бейджи). Поиск и чипы ниже не меняются; scroll-aware панель не
затрагивается (строка живёт в потоке шапки и уезжает со скроллом).

**Экран альбома `app/collection.tsx`** — корневой стек ПОД ГАРДОМ онбординга (`_layout.tsx`,
урок 09), прозрачная нативная шапка с подписью «Карты» на кнопке назад (`headerBackTitle:
tr('tabs.cards')`), `useBackHaptic`, паддинг `insets.top + 64 + spacing.l` (как у тренажёра).
Список — `FlatList` с типизированными элементами (виртуализация: до 78 миниатюр), данные —
из `collectionSections`:
- шапка (`FadeUp 0`): Overline «КОЛЛЕКЦИЯ» 9.5/ls3.5 `muted` + «Альбом» Cormorant 28 `head` (`.date`/`.h2`);
- панель прогресса `.colprog` (`FadeUp 1`): `panel`, бордер `line`, radius 15, паддинг 12/15,
  отступ 14 сверху; строка «Открыто N из 78» Cormorant 600 16 `head`; под ней `ProgressBar`
  6px radius 3 с отступом 8 — заливка `open/total`, анимируется от 0 при входе (задержка 400,
  ход 1400, `PROGRESS_EASE`, `ReduceMotion.System` — тайминг `LevelCard`);
- подсказка (только при `open === 0`, в том же `FadeUp 1`): «Карты открываются по мере
  прохождения уроков курса» 12.5 `muted` по центру, maxWidth 270, отступ 18 сверху. Не
  `EmptyState`: экран не пуст — структура альбома (пять строк) видна, мини-рубашка ✦ рядом
  с пятью мини-рубашками ✶ строк дала бы два плейсхолдера на одном экране;
- секции в порядке `COLLECTION_GROUPS`, режим по `sectionMode`:
  - **сетка**: заголовок «СТАРШИЕ АРКАНЫ · 8 ИЗ 22» / «ЖЕЗЛЫ · 5 ИЗ 14» (`.date` влево:
    9.5/ls3.5 `muted`, капс; название — `collection.major` для старших, `cards.<suit>` для
    мастей, оба через `toUpperCase()` (прецедент `SpreadRow`); счётчик — `collection.ofTotal`),
    отступ 18 сверху;
    ряды по 3 `CardCell` (зазор 11 в обе стороны, отступ 10 от заголовка), открытые —
    картинка + название, `from="collection"`; закрытые — `closed`;
  - **строка**: `CountRow` без иконки (мини-рубашка), название как есть (`collection.major`
    «Старшие арканы» / `cards.<suit>` «Жезлы»), счётчик «0 ИЗ 14», без chevron и без onPress;
    зазор между строками 8, отступ 16 от предыдущей сетки (`.fadeup d5 margin-top:16`).
  Каскад: первые 6 элементов списка после панели — `FadeUp 2` (строки мастей — `FadeUp 3`),
  дальше без анимации (правило тела списков, задача 17).
- Тап по открытой ячейке → `/card/<id>?from=collection`, страница карты подписывает «назад»
  «Коллекция» (`BACK_TITLES.collection → card.backCollection`). Закрытая ячейка и строка масти
  не реагируют.
- Хаптика: у навигации нет (как у ячеек справочника и `ReviewPanel`), `useBackHaptic` на возврате.

**Маршрут**: `<Stack.Screen name="collection" options={transparentHeader(t)} />` внутри
`Stack.Protected guard={onboarded}` в `app/_layout.tsx` — прямой `/collection` до онбординга
редиректит на онбординг (проверяется в 6б).

**i18n** (ru + en, `src/lib/i18n.ts`): `cards.collection` «Коллекция»/«Collection»;
`card.backCollection` «Коллекция»/«Collection»; новое пространство `collection`: `overline`
«КОЛЛЕКЦИЯ»/«COLLECTION», `title` «Альбом»/«Album», `opened` «Открыто {{open}} из {{total}}»/
«Collected {{open}} of {{total}}», `ofTotal` «{{open}} ИЗ {{total}}»/«{{open}} OF {{total}}»,
`major` «Старшие арканы»/«Major arcana» (в заголовке секции — через `toUpperCase()`), `hint`
«Карты открываются по мере прохождения уроков курса»/«Cards open as you complete course lessons».
Числительных с согласованием нет («из 78», «ИЗ 14» — число без склоняемого слова), плюрализация
не нужна.

### Г. Документы и макет

- `docs/product-spec.md` §3 — абзац «Коллекция (46)»: вход, экран, правило секций, что
  открывает карту; `docs/logic-spec.md` — §13 «Коллекция»: выводится из `learnedCardIds`,
  не хранится, правило секции.
- `docs/design-system.md` §5 — строка-вход «Коллекция» на «Картах», панель прогресса альбома,
  заголовок секции, закрытая ячейка (уточнить: opacity .42 без grayscale, подпись «· · ·»
  opacity .5), строка масти с мини-рубашкой (вместо глифа).
- `docs/design-reference.html` — дорисовать: (1) строку-вход `.suitrow`-формы на `v-cards`
  между `.h2` и `.stickysearch` («Коллекция · 8 ИЗ 78 ›» с иконкой); (2) в `v-collection`
  заменить глиф `.sg` на мини-`.ph` 22×36 (если решение по глифам не откатится).
- `docs/backlog.md` — задача 46 с решениями и хвостами (уровни мастерства; «закрыть коллекцию» —
  момент для оценки в сторе, master-plan п. 19); `AGENTS.md` «Архитектура» — строка про
  `collection.ts` / `app/collection.tsx`; `CLAUDE.md` «Статус» — по закрытии.

## Что НЕ делаем (осознанно)

- Уровни мастерства карты, ачивки, празднование «78 из 78», шаринг альбома — нет макета, YAGNI.
- Открытие карты чем-либо кроме пройденного урока (карта дня, тренажёр, расклад) — решение 6.
- Тап по закрытой ячейке / строке масти — ничего (ни подсказки, ни покачивания: карта скрыта
  намеренно, переход невозможен).
- Фильтры, поиск, сортировка в альбоме — это справочник.
- Новых полей стора, бэкапа, миграций — нет (альбом выводится).
- Коллекция в профиле (стата «Открыто N») — не добавляем; третья стата-коробка «ЗАМОРОЗКА»
  уже заняла ряд из трёх.

## Расхождения с макетом (правило 6а-0)

| Место | Макет | Приложение | Почему |
|---|---|---|---|
| Вход на «Картах» | не нарисован (демобар) | строка «Коллекция · N ИЗ 78 ›» под заголовком | макету дорисовка (п. Г) |
| Глиф масти в строке | 🜂🜄🜁🜃 текстом 17 `accent` | мини-рубашка `StarBack` 22×36 | рендер глифов на iOS не гарантирован; макету дорисовка; откат возможен по проверке Артёма |
| Закрытая ячейка | opacity .42 + grayscale(.4) | opacity .42 | RN-фильтры на iOS частичны, разница неразличима |
| Состояние «0 из 78» | не нарисовано | прогресс + подсказка + пять строк | правило секций применено единообразно |

## Критерии приёмки — «готово, когда…»

- [ ] `npx tsc --noEmit` чистый, `npm test` зелёный, новый сьют `collection.test.ts` — 8 тестов (все функции модуля: `groupOf`, `collectionSections`, `collectionProgress`, `sectionMode`).
- [ ] На «Картах» под «78 карт» строка «Коллекция · N ИЗ 78 ›», N = числу карт с бейджем «ИЗУЧЕНО ✓»; тап открывает альбом, «назад» подписан «Карты».
- [ ] Альбом без пройденных уроков: «Открыто 0 из 78», пустая полоса, подсказка, пять строк
  «Старшие арканы · 0 ИЗ 22», «Жезлы · 0 ИЗ 14» … — в этом порядке.
- [ ] Альбом после m2l1–m2l4 (8 старших) = макет: «Открыто 8 из 78», полоса ≈10 %, «СТАРШИЕ
  АРКАНЫ · 8 ИЗ 22», сетка 22 ячейки (8 с картинками и названиями, 14 рубашек «· · ·»),
  четыре строки мастей «0 ИЗ 14» — скриншоты 390×844 в обеих темах, сверка с `v-collection`.
- [ ] После дополнительно m4l1: «ЖЕЗЛЫ · 5 ИЗ 14» сеткой МЕЖДУ сеткой старших и строками
  Кубков/Мечей/Пентаклей (порядок секций неизменен).
- [ ] Тап по открытой ячейке → страница карты с «назад · Коллекция», возврат — в альбом;
  тап по закрытой ячейке и по строке масти — ничего.
- [ ] Прямой `/collection` до онбординга → онбординг.
- [ ] Расклады после выноса `StarBack`: рубашки карт на доске и мини-карты строк не изменились
  (скриншот до/после).
- [ ] Консоль браузера без ошибок; светлая тема проверена отдельно.
- [ ] Лайв-проверка Артёма на iPhone (сценарий ниже) ✓, ветка влита, push.

## План (кратко; детально — `docs/plans/46-collection.md`)

1. Выносы: `StarBack` (+ перевод `SpreadCard`/`SpreadRow`), `CardCell` (+ перевод `cards.tsx`).
2. `src/lib/collection.ts` + тесты.
3. i18n-ключи, `CountRow`.
4. Вход на «Картах», `BACK_TITLES.collection`.
5. Экран `app/collection.tsx`, маршрут под гардом.
6. Документы и макет; веб-проверка 6а/6б; отчёт; лайв-проверка.

## Сценарий лайв-проверки для Артёма (iPhone)

1. «Карты»: под «78 карт» строка «Коллекция · N ИЗ 78 ›». Тап → альбом, «назад» подписан «Карты».
2. DEV-сброс прогресса курса → альбом: «Открыто 0 из 78», подсказка, пять строк со
   мини-рубашками; шрифты, паддинги, обе темы.
3. Пройти (или DEV-завершить) уроки m2l1–m2l4 → альбом: «8 из 78», полоса заполняется плавно
   при входе, сетка старших: 8 картинок + 14 рубашек «· · ·»; тап по открытой → страница карты
   («назад · Коллекция»), тап по закрытой — ничего.
4. Завершить m4l1 → «ЖЕЗЛЫ · 5 ИЗ 14» сеткой между старшими и строками остальных мастей.
5. Расклады: разложить «Три карты» — рубашки карт и мини-карты в дневнике выглядят как раньше.
6. Хаптика на возврате из альбома (Light), свайп «назад» работает.
7. Прокрутить альбом до конца при 8+ открытых картах — плавность, ряды ниже первого экрана
   появляются без мини-каскадов.
8. Со страницы карты вернуться в альбом — полоса прогресса НЕ переигрывается заново.
9. На «Картах» прокрутить на 150–250 px и свистнуть вверх — компактная панель поиска выезжает
   и читается (шапка выросла на строку-вход).

## Отчёт веб-проверки (19.08)

Playwright headless, chromium, viewport 390×844 (dp2), dev-сервер `http://localhost:8081`
(уже запущенный, ветка `feat/46-collection`, HEAD `eb2a472`). Сид — `goto → evaluate(localStorage
'arcanum-app') → reload`, три состояния: `empty` (`lessonsProgress: {}`), `m2` (m2l1–m2l4 → 8
старших), `m4` (+m4l1 → ещё 5 Жезлов). Тексты-признаки взяты из `src/lib/i18n.ts` построчным
чтением файла, не из памяти.

### Красный / зелёный прогон (правило проекта)

Временная поломка `src/lib/collection.ts`: `sectionMode` заставлен всегда возвращать `'row'`
(секции никогда не переключаются в сетку). Красный прогон покраснел ожидаемо и глубже: проверки
заголовка сетки и закрытых ячеек «· · ·» упали, а сценарий клика по открытой карте «Дурак» рухнул
исключением — при `'row'` сетка не рендерится вовсе, карту негде тапнуть (это не баг скрипта,
а честное падение вглубь сценария на сломанном коде):

```
FAIL заголовок «СТАРШИЕ АРКАНЫ · 8 ИЗ 22» (dark)
FAIL 14 закрытых «· · ·» в видимой части или ниже (dark)
... (то же в light)
locator.click: Timeout 30000ms exceeded — getByText('Дурак').first() — element is not visible
```

Файл возвращён `git checkout -- src/lib/collection.ts` (проверено — рабочее дерево чистое,
`git status --short src/` пуст). Зелёный прогон после отката:

```
ИТОГО: 31/31
ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ
Консоль, ошибок: 2 (см. «Консоль» ниже — не относится к красному/зелёному различию)
```

⚠️ Два клика по тексту (`Дурак`, `Жезлы`) изначально ловили `.first()` — DOM-порядок отдавал
невидимый узел смонтированного под альбомом таб-навигатора (тот же класс, что нашла задача 39:
«под карточным экраном в DOM остаётся смонтированный таб-навигатор»), а не видимую ячейку
альбома. `force: true` это не лечит: у невидимого узла нулевой bounding box, кликать некуда.
Исправлено на `.last()` — видимый узел идёт в DOM позже.

### Таблица проверок (31/31 ✓)

| № | Проверка | Ожидание | Факт | ✓/✗ |
|---|---|---|---|---|
| 1 | Вход на «Картах», пусто (dark/light) | «0 ИЗ 78» ровно один раз | найден 1 раз в обеих темах | ✓ |
| 2 | Альбом пустой: панель (dark/light) | «Открыто 0 из 78» | найдено | ✓ |
| 3 | Подсказка при нуле (dark/light) | «Карты открываются по мере…» | найдена | ✓ |
| 4 | Пять строк «0 ИЗ …» (dark/light) | 5 совпадений `0 ИЗ (22\|14)` | ровно 5 | ✓ |
| 5 | Ячеек сетки нет при нуле (dark/light) | «· · ·» не встречается | 0 | ✓ |
| 6 | Вход на «Картах», m2 (dark/light) | «8 ИЗ 78» | найдено | ✓ |
| 7 | Панель альбома, m2 (dark/light) | «Открыто 8 из 78» | найдено | ✓ |
| 8 | Заголовок сетки старших (dark/light) | «СТАРШИЕ АРКАНЫ · 8 ИЗ 22» | найден | ✓ |
| 9 | Подсказки нет при m2 (dark/light) | 0 совпадений | 0 | ✓ |
| 10 | Закрытые ячейки (dark/light) | «· · ·» ≥ 1 | найдены | ✓ |
| 11 | Четыре строки мастей (dark/light) | «0 ИЗ 14» ровно 4 раза | 4 | ✓ |
| 12 | Полоса прогресса ≈ 8/78 (dark/light) | доля заливки в пределах ±0.01 от 8/78=0.1026 | 0.10252 (обе темы) | ✓ |
| 13 | Открытая ячейка → страница карты | «назад · Коллекция», «Общее значение» на экране | оба условия выполнены | ✓ |
| 14 | Возврат в альбом | «Открыто 8 из 78» снова на экране | найдено | ✓ |
| 15 | Тап по закрытой ячейке | URL не меняется, панель на месте | без изменений | ✓ |
| 16 | Тап по строке масти («Жезлы») | URL не меняется | без изменений | ✓ |
| 17 | Порядок секций после m4l1 | `[СТАРШИЕ АРКАНЫ·8/22, ЖЕЗЛЫ·5/14, Кубки, Мечи, Пентакли]` | ровно этот массив | ✓ |
| 18 | Гард: прямой `/collection` до онбординга | «НАЧАТЬ ПУТЬ» (`ob.start`, кнопка welcome-шага) ≥ 1 И «Открыто…» = 0 | найдено 1 раз, «Открыто» — 0 | ✓ (фикс-раунд 1: старое условие проверяло только отсутствие «Открыто», что не может покраснеть ни на онбординге, ни на 404-документе, ни на пустом экране — заменено на позитивный признак) |
| 19 | Расклад «Три карты» после выноса `StarBack` | «✶» ≥ 1 на доске | найдено | ✓ |

(19 смысловых проверок, две первые группы прогоняются в обеих темах — отсюда 31 факт в консоли
скрипта.)

### Визуальная сверка (6а, чек-лист `ui-verification.md`)

Сверка «приложение = макет» и «макет = спека» — обе пары. Скриншоты макета получены через
`page.evaluate(() => { document.getElementById('sc').setAttribute('data-mode', X); show('v-…'); })`
на `design-reference.html` (демобар не кликался напрямую — `demo('collection', this)` делает
ровно `show('v-collection')`, тот же вызов использован без DOM-клика по кнопке).

| Пункт чек-листа | Факт |
|---|---|
| Композиция (порядок блоков) | панель прогресса → заголовок секции → сетка/строки, тот же порядок, что в `v-collection`; ничего не потеряно и не добавлено от себя |
| Шрифт Cormorant у «Альбом» и «Открыто…» | подтверждено визуально (`mockup-collection-dark.png` vs `collection-m2-dark.png`) — оба засечками, размер и стиль совпадают |
| Золото полосы прогресса | заливка `accent`-градиентом слева, дорожка `line`; совпадает с макетом в обеих темах |
| Отступы 24 (поля экрана) | сохранены и в альбоме, и во входе на «Картах» — ничего не прилипает к краям |
| Закрытые ячейки: opacity .42 + «· · ·» | подтверждено (`collection-m2-dark.png`, `collection-m4-dark.png`): рубашка `StarBack` притушена, подпись «· · ·» на месте названия |
| Строки мастей с мини-рубашкой вместо глифа | подтверждено (`collection-empty-{dark,light}.png`) — 22×36 плейсхолдер со звездой вместо 🜂🜄🜁🜃 |
| Светлая тема отдельно | проверена (`*-light.png`): фон кремовый, текст не белый/не чёрный чистый, контраст программно не измерялся; `muted`-текст светлой темы (подсказка, заголовок секции, счётчики) — известное свойство темы, вынесено кандидатом в задачи спекой 25, полоса прогресса и мини-рубашки читаются |
| Обе пары «приложение=макет» и «макет=спека» | согласуются: вход «Коллекция · N ИЗ 78 ›» на «Картах» уже дорисован в макет (задача Г предыдущих шагов) и совпадает и с приложением, и со спекой |

Композиция, типографика, цвета и отступы по всем шести скриншотам приложения (`cards-entry-*`,
`collection-empty-*`, `collection-m2-*`, `collection-m4-dark`) сверены глазами с соответствующими
кадрами макета (`mockup-collection-*`, `mockup-cards-dark`) — расхождений сверх уже задокументированных
в спеке (см. ниже) не найдено.

### Расхождения с макетом

Все — заранее осознанные и уже зафиксированы в спеке (раздел «Расхождения с макетом», п. 6а-0
пройден на этапе спеки, не на этапе проверки):

| Место | Макет | Приложение | Причина |
|---|---|---|---|
| Глиф масти в строке | мини-рубашка (макет уже дорисован под решение спеки) | мини-рубашка `StarBack` 22×36 | решение спеки — совпадает, расхождения нет |
| Закрытая ячейка | opacity .42 (grayscale в макете CSS есть, но некритична) | opacity .42 без grayscale | RN-фильтры на iOS частичны, разница неразличима на двухцветной плашке — решение спеки |
| Состояние «0 из 78» | не нарисовано (мокап всегда стартует с прогрессом) | прогресс + подсказка + пять строк, проверено `collection-empty-*` | правило секций применено единообразно, макету рисовать нечего сверх решения |

Новых (незадокументированных) расхождений с макетом веб-проверка не нашла.

### Консоль браузера

- Красных ошибок (`console.error`) на функциональных сценариях (клики, навигация, сид) — 0.
- Предупреждение `props.pointerEvents is deprecated. Use style.pointerEvents` — присутствует,
  известное, не наше (стек ведёт в `@react-navigation/elements`/`react-native-screens`,
  задокументировано задачей 39, печатается на всех экранах проекта).
- Два `console.error` вида `Failed to load resource: the server responded with a status of 404
  (Not Found)` — оба привязаны к **жёсткой** (полной, не client-side) навигации
  `page.goto('http://localhost:8081/collection')` (шаги 4 и 5 сценария — проверка порядка секций
  и гард до онбординга). Раскопано отдельно: `curl` на `/collection` отдаёт статус 404 при валидном
  HTML-теле, тогда как `/review`, `/about`, `/settings`, `/card/fool`, `/lesson/m1l1` — 200 на той
  же живой сессии дев-сервера. Это не дефект кода задачи 46: маршрут корректно объявлен в
  `app/_layout.tsx` (та же форма, что у `review`), клиентская навигация на `/collection` кликом
  (проверки 6, 13, 14 выше) отрабатывает штатно и без единой ошибки. Похоже на то же семейство,
  что уже описано в CLAUDE.md по задаче 07 — Metro/expo-router (`web.output: "static"` в `app.json`)
  строит список статических маршрутов при старте дев-сервера; `app/collection.tsx` мог появиться
  в дереве уже ПОСЛЕ того, как этот конкретный процесс `expo start --web` поднялся, и жёсткая
  загрузка документа по такому URL получает 404 от дев-сервера, хотя приложение всё равно
  догружается и работает (проверка 18 «гард» зелёная). На проде/после `expo export` такого класса
  проблем не бывает (там весь список маршрутов строится один раз при экспорте). Рекомендация:
  если это будет мешать — перезапустить `npx expo start --web` (не делал сам, сервер общий,
  использую по инструкции задачи «не поднимать свой»); функциональность задачи 46 при этом
  не под вопросом — обе проверки (17, 18), зависящие от этой навигации, зелёные.
- Подпись кнопки «назад» (`headerBackTitle`) визуально не отображается рядом со стрелкой на
  скриншотах (`collection-m2-dark.png`, `card-from-collection-dark.png` — виден только сам ‹ без
  текста «Карты»/«Коллекция»), при этом текст присутствует в DOM (проверка 13 находит «Коллекция»
  через `getByText`). Не новая находка: то же самое уже задокументировано в `docs/specs/07-course-path.md`
  («Веб не показывает подпись «Курс» у кнопки «назад» на экране урока — `headerBackTitle`
  существует только на iOS; на устройстве подпись будет») — известное ограничение web-версии
  `@react-navigation/native-stack`, не относится к коду задачи 46, подпись появится на лайв-проверке.

### Скриншоты

`docs/screenshots/46/`:
- `cards-entry-dark.png`, `cards-entry-light.png` — вход «Коллекция · 0 ИЗ 78 ›» на «Картах»
- `collection-empty-dark.png`, `collection-empty-light.png` — альбом без прогресса (подсказка + 5 строк)
- `collection-m2-dark.png`, `collection-m2-light.png` — состояние макета (8 из 78, старшие сеткой)
- `collection-m4-dark.png` — граница «сетка старших → ЖЕЗЛЫ · 5 ИЗ 14 → сетка Жезлов» после m4l1
  (не `fullPage`: `FlatList` скроллится ВНУТРИ себя через `overflow`, а не через рост `document`,
  поэтому Playwright `fullPage: true` физически снимает тот же кадр 390×844, что и обычный
  скриншот — документ выше вьюпорта не растёт; вместо этого кадр получен прокруткой
  `scrollIntoView({block:'center'})` на заголовке «ЖЕЗЛЫ · 5 ИЗ 14», это и есть содержательный кадр
  для критерия приёмки «сеткой МЕЖДУ»)
- `card-from-collection-dark.png` — страница карты, открытая из альбома
- `spread-starback-after-dark.png` — расклад «Три карты» после выноса `StarBack` (регрессия)
- `mockup-collection-dark.png`, `mockup-collection-light.png`, `mockup-cards-dark.png` — эталонные
  кадры `design-reference.html` для сверки

### Число тестов и типы

```
Test Suites: 36 passed, 36 total
Tests:       1303 passed, 1303 total
```
(было 1295 в 35 сьютах на момент последней записи в CLAUDE.md; прирост — сьют `collection.test.ts`,
8 тестов, вошёл в ветку раньше задачи 8). `npx tsc --noEmit` — чисто, без вывода.

⚠️ Критерий приёмки спеки формулирует «новый сьют `collection.test.ts` ≥ 10 тестов» — по факту
в сьюте 8 тестов (`npx jest src/lib/__tests__/collection.test.ts` → `8 passed, 8 total`). Тесты
написаны раньше задачи 8 (шаг 2 плана), веб-проверка их не трогает и не добавляет — фиксирую
расхождение с числом из критерия как наблюдение, не как находку веб-проверки: содержательно
покрыты все функции модуля (`groupOf`, `collectionSections`, `collectionProgress`, `sectionMode`),
решение о достаточности — не в периметре задачи 8. Критерий исправлен волной правок финального
ревью.

### Итог

Все 31 проверка скрипта — ✓. Красный прогон подтверждён (поломка `sectionMode → 'row'` роняет
проверки 8, 10 и сценарий клика по открытой карте). Визуальная сверка (6а) и прокликивание (6б)
пройдены, значимых расхождений с макетом сверх задокументированных в спеке не найдено. Единственный
код-независимый артефакт — 404 на жёсткую навигацию к `/collection` (вероятная причина — маршрут
добавлен в дерево уже после старта текущего процесса дев-сервера), не блокирует ни один критерий
приёмки. Готово к лайв-проверке Артёма на iPhone.

## Отчёт финального ревью ветки (19.08)

Найдено: дублирующийся ряд сетки (`.row` + добивка неполного ряда) в `cards.tsx` и `collection.tsx`
дословно повторял друг друга — вынесен в `CardGridRow` (`src/components/CardCell.tsx`); критерий
приёмки называл «≥ 10 тестов» при фактических 8 — формулировка поправлена (см. выше); число тестов
в `AGENTS.md` не обновлено после мержа 46 — исправлено на 1303 в 36 сьютах; формулировка контраста
светлой темы в таблице расхождений завышала проверенность — уточнена. В бэклог: `BAR_DEPTH = 140`
(`src/lib/scrollAware.ts`) калибровался под прежнюю высоту шапки «Карт» и не пересматривался после
того, как в неё добавилась строка-вход в коллекцию; Overline 9.5/ls3.5 повторяется в шести файлах
(`cards.tsx`, `index.tsx`, `profile.tsx`, `review.tsx`, `collection.tsx` ×2) — кандидат на общий
компонент по правилу «2+ раза».

Оставлено без изменений, с обоснованием: 404 на жёсткий `goto` к `/collection` — артефакт дев-сервера
старше файла маршрута, не код-дефект; `headerBackTitle` не виден в вебе — известное ограничение
`react-navigation` на react-native-web, задокументировано раньше.

Повторный веб-прогон после рефакторинга (`webcheck-46.js`, добавлена проверка сетки справочника
после выноса `CardGridRow`): **32/32**. `npx tsc --noEmit` — чисто. `npm test` — 1303 теста
в 36 сьютах, все зелёные.

---

# 46б — альбом внутри справочника (решение Артёма 19.08 на лайв-проверке 46)

Статус: спека 46б написана 19.08, ждёт «ок». Делается В ТОЙ ЖЕ ветке `feat/46-collection` поверх
первой редакции (коммит 58e23d4), одна лайв-проверка на обе части, мерж после неё. План —
`docs/plans/46b-collection-in-reference.md`.

## Что случилось

Лайв-проверка первой редакции пройдена («всё чётко работает»), но Артём заметил дефект
ПОСТАНОВКИ: на вкладке «Карты» лежат все 78 карт открытыми, а альбом, куда ведёт строка с той же
вкладки, показывает те же карты — половину «запертыми». Читается как дубль, и только хуже.
Причины две, обе мои: (1) вход стоит там, где пользователь только что видел всё открытым;
(2) альбом не показывает ничего, чего нет в справочнике (бейдж «ИЗУЧЕНО ✓» уже есть).

## Решения (19.08)

| Вопрос | Решение |
|---|---|
| Объединить альбом и справочник? | **Да**: справочник и есть альбом. Отдельный экран `/collection`, строка-вход «Коллекция» и строки мастей удаляются. |
| Запирать неизученные карты («чтобы узнать — изучи»)? | **Нет.** Справочник — причина №1 открыть таро-приложение («значение карты таро»); карта дня и расклады постоянно ведут на неизученные карты (ритуал сломался бы или появилось бы второе понятие «открыта, потому что встречал»); freemium закрыл бы 70 карт из 78. Ничего не запирается: тап по любой карте открывает страницу. |
| Как показать «не изучена» | **Вариант (а)**: картинка приглушена — `opacity .55` на самой картинке (рамка, тень, подпись — как у всех), бейджа нет. Изученная — полный цвет + «ИЗУЧЕНО ✓». Рубашка с названием (вариант б) отклонена: новичок увидел бы 70 рубашек и не нашёл бы карту по картинке. |
| Ссылка на урок со страницы карты | **Не делаем** (решение Артёма): уроки сквозные, перепрыгнуть через модули нельзя — строка была бы лишней. |
| Слово | **«Изучено»** везде — то же, что на бейдже; «Открыто» уходит. |
| Момент «переворота» (карты урока раскрываются при следующем заходе в «Карты») | Не в 46б — хвост 46в: отдельная мини-задача с записью в motion-spec (варианты: opacity .55→1 + scale, или FlipCard; триггер — рост `learned` между фокусами вкладки, без персиста). |

## Что делаем

**Справочник `app/(tabs)/cards.tsx`:**
- Вместо строки-входа под заголовком «78 карт» — **панель прогресса** (геометрия `.colprog` первой
  редакции: `panel`/`line`, radius 15, паддинг 12/15, отступ 14; строка Cormorant 600 16 `head`;
  `ProgressBar` 6/3 с отступом 8). Текст следует за активным чипом: «Все»/«Изучено» → «Изучено 8 из 78»
  (`cards.learnedCount`); масть/старшие → «Старшие · изучено 8 из 22» (`cards.learnedCountIn`,
  `{{label}}` = подпись чипа). Заливка = `open/total` выбранной группы: при входе анимируется от 0
  (задержка 400, ход 1400, `PROGRESS_EASE`, `ReduceMotion.System`); при смене чипа — перетекает
  к новому значению за 600 мс без задержки. Поиск панель не меняет (ввод сбрасывает чип на «Все»,
  как и раньше).
- **Чип «Изучено»** — седьмой, последний в ленте (`CARD_FILTERS` + `'learned'`), подпись
  `cards.learnedFilter`; показывает только карты из `learnedCardIds`. Пустой результат при нуле
  изученных — подпись `cards.learnedEmpty` «Изученные карты появятся здесь после первого урока
  курса» (вместо «Такой карты нет…»). Ввод поиска сбрасывает и этот чип (правило одно).
- **Ячейки**: `CardCell` вместо режима `closed` получает `dimmed` — `opacity .55` на `Image`;
  справочник передаёт `dimmed={!learned.has(id)}` и `badge` только изученным. Тап — как всегда,
  страница карты с `from=cards`.
- Парящая компактная панель (`useScrollAwareBar`) не меняется: чипы там те же (лента прокручивается,
  седьмой чип уезжает за край — правило лент «от края до края», задача 19).

**Чистая логика:**
- `src/lib/cardSearch.ts`: `CardFilter` += `'learned'`, `CARD_FILTERS` += `'learned'`;
  `filterCards(list, { query, filter, lang, learned? })` — `learned?: ReadonlySet<string>`; для
  `'learned'` остаются карты из множества (нет множества — пусто), запрос складывается как у других.
- `src/lib/collection.ts`: `sectionMode`/`SectionMode` удаляются (секций-строк больше нет);
  добавляется `filterProgress(sections, filter: CardFilter): { open; total }` — `'all'`/`'learned'` →
  `collectionProgress`, группа → её секция. `collectionSections`/`collectionProgress`/`groupOf` остаются.
- Тесты: `cardSearch.test.ts` — фильтр «Изучено» (множество; без множества — пусто; с запросом
  складывается); `collection.test.ts` — `filterProgress` (all/learned/группа), тесты `sectionMode`
  удаляются.

**Удаляется:** `app/collection.tsx`, маршрут `collection` в `_layout.tsx`, `src/components/CountRow.tsx`,
`BACK_TITLES.collection`, ключи `card.backCollection`, `cards.collection`, `collection.*`; режим
`closed` и `CLOSED_LABEL` в `CardCell`; проп `from` у `CardCell` (потребитель один — `'cards'`
зашивается). Остаются: `StarBack` (расклады), `CardGridRow` (ряд сетки), `collection.ts`.

**i18n (ru/en):** `cards.learnedCount` «Изучено {{open}} из {{total}}» / «Learned {{open}} of {{total}}»;
`cards.learnedCountIn` «{{label}} · изучено {{open}} из {{total}}» / «{{label}} · learned {{open}} of {{total}}»;
`cards.learnedFilter` «Изучено» / «Learned»; `cards.learnedEmpty` «Изученные карты появятся здесь после
первого урока курса» / «Learned cards will appear here after your first lesson». Плюрализации нет
(число без склоняемого слова).

**Документы и макет:** product-spec §3 (абзац «Коллекция-альбом» → «Прогресс изучения в справочнике»),
logic-spec §13 (панель, фильтр, без экрана), design-system §5 (блок «Коллекция» → панель прогресса,
приглушение .55, чип), `design-reference.html` (`v-cards`: панель `.colprog` вместо `.colentry`, чип
«Изучено» в `.seg`, неизученные `.gc img` с `opacity:.55`; `#v-collection`, кнопка демобара
«Коллекция», JS `colgrid`, CSS `.colentry/.mini/.suitrow*/.gc.closed` — удалить), AGENTS.md, бэклог;
CLAUDE.md — при закрытии.

## Что НЕ делаем

- Запирание карт/страниц; ссылка в урок; уровни мастерства (хвост, после макета); момент
  «переворота» (46в); персист не трогаем.

## Критерии приёмки 46б — «готово, когда…»

- [ ] `tsc` чистый, `npm test` зелёный (+3 теста `cardSearch`, `collection` — `filterProgress` вместо `sectionMode`).
- [ ] «Карты» без пройденных уроков: панель «Изучено 0 из 78», пустая полоса, все 78 карт приглушены,
  бейджей нет; чип «Изучено» → подпись «Изученные карты появятся здесь…».
- [ ] После m2l1–m2l4: «Изучено 8 из 78», полоса ≈10 %, 8 карт в полный цвет с «ИЗУЧЕНО ✓», 70 приглушены;
  чип «Старшие» → «Старшие · изучено 8 из 22», полоса перетекает; чип «Изучено» → ровно 8 карт.
- [ ] После m4l1: «Жезлы · изучено 5 из 14» на чипе «Жезлы»; «Изучено» → 13 карт.
- [ ] Маршрута `/collection` нет (файл удалён, `_layout.tsx` без него); строки «Коллекция» на «Картах» нет.
- [ ] Опасити картинок: неизученная 0.55, изученная 1 (веб — `getComputedStyle`).
- [ ] Скриншоты 390×844 обеих тем (`docs/screenshots/46/cards-progress-*`, `cards-learned-*`), сверка
  с обновлённым `v-cards`; консоль без ошибок.
- [ ] Лайв-проверка Артёма ✓ → мерж, push, бэклог `[x]`, CLAUDE.md.

## Сценарий лайв-проверки 46б (iPhone)

1. «Карты»: под «78 карт» панель «Изучено N из 78» с полосой; неизученные карты приглушены,
   изученные — ярче и с бейджем «ИЗУЧЕНО ✓»; читаемость приглушённых в обеих темах (если .55
   слишком тускло — скажи, это одна константа).
2. Чип «Старшие» → «Старшие · изучено 8 из 22», полоса перетекает плавно; «Жезлы» → «Жезлы ·
   изучено 0 из 14» (или 5 из 14 после m4l1).
3. Чип «Изучено» → только изученные; после DEV-сброса курса — подпись «Изученные карты появятся
   здесь после первого урока курса».
4. Набрать букву в поиске — чип сбрасывается на «Все», панель показывает «Изучено N из 78».
5. Прокрутить вниз и свистнуть вверх — компактная панель поиска с чипами (семь, лента
   прокручивается до «Изучено»).
6. Страница карты из справочника — «назад · Карты», как раньше; расклады — рубашки как раньше.
7. Прокрутить «Карты» примерно на треть экрана и свистнуть вверх — парящая панель поиска
   поднимается поверх ещё видимого поля в потоке: не выглядит ли это «двумя полями поиска»
   (хвост г — BAR_DEPTH).
8. Светлая тема: читается ли бейдж «ИЗУЧЕНО ✓» на ярких картах (хвост ж).

## Отчёт веб-проверки 46б (19.08)

Playwright headless, chromium, viewport 390×844 (dpr 2), dev-сервер `http://localhost:8081`
(уже запущенный, ветка `feat/46-collection`, HEAD `453dd55`). Скрипт —
`scratchpad/webcheck-46b.js` (рядом с `webcheck-46.js` первой редакции), написан заново по
приёмам предыдущего: сид `goto → evaluate(localStorage 'arcanum-app') → reload`, три состояния
(`empty`, `m2` — m2l1–m2l4, `m4` — +m4l1), листовые `div` для поиска текста, `.last()` для
таба «Карты» (под экраном остаётся смонтированный таб-навигатор — прецедент задачи 39),
`click({force:true})` (карта дня и герои качаются бесконечно, обычный `.click()` ждёт
неподвижности и падает по таймауту). Тексты-признаки взяты построчным чтением `src/lib/i18n.ts`
и `src/lib/cardSearch.ts` (`CARD_FILTERS`), не из памяти.

### Красный / зелёный прогон (правило проекта)

Временная поломка `src/components/CardCell.tsx`: `DIM_OPACITY = 1` вместо `0.55` (`git diff`
проверен точечно, только эта строка). Прогонялись именно те два узла, что проверяют опасити —
изолированным скриптом (полный `webcheck-46b.js` пишет скриншоты, гонять его на сломанном коде
поверх уже собранных зелёных кадров не нужно):

```
[СЛОМАНО] «Дурак» при 0 изученных, ожидание 0.55: 1  FAIL (красный прогон корректен)
[СЛОМАНО] «Туз Жезлов» неизучен, ожидание 0.55: 1    FAIL (красный прогон корректен)
[СЛОМАНО] «Дурак» изучен, ожидание 1 (не должна упасть): 1  PASS (ожидаемо)
```

Обе проверки на 0.55 красные ровно там, где обязаны быть; проверка на «изучена → opacity 1»
осталась зелёной, как и должна (поломка не трогает изученные карты) — сценарий ловит именно
свою цель, а не разваливается целиком. Файл возвращён `git checkout -- src/components/CardCell.tsx`,
`git status --short src/ app/` пуст. Полный прогон после отката — 37/37 (таблица ниже), скриншоты
пересобраны на зелёном коде.

### Таблица проверок (37/37 ✓)

| № | Проверка | Ожидание | Факт | ✓/✗ |
|---|---|---|---|---|
| 1 | Панель при нуле, dark/light | «Изучено 0 из 78» | найдено 1 раз в обеих темах | ✓ |
| 2 | Полоса при нуле, dark/light | ratio < 0.01 | 0 в обеих темах | ✓ |
| 3 | «Дурак» приглушён при нуле, dark/light | opacity 0.55 | 0.55 в обеих темах | ✓ |
| 4 | Бейджей нет при нуле, dark/light | 0 | 0 в обеих темах | ✓ |
| 5 | Чип «Изучено» при нуле → подсказка, dark/light | `cards.learnedEmpty` | найдена в обеих темах | ✓ |
| 6 | Панель, m2, dark/light | «Изучено 8 из 78» | найдено | ✓ |
| 7 | Полоса, m2, dark/light | ratio ≈ 8/78 = 0.1026, допуск ±0.01 | 0.0982–0.0989 | ✓ |
| 8 | «Дурак» изучен, m2, dark/light | opacity 1 | 1 в обеих темах | ✓ |
| 9 | «Туз Жезлов» не изучен, m2, dark/light | opacity 0.55 | 0.55 в обеих темах | ✓ |
| 10 | Чип «Старшие», m2, dark/light | «Старшие · изучено 8 из 22» | найдено | ✓ |
| 11 | Полоса чипа «Старшие», dark/light | ratio ≈ 8/22 = 0.3636, допуск ±0.02 | 0.3636 в обеих темах | ✓ |
| 12 | Чип «Изучено», m2, dark/light | панель «Изучено 8 из 78» держится | найдено | ✓ |
| 13 | Чип «Изучено», m2, dark/light | ровно 8 бейджей «ИЗУЧЕНО ✓» | 8 в обеих темах | ✓ |
| 14 | Чип «Жезлы», m4 (dark) | «Жезлы · изучено 5 из 14» | найдено | ✓ |
| 15 | Полоса чипа «Жезлы», m4 | ratio ≈ 5/14 = 0.3571, допуск ±0.02 | 0.3571 | ✓ |
| 16 | Чип «Изучено», m4 | панель «Изучено 13 из 78» | найдено | ✓ |
| 17 | Чип «Изучено», m4 | ровно 13 бейджей | 13 | ✓ |
| 18 | Поиск сбрасывает чип на «Все» | панель возвращается к «Изучено 13 из 78» (не «Жезлы · …») | найдено | ✓ |
| 19 | Чип «Все» активен цветом после сброса | `color === accent` темы (не `muted`) | `rgb(226,189,114)` (dark) | ✓ |
| 20 | Клик «Дурак» → страница карты | «ОБЩЕЕ ЗНАЧЕНИЕ» на экране | найдено | ✓ |
| 21 | Возврат на «Карты» | панель «Изучено 8 из 78» снова | найдено | ✓ |
| 22 | Маршрут `/collection` | файл `app/collection.tsx` физически отсутствует | подтверждено (`fs.existsSync` → false) | ✓ |
| 23 | Регрессия расклада «Три карты» | рубашки со звездой ✶ на доске | найдено | ✓ |

(23 смысловые проверки, первые пять групп прогоняются в обеих темах — отсюда 37 фактов в консоли
скрипта: 5×2 + 2×2 + 2×2 + 2×2 + 2×2 + 6 + 3 = 37.)

⚠️ Проверка 20 сначала падала: ключ `card.general` («Общее значение») используется как заголовок
блока и в разметке приведён через `.toUpperCase()` (не CSS `text-transform`, а сам код) —
`textContent` реально равен «ОБЩЕЕ ЗНАЧЕНИЕ», а не «Общее значение». Проверено чтением DOM
(`textContent`, не `innerText`: у последнего браузер сам применяет `text-transform`, и разница
между этими двумя источниками edit ложный зелёный дал бы одинаково, а ложный красный увёл бы
не в ту сторону). Прогон покраснел один раз до фикса — по правилу проекта это ожидаемо и не
задокументировано отдельным красным прогоном, поскольку ловил ошибку СКРИПТА, а не кода.

⚠️ Проверка 19 (цвет активного чипа) добавлена, чтобы независимо подтвердить возврат фильтра
на «Все» после ввода в поиск: `Filters` смонтирован в приложении ДВАЖДЫ одновременно (шапка
в потоке списка + парящая scroll-aware панель, комментарий в `cards.tsx`: «Живут в двух местах
сразу»), поэтому одного текстового совпадения панели недостаточно как позитивного признака —
цвет `t.accent` активного чипа (`rgb(226,189,114)` dark / `rgb(168,128,46)` light) даёт второй,
независимый сигнал того же факта. Оба экземпляра чипов и полей идут в DOM по одному правилу:
контент `AnimatedList` (шапка) в JSX предшествует парящей `Animated.View`-панели, поэтому
`.first()` стабильно берёт видимую при `scrollY=0` копию — свойство проверено на всех клика́х
скрипта, ни один не попал по невидимому дублю.

### Визуальная сверка (6а, чек-лист `ui-verification.md`)

Обе пары — «приложение = макет» и «макет = спека» (правило 6а-0). Мокап уже дорисован под
решения 46б (проверено чтением `design-reference.html` ДО прогона, не по памяти): `#v-cards`
несёт `.colprog` с «Изучено 8 из 78» и семь чипов, включая «Изучено» последним
(`grep 'id="v-cards"'`), `.gc img.dim{opacity:.55}` — то же число, что `DIM_OPACITY` в коде.

| Пункт чек-листа | Факт |
|---|---|
| Композиция | панель прогресса → поиск → чипы → сетка, тот же порядок, что в `v-cards`; строка-вход и строки мастей первой редакции нигде не остались |
| Шрифт Cormorant у «78 карт» и «Изучено N из M» | подтверждено визуально (`mockup-cards-{dark,light}.png` vs `cards-progress-{dark,light}.png`) — оба засечками, размер и стиль совпадают |
| Золото полосы прогресса | заливка `accent`-градиентом слева, дорожка `t.line`; совпадает с макетом в обеих темах |
| Приглушение неизученных | `.55` в коде = `.55` в CSS макета (`.gc img.dim`); на скриншотах разница тонкая на ярком art деке — визуально её легко принять за особенность самой иллюстрации, поэтому решающий довод — измеренная `getComputedStyle`, не взгляд на кадр (см. проверки 3/9) |
| Бейдж «ИЗУЧЕНО ✓» | только у изученных, тот же `CornerBadge`, что был в задаче 08/39 — расхождений нет |
| Седьмой чип «Изучено» | на своём месте последним, активный — золотая рамка и текст `accent`, как остальные чипы в активном состоянии |
| Отступы 24 (поля экрана) | сохранены и у панели, и у чипов (лента чипов — от края до края, задача 19, панель и поиск — в паддинге) |
| Обе темы отдельно | проверены (`*-light.png`): фон кремовый, панель и чипы читаются, полоса прогресса видна |

Новых (незадокументированных в спеке) расхождений с макетом не найдено — единственные два
(глиф отсутствует по построению, поскольку макет уже не рисует альбом отдельно; grayscale
закрытой ячейки был расхождением ПЕРВОЙ редакции и снят вместе с самим понятием «закрытая
ячейка» в 46б) сняты самой задачей, а не оставлены проверкой.

### Консоль браузера

- Приложение (все сценарии — сиды, клики, чипы, поиск, навигация, расклад): **0** ошибок
  (`console.error`). Предупреждение `props.pointerEvents is deprecated` не встретилось в этом
  прогоне ни разу (в отличие от отчёта первой редакции) — оно печатается `warnOnce`, то есть
  один раз за загрузку страницы, и в это окно не попало ни одного из перезагруженных состояний;
  не свидетельство того, что предупреждение исчезло из кода (стек ведёт в
  `@react-navigation/elements`/`react-native-screens`, не в код задачи, задокументировано в
  задаче 39).
- Мокап (`design-reference.html`, статический файл вне приложения): **3** ошибки
  `Failed to load resource: net::ERR_INVALID_URL` — проверено ИЗОЛИРОВАННО: они воспроизводятся
  при простом открытии файла без единого клика и без вызова `show('v-cards')`, то есть не связаны
  ни с прогоном скрипта, ни с правками 46б в самом макете. Не относятся к коду приложения —
  зафиксированы как известное свойство файла-макета, чинить вне периметра задачи 46б.

### Скриншоты

`docs/screenshots/46/` (старые кадры первой редакции удалены — `git rm`, список ниже):
- `cards-progress-empty-dark.png`, `cards-progress-empty-light.png` — панель «Изучено 0 из 78»,
  вся сетка приглушена, бейджей нет
- `cards-learned-empty-dark.png` — чип «Изучено» при нуле: подсказка `cards.learnedEmpty`
- `cards-progress-dark.png`, `cards-progress-light.png` — состояние макета (8 из 78, 8 карт
  в полный цвет с бейджем, остальные приглушены)
- `cards-learned-filter-dark.png` — чип «Изучено» (m2): восемь изученных карт, без приглушённых
- `spread-starback-after-46b-dark.png` — расклад «Три карты» после правок 46б (регрессия `StarBack`)
- `mockup-cards-dark.png` (обновлён поверх кадра первой редакции — тот же файл, то же место
  в макете, содержимое актуализировано), `mockup-cards-light.png` — эталонные кадры `v-cards`

Удалены (`git rm`, не соответствуют текущему приложению — экран `/collection` физически убран):
`collection-empty-{dark,light}.png`, `collection-m2-{dark,light}.png`, `collection-m4-dark.png`,
`card-from-collection-dark.png`, `cards-entry-{dark,light}.png`, `mockup-collection-{dark,light}.png`,
`spread-starback-after-dark.png` (заменён на `spread-starback-after-46b-dark.png` — та же
регрессионная проверка, актуальное имя).

### Число тестов и типы

```
Test Suites: 36 passed, 36 total
Tests:       1307 passed, 1307 total
```
(было 1303 в 36 сьютах на момент финального ревью первой редакции 46 — прирост четыре теста
в уже существующих сьютах `cardSearch.test.ts`/`collection.test.ts`: фильтр «Изучено» с/без
множества и в сочетании с запросом, `filterProgress` для `all`/`learned`/группы взамен
удалённых тестов `sectionMode`). `npx tsc --noEmit` — чисто, без вывода.

### Итог

Все 37 проверок скрипта (23 смысловые, часть — в обеих темах) — ✓. Красный прогон подтверждён
(`DIM_OPACITY=1` роняет ровно те две проверки, что и должен, не задевая соседние). Визуальная
сверка (6а) и прокликивание (6б) пройдены, консоль приложения чистая, новых расхождений
с макетом не найдено. `npm test` — 1307/1307, `tsc` чистый. Готово к лайв-проверке Артёма
на iPhone (сценарий выше).
