# 62 · Пейвол без магазина: честное состояние вместо плейсхолдерных цен

Статус: **«ок» Артёма 26.08** с уточнениями сверки с кодом (Д1′/Д3′/Д4′ ниже, разбор —
`docs/plans/62-paywall-no-store.md`, раздел «Вердикт»). Найдено при разборе «что дальше» после
верификации аккаунта Google — не аудитом и не тестом, а вопросом «что увидит тестер в сборке».

**Решения Артёма 26.08.** (1) Текст состояния «нет предложений» — нейтральный, без обещания
срока: «Оформить подписку пока нельзя» / «Попробуйте позже — всё, что уже открыто, останется
доступным»; то же для диалога. (2) Порядок работ: **61 (первая сборка) → 53б (RevenueCat,
подписки в консолях, цены) → закрытый тест** — тестеры увидят настоящие покупки; 62 — страховка
внутреннего теста и постоянное состояние «магазин не ответил» после 53б, а не временный хак:
в 53б меняется только причина, по которой оно показывается.

## Что не так сейчас (проверено по коду 25.08)

`src/lib/purchases.ts` — заглушка задачи 53а, и она честна ровно в одном окружении, Expo Go:

- `getOffers()` **всегда** возвращает `PLACEHOLDER_OFFERS`: «2 890 ₽» за год, «399 ₽» за месяц,
  «≈ 241 ₽ в месяц», бейдж «−40 %». Экран рисует из них два тарифа и надпись кнопки
  «Оформить за 2 890 ₽ в год» (`app/paywall.tsx`, `offers.map` и `CtaButton`).
- `purchase()` и `restore()` возвращают `{ ok: false, reason: 'unavailable' }`, экран показывает
  диалог «Пока недоступно · Покупки появятся в сборке из App Store и Google Play. В Expo Go
  оформить подписку нельзя.»

В Expo Go текст диалога совпадает с реальностью. **В сборке из магазина он становится ложью**, а
цены — вдвойне: аккаунт продавца белорусский, валюта витрины USD, и рубли не появятся ни у одного
покупателя. Тестер закрытого трека видит витрину с ценой, нажимает и получает «в Expo Go нельзя»,
хотя никакого Expo Go у него нет.

Почему это блокер, а не косметика:

1. **Закрытые треки Google ревьюит.** Неработающая покупка и цена, которой нет в магазине, — прямой
   кандидат на «broken functionality» и на вопросы про подписку, которой ещё нет в консоли.
2. **Класс дефекта переживёт 53б.** Когда покупки станут настоящими, `getOffers` начнёт спрашивать
   магазин, а магазин умеет не ответить (нет сети, продукт не одобрен, страна без продаж). Если
   фолбэком останется массив плейсхолдеров, первый же сбой магазина покажет пользователю
   выдуманную цену. Правило проекта («правка, снимающая дефект, вносит дефект того же класса»)
   требует закрыть источник, а не текст.

## Цель

Пейвол никогда не показывает цену, которой не назвал магазин, и никогда не предлагает нажать
кнопку, которая не может купить.

## Решения

- **Д1. Источник правды — не флаг окружения, а наличие предложений.** Экран смотрит не на
  `PURCHASES_AVAILABLE`, а на то, вернул ли `getOffers()` непустой список. Тогда одно и то же
  состояние покрывает и заглушку 53а, и сбой магазина в 53б, и страну без продаж — без новых
  ветвлений. `PURCHASES_AVAILABLE` остаётся для 53б (решение, звать ли SDK вообще).
  **Д1′ (сверка 26.08):** состояний предложений ТРИ — `null` «ещё не запрошены» (первый кадр,
  слот пустой), `[]` «нет» (панель), непустой (тарифы). Со стартовым `[]` в 53б ветка «скоро»
  мигала бы первым кадром, пока магазин грузит витрину (класс «нет данных» ≠ «нет совпадений», урок 46).
- **Д2. Что показываем вместо тарифов.** Шапка, эмблема и четыре строки «что даёт Premium»
  остаются как есть — это описание, оно правдиво всегда. Вместо тарифов и CTA — одна панель
  того же вида, что панель «Подписка активна» (`st.panel`), с текстом **«Оформить подписку пока
  нельзя»** и второй строкой **«Попробуйте позже — всё, что уже открыто, останется доступным»**
  (решение Артёма 26.08: без обещания срока — то же состояние после 53б значит «магазин не
  ответил»). Ни цифр, ни кнопки, ни диалога.
- **Д3. «Восстановить покупки» в состоянии «нет предложений» не показываем.** Восстанавливать
  нечего, а нажатие ведёт в тот же диалог-заглушку. Ссылки «Условия · Конфиденциальность»
  **остаются** — Apple 3.1.2 требует их на экране подписки, и они нужны там независимо от
  состояния покупок. **Д3′ (сверка 26.08):** ссылка живёт внутри каждого состояния, как `.trlink`
  внутри `#pwOff`/`#pwOn` макета (в «активна» остаётся как сейчас); в ветке «нет предложений» она
  рендерится под `PURCHASES_AVAILABLE &&` — в 62 скрыта, в 53б при пустом ответе магазина
  появится сама: восстановление права не зависит от витрины, и платящий пользователь на новом
  телефоне без сети до витрины не должен терять «Восстановить».
- **Д4′ (заменяет Д4 от 25.08). Плейсхолдерные цены удаляются целиком, `purchases.web.ts` в 62
  не заводится.** Первая редакция хотела отдавать плейсхолдеры только в вебе ради сверки 6а
  с витриной макета — но тогда сценарий 6б на вебе никогда не увидел бы экран без «₽», а
  состояние, которое реально уедет пользователям, осталось бы единственным, которое веб не умеет
  отрисовать (требования противоречили друг другу в одном прогоне). Ветку тарифов 62 не меняет —
  она сверена в 53а/56 и изменится только в 53б с настоящими ценами, где будет сверена заново.
  Итог: `getOffers()` → `[]` на всех платформах; витрина с ценами до 53б живёт только в макете
  (состояние А) и в тесте как мутация; `purchases.web.ts` появится в 53б вместе с нативным
  импортом RevenueCat. Механизм `.web.ts` при этом исправен (пересборка 06б); jest с пресетом
  `jest-expo` = ios, `.web.ts` он не видит — проверялся бы явным импортом.
- **Д5. Диалог «Пока недоступно» не удаляем**: он остаётся для `restore` и «Управлять подпиской»
  в состоянии «активна» (DEV-тумблер), но его текст перестаёт называть Expo Go — **«Покупки пока
  недоступны. Попробуйте позже.»** (в сборке из стора фраза про Expo Go — вранье).

## Что НЕ делаем

- Не подключаем RevenueCat, не заводим продукты, не считаем скидку — это 53б целиком.
- Не трогаем гейты, право `premium` в сторе, DEV-тумблер и `premium.ts` — они уже верны.
- Не меняем макет `v-paywall`: он рисует состояние с ценами, и оно вернётся в 53б. Правило
  «задача, меняющая экран, правит макет» здесь не срабатывает: мы добавляем состояние, которого
  в макете не было, и оно временное. ⚠️ Но в макет **дописывается блок** `.pwsoon` рядом с
  `.pwstate`, иначе следующий аудит (56-подобный) объявит расхождением уже исправленный экран.

## Критерии приёмки

- [x] `getOffers()` отдаёт `[]` на всех платформах (юнит `purchases.test.ts`); в исходниках
      `purchases.ts`, `paywall.tsx` и в строках `paywall.*` четырёх языков нет знаков валют —
      цена бывает только подстановкой `{{price}}` (контракт там же).
- [x] На экране пейвола без предложений: нет строки с «₽», нет кнопки «Оформить», нет ссылки
      «Восстановить покупки»; есть панель «Оформить подписку пока нельзя»; ссылки
      «Условия» и «Конфиденциальность» на месте и открываются. До ответа `getOffers()` слот пуст.
- [x] Состояние «подписка активна» (DEV-тумблер) не изменилось: панель, «Управлять подпиской»,
      чип «✦ АКТИВНА».
- [x] Строки добавлены во все **четыре** языка (`i18n.ts`), плюрализации в них нет; текст
      «Expo Go» из `unavailableText` убран во всех четырёх.
- [x] Контракт: тест падает, если `getOffers` нативной платформы вернёт непустой список при
      `PURCHASES_AVAILABLE === false` (мутация — вернуть плейсхолдеры).
- [x] `npx tsc --noEmit` чист, `npm test` зелёный, веб-проверка 6б по экрану пейвола (переход
      из курса, раскладов, тренажёра, лунной панели и настроек) — все входы ведут на экран
      без цен.
- [x] Красный прогон: до правки сценарий 6б обязан найти «₽» на экране, после — нет.

## План по файлам

Порядок и точный код — `docs/plans/62-paywall-no-store.md` (сценарий и красный прогон идут ПЕРВЫМИ).

1. `scripts/check_62_web.js` — сценарий 6б: пять входов на пейвол, отсутствие «₽» и кнопки,
   наличие ссылок, состояние Б, диалог без Expo Go; красный прогон на коде 53а.
2. `src/lib/__tests__/purchases.test.ts` + `src/lib/purchases.ts` — `getOffers` → `[]`,
   плейсхолдеры удалены, контракт «цен в коде нет».
3. `src/lib/i18n.ts` — четыре языка: `paywall.soonTitle`, `paywall.soonSub`, правка
   `paywall.unavailableText`.
4. `app/paywall.tsx` — `Offer[] | null`, ветка «нет предложений», «Восстановить» внутри состояний,
   ссылки условий вне ветки.
5. `docs/design-reference.html` — блок `#pwSoon` + `pwSoon(on)`; `scripts/shoot_56.js` — хук
   `mockSetup` у пары `paywall`, чтобы аудит макета не считал экран расхождением.
6. Зелёный прогон + мутация сценария, `npm test`, доки. Коммит `feat: пейвол без магазина —
   состояние «скоро» вместо плейсхолдерных цен (spec 62)`.

## Отчёт веб-проверки 6а/6б (26.08.2026)

Реализация шла по `docs/plans/62-paywall-no-store.md`, шесть задач, шесть коммитов
(`92af254`, `6c93b88`, `b35613a`, `2d2b4ba`, `946ce6b` + этот).

**6б — сценарий `scripts/check_62_web.js`** (пять входов на пейвол: настройки, курс, расклады,
тренажёр, луна; плюс ссылки условий, состояние Б и светлая тема):

| прогон | код | результат |
|---|---|---|
| КРАСНЫЙ (первым, до правок) | `56a25af` — заглушка 53а | **упало 31 из 58**: по 5 на каждом из шести экранов «скоро» (`₽`, «Оформить за …», «Восстановить покупки», soonTitle «ключа нет», soonSub «ключа нет») + «диалог не упоминает Expo Go». Все пять входов зелёные, консоль 0 |
| МУТАЦИЯ на слепоту | `getOffers → [{ id: 'year', price: '1' }]` | **упало 24 из 58** — по 4 на каждом из шести экранов; «₽» законно осталась зелёной (в мутации знака валюты нет) |
| ЗЕЛЁНЫЙ | итоговый код | **58 из 58**, ошибок в консоли 0 |

⚠️ **Находка о самой мутации:** первая редакция подсовывала `{ id: 'month' }` и роняла только
**18** — проверка «нет кнопки „Оформить за …“» оставалась зелёной, потому что `plan` по умолчанию
`'year'`, `chosen` не находится и кнопка берёт запасную надпись (заголовок экрана). Мутация
проверяла меньше, чем казалось. Годовой тариф добирает недостающие 6 проверок; правило записано
в шапку сценария.

⚠️ **Дефект плана, снятый по ходу (задача 2):** докстринг `purchases.ts`, приведённый в плане
дословно, содержал «2 890 ₽» — и его же контракт-тест запрещает знаки валют во ВСЁМ файле
(регулярка идёт по тексту файла, комментарии включительно). Сумма из комментария убрана совсем
(«Плейсхолдерных цен задачи 53а здесь больше нет»).

⚠️ **`page.clock.install` заменён на `page.clock.setFixedTime`:** сценарию нужна только
подменённая дата (27.08.2026 — ближайшее лунное событие полнолуние 28.08, вход «луна» ведёт
на пейвол в любой день прогона), а `install` морозит ещё и таймеры, на которых держатся
анимации входа и переходы роутера.

**6а — сверка с макетом** (`node scripts/shoot_56.js docs/screenshots/62 paywall`): снято пар 1
из 1, четыре кадра `paywall-{app,mock}-{dark,light}.png`. Композиция совпадает в обеих темах:
шапка → эмблема → заголовок → подзаголовок → четыре строки «что даёт Premium» → панель «скоро»
(две строки, влево, вид `.pwstate`) → абзац автопродления со ссылками. Ни цен, ни кнопки, ни
«Восстановить покупки» — ни в приложении, ни в макете. Расхождений нет. Полный аудит макета
после правки скрипта — **16 пар из 16**.

**Типы и тесты:** `npx tsc --noEmit` чист; `npm test` — **1704 теста в 47 сьютах** (было 1695
в 46, добавился `purchases.test.ts` — 9 проверок). Правкой финального ревью периметр контракта
`purchases.test.ts` расширен с блока `paywall.*` на ВСЕ исходники `src/**`+`app/**` (по одной
проверке `it.each` на файл) и на все строки интерфейса всех четырёх языков — кейсов стало
**1849** (сьютов по-прежнему 47).

## Сценарий лайв-проверки 6в (Артём, iPhone, Expo Go)

1. Настройки → «Arcanum Premium» → экран без цен и без кнопки: шапка, эмблема, четыре строки
   «что даёт Premium», панель «Оформить подписку пока нельзя / Попробуйте позже — всё, что уже
   открыто, останется доступным», ниже — только абзац про автопродление со ссылками «Условия» и
   «Конфиденциальность» (открываются во внутреннем браузере). «Восстановить покупки» нет.
2. Курс → тап по узлу модуля 3 → тот же экран, кнопка «назад» подписана «Курс».
3. Настройки → DEV «Premium» вкл → «Arcanum Premium»: чип «✦ АКТИВНА», панель «Подписка активна /
   DEV-режим», «Управлять подпиской», под ней «Восстановить покупки». Тап «Управлять» → диалог
   «Пока недоступно / Покупки пока недоступны. Попробуйте позже.» — без слов Expo Go.
4. Язык → English → пейвол: «Subscribing isn't available yet» — тексты панели переведены.
5. Светлая тема — панель читается, ничего не пропало.

## Что осталось от спеки в 53б (не терять)

- `purchases.web.ts` появляется вместе с нативным импортом RevenueCat; веб-заглушка отдаёт `[]`.
- `purchases.test.ts`: снять `expect(PURCHASES_AVAILABLE).toBe(false)`, добавить мок SDK; контракт
  «цен в коде нет» остаётся навсегда.
- Ветка «скоро» при `PURCHASES_AVAILABLE` показывает «Восстановить покупки» — уже заложено.
- Анкета «безопасность данных» в Play Console переписывается ДО релиза (release-checklist).
