# Спека 26 (заход 1 из 4): контент М3 «Старшие арканы II: испытания и свет»

Статус: согласована Артёмом 14.08; реализация сделана 14.08 (три коммита по плану),
веб-проверка 6б пройдена — детали в отчёте внизу. Ждёт: вычитка редактора → reviewed,
лайв-проверка Артёма.
Задача 26 бэклога, решение 13.08: теория и викторины М3–М6 по схеме М1–М2, по одному модулю
за заход. Этот заход — только М3. М4–М6 — следующие заходы, каждый со своим коммитом-циклом
по этой же спеке (новая спека не нужна, меняется только номер модуля и staging-файл).

## Цель

22 урока курса стоят без текстов — путь обрывается после М2. Наполнить М3 (6 уроков, 14 старших
арканов VIII–XXI), чтобы курс продолжался: движок готов (задача 08), тексты появляются — уроки
сразу играют.

## Что делаем

### 1. Теория — 6 текстов прямо в `content/course.json`

Формат в точности как у М1–М2: поле `theory: {ru, en, status: "draft"}` у каждого урока М3.

- Объём ru ~1700–1900 знаков (диапазон М1–М2: 1697–1927).
- Структура по образцу М2 (эталон — m2l1 «Дурак и Маг»):
  связка с аркой «путешествие Дурака» → карта за картой (что изображено у Уэйта → смысл →
  ключевые слова → сила/риск) → синтез пары/тройки (чему учат вместе) → упражнение
  «Попробуйте сегодня» с конкретным бытовым действием.
- ru и en пишутся сразу оба; en — не подстрочник, глоссарий из content-guide
  (Strength/Hermit/Wheel of Fortune…, названия как в cards.json).
- Согласованность: смыслы карт не противоречат текстам значений в `content/cards.json`
  (general/symbolism) и чипам `keywords` — сверять при написании.
- Тон — content-guide: на «вы», тепло, без эзотерического жаргона, про рефлексию,
  НЕ про предсказание. Трудные карты М3 (Смерть, Дьявол, Башня, отчасти Луна) — бережно:
  назвать трудность честно + дать опору и выход. «Сбылось» и обещания сбычи запрещены.

Уроки и карты (из course.json, состав не меняем):

| Урок | Карты | Особенность |
|---|---|---|
| m3l1 Сила и Отшельник | strength, hermit | мягкая сила vs уединение |
| m3l2 Колесо Фортуны и Справедливость | wheel-of-fortune, justice | внешние повороты vs собственный выбор |
| m3l3 Повешенный, Смерть, Умеренность | hanged-man, death, temperance | тройка; Смерть — бережно, «завершение, не событие» |
| m3l4 Дьявол и Башня | devil, tower | обе трудные: несвобода и обрушение — честно + выход |
| m3l5 Звезда, Луна, Солнце | star, moon, sun | тройка света: надежда → сомнение → ясность |
| m3l6 Суд и Мир. Путь Дурака целиком | judgement, world | финал: замкнуть арку всех 22 карт |

### 2. Викторины — 30 вопросов в новом staging-файле `content/quiz-m3.json`

- Схема файла та же, что у quiz-m1-m2.json: `{_comment, status: "draft", lessons: [{lessonId,
  questions: [5 шт.]}]}`; вопрос: `type single|card`, ровно 3 варианта, `correct` 0..2,
  `q`/`options`/`explain` двуязычные, у card — `cardId` из колоды.
- По образцу М2: 1–2 card-вопроса на урок (изображение над вопросом, «узнай карту»),
  остальные single на понимание теории.
- Пояснения `explain` — БЕЗ утвердительных зачинов («Верно!», «Да!», «Именно!»):
  показываются после любого ответа, префикс «Верно. / Не совсем.» добавляет код (задача 21).
  Формулировка подходит под оба исхода.
- Правильные ответы распределены по индексам (контракт-тест: не всё на одном индексе).

### 3. Код-правка: скрипты учатся работать с несколькими staging-файлами

Решение Артёма 14.08: файл на модуль + глоб (для М4–М6 скрипты трогать уже не придётся).

- `scripts/merge_quiz.py`: константа `QUIZ` → перебор всех `content/quiz-*.json`;
  каждый файл несёт свой `status`, уроки каждого файла получают его в `quizStatus`.
  Валидация и формат записи не меняются.
- `scripts/set_status.py`: часть `quiz` двигает статус во ВСЕХ `content/quiz-*.json`
  (у каждого файла статус свой — reviewed у М1–М2 не откатывается и не перескакивает).
- `src/lib/__tests__/courseContent.test.ts`: контракт расширяется на m3
  (16 уроков, те же проверки схемы) — в том же коммите, что и контент.

## Что НЕ делаем

- М4–М6 — следующие заходы (по модулю за раз, решение 13.08).
- Не трогаем состав уроков, названия, `cards`, флаг `free` — только добавляем `theory`/`quiz`.
- Не меняем движок урока, типы `QuizType`/`QuizQuestion`, схему стора (persist не трогается).
- Не двигаем статусы дальше draft — reviewed ставит только редактор после вычитки.
- Тексты значений карт (cards.json) не правим ни словом.

## Порядок и git

В main мелкими коммитами (решение Артёма 14.08; черновики сразу видны жене в приложении —
прецедент birth_path):

1. `feat: merge_quiz и set_status работают с quiz-*.json (spec 26)` — правка скриптов,
   прогон на существующем quiz-m1-m2.json — идемпотентность: course.json не меняется.
2. `content: теория М3, 6 уроков, draft (spec 26)` — тексты в course.json + расширение
   контракт-теста на теорию m3.
3. `content: викторины М3, 30 вопросов, draft (spec 26)` — quiz-m3.json + `merge_quiz.py`
   + расширение контракт-теста на викторины m3.

После каждого шага: `npx tsc --noEmit` + `npm test` зелёные, потом push.

## Критерии приёмки («готово, когда…»)

- [x] У всех 6 уроков М3 есть `theory` (ru+en, status draft) и `quiz` (5 вопросов, quizStatus draft).
- [x] `python scripts/merge_quiz.py` идемпотентен и не трогает М1–М2 (quizStatus остаётся reviewed).
- [x] `set_status.py --dry-run` показывает раздельные статусы файлов; статус М1–М2 не движется
      (сухой прогон 14.08: `draft → reviewed` для quiz = 0 у М1–М2, `reviewed → final` не задевает М3).
- [x] Контракт-тест покрывает m3 (сьют «М1–М3, 16 уроков»); `npm test` (401) и `npx tsc --noEmit` зелёные.
- [x] Веб-проверка 6б пройдена 14.08 (Playwright, вьюпорт 390×844): DEV-строками «Сбросить прогресс
      курса» + 10 × «Пройти следующий урок» открыт М3; урок m3l1 пройден целиком — 4 страницы теории,
      2 карты урока с чипами ключевых слов, 5 вопросов. Проверено: ответ с ошибкой даёт префикс
      «Не совсем.» без двойного зачина, верный — «Верно.»; card-вопрос показывает изображение;
      финал «+8 XP» (10 − 2×1 ошибка — формула верна), «ПРОЙДЕНО 1 ИЗ 6 УРОКОВ МОДУЛЯ»
      (плюрализация верна); возврат на путь — М3 17%, золотой узел уехал на «Колесо Фортуны
      и Справедливость». Консоль: 0 ошибок; 1 warning «props.pointerEvents is deprecated» —
      существующий, всплывает при загрузке приложения ДО открытия урока, к задаче 26 не относится.
- [x] Вычитка редактором пройдена (подтверждено Артёмом 15.08) → `set_status.py reviewed`
      двинул ровно 12 статусов (theory: 6, quiz: 6 — включая файл `quiz-m3.json`), других
      draft-блоков в курсе не было; 401 тест и `npx tsc --noEmit` зелёные после переноса.
- [x] Лайв-проверка Артёма на iPhone пройдена 15.08 — заход М3 закрыт.

Задача 26 целиком закрывается после М6; в бэклоге по мере заходов помечаются модули.

## Отчёт о реализации (14.08)

Три коммита по плану: `feat: merge_quiz и set_status работают со всеми quiz-*.json` →
`content: теория М3 — 6 уроков, draft` → `content: викторины М3 — 30 вопросов, draft`.
Тестов стало 401 в 22 сьютах (было 389): контракт-сьют курса расширен с М1–М2 на М1–М3.
Викторины: 24 single + 6 card (по 1 на урок: strength, justice, hanged-man, tower, star, world);
правильные ответы распределены по индексам в каждом уроке. Объём теории ru 1678–1843 знака
(диапазон М1–М2: 1697–1927). EN-имена карт — канон cards.json: Fortitude (не Strength),
The Last Judgment (не Judgement) — в текстах теории и вариантах викторин соблюдены.
До `final` у М3 тот же путь, что у М1–М2: вычитка редактора двигает draft → reviewed
(`python scripts/set_status.py reviewed --only theory` заденет только М3 — других draft-теорий
нет; для викторин `--only quiz` — статус М1–М2 уже reviewed и не совпадёт с исходным draft).
