# 04 · Справочник карт = эталон

Статус: утверждена Артёмом 10.08, в реализации · экран `app/(tabs)/cards.tsx`

## Цель

Довести таб «Карты» до эталона: карту можно найти по названию или по смыслу, фильтры не разъезжаются
на две строки, а поиск остаётся под рукой на любой глубине списка (78 карт = 26 рядов сетки).

## Решения, принятые до начала (10.08)

1. **Липкая панель (вариант B).** Поиск и чипы едут вместе с содержимым, но прилипают к верху при
   скролле — на blur-подложке `navBg`, как таб-бар. Зафиксировано в `design-reference.html`
   (`.stickysearch`) и в product-spec §3.
2. **Без счётчиков в чипах** — прав макет; product-spec §3 поправлен.
3. **Бейдж «Изучено ✓» — не в этой задаче.** Он ставится по пройденным урокам, а движка уроков нет
   (задача 08). Переносится туда целиком, вместе с полем прогресса в сторе.
4. **Поиск ищет по названию И по ключевым словам** текущего языка (`keywords` в `cards.json`):
   новичок чаще помнит смысл («любовь»), чем имя. Формулировку в product-spec §3 обновить.
5. **Поиск всегда по всей колоде**: первый введённый символ сбрасывает чип на «Все». Иначе
   «Мечи» + «шут» дают пустой экран, и причина неочевидна.

## Что делаем

### 1. Липкая панель «поиск + чипы»

Композиция экрана сверху вниз: overline «СПРАВОЧНИК · РАЙДЕР–УЭЙТ 1909» → заголовок «78 карт»
(оба скроллятся и уезжают) → **липкая панель** (поиск + чипы) → сетка 3 колонки.

**Вид панели — «невидимое стекло»** (доработка по фидбеку Артёма 10.08, первая версия с `navBg`
и границей отклонена как «полоса»): фон — новый токен `glass` (тёмная `rgba(12,15,34,.42)`,
светлая `rgba(250,246,236,.45)`), размытие 18, **без границ**; ОБА края растворяются маской
`linear-gradient(transparent, #000 22%, #000 78%, transparent)`. Общее правило записано
в design-system §5: липкие панели никогда не используют `navBg` и линии.

Маска накрывает панель ЦЕЛИКОМ, вместе с содержимым, поэтому размытие нельзя рисовать отдельным
слоем под контентом — маскируется всё вместе. **Зоны растворения — в пикселях (18 сверху, 16 снизу),
а не в процентах:** на реальной высоте панели стопы 22%/78% из макета заходили на поле поиска
и на чипы, и те выглядели размытыми (правка по фидбеку Артёма 11.08). Вертикальные отступы панели
увеличены до 22/20, чтобы зоны растворения умещались в них целиком.
Отсюда компонент `StickyGlass` с платформенной развилкой:
- **web** — тот же CSS, что в эталоне: `backdrop-filter: blur(18px)` + `mask-image`;
- **iOS/Android** — `MaskedView` (маска = `LinearGradient` с теми же стопами) поверх
  `BlurView` + слоя `glass`. У `@react-native-masked-view/masked-view` нет веб-реализации —
  отсюда развилка; пакет включён в Expo Go SDK 54, кастомный клиент не нужен.

Панель растянута на всю ширину экрана (в макете `margin:0 -18px` — выход за поля контента),
внутренние отступы 18 по бокам, 14 сверху, 12 снизу.

Реализация: `SectionList` с одной секцией — `ListHeaderComponent` = overline + заголовок (скроллится),
`renderSectionHeader` = панель (прилипает), `stickySectionHeadersEnabled` включаем явно, чтобы работало
и на Android (по умолчанию RN включает липкость только на iOS). Сетку 3 колонки делаем рядами:
`SectionList` не умеет `numColumns`, поэтому данные — массив рядов по 3 карты, элемент списка = ряд.

Известное отличие от макета: в CSS панель прилипает с `top:-16px`, то есть первые 16px её верхнего
поля успевают уехать под статус-бар. В RN липкая секция прилипает вплотную к верху области контента;
отличие — 16px в момент прилипания, повторять его не будем.

### 2. Поиск

Поле — `TextInput` внутри панели: плейсхолдер «Найти карту…» / «Find a card…», иконка-лупа
`search-outline` слева, крестик очистки `close-circle` справа (появляется, когда есть текст).
Ввод фильтрует мгновенно, без задержки: 78 карт фильтруются за доли миллисекунды, дебаунс не нужен.

Правила сопоставления (чистая функция, см. §5):
- регистр не важен; «ё» приравнивается к «е» (иначе «жрец» не найдёт «Иерофанта», а «еж» — «ежа»);
- пробелы по краям срезаются, пустой запрос = нет фильтра;
- совпадение — подстрокой, по названию карты и по любому её ключевому слову текущего языка;
- первый символ в пустом поле сбрасывает активный чип на «Все».

Пустой результат: по центру, вместо сетки — «Такой карты нет — проверьте написание» /
«No such card — check the spelling» (product-spec §3), цвет `muted`, шрифт 12.5.

Клавиатура: `keyboardDismissMode="on-drag"` (прячется при скролле сетки),
`keyboardShouldPersistTaps="handled"` (тап по карте срабатывает с первого раза, а не сначала
закрывает клавиатуру), `returnKeyType="search"`, автокоррекция и автокапитализация выключены.

### 3. Чипы-фильтров

Одна строка с горизонтальной прокруткой, без видимой полосы прокрутки (`.seg`: `overflow-x:auto`,
`scrollbar-width:none`), отступ от поиска 9, между чипами 6. Активный — фон `chipBg`, бордер `frame`,
текст `accent`; остальные — бордер `line`, текст `muted`. Хаптика на тапе уже есть.
Подпись «Старшие арканы» укорачивается до «Старшие» / «Major» — ключ `cards.major` используется
только здесь (на странице карты подпись аркана — отдельная строка, `app/card/[id].tsx:153`).

### 4. Мелкие расхождения с макетом, чинимые заодно

Замечены при сверке кода с эталоном 10.08:

| Что | Сейчас | В эталоне |
|---|---|---|
| Заголовок | «Все карты» (`cards.title`) | «78 карт» |
| Overline, межбуквенное | 2.5 | 3.5 (`.date`) |
| Шаг сетки | 10 по горизонтали, 14 по вертикали | 11 в обе стороны (`.grid`) |
| Тень миниатюры | нет | `0 8px 20px rgba(0,0,0,.28)` (`.gc .im`) |
| Подпись под картой | 10px | 9.5px, межбуквенное 0.3, интерлиньяж 1.3 (`.gc small`) |

### 5. Логика поиска — отдельный модуль с тестами

`src/lib/cardSearch.ts` (чистые функции, без React):
- `normalize(s: string): string` — нижний регистр, «ё»→«е», обрезка пробелов;
- `matchesQuery(card, query, lang): boolean` — совпадение по названию или ключевому слову;
- `filterCards(cards, { query, filter, lang }): TarotCard[]` — фильтр по аркану/масти + поиск;
- `toRows<T>(items: T[], size: number): T[][]` — нарезка на ряды для сетки.

Юнит-тесты `src/lib/__tests__/cardSearch.test.ts` (правило проекта: новый алгоритм — тест в том же
коммите): совпадение по имени; по ключевому слову; регистронезависимость; «ё/е» в обе стороны;
пустой запрос возвращает всё; несуществующий запрос — пустой массив; фильтр по масти без запроса;
`toRows` при 78 картах даёт 26 рядов, последний ряд неполный при 77.

### 6. Что НЕ делаем

- Бейдж «Изучено ✓» — переезжает в задачу 08 (нет данных о пройденных уроках).
- Скелетоны — уже реализованы (`Skeleton` поверх миниатюры до загрузки), доделывать нечего.
- Сортировки, избранное, счётчики в чипах, поиск по английским именам в русском интерфейсе.
- Перелёт миниатюры на страницу карты — работает с задачи 03, не трогаем.

## Точные значения (из `design-reference.html`)

**Липкая панель** `.stickysearch`: паддинги 22 / 18 / 20 / 18, фон `glass`
(тёмная `rgba(12,15,34,.42)`, светлая `rgba(250,246,236,.45)`), размытие 18px CSS →
на нативе `BlurView intensity={40}`, границ нет, края растворены на 18px сверху и 16px снизу.

**Поле поиска** `.search`: фон `panel`, бордер 1px `line`, радиус 15, паддинг 11×15, зазор до иконки 9,
шрифт 12.5, цвет текста `text`, плейсхолдера — `muted`; иконка 15px, цвет `muted`.

**Чипы** `.seg button`: радиус 20, паддинг 7×14, шрифт 10.5, начертание bold, межбуквенное 0.6,
зазор 6, отступ сверху 9. Активный: фон `chipBg`, бордер `frame`, текст `accent`.

**Сетка** `.grid`: 3 колонки, шаг 11, отступ сверху 15. Миниатюра: радиус 11, бордер 1px `line`,
пропорции 3:5.15, тень `0 8px 20px rgba(0,0,0,.28)` (прямоугольная → проп `boxShadow`).
Подпись: 9.5px, `muted`, отступ 5, начертание 600, межбуквенное 0.3, интерлиньяж 1.3.

**Появление** (`fadeup`): overline+заголовок — d1, панель — d2, сетка — d4 (компонент `FadeUp`).

## План по файлам

1. `docs/design-system.md` — добавить компонент «Панель поиска» в двух состояниях (в потоке и липкая)
   со значениями выше. Правило проекта: сначала дизайн-док, потом код.
2. `docs/product-spec.md` §3 — поиск по имени **и ключевым словам**; бейдж «Изучено» помечен как 08.
3. `docs/backlog.md` — бейдж «Изучено ✓» перенести из 04 в 08.
4. `src/lib/cardSearch.ts` — новые чистые функции (§5).
5. `src/lib/__tests__/cardSearch.test.ts` — тесты, `npm test` зелёный.
6. `src/lib/i18n.ts` — `cards.title` → «78 карт»/«78 cards»; `cards.major` → «Старшие»/«Major»;
   новые ключи `cards.searchPlaceholder`, `cards.empty`.
7. `src/components/BlurSurface.tsx` — общая подложка (BlurView + слой `navBg`); перевести на неё
   `TabBarBackground` в `app/(tabs)/_layout.tsx`. **Панель поиска её НЕ использует** — у липких
   панелей своя подложка-«стекло» (`src/components/StickyGlass.tsx`, токен `glass` в `theme.ts`,
   пакет `@react-native-masked-view/masked-view`).
8. `src/components/SearchField.tsx` — поле поиска (иконка, ввод, крестик очистки).
9. `app/(tabs)/cards.tsx` — переход с `FlatList` на `SectionList` с липкой секцией, ряды по 3,
   подключение поиска и чипов, пустое состояние, мелкие правки из §4.
10. `npx tsc --noEmit` после каждого шага; в конце `npm test`.

Отдельный файл плана в `docs/plans/` не заводим: задача не из «крупных» (07+), шаги по файлам выше
достаточно мелкие.

## Готово, когда

- [x] Заголовок «78 карт», overline с межбуквенным 3.5.
- [x] Панель поиск+чипы прилипает к верху при скролле; фон `glass` + размытие 18, границ нет,
      оба края растворены маской; до прилипания едет вместе с содержимым.
- [x] Поиск находит по названию и по ключевому слову; регистр и «ё/е» не мешают.
- [x] Первый символ запроса сбрасывает чип на «Все».
- [x] Крестик очищает поле и возвращает полную сетку.
- [x] Пустой результат показывает «Такой карты нет — проверьте написание».
- [x] Чипы — одна строка с горизонтальной прокруткой, подпись «Старшие», счётчиков нет.
- [x] Сетка: шаг 11, тень под миниатюрами, подпись 9.5.
- [x] Клавиатура прячется при скролле; тап по карте с первого раза открывает страницу карты.
- [x] `npx tsc --noEmit` чистый, `npm test` зелёный (включая новые тесты `cardSearch`).
- [x] Веб-сверка 6а: скриншоты 390×844 обеих тем в `docs/screenshots/04/`, расхождения с макетом
      объяснены; 6б: прокликаны все чипы, поиск, очистка, тап по карте, консоль без ошибок.
- [ ] Лайв-проверка Артёмом на iPhone (липкость и размытие в вебе и на устройстве выглядят по-разному).

## Отчёт о проверке (веб, 10.08; панель-«стекло» перепроверена после доработки)

**6а — сверка с макетом.** Скриншоты 390×844 в `docs/screenshots/04/`: приложение —
`04d-cards-rest-{dark,light}.png` (в покое) и `04d-cards-glass-{dark,light}.png` (при скролле),
макет — `04d-mockup-glass-{dark,light}.png`. Совпало: заголовок, overline, панель поиска,
чипы в одну строку, сетка 3×11, подписи; при скролле — ни одной горизонтальной линии,
края панели растворены, карты под ней затуманены, чипы снизу подтаивают ровно как в макете.
Замеры в вебе: фон панели `rgba(12,15,34,.42)` / `rgba(250,246,236,.45)`, `backdrop-filter: blur(18px)`,
`border-bottom: 0`, маска `transparent 0 → #000 18px → #000 calc(100% − 16px) → transparent`;
высота панели 122, поле поиска начинается на 22px ниже её верха, чипы заканчиваются за 20px
до низа — зоны растворения по содержимому не идут (`04e-cards-glass-{dark,light}.png`).

Осознанные отличия от макета:
1. **Панель во всю ширину.** В CSS у неё `margin:0 -18px` при поле экрана 22 — остаётся 4px
   градиента по бокам. Сделано во всю ширину: полоска фона по краям липкой панели
   на устройстве читалась бы как дефект вёрстки.
2. **Прилипает вплотную к верху.** В макете `top:-16px` — первые 16px панели уезжают, и над ней
   видны верхушки карт. В RN липкая секция останавливается на границе области контента.
3. **Бейджа «ИЗУЧЕНО ✓» нет** — перенесён в задачу 08 (решение 10.08, данных о пройденных уроках нет).

**Что проверить именно на устройстве** (веб этого не покажет): маска поверх `BlurView` на iOS —
в вебе работает CSS-путь, на устройстве другой (`MaskedView`). На Android размытие у expo-blur
экспериментальное (`dimezisBlurView`), и его сочетание с маской не проверено — если на Android
появится прямоугольник размытия, запасной вариант из фидбека Артёма: убрать блюр и оставить
только градиент `glass`.

**6б — прокликивание.** Чип «Жезлы» → 14 карт, чип подсвечен золотым. Ввод «наставник» → Иерофант
(поиск по ключевому слову), «влюбленные» без «ё» → Влюблённые, «любовь» → пустое состояние с текстом
из спеки, крестик → снова 78 карт, ввод при активном чипе сбрасывает его на «Все». Тап по карте →
`/card/hierophant?from=cards`. Консоль после чистой загрузки: 0 ошибок; из предупреждений — только
известное `props.pointerEvents is deprecated` из библиотеки (было до задачи).

**Найдено попутно (контент, не код):** ни у одной карты в `keywords` нет слов «любовь», «деньги»,
«работа» — всего 85 уникальных ключевых слов на 78 карт. Поиск по смыслу будет находить мало,
пока редактор не расширит списки. Занесено в backlog отдельной задачей для жены.
