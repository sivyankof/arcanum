# Спека 45 — флеш-карты: повторение изученных карт (SM-2), этап 4 плана

Статус: спека согласована 19.08 (брейншторм с Артёмом, решения ниже). Реализация в ДВЕ части
одной ветки `feat/45-flashcards`:
- **45а — логика и данные** (сессия на Opus 5, код по шагам плана отдаётся sonnet-сабагентам):
  алгоритм, сборка сессии, стор, бэкап, DEV-строки, тесты. Ни одного нового экрана. Проверяется
  тестами и DEV-диалогом на устройстве, мержится в main без лайв-проверки экранов (прецедент 28а:
  «если проверять нечего, мержи»). Детальный план — `docs/plans/45-flashcards.md`.
- **45б — экраны** (после дорисовки макета, см. «Расхождения с макетом»): карточка «Повторение»
  в шапке курса, экран тренажёра `app/review.tsx`, результат, пустые состояния. Свой план,
  веб-проверка 6а/6б и лайв-проверка Артёма — как у любой UI-задачи.

Контекст: п. 4 и п. 12 master-plan §3.1 («флеш-карты SM-2», «тренажёр запоминания»), первая
задача этапа 4. `src/lib/srs.ts` лежал в репо с этапа 0 без единого вызова и без тестов —
переписывается здесь. Макет — `v-trainer` в `design-reference.html` (Cowork, коммит 1a9a2c2),
нарисован «простейшим вариантом», поведение оставлено вопросами 1–4 в бэклоге — они решены ниже.

## Решения (брейншторм 19.08, ответы Артёма)

| Вопрос | Решение |
|---|---|
| Формат тренировки | **Флип + самооценка** (классика Anki): лицо-вопрос → тап → оборот-ответ → кнопки оценки. Викторина с вариантами и «оба режима» отклонены (викторина по изученному — расширение позже, master-plan п. 12). |
| Вход | **Таб «Курс», карточка «Повторение» над путём** (как Practice в Duolingo). Справочник и «Сегодня» отклонены: справочник — справочник, «Сегодня» — ритуал. Совпадает с вопросом 1 Cowork («нарисован вход с Курса»). |
| Колода | **Только карты из пройденных уроков** (`learnedCardIds`). Все 78 и «плюс карты дня» отклонены: встреча ≠ изучение, бейдж «Изучено ✓» стал бы двусмысленным. |
| XP | **+1 за каждую вспомненную карту** (`XP_REVIEW = 1`). Потолок естественный: оценённая карта уходит на дни. Задача 22 сама называла флеш-карты будущим источником XP. Вопрос 4 Cowork. |
| Кнопки оценки | **Три**: «Не помню / Помню / Легко» (оценки 0 / 2 / 3). Макет рисует четыре — правится (см. расхождения). Род безличный (content-guide): «Не вспомнила» из макета не берём. |
| Интервал на кнопках | **Не показываем** («Легко · 6 дн» как в Anki) — тон приложения ритуал, а не отчётность (то же решение, что про графики рефлексии). Вопрос 2 Cowork. |
| Сессия | **Порциями по 10** (вопрос 3 Cowork). |

## Цель

- После пройденных уроков у пользователя появляется ежедневная причина вернуться в курс: карточка
  «Повторение · N карт ждут» → короткая сессия ≤10 карт с переворотом и самооценкой → интервалы
  растут по SM-2, забытое возвращается.
- Всё офлайн, все тексты — `keywords` и `general` из `cards.json`; ни одной строки контента
  в коде не выдумываем.
- Логика целиком чистая и под тестами; экраны — тонкий слой над ней.

## Что делаем

### А. Чистые модули (всё под юнит-тестами)

**`src/lib/srs.ts`** — ПЕРЕПИСАТЬ (старая версия: `dueAt` timestamp, `cardId` внутри состояния,
без тестов, нигде не вызывается):
- `type SrsGrade = 0 | 1 | 2 | 3` — 0 «не помню», 1 «с трудом» (в UI v1 кнопки нет, оставлен
  для будущего режима-викторины), 2 «помню», 3 «легко».
- `interface SrsState { reps: number; intervalDays: number; ease: number; due: string }` —
  `due` = ЛОКАЛЬНАЯ дата `YYYY-MM-DD` (как `lastDrawDate`), НЕ timestamp: оценил в 23:00 →
  «завтра» = следующий календарный день, а не 23:00 завтра, иначе утром очередь пуста, а вечером
  полна. `cardId` в состоянии не хранится — он ключ словаря.
- Константы: `EASE_START = 2.5`, `EASE_MIN = 1.3`, `EASE_MAX = 5`, `MAX_INTERVAL_DAYS = 365`.
- `reviewState(prev: SrsState | undefined, grade: SrsGrade, todayISO: string): SrsState` — SM-2:
  `q = [1, 3, 4, 5][grade]`; при `q < 3`: `reps = 0`, `intervalDays = 0`, `due = today` (карта
  остаётся к повторению сегодня и возвращается в конец сессии); при `q ≥ 3`: `reps + 1`,
  интервал `1 → 6 → round(prev × ease)`, потолок 365; `ease' = clamp(ease + (0.1 − (5−q)·(0.08
  + (5−q)·0.02)), 1.3, 5)` — обновляется при ЛЮБОЙ оценке (оригинальный SM-2): q=5 → +0.10,
  q=4 → 0, q=3 → −0.14, q=1 → −0.54. `prev === undefined` (новая карта) стартует от
  `{reps: 0, intervalDays: 0, ease: 2.5}`. Легко у новой карты даёт тот же 1 день, что и
  «помню», — отличие только в ease (SM-2 без easy-bonus, YAGNI).
- `isDue(s, todayISO) = s.due <= todayISO` (ISO сравнивается строкой).
- Тест-кейсы: новая → 2 → due завтра, reps 1; → 2 → +6 дней; → 2 → round(6 × 2.5) = 15; ease по
  всем четырём оценкам; потолки 365 и 5 / пол 1.3; 0 после длинного интервала → reps 0, due =
  сегодня, ease упал; `isDue` на вчера/сегодня/завтра; переход через месяц и год у `due`.

**`src/lib/review.ts`** (новый) — сборка сессии и сводка, без импортов react/expo:
- `SESSION_MAX = 10`, `NEW_PER_DAY = 10`.
- `type Direction = 'toMeaning' | 'toCard'`; `interface SessionItem { cardId; direction; isNew }`;
  `interface ReviewDay { date: string; newCount: number }` — сколько НОВЫХ карт введено за день;
  `REVIEW_DAY_DEFAULT = { date: '', newCount: 0 }`, счётчик действителен только если
  `date === today` (иначе читается как 0).
- `deckOrder(course, progress): string[]` — `[...learnedCardIds(course, progress)]`: порядок
  курса (Set хранит порядок вставки — карты первых уроков первыми).
- `reviewSummary(deck, srs, todayISO, day): { deckSize; due; newAvailable; dueTomorrow }` —
  `due` = карты колоды с состоянием и `isDue`; `newAvailable` = min(карты колоды БЕЗ состояния,
  `NEW_PER_DAY − newCount за сегодня`), не меньше 0; `dueTomorrow` = карты с состоянием и
  `due <= завтра` (показывается только в состоянии «всё повторено», где просроченных нет, поэтому
  равен «ровно завтра»). Состояния карт, выпавших из колоды (DEV-сброс курса), не считаются.
- `buildSession(deck, srs, todayISO, day, rng): SessionItem[]` — просроченные по `due` ↑, при
  равенстве по порядку колоды, первые ≤ `SESSION_MAX`; остаток слотов — новые в порядке колоды,
  не больше `newAvailable`; итоговый список тасуется Фишером–Йетсом (`rng` — параметр, для
  тестов); направление: новая карта всегда `toMeaning` (сначала показать образ), повторяемая —
  `rng() < 0.5 ? 'toMeaning' : 'toCard'`.
- `applyGrade(queue: SessionItem[], grade): { queue; passed }` — снимает голову; `grade === 0`
  → тот же элемент в конец очереди (карта повторяется, пока не будет вспомнена); `passed =
  grade >= 1`.
- `sessionStats(log: { cardId; grade }[]): { cards; firstTry; xp }` — `cards` = уникальных карт,
  `firstTry` = карт, чья ПЕРВАЯ оценка ≥ 1, `xp` = число событий «оценка ≥ 1 карты, которая до
  этой оценки была к повторению» (см. правило XP ниже) — используется экраном результата и
  тестом-инвариантом «xp экрана = xp стора».
- `promptSentence(text: string): string` — первое предложение `general` для оборота/подсказки:
  срез по первому `.`/`!`/`?`/`…` с пробелом или концом строки после; если срез короче 20 знаков
  («Дурак.») — берём до второго; потолок 160 знаков с «…». Под тестами (`«Т. е.»` в корпусе
  нет — проверяется контракт-тестом по `cards.json`: у каждой карты первое предложение
  `general.ru/en` не короче 20 и не длиннее 160 знаков после среза).
- Общий `shuffle(arr, rng)` — если в `spread.ts` Фишер–Йетс инлайн, вынести и переиспользовать
  (правило DRY), `dealSpread` переводится на него.
- Тест-кейсы `review`: пустая колода → пустая сессия и сводка нулей; 12 просроченных → 10 самых
  старых; 3 просроченных + 20 новых → 3 + 7 новых, `newAvailable` учитывает `newCount` сегодня и
  игнорирует вчерашний; `newCount = 10` → новых 0; сидированный rng → детерминированный порядок
  и направления; новые всегда `toMeaning`; `applyGrade(0)` возвращает карту в хвост, `2` — нет;
  `sessionStats` на логе «0, 2» одной карты = cards 1, firstTry 0, xp 1; `dueTomorrow` при
  пустом due; `promptSentence` на «Дурак. Карта начала пути.», на «…» и на тексте без точки.

**`src/lib/xp.ts`** — `XP_REVIEW = 1`.

### Б. Стор, бэкап, DEV

**`src/store/useApp.ts`** — persist **version 10** (`SCHEMA_VERSION` в `backup.ts` — 9 → 10):
- поля `srs: Record<string, SrsState>` (ключ — `cardId`) и `reviewDay: ReviewDay` — оба ключи
  ВЕРХНЕГО уровня, дефолты доливаются поверхностным слиянием сами, `migrate` не меняется.
  ⚠️ `reviewDay` — вложенный объект: следующая задача, добавляющая поле ВНУТРЬ него, обязана
  поднять версию и дописать слияние руками (ловушка 06а).
- `reviewCard(cardId, grade): number` — `prev = srs[cardId]`; `wasDue = prev === undefined ||
  isDue(prev, today)`; `srs[cardId] = reviewState(prev, grade, today)`; если `prev === undefined`
  — `reviewDay = { date: today, newCount: (date === today ? newCount : 0) + 1 }`; XP: `+XP_REVIEW`
  если `grade >= 1 && wasDue`, иначе 0; возвращает начисленное. Правило XP сформулировано без
  понятия «сессия» нарочно: карта, провалённая (due = сегодня) и затем вспомненная, даёт +1;
  вспомненная повторно в тот же день (due уже завтра+) — 0; накрутка невозможна.
- `resetSrs()` (DEV) — `srs = {}`, `reviewDay = REVIEW_DAY_DEFAULT`.
- `devAgeSrs()` (DEV) — каждому `due` минус 1 день (`daysAgoISO(1, parseISODate(due))`) и
  `reviewDay = REVIEW_DAY_DEFAULT` — иначе «ждут завтра» и дневной лимит новых на устройстве
  проверяются только календарём (та же роль, что у `devSkipYesterday`).
- `resetCourse` (DEV) `srs` НЕ трогает — колода вычисляется, лишние состояния безвредны;
  для чистого листа рядом лежит `resetSrs`.

**`src/lib/backup.ts`** — `srs` и `reviewDay` в `BackupState`/`PERSIST_DEFAULTS` (тип-контроль
стора этого потребует сам); `validState`: `srs` — объект, ≤ 78 ключей, каждый ключ есть в
`cardById`, каждое значение `{reps: count, intervalDays: 0..365, ease: число 1.3..5, due: ISO-день}`;
`reviewDay`: `date` — `''` или ISO-день, `newCount` — count ≤ 78. Старый бэкап (schemaVersion 9)
без этих ключей валиден — доливается дефолтами (уже так устроено). Тесты: файл v9 принимается,
`srs` с чужим `cardId`/`ease 1.0`/`interval 400` — `corrupt`.

**`app/settings.tsx`** (под `__DEV__`, три `SettingsRow` рядом с DEV-строками курса):
- «Очередь повторения» → `ConfirmDialog` (`confirmTone 'accent'`, одна кнопка) с текстом
  `reviewSummary`: «В колоде N · ждут сегодня D · новых доступно A · завтра T · новых введено
  сегодня C» — тот же приём, что «План пушей».
- «Состарить повторение на день» → `devAgeSrs`.
- «Сбросить повторение» → `resetSrs`.
Строки — только DEV, в релиз не идут (правило product-spec §5 про раздел «Для разработки»).

**`src/lib/i18n.ts`** — в 45а только ключи DEV-строк (`settings.devReviewQueue`,
`settings.devAgeSrs`, `settings.devResetSrs`, строки диалога очереди) в ru и en. Ключи экранов
`review.*` — в 45б, числительные через `count` (`_one/_few/_many` для ru — logic-spec §10):
`review.due` «{{count}} карта ждёт / карты ждут / карт ждут», `review.new` «Новых карт:
{{count}}», `review.tomorrow` «завтра: {{count}}», `review.reviewed` «Повторено {{count}}»,
`review.more` «Ещё {{count}}».

### В. Экраны (45б — по спеке ниже и по макету `v-trainer` после дорисовки)

**Карточка «Повторение» в шапке таба «Курс»** — между заголовком «Курс»/`Rule` и первым модулем,
внутри `FadeUp index={1}` вместе с первым модулем (без нового шага каскада). Панель по
design-system §5 (radius, паддинг как у `ModuleHeader`), Overline «ПОВТОРЕНИЕ», строка состояния,
справа иконка Ionicons `sync-outline` (или по макету). Состояния:
- колода пуста (ни одного пройденного урока с картами) — карточка **не показывается вовсе**;
- `due > 0` — «N карт ждут» (Display-строка), тап → `/review`;
- `due = 0`, `newAvailable > 0` — «Новых карт: N», тап → `/review`;
- `due = 0`, `newAvailable = 0` — «Всё повторено ✓ · завтра: M» цветом `success`, НЕ тап.

**Экран тренажёра `app/review.tsx`** — корневой стек ПОД ГАРДОМ онбординга (урок 09: маршрут
обязан быть вписан в `_layout.tsx`), прозрачная нативная шапка с подписью «Курс» на кнопке назад
(как у урока), «назад» без подтверждения (каждая оценка уже в сторе, `useLeaveGuard` не нужен).
Композиция по макету: Overline «ПОВТОРЕНИЕ · ИНТЕРВАЛЬНЫЙ МЕТОД» → «Тренажёр» → `Rule` → счётчик
«К ПОВТОРЕНИЮ · N» (длина очереди; при «не помню» не уменьшается — карта вернётся) → карта в
`ringwrap` 190×330 → плашка названия → панель «ЗНАЧЕНИЕ» → ряд из ТРЁХ кнопок оценки.
Сессия — `buildSession(...)` один раз при монтировании (`rng = Math.random`), очередь —
состояние экрана; переход к следующей карте после оценки, повторный тап по кнопке до перерисовки
игнорируется (busy-guard, как у экспорта/импорта).
- Направление `toMeaning` («карта → значение»): карта лежит **лицом вверх** с начала (образ и
  название на плашке — вопрос), панель «ЗНАЧЕНИЕ» скрыта, под картой подпись «ВСПОМНИТЕ ЗНАЧЕНИЕ ·
  НАЖМИТЕ»; тап по карте (или по подписи) → панель появляется fade 350 мс (как пояснение
  викторины) с 4 `keywords` золотыми чипами + `promptSentence(general)` + строка-ссылка «Страница
  карты →» + три кнопки. Переворота в этом направлении нет — акцент экрана остаётся один.
- Направление `toCard` («значение → карта»): **рубашка**, в зоне эмблемы — 4 `keywords` столбиком
  + `promptSentence(general)` под маской имени (`maskCardName`: «···» вместо названия карты —
  первое предложение `general` у ВСЕХ 78 карт НАЧИНАЕТСЯ с имени, без маски рубашка выдавала бы
  ответ на собственном вопросе; корпусный контракт 78 × ru/en это стережёт). Подпись «ВСПОМНИТЕ
  КАРТУ · НАЖМИТЕ» — ПОД картой, как и у `toMeaning` (одно место, одна тап-зона), а НЕ на самой
  рубашке (правка формулировки 45б: изначально спека держала подпись в зоне эмблемы). Тап →
  3D-переворот (`SpreadCard`-механика = общий `FlipCard`, 500 мс) → образ + название на плашке +
  та же панель «ЗНАЧЕНИЕ» + кнопки. Значения берутся через `blockText(general, lang)`: при `todo` —
  только чипы (`keywords` у всех 78 карт вычитаны, «Текст готовится» на флеш-карте не бывает).
- Кнопки: «Не помню» (`danger`), «Помню» (`text`), «Легко» (`success`) — три равные, бордер
  `line`, radius 12, текст 10/700 (design-system §5, правится с четырёх на три). Хаптика у всех трёх — Light
  (самооценка «не помню» — не ошибка, Error-вибрация наказывала бы честность). Тап →
  `reviewCard` → `applyGrade` → следующая карта; лог сессии для результата.
- Пустое состояние (открыли без карт к повторению — глубокая ссылка или после «Ещё»): рубашка-
  пунктир ✶ + «Повторять пока нечего — пройдите урок, и карты появятся здесь.» + CTA «К курсу»
  (текст макета) — если колода пуста; «Всё повторено ✓ · завтра: M» + CTA «К курсу» — если колода
  есть, а очередь пуста.
- **Результат** — панель в стиле `LessonResult` (общая часть выносится, если разметка совпадёт
  на 2+ места: заголовок, счётчик XP, CTA): «Повторено N · с первого раза K · +X XP» (счётчик
  катится, как у урока), CTA «Готово» (→ назад в курс), под ней «Ещё N» — если `reviewSummary`
  после сессии показывает `due > 0 || newAvailable > 0` (новая сессия на том же экране). Конфетти
  нет (motion-spec: 1–2 акцентных анимации на экран, здесь это переворот).

**Профиль/статы** — не меняются (см. «Не делаем»).

### Г. Документы

- `docs/product-spec.md` §2 «Курс» — абзац «Повторение (45)» (карточка, состояния, вход) и
  подраздел «Тренажёр» (грани, кнопки, результат, пустые состояния).
- `docs/logic-spec.md` — новый §12 «Повторение (SM-2)»: формулы, константы, правило XP,
  тест-кейсы; §4 XP — строка «+1 за вспомненную карту повторения»; §7 хранение — `srs`,
  `reviewDay`, persist 10.
- `docs/design-system.md` §5 «Оценки повторения» — ТРИ кнопки, безличные подписи; композиция
  карточки «Повторение».
- `docs/backlog.md` — задача 45 (спека, статус частей), вопросы 1–4 Cowork помечены решёнными;
  хвост дорисовки макета (ниже) — в задачу «Макет: правки дорисовок».
- `CLAUDE.md` «Статус», `AGENTS.md` (persist 10, число тестов) — при закрытии 45а.

## Что НЕ делаем (осознанно)

- Режим-викторина по изученному (варианты ответов) — расширение позже (master-plan п. 12);
  тип оценок 4-уровневый оставлен ровно под него.
- Перевёрнутые значения во флеш-картах — `keywords` описывают прямое положение.
- Статистика повторений в профиле (master-plan п. 6 «статистика повторений») — ряд стат уже
  из трёх коробок, добавлять четвёртую без макета нельзя.
- Пуш «пора повторить» — лимит 2/день занят утренним и вечерним ритуалом (logic-spec §8).
- Премиум-ограничение («безлимит флеш-карт» в master-plan §3.4) — пейвола нет; константы
  `SESSION_MAX`/`NEW_PER_DAY` — единственное место, где гейт появится.
- «Дополнительная практика» после «всё повторено» — SRS-очередь на день закрыта, как в Anki.
- Интервал на кнопках («Легко · 6 дн»), easy-bonus, отдельные карточки на два направления
  (Anki-модель) — YAGNI.
- Изменение алгоритма урока «Повторение: …» в course.json (это уроки курса, не тренажёр).

## Расхождения с макетом (правило 6а-0 — спека главнее, макету дорисовка)

Хвост для Cowork-сессии (в бэклог, задача «Макет: правки дорисовок»):
1. **Кнопок оценки три, а не четыре**, подписи безличные: «Не помню / Помню / Легко» (сейчас
   «Не вспомнила / С трудом / Вспомнила / Легко»).
2. **До оборота в макете нет вопроса** (рубашка с «?» → тап открывает образ, имя и значение
   разом): дорисовать оба направления — `toMeaning` лицом вверх с подписью «ВСПОМНИТЕ ЗНАЧЕНИЕ ·
   НАЖМИТЕ» и скрытой панелью; `toCard` — рубашка с четырьмя ключевыми словами и предложением
   в зоне эмблемы, подпись «ВСПОМНИТЕ КАРТУ · НАЖМИТЕ».
3. **Карточки «Повторение» в шапке курса нет** — вход только кнопкой демобара; дорисовать
   панель над первым модулем с тремя состояниями.
4. **Панель результата сессии** отсутствует — дорисовать по образцу `.lresult` без конфетти,
   с CTA «Готово» и ссылкой «Ещё N».
5. Панель «ЗНАЧЕНИЕ» — чипы `keywords` + одно предложение + ссылка «Страница карты →» вместо
   абзаца-заглушки.
Что совпало и не трогается: вход с курса, Overline/заголовок/`Rule`, счётчик, `ringwrap` 190×330,
плашка названия, пустое состояние с CTA «К курсу», геометрия кнопок (бордер `line`, radius 12,
10/700, danger слева, success справа).

## Критерии приёмки — «готово, когда…»

**45а (логика и данные):**
- [x] `src/lib/srs.ts` переписан: `SrsState` с `due`-датой, `reviewState`, `isDue`, константы;
      тесты по всем кейсам раздела А зелёные, старой сигнатуры (`review`, `dueCards`, `initialSrs`,
      `dueAt`) в репо нет (`grep`).
- [x] `src/lib/review.ts`: `deckOrder`, `reviewSummary`, `buildSession`, `applyGrade`,
      `sessionStats`, `promptSentence`, константы — под тестами; контракт-тест первого предложения
      `general` по всем 78 картам × ru/en зелёный.
- [x] Стор: `srs`, `reviewDay`, `reviewCard` (SM-2 + счётчик новых + XP по правилу «была к
      повторению»), `resetSrs`, `devAgeSrs`; persist и `SCHEMA_VERSION` = **10**; тест-инвариант
      «xp по `sessionStats` = xp, начисленный стором» на сессии со сбоем и повтором.
- [x] Бэкап: новые поля в `BackupState`/`PERSIST_DEFAULTS`/`validState`, файл v9 принимается,
      битые `srs` — `corrupt`; круг «экспорт → импорт» возвращает `srs` и `reviewDay`
      (тест `backup`).
- [x] Три DEV-строки в настройках работают на устройстве: диалог «Очередь повторения» показывает
      числа, «Состарить» сдвигает `due`, «Сбросить» обнуляет — единственная проверка Артёма
      в 45а (лайв-проверки экранов нет по существу).
- [x] i18n: ключи DEV-строк в ru и en (ключи `review.*` с плюрализацией — критерий 45б,
      проверяется тестом плюралей по образцу `i18nPlurals.test.ts`).
- [x] `npx tsc --noEmit` чистый, `npm test` зелёный; документы Г обновлены; спека получила
      отчёт 45а; ветка влита в main.

**45б (экраны):**
- [x] Карточка «Повторение» в шапке курса с четырьмя состояниями (скрыта / ждут / новые / всё
      повторено), числительные через `count`.
- [x] Экран `/review` под гардом: два направления по разделу В, переворот `toCard`, панель
      «ЗНАЧЕНИЕ», три кнопки, хаптика, счётчик очереди, busy-guard, пустое состояние двух видов,
      результат с «Ещё N».
- [x] Веб-проверка 6а (390×844, обе темы, скриншоты в `docs/screenshots/45/`, сверка с
      дорисованным макетом) и 6б (прокликивание: сессия из ≥3 карт с одним «Не помню» —
      карта возвращается; XP на «Сегодня» вырос ровно на число вспомненных), консоль без ошибок.
- [ ] Лайв-проверка Артёма на iPhone: переворот, хаптика, «Ещё N», карточка на курсе после
      «Состарить на день».

## План (кратко; детально — `docs/plans/45-flashcards.md`)

45а: ветка → `srs.ts` (тесты первыми) → `review.ts` + `shuffle` → `xp.ts` → бэкап и стор
(persist 10) → i18n → DEV-строки в настройках → документы → tsc/test → ревью → merge.
45б: после дорисовки макета — свой план: карточка в шапке курса → экран `/review` (карта,
панель, кнопки) → результат → пустые состояния → веб-проверка → лайв-проверка → merge.

## Отчёт 45а (19.08)

**Сделано** — ветка `feat/45-flashcards`, 9 коммитов (37636ee..9638f69), код по плану без
отклонений:
- `src/lib/srs.ts` переписан целиком: `SrsState` с `due`-ISO-днём вместо timestamp, `reviewState`,
  `isDue`, константы `EASE_START/EASE_MIN/EASE_MAX/MAX_INTERVAL_DAYS`. Старой сигнатуры
  (`review`, `dueCards`, `initialSrs`, `dueAt`) в репо не осталось — проверено `grep`.
- `src/lib/review.ts` (новый): `deckOrder`, `reviewSummary`, `buildSession`, `applyGrade`,
  `sessionStats`, `promptSentence`, `applyReview` (единственная функция, реально меняющая
  `srs`/`reviewDay`/XP — стор только применяет её результат), константы `SESSION_MAX`,
  `NEW_PER_DAY`, `PROMPT_MIN/MAX`.
- Общий `src/lib/shuffle.ts` — Фишер–Йетс вынесен из `spread.ts`, `dealSpread` переведён на него;
  сессия повторения — второй потребитель.
- `XP_REVIEW = 1` в `src/lib/xp.ts`; `plusDaysISO(iso, n)` в `src/lib/dates.ts`.
- Стор: поля `srs`, `reviewDay`, экшен `reviewCard(cardId, grade)`, DEV-экшены `resetSrs`,
  `devAgeSrs`; persist `SCHEMA_VERSION` 9 → **10** (`src/lib/backup.ts` — единственный источник).
- Бэкап: `srs`/`reviewDay` в `BackupState`/`PERSIST_DEFAULTS`/`validState` (коридоры ease
  1.3..5, интервал 0..365, id из колоды, ≤78 записей); файл v9 без этих ключей доливается
  дефолтами; круг «экспорт → импорт» проверен тестом.
- `app/settings.tsx`: три DEV-строки («Очередь повторения» → диалог со сводкой,
  «Состарить повторение на день», «Сбросить повторение») + ключи `settings.devReviewQueue` /
  `devAgeSrs` / `devResetSrs` в ru и en (`src/lib/i18n.ts`).
- Задача 2б (сверх исходного плана) — `bfca558`: `shuffleOptions` в `src/lib/lesson.ts` (задача 08)
  переведён на общий `shuffle`. Заведена по находке ревью: вынося тасование в общий модуль, задача
  сама не выполнила правило DRY — второй экземпляр цикла Фишера–Йетса нашёлся в соседнем модуле,
  которого план не касался.

**Тесты.** Прогон 19.08: `npm test` — **1273 теста в 35 сьютах**, все зелёные (было 1181 в 32 до
ветки, +92). Разбивка прироста, замерена прогоном каждого файла отдельно (не выведена из общей
суммы): три новых сьюта — `srs.test.ts` (10), `review.test.ts` (25), `shuffle.test.ts` (4) — и
прирост в четырёх существующих файлах — `backup.test.ts` (+17: валидация `srs`/`reviewDay`, приём
v9-файла, битые значения → `corrupt`), `dates.test.ts` (+3: `plusDaysISO`), `xp.test.ts` (+1:
`XP_REVIEW`), `courseContent.test.ts` (+32: контракт каждой карты урока в колоде). Итого 10+25+4+17+3+1+32 = 92, сходится с 1181 → 1273. Контракт по корпусу:
`promptSentence` проверен на первом предложении `general` всех 78 карт × ru/en — в коридоре
20..160 знаков у всех. Инвариант «сумма `gained` по `applyReview` = `sessionStats(log).xp`»
проверен на сценарии с двумя провалами и повтором. `npx tsc --noEmit` — чисто.

**UI-код и веб-проверка.** У 45а UI-код ЕСТЬ — коммит 9638f69 добавил в `app/settings.tsx` три
`SettingsRow` («Очередь повторения», «Состарить повторение на день», «Сбросить повторение») и
`ConfirmDialog` со сводкой; НЕТ только новых ЭКРАНОВ (карточка «Повторение» в шапке курса, экран
тренажёра `/review`, панель результата) — они в 45б, ждут дорисовки макета. Дымовая веб-проверка
этих трёх строк СОСТОЯЛАСЬ в задаче 8 (не «не проводилась», как утверждала прежняя редакция этого
отчёта) — отчёт `.superpowers/sdd/45-flashcards/task-8-report.md`. Прогон Playwright, 390×844:
на пустых `srs`/`reviewDay` все три строки найдены на экране, диалог «Очередь повторения» показал
пять нулевых строк; после пяти нажатий DEV «Пройти следующий урок» — «В колоде: 2 / Ждут сегодня: 0 /
Новых доступно: 2 / Завтра: 0 / Новых введено сегодня: 0»; «Состарить повторение на день» и
«Сбросить повторение» отработали без падения экрана (на пустом `srs` цифры ожидаемо не меняются);
консоль браузера чистая — 0 ошибок, один задокументированный чужой warning `pointerEvents`
(`@react-navigation/elements`/`react-native-screens`, не наш код).

**Что остаётся пункту 6в (устройство).** Веб не судит перенос строк: пять строк диалога «Очередь
повторения» на реальной ширине 390px (числа разной длины после правки 2 — «Ждут к завтра (вкл.
сегодняшние): {{tomorrow}}» длиннее прежней «Завтра: {{tomorrow}}») и поведение DEV-строк
«Состарить»/«Сбросить» при настоящем сдвиге календаря, а не искусственном сиде localStorage.

**Расхождения.** Нет: код совпадает с планом и спекой без правок по ходу реализации. 45б (карточка
«Повторение» в шапке курса, экран `/review`, панель результата) не начата — ждёт дорисовки макета
`v-trainer`, критерии приёмки раздела «45б» этой спеки остаются невыполненными.

## Отчёт 45б (19.08)

**Сделано** — ветка `feat/45b-review-screens` (от `main` после мержа 45а и дорисовки макета
Cowork, коммит 9a33503), 16 коммитов (9276eba..c891052), план `docs/plans/45b-review-screens.md`
выполнен без содержательных отклонений (единственная механическая правка — дефолтный параметр
тестового хелпера, см. ниже):

*Выносы общих компонентов (правило «2+ раза → общий модуль»):*
- `KeywordChips` (`src/components/KeywordChips.tsx`) — золотые чипы `keywords`, вынесены со
  страницы карты; второй потребитель — рубашка `toCard` (столбиком, `layout="column"`) и панель
  «ЗНАЧЕНИЕ» тренажёра (лентой).
- `MeaningPanel` (`src/components/MeaningPanel.tsx`) — панель `.mean` (бокс + Overline), вынесена
  с блока «ЗНАЧЕНИЕ ДНЯ» на «Сегодня»; второй потребитель — панель «ЗНАЧЕНИЕ» тренажёра.
- `faceShadow` (`src/theme/glow.ts`) — тень граней крупной карты, вынесена из локальной константы
  `FACE_SHADOW` на «Сегодня»; второй потребитель — `ReviewFlashcard`. Плюс проп `content` у
  `CardBack` — заменяет зону эмблемы целиком (рубашка тренажёра кладёт туда чипы и предложение
  вместо компаса и слова ARCANUM).
- `FlipCard` (`src/components/FlipCard.tsx`) — 3D-переворот, вынесен из `SpreadCard` БЕЗ изменений
  логики (лечение «осиротевших 3D-пропов» задач 36/42 переехало вместе с комментарием-уроком);
  `SpreadCard` стал тонкой обёрткой над ним; второй потребитель — `ReviewFlashcard`.
- `ResultPanel` (`src/components/ResultPanel.tsx`) — панель итога (въезд, хаптика Success,
  катящийся XP-счётчик, порядок title → XP/zeroTitle → line → children → CTA → footer), вынесена
  из `LessonResult`; тайминги и ветка `gained===0`/reduce-motion подтверждены построчным сравнением
  со старым кодом — расхождений нет. Второй потребитель — `ReviewResult`.
- `moduleBox` — геометрия панели `.mhead`, вынесена именованным экспортом из `ModuleHeader`
  (использует и сам `ModuleHeader`, и новая `ReviewPanel`).

*Новая логика (`src/lib/review.ts`):* `reviewCardState(summary)` (`hidden`/`due`/`new`/`done` по
приоритету «колода пуста → просроченные → новые → всё повторено»), `nextSessionSize(summary)`
(`min(SESSION_MAX, due + newAvailable)`, согласие с `buildSession` под тестом), `maskCardName` +
`NAME_MASK = '···'` — маскирует имя карты в подсказке `toCard` целым словом, включая артикль
«The» и все вхождения.

*Новые презентационные компоненты:* `ReviewPanel` (карточка «Повторение» над первым модулем курса),
`ReviewFlashcard` (флеш-карта 190×330 на `FlipCard`, две грани вопроса), `ReviewResult` (итог
сессии на `ResultPanel` + ссылка «Ещё N»); экран `app/review.tsx` под гардом онбординга
(`app/_layout.tsx`, внутри `<Stack.Protected guard={onboarded}>`, прозрачная шапка), кнопка
«назад» со страницы карты подписана «Тренажёр»/«Trainer» (`BACK_TITLES.review` →
`card.backReview`). Карточка подключена в `app/(tabs)/course.tsx` внутри `FadeUp index={1}` перед
первым модулем (без нового шага каскада), день синхронизирован ДВУМЯ источниками —
`useFocusEffect` (фокус таба) и `useAppActive` (возврат из фона), тот же приём, что у карты дня.

*i18n:* блок `review.*` (18 ключей ru/en, `due` с формами `_one/_few/_many`/`_one/_other`) +
`card.backReview` — оба языка симметричны, под структурным тестом `i18nPlurals.test.ts`.

*Сверх плана* — коммит `c891052`: подпись «МОДУЛЬ N ИЗ 6» в шапке модуля перекрашена с `muted` на
`accent` по замечанию Артёма при просмотре экрана курса (эталон `.mh2 small` золотой, приложение
рисовало серым; design-system.md прежде не фиксировал цвет этой подписи — конфликта макета со
спекой не было, просто пробел). Правка — один проп `color` в `ModuleHeader.tsx`, затрагивает
единственное место использования компонента (все шесть шапок модулей на пути курса); подтверждена
и глазами на скриншоте, и программно (`getComputedStyle().color` = `#e2bd72`, токен `accent`
тёмной темы). `docs/design-system.md` §5 дописан.

*Отклонения от плана:*
1. Тестовый хелпер `sum(p: Partial<ReviewSummary>)` в брифе задачи 1 не имел значения по
   умолчанию, а тесты вызывали его без аргументов (`sum()`) — `tsc` не проходил без дефолта `= {}`;
   правка чисто механическая, семантика тестов не менялась.
2. Раунд правок к задаче 1 (находка ревью): в `maskCardName` необязательная группа `(?:the\s+)?`
   ловила литеральное «the» ВНУТРИ соседнего слова («soothe Empress», «bathe…») — проверка целого
   слова верно отклоняла такое совпадение, но `String.replace` с флагом `g` уже сдвигал `lastIndex`
   мимо настоящего вхождения имени сразу за ним, и оно оставалось немаскированным (утечка ответа
   на английском). Правка: два прохода — основная регулярка ищет только само имя, отдельная
   функция `dropArticle` вторым проходом снимает «the», приклеившееся к уже поставленной маске.
3. Задача 13 (контрольная сверка с макетом) внесла три точечные правки: `gap: 5` в столбике рубашки
   `.trkwcol` (зазор между чипами и предложением совпадал не полностью — 8px вместо 5+8=13);
   снят лишний `fontWeight: '600'` у ссылок «Страница карты →»/«Ещё N» (макет веса не задаёт —
   `Txt` транслирует вес в отдельное семейство шрифта, поэтому это не косметика, а реальная смена
   начертания с `Manrope_600SemiBold` на `_400Regular`); добавлена страховка `empty` в
   `app/review.tsx` на случай, если карта из очереди не найдётся в `cardById` (рассинхрон колоды —
   недостижим при исправном контенте, но без страховки экран завис бы пустым).

**Тесты.** Итоговый прогон 19.08 (после волны правок финального ревью): `npm test` — **1295 тестов
в 35 сьютах**, все зелёные (было 1273 до 45б, +22, число сьютов не изменилось — новых тестовых файлов не заводилось, компоненты юнит-тестами
не покрываются). Разбивка (перепроверена прогоном каждого файла отдельно):
- `review.test.ts`: 25 → **38** (+13) — `reviewCardState` (4 теста), `nextSessionSize` (2),
  `maskCardName` (5 обычных кейсов + параметризованный корпусный тест на 78 картах × ru/en = 2
  случая → 7) в основном раунде (12 тестов), плюс 1 тест из раунда правок («the» внутри соседнего
  слова не съедает настоящее вхождение имени).
- `i18nPlurals.test.ts`: 45 → **51** (+6) — три кейса `review.due` (count 1/3/12) добавлены
  в общий массив `CASES` и участвуют в ДВУХ структурных проверках сьюта («формы без
  `Intl.PluralRules`» и «паритет браузер/устройство»), поэтому 3 кейса дают 6 тестов.
- Остальные 32 сьюта не менялись (задачи 3–14 — рефакторинг компонентов и экранов, юнит-тестами
  не покрываются, testing-strategy п.2); прирост 13 + 6 = 19 сходится с 1273 → 1292.
- Волна правок финального ревью добавила ещё **+3** в `review.test.ts` (38 → 41): корпусный
  контракт маски развёрнут с жёсткого `['ru','en']` на все языки приложения (`LANGS`, +2 случая)
  и добавлен синтетический кейс на язык маски. Итог — **1295**.
`npx tsc --noEmit` — чисто.

**Веб-проверка (задача 14, Playwright 390×844).** Красная фаза: намеренно сломан `onGrade` в
`app/review.tsx` (`queue.slice(1)` вместо `applyGrade(queue, grade).queue` — «не помню» перестаёт
возвращать карту в очередь). Прогон дал ровно **6 FAIL**, все внутри сценария A (счётчик после
«Не помню» остаётся 8; было 7 оценок «Помню» вместо 8; «+8 XP»; «Ещё» отсутствует; xp в сторе = 8;
курс после сессии «завтра: 8») — остальные 40 проверок (сценарии B/C/D/E и регрессия R1–R4)
закономерно остались зелёными: для оценок ≥1 сломанный код совпадает с правильным (голова очереди
снимается в обоих случаях), дефект специфичен для ветки `grade===0`. Код восстановлен, финальный
прогон на рабочем коде — **все 46 проверок `ok`**, `console errors: 0` (единственный
задокументированный чужой warning `props.pointerEvents is deprecated` из
`@react-navigation/elements`/`react-native-screens`, печатается на всех экранах проекта, не наш
код). Прокликивание (6б): сценарий A (сессия из 8 новых карт с одним «Не помню») — карта после
провала вернулась в конец очереди, счётчик остался 8, итог «Повторено 8 · с первого раза 7 · +7
XP» (не +8 — засчитались только реально вспомненные), xp в сторе вырос ровно на 7; сценарий C
(12 просроченных) — сессия 10 → результат «Ещё 2» → вторая сессия 2 → xp суммарно 12. Скриншоты —
13 файлов в `docs/screenshots/45/` (`course-new/due/done-dark`, `course-new-light`,
`review-meaning-question/revealed-dark/light`, `review-card-question/revealed-dark`,
`review-result-dark`, `review-empty-deck/done-light`). Сверка 6а с макетом сделана дважды: задача
13 построчно сверила каждое значение стилей `#v-trainer`/`.revcard` с макетом (нашла и поправила
2 расхождения — `gap`, `fontWeight`, см. «Отклонения» выше), задача 14 подтвердила по свежим
скриншотам в обеих темах, что рефакторинги задач 3–13 ничего не сдвинули.

**Расхождения с макетом (осознанные, из контрольной сверки задачи 13):**
1. Пустое состояние — общий `EmptyState` (design-system §7, уже в четырёх местах проекта), а не
   отдельный рисунок `#trempty` (64×104 пунктиром).
2. Чип на рубашке — тот же общий `KeywordChips` (radius 14 / бордер `line` / вес 600), что и везде,
   а не второй стиль чипа из макета (radius 12 / бордер `frame` / вес 700) — второй стиль для тех
   же слов был бы дубликатом.
3. Порядок блоков в панели результата — «+X XP → строка» (ритм общего `ResultPanel`, как в финале
   урока), у макета «строка → +X XP»; Overline «ПОВТОРЕНИЕ» сверху — как в макете.
4. Кегль «+X XP» 36px против 34px у макета — решение задачи 08 (общий `ResultPanel` для урока
   и тренажёра держит один размер).
5. Подпись панели «ЗНАЧЕНИЕ» 9.5px против 8.5px у макета (`.mean .lbl`) — общий `MeaningPanel`
   (тот же, что «ЗНАЧЕНИЕ ДНЯ»), взят канон design-system.md «Overline 9.5–10».
6. Подпись «ВСПОМНИТЕ … · НАЖМИТЕ» после ответа в макете остаётся видимой — в приложении гаснет
   (одна тап-зона под картой держит место).
7. `.kws` margin-top в панели «ЗНАЧЕНИЕ»: макет 11px, код 10px — разница 1px, ниже порога
   значимости проекта, чек-лист требовал сверить только центрирование.

## Сценарий лайв-проверки для Артёма (iPhone)

Открыть приложение (таб «Курс»), пройти шаги по порядку.

**1. Набрать колоду ≥11 карт (нужно для пункта 4).** Настройки (шестерёнка) → блок «Для
разработки» → нажимать «Пройти следующий урок», поглядывая на карточку «Повторение» на экране
«Курс» (строка «Новых карт: N») — обычно хватает 5–6 нажатий, чтобы N перевалило за 11 (уроки без
карт в счёт не идут, поэтому число может расти не на каждое нажатие).

**2. Первая сессия, направление «карта → значение» (toMeaning) и хаптика.** На «Курс» тапнуть
карточку «Повторение» (строка «Новых карт: N»). Открывается тренажёр: карта лежит лицом вверх
(образ + название), под ней подпись «ВСПОМНИТЕ ЗНАЧЕНИЕ · НАЖМИТЕ». Тап по карте или по подписи —
появляется панель «ЗНАЧЕНИЕ» (4 золотых слова + предложение + ссылка «Страница карты →») и три
кнопки. Нажать по очереди все три вида кнопок в разных карточках сессии («Не помню», «Помню»,
«Легко») — на каждом нажатии должна ощущаться одинаковая лёгкая вибрация (Light); «Не помню» не
должна вибрировать «ошибкой» (Error/резкой) — самооценка не наказывается. После «Не помню» карта
должна вернуться в сессию позже (счётчик «К ПОВТОРЕНИЮ · N» при этом НЕ уменьшается).

**3. Ссылка «Страница карты →» и подпись «назад».** На любой раскрытой панели «ЗНАЧЕНИЕ» тапнуть
«Страница карты →» — откроется страница карты; убедиться, что чипы ключевых слов под названием
выглядят как обычно (регрессия выноса `KeywordChips`). Нажать кнопку «назад» в левом верхнем углу —
рядом со стрелкой должна стоять подпись «Тренажёр» (не «Курс» и не название карты), и после
возврата сессия должна оказаться на том же месте (счётчик очереди не сбросился).

**4. Дойти до результата и «Ещё N».** Довести первую сессию до конца (10 карт из набранных в шаге
1). Появляется панель итога: «ПОВТОРЕНИЕ» → «+N XP» (цифра катится вверх) → «Повторено 10 · с
первого раза K» → кнопка «ГОТОВО» → под ней ссылка «Ещё M» (M — остаток колоды сверх первых 10).
Тапнуть «Ещё M» — открывается вторая сессия ровно на M карт. Если ссылки «Ещё» нет — значит колода
из шага 1 не набрала 11 карт, вернуться к шагу 1.

**5. Направление «значение → карта» (toCard) и маска «···».** Направление выпадает случайно
(50/50) только у карт, которые УЖЕ хотя бы раз оценивались (не у новых) — поэтому его проще
поймать во второй-третьей сессии. Признак: вместо лица карты — рубашка со столбиком из 4 золотых
слов и предложением под ними, где имени карты НЕТ — на его месте «···» (например: «··· — карта
начала пути.»). Под картой та же подпись «ВСПОМНИТЕ КАРТУ · НАЖМИТЕ». Тап — карта с 3D-переворотом
(как расклад/финал теста п.7) показывает лицо и открывает панель «ЗНАЧЕНИЕ», где имя карты в
предложении уже видно целиком (маска снимается только на рубашке-вопросе). Если с первого раза не
попалась — в настройках нажать «Состарить повторение на день», это переведёт уже оценённые карты
в «ждут сегодня»; открыть новую сессию с «Курса» и повторить попытку.

**6. Карточка «Повторение» в трёх состояниях.** a) «Новые» — уже видели на шаге 1/2 («Новых карт:
N»). b) «Ждут» — в настройках нажать «Состарить повторение на день» (сдвигает срок повтора уже
оценённых карт на сутки назад) и вернуться на «Курс»: карточка должна показать «N карт ждут»
(обычным текстом, не зелёным). c) «Всё повторено» — довести очередь до конца (нажимать любые
кнопки, включая «Ещё N», пока сессии не кончатся): карточка окрасится в зелёный и покажет «Всё
повторено ✓ · завтра: M», по тапу больше никуда не ведёт.

**7. Регрессия соседних экранов (проверить, что выносы общих компонентов ничего не сломали).**
- Расклад «Три карты» (таб «Расклады» → «Три карты» → «Разложить карты» → тап по каждой из трёх
  рубашек): переворот карт должен выглядеть и звучать так же, как раньше — плавный оборот, лицо
  карты после переворота чёткое, не мутное.
- Финал урока (открыть любой непройденный урок из «Курс», дойти до конца): панель итога с
  катящимся счётчиком XP, конфетти над кнопкой «ГОТОВО», полоса прогресса модуля — всё как раньше
  (тренажёр использует ту же панель БЕЗ конфетти, урок — с ним).
- Карта дня (таб «Сегодня», перевернуть карту дня, если ещё не открыта сегодня): панель «ЗНАЧЕНИЕ
  ДНЯ» на месте, вокруг карты тёплое золотое свечение, изображение чёткое (не мыльное).
- Чипы ключевых слов на странице любой карты — те же золотые чипы под названием, что и раньше
  (см. также шаг 3).

Если все семь пунктов прошли без сюрпризов — 45б готова к мержу.
