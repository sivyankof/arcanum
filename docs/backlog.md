# Backlog — очередь задач (порядок = приоритет)

Правило: задача берётся только сверху; перед началом — спека по процессу из CLAUDE.md.
Статусы: [ ] не начата · [S] спека готова · [~] готово в коде и прошло ревью, ждёт веб- и/или
лайв-проверки · [x] готово и проверено на телефоне.

## 🔥 Хотфиксы по аудиту логики 09.08 — делать ДО задач очереди

Закрыты 10.08: спека `docs/specs/hf-01-logic-audit.md`, коммиты hf-01/H1–H3, tsc чистый,
29 юнит-тестов зелёные (3 сьюта), веб-проверка пройдена, лайв-проверка Артёма на iPhone ✓.
Push — после настройки remote (отложено решением Артёма).

- [x] **H1 · Луна: симметричные окна фаз** — logic-spec §6 обновлён; поправить moon.ts под новые границы
      (новолуние <0.92 или ≥28.61; полнолуние |age−14.765|<0.92) + юнит-тесты на 4 тест-кейса из спеки.
- [x] **H2 · Границы дня — локальные, не UTC** — в useApp.ts `todayStr()`/`yesterday` через
      `toISOString()` считают день по UTC: в UTC+3 до 03:00 ночи карта «вчерашняя», серия рвётся.
      Заменить на локальную дату (`getFullYear/Month/Date` с паддингом). Тест: полночь локальная.
- [x] **H3 · installSeed для карты дня** — сейчас seed=0 у всех → вся аудитория видит одну карту.
      Реализовать по logic-spec §1: installSeed при первом запуске (в store), FNV-1a хеш,
      анти-повтор окна 7 дней. Тест: детерминизм + разные сиды → разные последовательности.

## 🔥 Хотфикс 13.08 — русские числительные ломаются на устройстве (делать ДО задач очереди)

Найдено Артёмом на iPhone при лайв-проверке задачи 08. Полное исследование с доказательством,
радиусом и планом: `docs/specs/hf-02-plurals-research.md` — **читать перед спекой**.

- [x] **hf-02 · Плюрализация падает в английский фолбэк на телефоне** — ЗАКРЫТА 13.08:
      лайв-проверка Артёма на iPhone пройдена ✓ (веб-проверки у задачи нет и быть не может —
      в браузере полный ICU, смотреть не на что). Спека и отчёт: `docs/specs/hf-02-hermes-plurals.md`.
      Сделано: пакет `intl-pluralrules` импортом первой строкой `src/lib/i18n.ts`; под `__DEV__`
      предупреждение в лог, если правила всё-таки не встали (иначе «не починилось» неотличимо
      от «диагноз был не тот»); экран раскладов целиком переведён на i18n — «ПРАКТИКА», «Расклады»,
      «3 карты» вместо «3 карт», плашка «ПРЕМИУМ» вместо «PREMIUM»; новый сьют
      `src/lib/__tests__/i18nPlurals.test.ts` — 41 тест, всего стало 281 в 16 сьютах (было 240 в 15).
      Тест-симуляция устройства писалась ДО починки и была красной: на первой редакции файла
      26 упавших из 27, прошла ровно контрольная строка «1 УРОК» — та самая форма, что работала
      и на сломанном коде. Контрольный замер на финальной редакции (импорт полифилла временно
      снят): 30 из 40 красных.
      Правило записано в `logic-spec.md` §10, ловушка — в `CLAUDE.md`.
      Исходная постановка:
      в Hermes нет
      `Intl.PluralRules`, i18next 26 молча переходит на заглушку «1 → one, всё остальное → other»,
      а у русских ключей формы `_other` нет ни у одного семейства → фолбэк на `fallbackLng: "en"`.
      Диагноз воспроизведён: удаление `Intl.PluralRules` в Node даёт обе жалобы посимвольно.
      В браузере механизм полный — поэтому веб-проверка 6а/6б это пропустила и пропустит впредь.
      ⚠️ При count = 1 строка остаётся русской, ломается начиная с 2 — отсюда живучесть бага.
      **Решение (выбрано Артёмом 13.08):** пакет **`intl-pluralrules`** (автор eemeli) — одна строка
      импорта до `init` в `src/lib/i18n.ts`, CommonJS, ноль зависимостей, проходит jest-expo без
      правки конфигов, самоустанавливается только когда движок не тянет.
      ⚠️ НЕ перепутать с `@formatjs/intl-pluralrules` — тот ЕСМ-только, ломает `npm test`
      и требует правки `babel.config.js`. ⚠️ Вариант «просто дописать русским ключам `_other`»
      отвергнут: английский исчезнет, но грамматика останется сломанной («2 УРОКОВ»), а веб
      и устройство разойдутся навсегда.
      ⚠️ Меняется `package.json` → нужен `npm install` и перезапуск `npx expo start --tunnel`.
      **Сломано 11 мест, 9 всегда:** подпись таба «Курс» («6 MODULES · 32 LESSONS»), шапки всех
      шести модулей, финал урока («2 of 4 MODULE LESSONS DONE»), дневник («5 entries»,
      «Карта месяца · N раз», «Отозвалось X из Y дней»), страница карты («Drawn 2 times»),
      пуш спасения серии (заголовок «3-day streak» + гибрид «Серия 3 days — ещё можно сохранить»).
      ⚠️ Тексты уже поставленных пушей запечены в очередь ОС и останутся английскими до следующего
      пересчёта плана — починка их задним числом не исправит.
      **Тесты:** обычный юнит-тест это НЕ ловит (у Node полный ICU, `pushBody.test.ts` зелёный,
      хотя на телефоне отдаёт «Серия 3 days»). Нужны три: симуляция Hermes (снести
      `Intl.PluralRules` до загрузки `i18n.ts` — обязан краснеть на сегодняшнем коде), оракул
      против настоящего ICU по `Object.keys(resources)` (не по литералам языков), структурный
      на полноту суффиксов и на ключи с `{{count}}` без суффиксов.
      **Заодно, пока открыт i18n** (другая причина, но то же место): `app/(tabs)/spreads.tsx:43,48` —
      захардкоженное «PREMIUM» на плашке расклада и «3 карт» вместо «3 карты». ✅ Сделано 13.08,
      причём весь экран раскладов: заодно ушли тернары «ПРАКТИКА»/«PRACTICE» и «Расклады»/«Spreads»
      (решение Артёма 13.08 — файл всё равно открыт). Остальные 8 хардкод-строк вне этого экрана
      остаются задаче про многоязычие.
      **Проверка на телефоне после починки:** шапки шести модулей (у М4 самая длинная —
      «8 УРОКОВ · 40 КАРТ», смотреть перенос), финал урока, дневник, страница карты, пуш серии,
      английская локаль, холодный старт.

- [x] **Подготовка к многоязычию** (заведено 13.08 по вопросу Артёма про 4–5 языков) —
      ЗАКРЫТА 15.08 как поглощённая: техническая часть («что мешает уже сейчас» ниже) целиком
      сделана задачей 27 — тип `Lang` один на 4 значения, `useLang()`, `presentLang` в поиске,
      определение языка устройства, пикер-список; языковой QA-процесс (глоссарий на язык,
      автопроверки, обратный перевод, бета-читатели) расписан в `docs/localization-plan.md`
      и исполняется в задаче 28. Единственный не закрытый пункт — **пер-языковый статус в схеме
      контента** (сейчас один `status` на все языки блока) — перенесён в постановку задачи 28
      как её предусловие. Исходная постановка:
      РЕШЕНИЯ 13.08 (разговор с редактором): английская версия ВЫХОДИТ в v1; вычитка EN
      и будущих 4–5 языков — на Claude (носителя-таролога нет). Значит, задача обязана включить
      формальный языковой QA-процесс (спека при взятии): пер-языковый статус в схеме контента
      (сейчас один на оба языка), глоссарий терминов на каждый язык (база — content-guide),
      автопроверки (пустые/непереведённые строки, плейсхолдеры `{{}}`, плюрализация по образцу
      i18nPlurals, длины строк в UI), вычитка через обратный перевод со сверкой против русского
      эталона, бета-читатели-носители перед релизом каждого языка. Делать
      ДО третьего языка, пока дёшево. Цифры и разбор: `docs/specs/hf-02-plurals-research.md`,
      раздел 4. Интерфейс — примерно день работы (151 строка), контент — тяжело: 285 000 знаков
      на язык при готовом курсе, то есть 158 страниц и отдельный нейтив-редактор, знающий таро.
      Что мешает уже сейчас: язык выбирается копипастой `startsWith('ru') ? 'ru' : 'en'` в 10 местах
      (третий язык молча станет английским, компилятор промолчит — см. задачу про `useLang()`);
      тип `Lang` объявлен трижды; в схеме контента ОДИН `status` на оба языка сразу — «русский
      вычитан, английский черновик» записать негде; `src/lib/cardSearch.ts:64` роняет справочник
      на карте с непереведённым названием; системный язык не определяется, переключатель — тумблер
      на два положения. Шрифты покрывают европейские языки; арабский, иврит, CJK, хинди — ноль
      глифов плюс RTL (48 мест физического направления, `I18nManager` не используется нигде).

## Этап 1–2: догнать эталонный дизайн + ежедневная петля

- [x] **01 · Экран «Сегодня» = эталон** — готово 09.08: луна, XP-пилюля, рубашка-компас, искры, плавный
      текст, цикл блика; первые юнит-тесты (jest-expo, moon.ts). Проверено на iPhone. Спека: `docs/specs/01-today-screen.md`.
- [x] **02 · Кнопка сброса карты дня (dev)** — была реализована ранее (`resetToday` + строка в профиле под
      `__DEV__` — вырезается из релизной сборки сам, надёжнее TODO-RELEASE). Оформлена задним числом:
      `docs/specs/02-dev-reset.md`. Прокликана 09.08.
- [x] **03 · Страница карты = эталон** — ЗАКРЫТА 10.08: лайв-проверка Артёма на iPhone пройдена ✓
      (коммиты 88260ff, 80f8efa, 861dcde). Спека `docs/specs/03-card-page.md`, веб-проверка 6а/6б
      и две итерации замечаний — там же. Сделано: виньетка-свечение, парение картинки, вкладки сфер
      (Общее/Любовь/Работа/Финансы/Здоровье) вместо простыни блоков, контекстный блок «ваша карта
      сегодня», нативная шапка с подписью «Карты»/«Сегодня» и вибрацией возврата.
      Общее из задачи, что пригодится дальше: компонент `Block`, токены `gold`, хелпер `glow.ts`
      (свечение по контуру), перевод всех теней на `boxShadow` — теперь свечения видны и на Android.
      БЕЗ золочёных уголков: уголки — знак ритуала, живут ТОЛЬКО на карте дня (решение 10.08 по флагу
      6а-0; формулировка задачи была ошибочной).
      **+ Контекстный блок «Как карта дня»** (добавлено 10.08): если карта = сегодняшняя карта дня —
      блок поднимается выше вкладок, золотая рамка, заголовок «✦ ВАША КАРТА СЕГОДНЯ»; иначе — обычная
      панель на обычном месте (product-spec §3).
      Блок «Ваша история с картой» из макета переехал в задачу 05: его данные (сколько раз выпадала,
      отклики, последняя заметка) появляются только с дневником.
- [x] **04 · Справочник = эталон** — ЗАКРЫТА 11.08: лайв-проверка Артёма на iPhone пройдена ✓
      (в т.ч. липкая панель-«стекло» на нативном пути MaskedView+BlurView).
      Спека и отчёт: `docs/specs/04-cards-catalog.md`.
      Сделано: липкая панель поиск+чипы (SectionList с липкой секцией, ряды по 3 — numColumns
      несовместим), поиск по имени и ключевым словам (`src/lib/cardSearch.ts`, 19 юнит-тестов),
      заголовок «78 карт», чипы в одну строку, тень и шаг сетки по эталону.
      Общее, что переиспользуется дальше: `BlurSurface` (подложка navBg+blur, на неё переведён
      таб-бар), `SearchField`, `toRows` для любых сеток.
      Спека `docs/specs/04-cards-catalog.md` (утверждена 10.08).
      Поиск по имени и ключевым словам, липкая панель поиск+чипы, скелетоны (уже есть).
      Бейдж «Изучено ✓» ПЕРЕЕХАЛ в задачу 08 — он ставится по пройденным урокам, а движка нет.
      Расхождения с макетом, зафиксированные при сверке 10.08 (Артём заметил на устройстве):
      · заголовок «Все карты» → в макете «78 карт»;
      · поля поиска нет вообще — в макете панель «Найти карту…» с лупой (radius 15, паддинг 11×15,
        фон panel, бордер line, отступ сверху 14), липкая при скролле на подложке navBg (решение 10.08);
      · чипы фильтров переносятся на две строки, т.к. подписи длиннее макетных («Старшие арканы»
        вместо «Старшие») — в эталоне это одна строка с горизонтальной прокруткой (`.seg`, overflow-x);
      · ✅ конфликт доков (правило 6а-0) РЕШЁН 10.08: счётчиков в чипах НЕ будет — прав макет,
        короткие подписи влезают в одну строку с прокруткой. product-spec §3 поправлен.
- [x] **04б · Свечение искр через `textShadow`** — ГОТОВО 10.08, спека `docs/specs/04b-sparks-textshadow.md`.
      Премиса задачи оказалась неточной: `textShadow*` — не то же, что `shadow*`, они работают
      и на iOS, и на Android, а react-native-web их конвертирует в CSS (с deprecation-warning).
      Строкового `textShadow` в RN 0.81 нет вовсе — прямая замена убила бы свечение на устройстве.
      Сделано: хелпер `textGlow` в `src/theme/glow.ts` (web → строка, натив → legacy-тройка),
      `Sparks.tsx` переведён на него. Веб: свечение `0 0 9px` в обеих темах (было 4.5px — теперь
      точно как в эталоне), предупреждения из консоли ушли. Лайв-проверка на iPhone пройдена 11.08 ✓.
- [x] **04а · Единый шрифт текстов значений — РЕШЕНО 10.08: Cormorant.** Макет и design-system §2
      расходились: в макете `.block p` набран Manrope 12.5, в спеке «Body serif — Cormorant 16–17/25».
      Артём выбрал по скриншотам (`docs/screenshots/03/font-*.png`) вариант с засечками — то есть
      правы спека и текущий код, менять ничего не нужно. Проигравшая сторона — макет, см. 04в.
- [x] **04г · Контент: расширить ключевые слова карт** — ЗАКРЫТА 12.08: вычитка редактором
      пройдена, лайв-проверка Артёма ✓. Единственное замечание ревью — слово «сбылось» в текстах
      Кубков — вынесено отдельной задачей 20 (тексты значений правит редактор, не код).
      Итог: `keywords` заполнены у всех 78 карт (было 22), добавлено поле `search` — 8–12 скрытых
      поисковых синонимов на карту; 794 уникальных ru-слова против 85. Все 27 контрольных
      запросов дают 1–8 карт («расставание» → Смерть + Пятёрка Кубков, «выгорание» → Десятка
      Жезлов). Заодно починен `build_cards.py` (затирал 624 draft-блока при пересборке) и
      поправлен product-spec §3, где поиск ещё описывался липкой панелью из отменённого решения 04.
      Отчёт, скриншоты и известное ограничение: `docs/specs/04g-keywords.md`, `docs/screenshots/04g/`.
      ⚠️ Хвост про «сбылось» (найден при финальном ревью 06а, 11.08) вынесен в задачу 20.
      Исходная постановка (найдено при задаче 04, 10.08; объём уточнён 11.08):
      **у 56 карт из 78 массив `keywords` ПУСТОЙ**, слова есть только у 22 карт (ровно по 4),
      всего 85 уникальных ru / 86 en. Замер: `python -c` по content/cards.json, 11.08.
      Ни один бытовой запрос не находит ничего: «любовь», «деньги», «работа», «здоровье»,
      «измена», «развод», «переезд», «одиночество», «беременность», «ссора» → 0 карт.
      Цель: 6–8 слов на каждую из 78 карт, включая формулировки, которыми люди реально ищут.
      Правила тона — content-guide.
      Спека: `docs/specs/04g-keywords.md` (11.08). Решения брейншторма: ключевые слова разводятся
      на ДВА поля — `keywords` (4 слова, видны чипами под названием карты) и новое `search`
      (8–12 слов, только для поиска, в UI не показывается никогда); в `search` идут житейские
      формулировки и смыслы перевёрнутой карты, без медицины, беременности и обещаний сбычи.
      Черновик пишет Claude, вычитывает редактор (решение Артёма 11.08).
      Кода немного всё же требует: поле в типе `TarotCard`, проверка `search` в `matchesQuery`,
      слияние вместо перезаписи в `build_cards.py`.
      NB: у `keywords` нет статусного поля (в отличие от блоков `content`), так что готовность
      по ним `content_stats.py` не видит — при заполнении отслеживать отдельно.
- [x] **04з · Поиск с учётом словоформ** — ЗАКРЫТА 12.08: лайв-проверка Артёма на iPhone ✓.
      Спека и замеры: `docs/specs/04z-prefix-search.md`, скриншоты `docs/screenshots/04z/`.
      Замер на 15 размеченных запросах показал, что чистый префикс (как задумывалось) даёт
      вдвое больше мусора, чем отсечение окончаний, — Артём пересогласовал алгоритм 11.08.
      Итог: слова сравниваются потокенно, совпадение = «слово начинается с запроса» ИЛИ
      «совпали основы» (таблица окончаний ru/en, два прохода, минимальная основа 4 буквы).
      Верных попаданий 55 из 59 против 24 было, лишних 6 против 13. «работе» → 9 карт вместо 1,
      «семьи»/«ссоры»/«учебе»/«тревоге»/«переезда»/«изменой» — с нуля до 2–4 карт,
      «рост» → 2 вместо 11. Цена: подстрока в середине слова больше не ищется («ерофант»
      не найдёт Иерофанта); чередования не по силам таблице («денег» не находит «деньги»).
      Исходная постановка (решение 11.08 при брейншторме 04г):
      сейчас `matchesQuery` ищет вхождение запроса в слово, поэтому русская морфология его ломает:
      «деньгами» и «денег» не находят слово «деньги», «работе» не находит «работу». Заваливать
      контент падежными формами — тупик (по 30 форм на карту). Решение: считать совпадением общий
      префикс от 5 букв (нормализация та же — регистр, ё→е). ~15 строк в `src/lib/cardSearch.ts`
      + юнит-тесты, включая защиту от ложных срабатываний на коротких корнях. Делать ПОСЛЕ 04г:
      без наполненного `search` проверять эффект не на чем.
- [x] **04д · Макет: зоны растворения липкой панели** — СДЕЛАНО 11.08 в Cowork-сессии.
      Было: паддинги 14/12 и маска в процентах (22%/78%) — растворение заходило на поле поиска
      и на чипы, они выглядели размытыми. Стало (как в приложении, коммит ff5fa1e): паддинги 22/20,
      маска в пикселях `transparent 0 → #000 18px → #000 calc(100% − 16px) → transparent`.
      Правило зафиксировано в design-system §5: содержимое липкой панели всегда в непрозрачной
      зоне, растворяются только отступы.
- [x] **04в · Макет: тексты значений — Cormorant** — ЗАКРЫТА 11.08 как неактуальная, делать нечего.
      Формулировка задачи была ошибочной: правило `.block p` (строка 212) уже задаёт
      `font-family:var(--serif)`, размер 14/1.6 — и таким оно было минимум с коммита `861dcde`.
      С поправкой на масштаб рамы макета (~340px против 390) это те самые 16/1.5 из design-system §2,
      то есть макет, спека и код уже согласованы — расхождения, ради которого заводилась задача,
      в файле нет. Откуда взялась исходная формулировка про Manrope 12.5 — не установлено.
- [x] **04е · Scroll-aware панель поиска** — ЗАКРЫТА 11.08: лайв-проверка Артёма на iPhone ✓
      (нативный путь `MaskedView` + `BlurView`, исключение с фокусом поля держится).
      Спека и отчёт: `docs/specs/04e-scroll-aware-search.md`.
      Липкая панель из задачи 04 всё равно читалась полосой и постоянно съедала ~120px верха.
      Стало (паттерн Material 3 / Telegram): поиск+чипы в потоке шапки, скролл вниз — уезжают
      с контентом, жест вверх на глубине >140px — компактная панель въезжает сверху экрана
      (320ms, стекло glass + blur 20 под статус-бар, растворяется только нижний край 14px).
      Повторный тап по табу «Карты» = прокрутка к началу. `StickyGlass` заменён на `GlassPanel`
      с настраиваемыми зонами растворения. Выросло общего: `useScrollAwareBar`,
      `useTabScrollToTop`, чистое правило `nextBarShown` (`src/lib/scrollAware.ts`, 7 юнит-тестов).
- [x] **04ж · Макет: горизонтальный отступ парящей панели** — ЗАКРЫТА 11.08, править нечего.
      Задача заводилась по черновику панели (`padding:52px 18px 14px`), но в макет она попала уже
      исправленной: в коммите 34673a8 у `.cardsbar` `padding:52px 22px 14px` — ровно как у `.view`,
      прыжка поля поиска вширь нет. Проверено чтением CSS, а не глазами по скриншоту.
      Заодно поправлена строка design-system §5, которая всё ещё называла это «огрехом макета»:
      22 в макете и 24 (`spacing.xl`) в коде — одно и то же значение с поправкой на масштаб рамы
      (внутренняя ширина макета 350px против 390 у устройства, разница ~10%).
- [x] **05 · Дневник, часть 1** — ЗАКРЫТА 11.08: лайв-проверка Артёма на iPhone пройдена ✓
      (в т.ч. долгий тап по сегодняшней записи — единственное, что нельзя было проверить в вебе).
      Заметка к карте дня (плашка под значением + экран `app/note/[date].tsx`), дневник в профиле
      (навигатор месяцев, карточка месяца, записи, пустое состояние), блок «Ваша история с картой»,
      новый экран `app/settings.tsx` за шестерёнкой. Отчёт и отклонения — в спеке.
      Выросло общего: `journal.ts` (чистая арифметика дневника, 17 юнит-тестов), `CtaButton`,
      `ConfirmDialog` (⚠️ `Alert.alert` в react-native-web — пустышка, системным алертом пользоваться
      нельзя), `EmptyState`, `SettingsRow`, `JournalRow`/`MonthNav`/`MonthCard`, `NotePlate`,
      форматирование дат в `dates.ts`; `Block` принимает содержимое.
      Спека: `docs/specs/05-journal-part1.md` (согласована 11.08).
      Решения брейншторма: дневник сразу в финальной структуре product-spec §5 (навигатор месяцев,
      карточка месяца, записи) — фильтры ✓/≈/✗ и полоска распределения ждут `outcome` из 06;
      ввод заметки — плашка на «Сегодня» + отдельный экран-модалка `app/note/[date].tsx` (один экран
      на три входа: «Сегодня», дневник, рефлексия 06); «Тема · Язык · DEV-сброс» переезжают на новый
      экран `app/settings.tsx` за шестерёнку (product-spec §5), иначе список записей мешается
      с переключателями; править можно только сегодняшнюю заметку (logic-spec §3).
      Вся арифметика — чистый `src/lib/journal.ts` с юнит-тестами.
- [x] **06а · Вечерняя рефлексия (без пушей)** — ЗАКРЫТА 12.08: лайв-проверка Артёма на iPhone
      пройдена ✓ (в т.ч. главный сценарий — возврат приложения из фона вечером, ради которого
      добавлен слушатель `AppState`: `useFocusEffect` возврат из фона не ловит, и час оставался
      утренним). Спека и отчёт: `docs/specs/06a-evening-reflection.md`.
      Блок на «Сегодня» с 18:00: вопрос + три кнопки «Отозвалась/Отчасти/Не отозвалась» + плашка
      заметки (ОДИН блок, который вечером растёт из «Заметки о дне»); `outcome` в записи дневника;
      всё его использование — отметка ✓/≈/✗ в строке, чипы-фильтры, полоска распределения
      и «Отозвалось X из Y» в карточке месяца, «отзывалась N» на странице карты; тумблер
      «Вечерняя рефлексия» в настройках. Решение 11.08: задача 06 разделена, потому что пуши
      не проверяются в вебе вообще, а рефлексия проверяется целиком.
      Выросло общего: `FilterChips` (`src/components/FilterChips.tsx` — второе появление разметки
      чипов, вынесена из справочника, справочник переведён на неё без визуальных изменений),
      `pickPhrase` (`src/lib/phrases.ts` — первое реальное применение `content/phrases.json`
      и правила вариативности logic-spec §9, до этой задачи файл не читался кодом ни разу),
      `src/lib/reflection.ts` (`reflectionVisible` — чистая функция показа блока по часу/
      тумблеру/наличию карты дня), `canEditNote` → `canEditEntry` (одно правило «правится только
      сегодняшняя запись» на заметку и на ответ). Тестов теперь 113 в 8 сьютах (было 89 в 6).
- [x] **06б · Локальные пуши** — ЗАКРЫТА 12.08: веб-проверка 6а/6б и лайв-проверка Артёма
      на iPhone пройдены ✓ (11 задач по плану, `docs/specs/06b-push-notifications.md`).
      На лайв-проверке нашлось одно: тестовый DEV-пуш не показывался при ОТКРЫТОМ приложении.
      Разбор по логам с устройства — в отчёте спеки; итог: регистрация обработчика показа
      перенесена из `initPushes` на верхний уровень модуля (показ уведомления и подготовка
      к постановке — разные обязанности, связывать их было изъяном).
      Четыре вида пуша, горизонт отсчитывается от сегодня: утренний — сегодня (только пока карта
      не открыта) плюс дни +1..+3; вечерний — только сегодня, если карта открыта и рефлексии ещё
      нет; спасение серии — только сегодня в 20:00, если карта не открыта и streak ≥3; один
      возвратный пуш на 4-й день тишины, дальше молчание до возвращения. Просроченные пуши
      отбрасываются, жёсткий потолок 2/день, приоритет при обрезке: спасение серии > вечерний >
      утренний > возврат (logic-spec §8). Разрешение — прелюдия «Напомнить утром?» показывается
      один раз за всё время, сразу после переворота первой карты дня (флаг `settings.pushAsked`);
      отказ не переспрашиваем, включить можно в настройках (product-spec §1). Напоминания живут
      строками в `app/settings.tsx` — отдельного экрана нет (решение 12.08): тумблер + время
      утреннего/вечернего + подпись про два молчаливых вида. DEV-строки под `__DEV__`: тестовый
      пуш через 10 секунд и диалог «План пушей» (свой насчитанный план + отдельный счётчик того,
      что реально стоит в очереди ОС).
      Выросло общего: `src/lib/settings.ts` (`AppSettings`/`DEFAULT_SETTINGS`/`mergeSettings` —
      persist поднят до `version: 3`), `src/lib/pushPlan.ts` (чистые правила без единого импорта
      expo/react, план отдаёт ключи фраз, а не текст, 18 юнит-тестов), `src/lib/pushes.ts`
      (единственный модуль, знающий про `expo-notifications`), `src/lib/useAppActive.ts` (реакция
      на возврат приложения из фона — `useFocusEffect` этого не ловит), `src/lib/usePushScheduler.ts`,
      `ModalPanel` (общая подложка `ConfirmDialog` и обоих `TimePicker`), `TimePicker`/`TimePicker.web.tsx`
      (системное колесо на устройстве, список целых часов в вебе — у пакета нет веб-реализации),
      `confirmTone` у `ConfirmDialog`.
      ⚠️ `expo-notifications` и `@react-native-community/datetimepicker` веб не поддерживают —
      весь `pushes.ts` на вебе не-операция; `getPermission()` там нарочно отдаёт `'undetermined'`,
      а не `'denied'` — иначе блок напоминаний пропал бы из браузера и веб-проверка стала бы
      невозможна. ⚠️ `Linking.openSettings` в react-native-web не существует, но TypeScript
      этого не ловит (тип берётся из react-native и платформенно-независим) — вызов защищён
      проверкой платформы. ⚠️ Триггеры, прочитанные обратно из ОС, не совпадают по форме
      с переданными (Android и iOS решают это по-разному) — декодировать ненадёжно, поэтому
      DEV-диалог берёт даты из своего расчёта, а из системы только count. ⚠️ Холодный старт
      планирует дважды (дефолт стора → гидрация) — сериализовано номером плана, побеждает
      самый свежий пересчёт. Тестов теперь 137 в 10 сьютах (было 113 в 8).
- [x] **07 · Экран курса = «путь»** — ЗАКРЫТА 13.08: лайв-проверка Артёма на iPhone пройдена ✓,
      ветка `feat/07-course-path` влита в main. Извилистая тропа с узлами (done/current/locked),
      пульс текущего, чип «НАЧАТЬ УРОК», качание замка с Warning-вибрацией. Без движка уроков —
      узлы ведут на заглушку `/lesson/[id]` (движок — задача 08).
      Спека `docs/specs/07-course-path.md` (там же отчёт о реализации и веб-проверке) + детальный
      план `docs/plans/07-course-path.md`; скриншоты `docs/screenshots/07/`.
      Решения: блокировка сквозная (один текущий узел на весь курс), все 6 модулей одной лентой
      с автоскроллом к текущему, счётчик «M КАРТ» считает уникальные карты модуля.
      Выросло общего: `src/lib/courseProgress.ts` (чистые функции пути, 25 тестов), компоненты
      `PathNode`/`CoursePath`/`ModuleHeader`, хук `useBackHaptic`, токен `gold.nodeGradient`,
      поле `lessonsProgress` в сторе (persist version 4). Тестов стало 171 в 12 сьютах.
- [x] **08 · Движок урока** — ЗАКРЫТА 13.08: лайв-проверка Артёма на iPhone пройдена ✓ (конфетти,
      вибрация и катящийся счётчик XP на устройстве работают). Проверялась уже после того, как
      ветка была влита в main, — обычный порядок обратный, merge после проверки.
      Реализация закончена 13.08 (ветка `feat/08-lesson-engine`): теория
      (из course.json) → разбор карт урока → викторина 5 вопросов → экран результата с XP и
      конфетти. XP-система настоящая (стор, три источника, пилюля «Сегодня»), persist поднят
      до version 5. Бейдж «Изучено ✓» в справочнике на месте. Идёт веб-проверка 6а/6б (задача 13);
      лайв-проверка Артёма впереди — в `[x]` переведёт только она.
      **+ Бейдж «Изучено ✓»** (перенесён из 04, решение 10.08): вместе с прогрессом уроков в сторе
      появляется список изученных карт → бейдж в углу миниатюры в справочнике (`.st2` эталона:
      сверху-справа 6/6, шрифт 8, цвет accent2, фон rgba(0,0,0,.4), radius 8, паддинг 2×6,
      letterSpacing 0.5) и, по желанию, отметка на странице карты. Раньше делать нечего — нет данных.
- [x] **09 · Онбординг** — ЗАКРЫТА 13.08: лайв-проверка Артёма на iPhone пройдена ✓,
      ветка `feat/09-onboarding` влита в main (merge 893daf2).
      Спека `docs/specs/09-onboarding.md`, детальный план `docs/plans/09-onboarding.md`,
      скриншоты `docs/screenshots/09/`.
      Три шага как в эталоне: welcome → имя+дата рождения → аркан рождения; расчёт — сумма цифр
      даты со свёрткой до 1–22 (`src/lib/birthArcana.ts`, 11 тестов). Гейт первого запуска —
      `Stack.Protected` в корневом layout (⚠️ НЕ `<Redirect>`: из layout редирект отдать некуда,
      пока навигатор не смонтирован), сплэш держится до гидрации persist. Отдельной кнопки
      «пропустить» нет — подпись CTA меняется сама. Фразы «ваш путь» — новый девятый блок
      `birth_path` у 22 старших арканов (черновики, вычитка — задача 23). Плюс золотой чип
      «✦ ВАШ АРКАН РОЖДЕНИЯ» на странице карты и DEV-строка сброса в настройках.
      Выросло общего: `Emblem` (эмблема-компас, проп `ticks` — у рубашки засечки есть, у онбординга
      в макете нет), `DatePicker`/`DatePicker.web.tsx`, `formatFullDate` в `dates.ts`,
      `profile` в сторе (persist **version 6**).
      ⚠️ Что поймало ревью и чего не видел браузерный прогон: экран `app/settings.tsx` не был
      объявлен в layout, а expo-router добавляет необъявленные файловые маршруты САМ — то есть мимо
      обоих гардов: настройки открывались по прямой ссылке до онбординга, а DEV-сброс, нажатый
      на самом экране настроек, никуда не уводил. Правило: любой новый маршрут корневого стека
      обязан быть вписан в `_layout.tsx` под гард.
      ⚠️ Второе: гейт превратил «битую запись в хранилище» из «старт с дефолтов» в вечный сплэш
      без шанса починить иначе, чем переустановкой (logic-spec §7).
- [x] **10 · Заморозка серии** — ЗАКРЫТА 14.08: веб-проверка 6а/6б и лайв-проверка Артёма
      на iPhone пройдены ✓, ветка `feat/10-streak-freeze` влита в main. Механика: старт 1 заморозка
      сразу у всех, +1 в месяц до потолка 2, трата при пропуске ровно одного дня (streak+1,
      пропущенный день не в счёт), пропуск 2+ дней подряд — сброс серии, заморозка цела. Хвост 16
      закрыт: третья стата «ЗАМОРОЗКА» в профиле (`StatBox` icon="snow"). Плюс строка «Серию
      спасла заморозка» на «Сегодня», пуш `freeze` (замена утреннего слота), DEV-строка
      «Пропустить вчера» в настройках, persist **version 7**. Чистый модуль `src/lib/streak.ts`
      (`advanceStreak`/`grantFreezes`) под юнит-тестами.
- [x] **11 · Экспорт/импорт данных (бэкап)** — ЗАКРЫТА 14.08: лайв-проверка Артёма на iPhone
      пройдена ✓ (круг «экспорт → сброс → импорт», очередь пушей после импорта, отмены share
      sheet/пикера), ветка `feat/11-export-import` влита в main. Спека `docs/specs/11-export-import.md`:
      бэкап всего стора в JSON через share sheet, импорт с валидацией «всё или ничего» и полной
      заменой состояния. Тестов 362 в 20 сьютах.
- [x] **12 · Экран «О приложении»** — ЗАКРЫТА 14.08: веб-проверка 6а/6б и лайв-проверка
      Артёма на iPhone пройдены ✓, ветка `feat/12-about` влита в main.
      Спека `docs/specs/12-about-screen.md` (там же отчёт), скриншоты `docs/screenshots/12/`.
      Сделано: экран `app/about.tsx` — эмблема + «Arcanum» + версия из app.json через
      expo-constants, четыре блока: дисклеймер, «О колоде» (Памела Колман Смит, 1909,
      public domain), политика конфиденциальности ТЕКСТОМ внутри приложения (решение Артёма
      14.08 — лендинга ещё нет), источники (tarot-api, MIT); строка в настройках после
      «Импорт из файла»; маршрут под гардом онбординга (проверено: прямой /about до
      онбординга редиректит). Выросло общего: `appVersion()` в `src/lib/appInfo.ts`
      (отдельный модуль, чтобы settings.ts не тянул expo в чистые модули), экспорт
      `paragraphStyle` из `Block` (абзац Cormorant 16/24 для блоков, где абзацы разного цвета).
      ⚠️ Текст политики в приложении НЕ заменяет URL политики для анкет сторов (пункт остался
      в release-checklist на этап лендинга); при добавлении PostHog текст обновить ДО релиза.
      Исходная постановка: версия, дисклеймер («для развлекательных и образовательных целей»),
      атрибуция Памелы Колман Смит и tarot-api (MIT), ссылка на политику. Обязателен к релизу
      (release-checklist), вход из профиля.
- [x] **13 · Обратная связь** — ЗАКРЫТА 14.08: веб-проверка 6а/6б и лайв-проверка Артёма
      на iPhone пройдены ✓, ветка `feat/13-feedback` влита в main.
      Спека и отчёт: `docs/specs/13-feedback.md`, скриншоты `docs/screenshots/13/`.
      Строка «Обратная связь и ошибки · Написать нам» в НАСТРОЙКАХ (формулировка «в профиле»
      писалась до выноса настроек за шестерёнку; правда — product-spec §5), между импортом
      и «О приложении». mailto через Linking: тема «Arcanum: отзыв», в теле блок диагностики
      «можно удалить» — версия, платформа+ОС, язык, тема, ЧИСЛО записей истории; личных данных
      нет. Адрес — ВРЕМЕННО личный gmail (решение 14.08), замена на адрес поддержки лендинга —
      пункт в release-checklist. Fallback: reject openURL → диалог с адресом и «Скопировать
      адрес» (`expo-clipboard`, есть в Expo Go SDK 54; менялся package.json → нужен npm install).
      Новое общее: `src/lib/feedback.ts` (`SUPPORT_EMAIL`, `buildMailto` — чистый, 5 тестов).
      Тестов 367 в 21 сьюте.
      Исходная постановка: строка в профиле → mailto с автодиагностикой (версия, ОС, язык, тема;
      без личных данных — состав в product-spec §5). Fallback без почтового клиента: показать адрес +
      «скопировать». Перехватывает негатив до того, как он станет «1★» в сторе.
- [x] **14 · Полноэкранный просмотр карты** — ЗАКРЫТА 14.08: лайв-проверка Артёма на iPhone
      пройдена ✓ (после хотфикса: краш pinch/свайпа — worklet-директивы; «две картинки» в конце
      закрытия — заморозка и скрытие источника), ветка `feat/14-fullscreen-card` влита в main.
      Веб-проверка 6а/6б пройдена (обе темы, оба входа, скриншоты `docs/screenshots/14/`),
      389 тестов в 22 сьютах. Отчёт — в спеке. ⚠️ package.json менялся
      (gesture-handler + expo-sensors) → на новом окружении нужен npm install.
      Спека `docs/specs/14-fullscreen-card.md` + детальный план (`docs/plans/14-fullscreen-card.md`,
      9 задач). Решения: весь §15 motion-spec в одной ветке
      (параллакс — последним коммитом, при капризах DeviceMotion выносится хвостом);
      ассеты не трогаем — мыльность зума ×3 на текущих сканах — известное ограничение,
      замена изображений на 2000px+ — отдельная задача. Новые пакеты:
      react-native-gesture-handler + expo-sensors (оба в Expo Go 54, нужен npm install).
      Исходная постановка (добавлено 10.08 по идее Артёма) — тап по изображению
      на странице карты → карта плавно раскрывается на весь экран (анимация от текущей позиции,
      motion-spec §15): затемнение фона, pinch-zoom и панорамирование, чтобы рассмотреть детали
      рисунка Памелы Колман Смит; закрытие — тап, свайп вниз или ✕. Работает также с карты дня
      после переворота. Спека поведения: product-spec §3.

- [x] **17 · Каскад появления при повторном входе на таб** — ЗАКРЫТА 11.08, лайв-проверка ✓
      (замечено Артёмом на лайв-проверке 05). Было: элементы выезжали снизу только при первом заходе на таб — `FadeUp`
      запускал анимацию в эффекте монтирования, а таб-навигатор держит открытый экран смонтированным,
      поэтому второй заход был мгновенным. Стало: анимация висит на фокусе экрана (`useFocusEffect`),
      первый показ — полные 450мс со ступеньками 70мс и подъёмом на 14px.
      ⚠️ Возвращение сначала сделали «коротким полным входом» (0 → 1 + сдвиг за 200мс) — Артём
      на устройстве увидел вспышку пустоты и рывок. Итог: при возврате только прозрачность
      0.6 → 1 за 180мс, без ступенек и без сдвига. motion-spec §4 обновлён.
      Вторым заходом (11.08, снова по наблюдению Артёма) закрыто ещё два хвоста: **тело списков**
      (сетка карт, записи дневника) раньше не участвовало в появлении — поднималась одна шапка,
      а середина просто была; теперь первые 4 ряда/записи входят одним блоком с индексом 3
      (ступенек между карточками нет — волна вынесена в задачу 18). И **повторный тап по активному
      табу = прокрутка к началу** теперь на всех пяти табах, а не только в «Картах»: общий хук
      `useTabTopRef` (ref + подписка одной строкой) в `src/lib/useTabScrollToTop.ts`.
      Позиция прокрутки при переходе между табами сохраняется — это решение, а не недоделка.
- [x] **19 · Чипы-фильтры прокручиваются до края экрана** — ЗАКРЫТА 11.08, лайв-проверка Артёма
      на iPhone ✓ (замечено им же). Было: лента чипов лежала внутри контейнеров с паддингом 24, поэтому
      прокрутка обрывалась за 24px до края и крайние чипы выглядели обрезанными посреди экрана.
      Стало: контейнеры без горизонтальных паддингов, отступ держат сами элементы — у поля поиска
      внешний (`marginHorizontal`), у ленты чипов внутренний (`contentContainerStyle`). Правка
      действует в обоих экземплярах панели — в потоке и в парящей. design-system §5 обновлён,
      скриншоты `docs/screenshots/19/`.
- [x] **18 · Появление сетки карт волной** — ЗАКРЫТА 15.08 решением Артёма: волну НЕ делаем,
      кода не было и не будет. Обоснование: в макете покарточной анимации нет (замер 11.08:
      `#grid` — один `.fadeup d4`, у ячеек `.gc` прозрачность всегда 1; ощущение «появляются
      по одной» давало декодирование 21 base64-картинки — случайный артефакт загрузки, а не
      задуманный эффект), motion-spec §4 прямо запрещает каскад для списков длиннее 8 элементов,
      а текущее появление (задача 17: шапка + первые 4 ряда одним мягким блоком) макету уже
      соответствует. Оба дока остаются как есть.
      Исходная постановка (заведено 11.08 по наблюдению Артёма): база уже сделана
      в задаче 17: сетка и записи дневника входят вместе с шапкой, но ОДНИМ блоком. Осталась сама
      волна: карточки проявляются по очереди (шаг 25–35мс), только по первым ~12 видимым ячейкам
      и только при ПЕРВОМ открытии экрана — дальше сразу, иначе прокрутка 78 карт превратится
      в мигание.
      ⚠️ Это правка сразу двух доков, поэтому нужна спека: в макете покарточной анимации НЕТ,
      а motion-spec §4 прямо запрещает каскад для списков длиннее 8 элементов.
      Сначала решение по обоим докам, потом код.
- [x] **15 · Макет: дорисовки по дневнику** — СДЕЛАНО 14.08 Cowork-сессией: все пункты (а)–(п), включая рефлексию (кнопки «Отозвалась/Отчасти/Не отозвалась», «изменить до полуночи» нажимаема, подсветка выбора), модалку заметки, прелюдию пушей, тумблер напоминаний, экран «О приложении», чип аркана рождения (аркан примера сменён на Дурака: дата 01.02.1990 → 22 → 0, всё консистентно), explain+ДАЛЕЕ и card-вопрос в уроке, дни недели в дневнике, чипы до края. Исходная постановка: (заведено 11.08 при спеке 05, правило 6а-0) —
      две правки в `design-reference.html`: (а) дата в строке дневника «11 АВГУСТА» → «ПН · 11 АВГУСТА»
      (product-spec §5 требует день недели + число, приложение делает по спеке); (б) экрана заметки
      в макете нет вовсе — ввод показан инлайновым `.rnote`, а в приложении это отдельный экран-модалка
      (клавиатура посреди длинного скролла + правка заметки из дневника); (в) ряд чипов `.seg`
      обрезается по контенту (лежит внутри `.view` с паддингом 22) — в приложении лента прокручивается
      от края до края экрана, отступ держат сами элементы (задача 19). Задача для Cowork-сессии.
      Дописано 11.08 при спеке 06а — пять правок в блоке `#reflect`: (г) кнопки «Сбылось/Отчасти/Мимо»
      → «Отозвалась/Отчасти/Не отозвалась» (content-guide и logic-spec §3 запрещают слово «сбылось»;
      та же правка нужна в product-spec §1); (д) overline «ВЕЧЕРОМ · 21:00» → «КАК ПРОШЁЛ ДЕНЬ» —
      блок появляется в 18:00, надпись обещает не то время; (е) строка «изменить можно до полуночи»
      не нажимаема, то есть изменить ответ в макете нечем, хотя logic-spec §3 это требует;
      (ж) нет состояния «кнопка выбранного ответа подсвечена» — оно нужно, когда кнопки возвращаются;
      (з) плашка заметки живёт только внутри блока рефлексии, а в приложении это один блок,
      который вечером растёт из «Заметки о дне» (до 18:00 заметку иначе негде писать).
      Дописано 12.08 при спеке 06б — две правки: (и) строка «Напоминания · 9:00 · 21:00» выглядит
      переходом на отдельный экран, а в приложении это тумблер + две строки времени + подпись
      про молчаливые пуши; (к) прелюдии разрешения на пуши в макете нет вовсе, хотя это первый
      экранный момент новичка после первой карты.
      Дописано 13.08 при реализации 09 — одна дорисовка: (о) на странице карты нет чипа
      «✦ ВАШ АРКАН РОЖДЕНИЯ», хотя product-spec §3 его требует и он реализован. Правило 6а-0:
      сделано по спеке, макет догоняет.
      Дописано 14.08 при реализации 12 — одна дорисовка: (п) экрана «О приложении» в макете
      нет вовсе (только строка в настройках «О приложении · v0.1») — дорисовать по реализации:
      эмблема + «Arcanum» + версия, четыре блока (дисклеймер, «О колоде», политика, источники);
      композиция — `docs/screenshots/12/`. (Эмблема онбординга дорисовки НЕ требует: `.emb2` без
      засечек — это осознанно, приложение повторяет макет через проп `ticks`.)
      Дописано 13.08 при реализации 08 — три дорисовки в уроке: (л) после ответа на вопрос
      в приложении появляются пояснение explain и кнопка «ДАЛЕЕ», в макете их нет; (м) у
      card-вопроса в приложении изображение карты над текстом вопроса — в макете вопрос
      только текстовый; (н) панель теории в приложении несёт Overline-заголовок «ТЕОРИЯ»
      (фирменный стиль Block), в макете `.theory` без заголовка.
- [x] **Макет: пустая карточка аркана рождения** — СДЕЛАНО 14.08: тап по карточке в макете переключает заполненное/пустое (пунктир + «Указать дату рождения»). Исходная постановка: (заведено 13.08 при задаче 16) —
  макет рисует только ЗАПОЛНЕННУЮ `.barc`, а приложение обязано показать что-то и тем, кто
  пропустил дату рождения в онбординге (product-spec §0 обещает предложить её позже).
  Приложение показывает пунктирную карточку-приглашение той же геометрии: Overline
  «АРКАН РОЖДЕНИЯ» + строка «Указать дату рождения», без изображения (пунктир = «сюда можно
  заполнить» — паттерн `NotePlate`, решение 4 спеки 16). Дорисовать это состояние в макет.
- [x] **Макет: курс одной лентой** — СДЕЛАНО 14.08: все 6 модулей вертикально, шапки-панели «МОДУЛЬ N ИЗ 6», М1 пройден, М2 с текущим узлом, М3–М6 свёрнуты («+ ещё N урока»), замки качаются. Исходная постановка: — design-reference.html рисует один модуль на экран
  («Модуль 2 из 6» шапкой экрана); в приложении по спеке 07 все 6 модулей одной вертикальной
  прокруткой (решение брейншторма 12.08). Дорисовать вкладку «Курс»: секция модуля =
  шапка-панель (overline «МОДУЛЬ N ИЗ 6» внутри) + тропа, экранный заголовок — просто «Курс».

- [x] **Макет: правки дорисовок 14.08 по сверке с приложением** — ПУНКТЫ 1–7 И МЕЛОЧИ СДЕЛАНЫ
      15.08 Cowork-сессией (диалог пушей; заметка+рефлексия одним блоком; модальный экран заметки;
      курс: полные тропы М3–М6 + счётчики «N УРОКОВ · M КАРТ»; explain префиксом без плашки, «ДАЛЕЕ»
      отдельно; настройки: Вкл + настоящий caption; «О приложении» — тексты из i18n about.* дословно;
      + мелочи: даты выправлены на реальный календарь августа-2026, ЛУНА исправлена на расчёт moon.ts
      для 9.08 — убывающая · 26-й день, дата в настройках 01.02.1990, qimg 110, пунктир без фона,
      «Версия 0.1» не капсом). Смоук jsdom: ошибок 0, курс 6 секций / 32 узла.
      П.8 УСТАРЕЛ — вычеркнут (задача 39 переделала вкладки: #tabbody + FOOL_REV уже в макете).
      П.9 (пикер языка) и П.10 (тренажёр по спеке 45: три кнопки «Не помню/Помню/Легко», вопрос
      до оборота в обоих направлениях toMeaning/toCard, карточка «Повторение» в шапке курса с
      тремя состояниями, панель результата без конфетти, панель «ЗНАЧЕНИЕ» с чипами keywords +
      предложением + «Страница карты →») — СДЕЛАНЫ 19.08 Cowork-сессией. Смоук jsdom: ошибок 0,
      курс 6/32, оба режима кликаются до результата, пикер 4 языка с галочкой. ЗАДАЧА ЗАКРЫТА.
      ⚠️ Флаг к 45б — ЗАКРЫТ 19.08 задачей 45б: `maskCardName` + корпусный контракт 78 × ru/en.
      (Исходная находка: promptSentence возвращает первое предложение general, а оно НАЧИНАЕТСЯ
      с имени карты — в направлении toCard подсказка выдавала бы ответ («Дурак — карта начала
      пути»). В макете имя замаскировано «···» — приложение теперь маскирует так же.)
      (заведено 14.08: сверка коммита
      186387e с реализацией, правило 6а-0 — приложение везде по спекам, правки только в макет;
      задача для Cowork-сессии). Что совпало — не трогать (шапка курса, названия модулей = course.json,
      кнопки/подсветка рефлексии, «ТЕОРИЯ», «ПРОЙДЕНО X ИЗ Y…», чипы до края, чип аркана, онбординг
      01.02.1990 → Дурак). Расхождения:
      1. **Прелюдия пушей** — в макете инлайн-панель «✦ Напомнить о вашей карте завтра утром?» с
         «НАПОМИНАТЬ»/«ПОЗЖЕ»; в приложении модальный диалог (product-spec §1): «Напомнить утром?»,
         текст «Пришлём одно тихое напоминание, когда придёт время новой карты. Выключить можно
         в любой момент в настройках.», кнопки «Напомнить»/«Не сейчас». Перерисовать диалогом.
      2. **Заметка+рефлексия = ОДИН блок**, а не два: до 18:00 «ЗАМЕТКА» с плашкой
         «+ Добавить заметку о дне…», после 18:00 тот же блок становится «КАК ПРОШЁЛ ДЕНЬ»
         (вопрос + кнопки НАД той же плашкой). В макете сейчас отдельные #daynote («ЗАМЕТКА О ДНЕ»,
         «+ Записать пару строк…») и #reflect без плашки — это ровно то, что пункт (з) задачи 15
         просил убрать.
      3. **Модалка заметки** — в приложении модальный экран (спека 05), не bottom-sheet: нативная
         шапка «Заметка», дата ДЛИННЫМ днём недели («СУББОТА · 8 АВГУСТА»), миниатюра 44×72 +
         название карты, placeholder «О чём этот день?». CTA «СОХРАНИТЬ» — совпал.
      4. **Курс: М3–М6 не сворачивать** — в приложении (спека 07) все уроки всех модулей полными
         тропами, закрытые узлы с замком и opacity 0.55; строки «+ ЕЩЁ N УРОКА» нет. В шапку модуля
         вернуть строку счётчиков «N УРОКОВ · M КАРТ» (у М1/М6 счётчик карт не показывается).
      5. **Explain в уроке** — без плашки-фона: просто текст, начинается цветным префиксом
         «Верно.» (success) / «Не совсем.» (danger), БЕЗ зачинов («Именно!» в макете противоречит
         задаче 21); кнопка «ДАЛЕЕ» (без стрелки) — отдельно внизу шага, не внутри пояснения.
      6. **Настройки, напоминания** — caption заменить на настоящий: «Ещё напомним, если серия
         под угрозой, и один раз — если вы давно не заходили» (макетное «Приходят тихо: без
         звука…» неверно по смыслу); «Напоминания» — строка со значением Вкл/Выкл, как
         «Вечерняя рефлексия», а не рисованный тумблер. Подписи времени «Утро · карта дня» /
         «Вечер · как прошёл день» ОСТАВИТЬ — здесь прав макет, приложение переведено на них
         14.08 (правка i18n, решение Артёма).
      7. **«О приложении» — тексты взять из приложения** (i18n `about.*`, задача 12): блоки
         «О ПРИЛОЖЕНИИ» (описание + дисклеймер вторым абзацем), «О КОЛОДЕ», «ДАННЫЕ И
         КОНФИДЕНЦИАЛЬНОСТЬ» (два абзаца, включая экспорт), «ИСТОЧНИКИ» (только tarot-api MIT).
         Макетные «ДИСКЛЕЙМЕР»/«КОНФИДЕНЦИАЛЬНОСТЬ» и фраза «Значения основаны на „The Pictorial
         Key to the Tarot“» — убрать: источник значений — редактор, утверждать книгу нельзя.
      8. **Страница карты: перевёрнутая приписка внутри вкладки-сферы** (добавлено 15.08,
         задача 25) — элемента в макете нет вовсе, он новее макета. Дорисовать в блок каждой
         сферы, КРОМЕ «Общего», после основного текста: разделитель 1px цветом line (отступ
         сверху 12), подпись «ПЕРЕВЁРНУТАЯ» Overline 9/ls 2.5 цветом muted (отступ 10),
         текст Cormorant 15/22 цветом ОСНОВНОГО текста (отступ 6) — не muted: замер контраста
         дал на светлой теме 2.88:1 при норме 4.5. Общий блок «Перевёрнутая» под вкладками
         остаётся как есть.
      9. **Пикер языка** (добавлено 15.08, задача 27) — в макете этого шага нет, экран новее
         макета. Дорисовать: строка настроек «Язык · Русский» открывает модальный список из
         четырёх названий языков на них самих с галочкой у текущего — композиция как у списка
         часов в вебе.
      10. **Тренажёр SM-2 — по спеке 45** (добавлено 19.08, `docs/specs/45-flashcards.md`, раздел
         «Расхождения с макетом»): (а) кнопок оценки ТРИ с безличными подписями «Не помню / Помню /
         Легко» вместо четырёх «Не вспомнила / С трудом / Вспомнила / Легко»; (б) до оборота нужен
         ВОПРОС — два направления: «карта → значение» = карта лицом вверх + подпись «ВСПОМНИТЕ
         ЗНАЧЕНИЕ · НАЖМИТЕ», панель скрыта; «значение → карта» = рубашка с 4 ключевыми словами
         и одним предложением в зоне эмблемы + «ВСПОМНИТЕ КАРТУ · НАЖМИТЕ» → переворот;
         (в) карточка «Повторение» в шапке курса над первым модулем — три состояния «N карт ждут»
         / «Новых карт: N» / «Всё повторено ✓ · завтра: M» (сейчас вход только кнопкой демобара);
         (г) панель результата сессии по образцу `.lresult` без конфетти: «Повторено N · с первого
         раза K · +X XP», CTA «Готово», ссылка «Ещё N»; (д) панель «ЗНАЧЕНИЕ» — чипы `keywords`
         + одно предложение + ссылка «Страница карты →». Что совпало — не трогать (вход с курса,
         Overline/заголовок, счётчик, `ringwrap` 190×330, плашка названия, пустое состояние с CTA
         «К курсу», геометрия кнопок).
      Мелочи: строка после ответа рефлексии — «Записано в дневник · X» + ссылка «изменить можно
      до полуночи»; вопрос рефлексии взять из реальных вариантов phrases.json (ключ
      `reflect.question`); дни недели демо-данных сдвинуты (8.08.2026 — СБ, а в макете ПТ; 9.08 —
      ВС, не СБ); пустая карточка аркана — пунктир БЕЗ фона и «Указать дату рождения» без
      многоточия; картинка card-вопроса 110px (в макете 86); «Версия 0.1» не капсом.
- [x] **16 · Профиль = эталон** (заведено 11.08 при спеке 05) — ЗАКРЫТО 14.08, спека
      `docs/specs/16-profile.md`, план `docs/plans/16-profile.md`, ветка `feat/16-profile`.
      Веб-проверка 6а/6б и лайв-проверка Артёма на iPhone пройдены ✓ (скриншоты
      `docs/screenshots/16/`). Замечание лайв-проверки — эмодзи-огонёк у серии вместо иконки —
      исправлено: `StatBox` рисует огонёк тем же `Ionicons name="flame"`, что и «Сегодня».
      Сделано: шапка «ВАШ ПУТЬ» + имя (без имени — «Профиль»), разделитель, карточка уровня
      с XP-полосой, две статы, карточка аркана рождения (тап → страница карты, «назад» подписан
      «Профиль»). Хвост 09 закрыт: **если дату рождения пропустили**, профиль показывает пунктирное
      приглашение «Указать дату рождения» → системный пикер → аркан появляется немедленно.
      ⚠️ Третьей статы «ЗАМОРОЗКА ❄» из макета НЕТ осознанно (решение 1 спеки): механики заморозки
      не существует до задачи 10, а показывать выдуманное число — врать на лайв-проверке.
      ⚠️ Persist version НЕ поднималась, и это правильно: `setBirthDate` пишет в УЖЕ объявленные
      опциональные поля `profile.birthDate`/`birthArcanaId` (тип `Profile` не менялся), новых ключей
      схемы нет. Предупреждение про версию касается добавления НОВОГО поля внутрь `profile` —
      оно в силе для следующих задач (следующая, меняющая схему, поднимает до 7).

- [x] **20 · Контент: убрать «сбылось» из текстов Кубков** — ЗАКРЫТА 14.08: четыре новые
      формулировки одобрены редактором («всё норм», передал Артём).
      ПРАВКА ВНЕСЕНА 13.08 по решению
      редактора («переименовать в рамку "отозвалось"»): `grep "сбыл" content/` пуст, 297 тестов
      зелёные. Что изменилось (ru + en зеркально): чип-ключевое «желание сбылось» → «заветное
      желание» (⚠️ вхождение оказалось в `keywords`, а не в `search`, как думала постановка, —
      то есть было ВИДНО чипом под названием карты); Девятка Кубков reversed — «желание сбылось,
      а радости меньше» → «желанное получено, а радости меньше»; Девятка day_card — «сколько
      в вашей жизни уже сбылось» → «сколько хорошего в вашей жизни уже есть»; Десятка general —
      «карта счастья, которое сбылось» → «карта состоявшегося счастья». Беглый взгляд редактора
      на четыре формулировки состоялся 14.08 — одобрены.
      ⚠️ Остаток ВНЕ задачи (решить редактору): в general Девятки Кубков осталось «то, о чём
      вы мечтали, сбывается или уже совсем близко» (en «is coming true») — слово другое,
      рамка предсказания та же; grep по «сбыл» его не ловит.
      Исходная постановка (заведено 12.08, найдено при финальном ревью 06а): в `content/cards.json`
      четыре вхождения слова «сбылось» — чип и три текста значений (Девятка Кубков — reversed
      и day_card, Десятка Кубков — general).
      Редполитика (content-guide, logic-spec §3) держит рамку «отозвалось, а не сбылось»: приложение
      про рефлексию, а не про предсказание, и обещание сбычи — лишний риск при ревью в сторах.
      В UI-строках и в `phrases.json` слова уже нет — остались только тексты значений.
      Переформулировку пишет редактор (правило: тексты значений не выдумывать), задача маленькая
      и ни от чего не зависит — берётся в любой момент. Проверка после правки:
      `grep -n "сбыл" content/cards.json` — пусто.

- [x] **21 · Контент: зачины `explain` викторины поздравляют за неверный ответ** — ЗАКРЫТА 14.08:
      лайв-проверка Артёма на iPhone пройдена ✓. КОД-ЧАСТЬ
      СДЕЛАНА 14.08 (спека `docs/specs/21-quiz-explain-prefix.md`, веб-проверка 6б ✓, скриншоты
      `docs/screenshots/21/`): перед пояснением префикс из i18n — «Верно.» цветом `success` при
      верном ответе, «Не совсем.» цветом `danger` при неверном; вложенный `Txt` в том же абзаце
      (`app/lesson/[id].tsx`), ключи `lesson.explainRight/explainWrong` в обеих локалях. Проверены
      обе темы и оба исхода. КОНТЕНТ-ЧАСТЬ СДЕЛАНА 14.08 (делегирована Claude решением Артёма
      «по 21 — да»): зачины срезаны из всех 30 пояснений М2 (ru + en зеркально), `merge_quiz.py`
      прогнан, 389 тестов зелёные, `_comment` черновика обновлён. ⚠️ «31 текст» из постановки —
      грубый grep: 31-е совпадение («Именно через такие маленькие истории…», m1l3) зачином
      не является, подходит под оба исхода и не тронуто. У m2l6 после среза «Да!» оставался
      осиротевший союз «И ответ обычно…» — сглажен до «Ответ обычно…» (en так же).
      Исходная постановка (заведено 13.08,
      найдено при отчёте задачи 13 по спеке 08) — РЕШЕНИЕ 13.08 (редактор): **вариант (б)** —
      код добавляет префикс «Верно» / «Не совсем» из i18n перед текстом пояснения (мелкая
      код-задача, обе локали), редактор убирает утвердительные зачины из 31 текста
      `content/quiz-m1-m2.json` (⚠️ после правки черновика — прогнать `merge_quiz.py`,
      статус викторин правится в двух местах). Код можно делать не дожидаясь текстов:
      до правки зачинов будет дублирование вида «Верно. Верно! …» — временно и безопасно.
      Исходная постановка: пояснение `explain` в викторинах М1–М2
      показывается ПОСЛЕ ЛЮБОГО ответа (и верного, и неверного — так по спеке), но 31 текст
      из 50 написан в расчёте только на верный ответ и начинается с утвердительного зачина
      («Верно!», «Да!», «Точно!», «Именно!»). На неверном ответе приложение отвечает, например,
      «Да! …» — то есть поздравляет пользователя за ошибку. Связка контента и поведения, а не
      баг кода. Два варианта решения, выбор за Артёмом:
      (а) редактор нейтрализует зачины в 31 тексте `content/quiz-m1-m2.json`, формулировки
      подходят под оба исхода;
      (б) в код добавляется префикс «Верно» / «Не совсем» из i18n перед текстом `explain`,
      а утвердительные зачины убираются из контента редактором.
      Тексты значений не выдумывать — правку зачинов делает редактор, задача контентная
      (по образцу задачи 20 про «сбылось» в текстах Кубков).

- [x] **25 · Контент+схема: перевёрнутые значения для каждой сферы** — ЗАКРЫТА 15.08
      (заведено 13.08 по замечанию редактора при проверке раздела «Карты»). Спека и отчёты:
      `docs/specs/25-reversed-spheres.md`. Веб-проверка 6а/6б, вычитка (делегирована Claude —
      редактор был недоступен) и лайв-проверка Артёма на iPhone пройдены ✓.
      Сделано: 4 блока `*_reversed` у всех 78 карт, 312 приписок ru+en, **корпус карт
      958/958 reviewed**; `build_cards.py` доливает недостающие ключи схемы сам; приписка
      в блоке сферы (разделитель + «ПЕРЕВЁРНУТАЯ» + текст), контракт-сьют `cardsContent.test.ts`
      (558 тестов в 23 сьютах).
      ⚠️ Пачечная генерация даёт однообразие, невидимое авторам пачек: «Забота о…» открывала
      7 русских текстов, «Your body…» — 11 английских, и подряд идущие карты начинались
      одинаково. Проверять ПОСЛЕ слияния, по всему корпусу и на каждом языке отдельно.
      ⚠️ Вычитка (65 находок) вскрыла два системных дефекта, видных только при чтении четырёх
      сфер одной карты подряд: приписка пересказывает общий блок «Перевёрнутая» вместо
      конкретизации, и две сферы одной карты говорят одно и то же. Автор, пишущий текст
      по одному, их не видит в принципе.
      Хвост: женский род читателя в двух блоках КАНОНА (Дьявол love, Восьмёрка Мечей health) —
      вопрос редактору в `editor-questions.md`, тексты не тронуты.
      ⚠️ Найдено веб-проверкой: `muted` для текста 15px не проходит по контрасту на светлой
      теме (2.88:1 при норме 4.5) — текст приписки идёт цветом `text`; подписи Overline
      цветом muted в светлой теме дают 2.69:1 по всему приложению, это системное свойство темы
      (кандидат в отдельную задачу, не в 25).
      РЕШЕНИЕ ПО ФОРМАТУ 15.08 (Артём): **вариант (б)**,
      короткая перевёрнутая приписка 1–2 предложения внутри каждой вкладки-сферы; общий блок
      «Перевёрнутая» остаётся как есть. Аргументы против остальных: (а) полный формат удваивает
      корпус и затем умножается на ES/PT (задача 28); (в) общий текст под вкладкой «Любовь»
      выглядит сферным, но им не является — ровно то, что редактор и заметила. ⚠️ Решить формат
      надо было ДО старта переводов (28) — сделано; спека и черновики — следующий шаг.
      Исходная постановка: сейчас у карты ОДИН блок «Перевёрнутая» на все
      сферы: на странице карты он лежит ПОД вкладками Общее/Любовь/Работа/Финансы/Здоровье
      и при переключении вкладок не меняется (`app/card/[id].tsx`, схема контента — 8 блоков).
      Редактор ожидает: у каждой сферы свой перевёрнутый текст. Это изменение схемы контента;
      при варианте (б): 4 новых коротких блока × 78 карт × 2 языка = 624 приписки. Перед кодом —
      брейншторм и спека (формат уже решён, остаётся схема поля и вид в UI);
      черновики по схеме М1–М2 (Claude → вычитка редактора). Затронет: тип `TarotCard`,
      `content_stats.py`, `build_cards.py` (слияние новых полей), страницу карты, и объём
      вычитки EN (задача «Подготовка к многоязычию») растёт вместе с русским.

- [x] **26 · Контент: теория и викторины М3–М6** — ЗАКРЫТА ЦЕЛИКОМ 15.08: все четыре захода
      (М3, М4, М5, М6) сделаны и прошли лайв-проверку Артёма. Курс из 32 уроков проходим
      от начала до конца. (РЕШЕНИЕ 13.08 — «делаем как договорились»,
      схема М1–М2: черновик Claude → вычитка редактора) — 22 урока без текстов: М3 «Старшие
      арканы II» (6), М4 «Младшие арканы: числа» (8), М5 «Придворные карты» (4), М6 «Практика
      чтения» (4). Порядок — по номерам модулей; формат урока прежний (теория 2–3 минуты →
      разбор карт урока → 5 вопросов); М4 — по два урока на масть (туз–пятёрка и
      шестёрка–десятка), как заложено в course.json. Работать порциями: один модуль за заход,
      цикл черновик → вычитка → `merge_quiz.py` → статусы (⚠️ статус викторин живёт в двух
      местах — черновике редактора и собранном course.json). Движок готов: тексты появляются —
      урок сразу играет.
      Прогресс по заходам:
      · [x] **М3 — код и контент готовы 14.08, веб-проверка 6б пройдена** (спека
        `docs/specs/26-course-content-m3.md`, там же отчёт): 6 теорий (ru+en, draft, прямо
        в course.json) + 30 вопросов (`content/quiz-m3.json`, draft, 24 single + 6 card);
        скрипты `merge_quiz.py`/`set_status.py` переведены на все `content/quiz-*.json`
        (файл на модуль — статус у каждого свой, М4–М6 правок скриптов уже не потребуют);
        контракт-тест расширен на М1–М3 (401 тест, tsc чистый). Веб-проверка: урок m3l1
        целиком, префиксы «Верно./Не совсем.» без двойных зачинов, card-вопрос
        с изображением, «+8 XP» по формуле.
      · [x] М3: вычитка редактора и лайв-проверка Артёма пройдены 15.08 — статусы theory + quiz
        переведены в reviewed (`set_status.py`, ровно 12, оба места), заход закрыт.
      · [x] **М4 «Младшие арканы: четыре стихии» — ЗАКРЫТ 15.08: лайв-проверка Артёма на iPhone
        пройдена ✓ («всё норм»), статусы theory + quiz уже в `reviewed` (двинуты сессией после
        вычитки Claude, оба места). Хвост 34 (заголовок m4l2) закрыт тем же днём.
        Код и контент были готовы 15.08 (спека и отчёт `docs/specs/26-course-content-m4.md`, скриншоты
        `docs/screenshots/26-m4/`). 8 теорий (ru+en) в course.json + 40 вопросов в новом
        `content/quiz-m4.json` (32 single + 8 card); контракт-тест расширен на М1–М4 (24 урока),
        574 теста зелёные, tsc чистый. Скрипты конвейера не менялись. **Вычитки редактором
        не будет** (решение Артёма 15.08) — вычитка делегирована Claude: четыре независимых
        агента-редактора, 85 находок, все правки внесены. Веб-проверка 6б пройдена: урок m4l1
        целиком, префиксы «Верно./Не совсем.», card-вопрос с изображением, «+8 XP» по формуле,
        «8 УРОКОВ · 40 КАРТ» и «ПРОЙДЕНО 1 ИЗ 8 УРОКОВ МОДУЛЯ» (плюрализация верна), консоль чистая.
        Хвосты в бэклог: задачи 29, 30 (обе поглощены задачей 31) и 34.
      · [x] **М5 + М6 — ЗАКРЫТЫ 15.08: лайв-проверка Артёма на iPhone пройдена ✓ («всё отлично
        и работает»), замечаний нет. Этим задача 26 закрыта целиком — все четыре захода готовы.**
        Код, контент, вычитка и веб-проверка сделаны 15.08
        (спека и подробный отчёт `docs/specs/26-course-content-m5-m6.md`, скриншоты
        `docs/screenshots/26-m5-m6/`). 8 теорий (ru+en) в course.json + 40 вопросов в новых
        `content/quiz-m5.json` и `content/quiz-m6.json` (33 single + 7 card); контракт-тест
        расширен на М1–М6 (32 урока) и получил **тест порога длин вариантов** — им закрыт хвост
        задачи 29; тестов **767 в 26 сьютах** (было 719), tsc чистый. Статусы theory + quiz —
        `reviewed` (16 штук, двинуты сессией после вычитки четырьмя независимыми opus-агентами,
        решение Артёма 15.08 — редактора нет). Скрипты конвейера не менялись.
        Веб-проверка 6б пройдена: m5l1 целиком (13 шагов, оба префикса, card-вопрос, «+8 XP»,
        «ПРОЙДЕНО 1 ИЗ 4 УРОКОВ МОДУЛЯ»), m6l3 целиком (9 шагов, **шагов карт нет**), шапка М5
        «4 УРОКА · 16 КАРТ» и М6 без счётчика карт, и главное — **весь курс доведён до конца**:
        32/32 урока done, ни одного золотого узла, модули 100 %, консоль чистая. Это состояние
        впервые достижимо: до задачи 26 путь обрывался.
        Хвосты в бэклог: задачи **37** (находки редакторов про справочник) и **38** (заголовок
        m6l4 обещает не то, чем урок является).
      · [x] **М5 «Придворные карты» + М6 «Практика чтения» — спека и план готовы 15.08**
        (решение Артёма: оба модуля одним заходом, вычитка снова на Claude как в М4): спека
        `docs/specs/26-course-content-m5-m6.md`, детальный план `docs/plans/26-course-content-m5-m6.md`
        (10 задач: инструменты конвейера → черновики М5 → викторины М5 + тест → корпус М5 →
        черновики М6 → викторины М6 + тест порога длин вариантов → корпус М6 → вычитка → веб-проверка,
        включая впервые достижимое «курс пройден целиком» → доки и лайв). Реализация — Opus 5.
        Решения спеки: ось М5 «ранг = зрелость роли × масть = стихия», «человек или роль»,
        без «элемента в элементе» и без соответствий внешности из Уэйта §7; М6 — уроки без карт
        (движок умеет), объём до 2200 ru, одна школа перевёрнутых («застряла / перегнула / ушла
        внутрь»), m6l3 = расклад «Три карты» ровно как в spreads.json с рамкой «будущее =
        направление», упражнения без интерактива раскладов (задача 36), финал курса без обещаний.

- [x] **39 · Страница карты: вкладки без общих блоков + ярлычок «можно увеличить»** — два
      замечания редактора с показа приложения 15.08. ЗАКРЫТА 15.08: лайв-проверка Артёма на iPhone
      пройдена ✓ («всё открывается и работает», замечаний нет), ветка `feat/39-card-page-notes`
      влита в main. Спека и план согласованы 15.08:
      `docs/specs/39-card-tabs-zoom-badge.md`, отчёт веб-проверки — там же; скриншоты
      `docs/screenshots/39/`. `npx tsc --noEmit` чистый, 767 тестов зелёные (логики не добавилось),
      веб-проверка 6а/6б — 25/25.
      **(а)** На вкладках сфер (Любовь/Работа/Финансы/Здоровье) сейчас под текстом сферы лежат
      ещё и общие блоки «Перевёрнутая», «Как карта дня», «Символика» — перевёрнутая при этом
      показана дважды (приписка сферы из задачи 25 + общий блок). Решение: на вкладке сферы ровно
      два блока — значение сферы и отдельный блок «Перевёрнутая» с текстом `<сфера>_reversed`
      (приписка-с-разделителем из задачи 25 уходит); общая перевёрнутая, карта дня, символика
      и «Ваша история с картой» — только на «Общем». Fade 130/350 гасит тело вкладки целиком;
      каскад появления по макету: значение d4, всё остальное одним шагом d5 (сейчас код даёт
      карте дня 4, символике 5). ⚠️ Ловушка: `FadeUp` играет полный вход при монтировании на
      сфокусированном экране — условные блоки «Общего» не оборачивать каждый в свой FadeUp,
      иначе при каждом возврате на «Общее» они всплывают мини-каскадом.
      **(б)** Тап по картинке открывает полноэкранный просмотр (задача 14), но подсказки нет.
      Решение: ярлычок-иконка Ionicons `expand-outline` 12px `accent2` на скриме
      `rgba(0,0,0,.4)`, radius 8, правый верхний угол (инсет 6/6) — та же форма, что бейдж
      «ИЗУЧЕНО ✓» в сетке, поэтому оба переводятся на новый общий `CornerBadge`. Стоит на
      двух входах в лайтбокс: герой страницы карты и лицевая грань карты дня. Копия карты
      в лайтбоксе и миниатюры сетки ярлычка не несут. Без текста (герой 128px, 4 языка).
      Хвост: макет `design-reference.html` дорисовать под то же поведение (вкладки прячут
      общие блоки; ярлычок на герое и карте дня) — в плане спеки, шаг 7. ✅ дорисован: тело
      вкладки уехало в `#tabbody` (гаснет целиком), блоки «Общего» получили класс `only-general`,
      рядом с `FOOL_TABS` появился `FOOL_REV` с перевёрнутыми Дурака по сферам; заодно блок
      истории переставлен ПОД символику — порядок сошёлся с product-spec §3.

- [x] **40 · Золочёные уголки лицевой грани карты дня: в макете есть, в приложении нет** — ЗАКРЫТА
      18.08 в составе задачи 42 (решение Артёма 18.08: вариант 2 — уголки на ОБЕИХ гранях, ярлычок
      увеличения переехал на инсет 9/9 внутрь правого верхнего уголка; макет обновлён тем же
      правилом). Исходная постановка:
      найдено при дорисовке макета задачи 39 (флаг 6а-0, 15.08). В `design-reference.html`
      лицевая грань карты дня несёт четыре орнамента `.cnr` (20×20, инсет 4, цвет accent),
      в коде (`app/(tabs)/index.tsx`, лицевая грань) их нет ни одного — расхождение
      существовало ДО задачи 39 и к ней отношения не имеет (решение 10.08 в задаче 03 звучало
      «уголки живут ТОЛЬКО на карте дня», то есть их отсутствие на карте дня выглядит недоделкой,
      а не решением). Задача 39 сделала расхождение видимым: ярлычок «можно увеличить» встаёт
      в правый верхний угол — ровно туда, где в макете `.cnr tr`, и в макете теперь его перекрывает.
      В приложении наложения нет, потому что уголков нет. Решение за Артёмом — одно из двух:
      (а) нарисовать уголки в приложении и развести их с ярлычком (сдвинуть ярлычок ниже или
      уголок внутрь); (б) убрать уголки из макета как неактуальные. **Решение Артёма 15.08:
      пока оставляем как есть** — расхождение осознанное и на поведение не влияет; задача висит
      как напоминание, к ней вернуться, когда дойдут руки до карты дня.

- [x] **38 · Заголовок урока m6l4 обещает не то, чем урок является** — ЗАКРЫТА 15.08: урок
      переименован в «Разбор: два чтения целиком» / «Two readings, start to finish» (кандидат
      из постановки, решение Артёма) в обоих местах — `content/course.json` и `MODS` в
      `docs/design-reference.html`; контракт-тест курса зелёный (98). Лайв-проверка не нужна —
      правка текстовая. Исходная постановка: найдено вычиткой
      задачи 26 (заход М5–М6, 15.08). Заголовок «Разбор реальных примеров» / «Walkthrough of
      real readings» обещает разбор настоящих чужих раскладов, а первый же абзац честно говорит,
      что примеры **гипотетические** («представьте, что кто-то задал такой вопрос») — так требует
      редполитика. Пользователь видит заголовок на пути курса ещё до открытия урока.
      Спека 26 прямо запрещает трогать названия уроков, поэтому заход М5–М6 текст не менял.
      Кандидат: «Разбор: два чтения целиком» / «Two readings, start to finish».
      ⚠️ Заголовок лежит в ДВУХ местах — `content/course.json` и `docs/design-reference.html`
      (прецедент задачи 34 с m4l2: правили оба).

- [x] **37 · Справочник: находки четырёх редакторов заходов М5–М6** — ЗАКРЫТА 15.08 (спека
      и подробный отчёт `docs/specs/37-court-cards-findings.md`, список правок
      `docs/specs/37-changed-addresses.md`). Лайв-проверка не требуется: правки чисто текстовые,
      экранов и логики не касаются. Внесён **51 адрес в 26 картах** (43 по постановке + 8 пойманных
      расширенными регулярками), корпус остаётся **958/958 reviewed**, схема не менялась.
      `check_canon.py --scope cards`: 122 → **121**, рамка предсказания на неисправленном корпусе
      давала 16 при новых регулярках, после правок — 3 (остаток из прежнего baseline, где «обещает»
      относится к людям). `npx tsc --noEmit` чистый, `npm test` — 767 в 26 сьютах.
      Оба «вопроса редактору» решены в спеке (делегировано Артёмом): тепло прямой Королевы Мечей
      закреплено по m6l1 — «её прямота не бьёт наотмашь» (формулировка нарочно не копирует урок
      дословно); цитата Уэйта «материальное счастье и довольство» у Солнца снята как гадальный
      слой Part III. Оба записаны в `editor-questions.md` разделами 6–7 — можно оспорить.
      ⚠️ Класс B проверился объективно: после правок уроки М5 и `symbolism` описывают одну позу
      дословно (m5l1 «Паж Мечей идёт по неровной земле… оглядываясь» ↔ `s11`; «пентакль, парящий
      над ладонями» ↔ `p11`; m5l2 «на тяжёлом выносливом коне, не поднимая его в галоп» ↔ `p12`;
      «Паж Жезлов возвещает новость» ↔ `w11`). До правок пользователь читал в уроке одно,
      а на странице карты — другое.
      ⚠️ Восемь правок оказались правками СОБСТВЕННЫХ правок (четыре волны): артикль создал
      3-грамму с соседним блоком, «вас заметят» → «есть чем заявить о себе» оставило английский
      с прежним «you will be noticed» (я сам сделал тот одноязычный рассинхрон, который приехал
      чинить), «Его благополучие» дало тавтологию с первой строкой блока. Вычитка агентами нашла
      ещё две — и обе тоже мои: `hermit.general` разошёлся модальностью между языками (ru — совет,
      en — констатация), а «дальше дело за временем» в `w03` осталось обещанием, просто идиомой.
      ⚠️ Замену агента нельзя брать готовой: предложенное «плоды могут прийти не сразу» внесло бы
      двойную тавтологию — «плоды» и «могут» уже стоят в том же блоке. Принимать надо диагноз
      агента, а не его текст. Исходная постановка: вычитка уроков М5–М6
      (задача 26, 15.08) читала уроки ВМЕСТЕ с исходниками карт и нашла дефекты не в уроках,
      а в самом справочнике. Здесь не правились (спека 26: `cards.json` не трогаем), корпус
      карт остаётся 958/958 reviewed. Классы находок:
      · **Пропущенное тире в `general.ru` одиннадцати придворных карт** (`w11, w12, w13, w14,
        c11, c13, c14, s11, s12, s13, s14`): «Как человек **это** независимая женщина…» вместо
        «Как человек — независимая женщина…». У `p11` рядом стоит верная форма. Правится одной
        волной, стоит прогнать регуляркой по всем 78 картам.
      · **`symbolism` расходится с описанием Уэйта из того же поля `source`**: `s11` — «стоит
        на возвышении» против «in the act of swift walking… passing over rugged land»; `p12` —
        «конь стоит неподвижно» против «rides a slow, enduring, heavy horse» (и против
        собственного `general.ru` той же карты: «методично едет к цели»); `p11` — «пентакль
        в поднятых руках» против «hovers over his raised hands»; `w11` — «разглядывает жезл»
        против «stands in the act of proclamation». Уроки М5 писались по Уэйту, поэтому на
        странице карты теория и `symbolism` описывают разные позы.
      · **Рамка предсказания и обещания в reviewed-каноне**: `tower.general` («на расчищенном
        месте **вы построите** прочнее» / «you will build»), `p05.reversed` («**возвращаются**
        силы, доход, вера в себя»), `w03.general` («первые результаты **приближаются**»,
        «рядом **могут появиться** надёжные партнёры»), `s13.general` («сердце скажет спасибо
        позже»), `hermit.general` («Ответы придут не извне»).
      · **Допущения об окружении читателя**: `c02.general` («Цените того, кто сейчас рядом» —
        карта выпадает и тем, у кого рядом никого нет; тот же класс, что «встреча с подругой»
        из задачи 31).
      · **Мужской род читателя**: `fool.general.ru` («делает вас свободн**ым**») — правило
        content-guide от 15.08 запрещает мужские формы наравне с женскими.
      · **Рассинхроны ru↔en**: `s08.reversed.en` потерял «будто бы» («options that weren't
        there before» вместо «которых будто бы не было» — смысл карты перевернулся);
        `w11.reversed.en` «Indecision lingers» (утверждение) против ru «Возможна нерешительность»;
        `p14.reversed` «человека рядом» → «a loved one's» (сужение).
      · **Кальки и тон в en**: `w14.general.en` «infects others with belief» (по-английски
        заражают болезнью), `c11.reversed.en` «escaping into fantasy from reality» (порядок слов).
      · **Юмор не в том блоке**: `w12.general` «только пристегнитесь» / «fasten your seatbelt» —
        ремень у всадника на коне, и по редполитике лёгкий юмор живёт в `day_card`, не в `general`.
      · **Афоризмы, читающиеся упрёком**: `p14.general` «Успех — это система, а не удача».
      · **Трактовка в блоке про рисунок**: `s14.symbolism.ru` «даже строгий суд оставляет место
        милосердию» — вывода нет у Уэйта, и по Королеве той же масти он пишет прямо обратное
        («It does not represent mercy»).
      Вопросы редактору (не дефекты, решение за ней): (1) прямая Королева Мечей нигде в каноне
      не описана «с теплом», а Уэйт пишет «It does not represent mercy» — если тепло прямой
      Королевы сознательная позиция курса, её стоит закрепить в `s13.general`; (2) `sun.general`
      цитирует Уэйта «материальное счастье и довольство» — это Part III, гадальный слой, который
      шапка `docs/reference/waite-pictorial-key.md` велит не переносить «ни в каком виде»:
      либо снять цитату, либо записать исключение в шапку.

- [x] **35 · Перечитка 50 карт, изменённых задачей 31** — СДЕЛАНА 15.08 (спека и отчёт
      `docs/specs/35-changed-cards-reread.md`, список правок `docs/specs/35-changed-addresses.md`):
      прочитаны все 110 адресов, внесено **30 правок в 22 карты**, корпусная проверка 125 → 122
      находок (новых ноль), 574 теста зелёные, `tsc` чистый, 5 спорных мест — редактору
      в `editor-questions.md`. Лайв-проверка Артёма не требуется: правки только текстовые,
      экранов и логики не касаются.
      ⚠️ **Прогон `check_edits.py` по СВОЕМУ списку нашёл три повтора с соседним блоком, которые
      внёс я сам**, разбирая чужие. Худший — у Жрицы: замена в `health.en` почти дословно совпала
      с припиской `health_reversed`, а она показывается ПРЯМО ПОД основным текстом на одной
      вкладке. Урок: свою правку надо гонять тем же инструментом, каким проверял чужую — я читал
      замену в абзаце, как требовал сам, но не в КАРТЕ. И тут же граница инструмента: у той же
      Жрицы русская пара пересекалась сильнее английской (объяснять/понаблюдайте/чувствуете),
      но без единой совпавшей 3-граммы — проверка молчала, нашлось чтением глазами.
      Что было сломано: 9 тавтологий, внесённых правками («Финансовая дисциплина… дисциплина
      ценится»), 4 повтора с соседним блоком (`judgement.general` пересказал `symbolism`),
      6 рассинхронов ru↔en, 5 грамматических сбоев («Маленькие суммы — хорошая привычка»,
      «будьте Пажом: любопытны»), 3 смысловых сдвига, 2 логических провала после снятия медицины.
      ⚠️ **Половина правок задачи 31 была одноязычной (39 блоков из 77), и там жил самый частый
      дефект**: ru смягчили, en остался с гарантией («имеет свойство возвращаться» против
      «will come back to you»), и наоборот. Механически не ловится — словари языков разной
      полноты, «will + глагол» в словарь не положишь. Проверка 8 `check_edits.py` поэтому не
      выдаёт находок, а печатает пару «что убрано → парный текст»: показывает человеку, где смотреть.
      ⚠️ **Правка, снимающая дефект, склонна вносить дефект того же класса**: снятие «карта
      обещает» дало «сейчас везёт на внимание», снятие женского рода сломало падеж, снятие
      медицины превратило финальную фразу в пересказ первой.
      ⚠️ **Свою замену надо проверять по корпусу**: правя две карты подряд, я дважды написал
      «подтягиваются следом» — сам заводил новый оборот-тик. Ловится повторным `check_canon.py`.
      Оба слоя снова поймали разное накрест: механика — 11 тавтологий, мимо которых прошли все
      восемь агентов; агенты — 6 рассинхронов и «крупный план» у Короля Мечей (в русском это
      кинематографический термин, образ обратный замыслу). Исходная постановка ниже.
      Хвост задачи 31, но отдельная работа: там правки
      писали, здесь их принимают. Задача 31 внесла **110 правок в 50 карт**, и статус `reviewed`
      на этих блоках относится теперь к тексту Claude, а не к тому, что человек читал 13.08;
      жена этих правок не видела ни в каком виде. Роль редактора на заход делегирована Claude
      (решение Артёма 15.08, как в задачах 21, 23, 25).
      Повод не гипотетический: задача 31 сама поймала у себя правку, ухудшившую текст
      («напишите первой» → «напишите первым делом» — в цитате верно, в абзаце «Сделайте шаг
      навстречу: напишите первым делом» смысл уехал с «первым из двоих» на «сначала»). Поймано
      случайно при сборке списка адресов, то есть систематического прохода по этому классу не было.
      Предмет проверки — САМА ПРАВКА, а не корпус: текст после правки проходит все корпусные
      проверки и при этом бывает хуже прежнего.
      Решения Артёма 15.08: правлю регрессии и бесспорные мелочи, спорное — списком в
      `editor-questions.md`; по заменам темы блока решаю сам с обоснованием в отчёте.
      Новое общее: `scripts/check_edits.py` — семь проверок правок «было → стало» (дополняет
      `check_canon.py`, не повторяет: тот про корпус, этот про правку), пригодится волнам ES/PT.
      Границы: только `content/cards.json`, статусы не двигаем, схему не меняем, курс не трогаем.

- [x] **33 · Кельтский крест: раскладка не Уэйтовская, а атрибуция — личная** — ЗАКРЫТА 15.08
      (решение делегировано Claude Артёмом): перепроверено по тексту книги (Part III §7, теперь
      в `docs/reference/waite-pictorial-key.md`) — расхождение подтверждено ровно как в постановке
      (3/4/5 переставлены, сигнификатор, 11-я карта). Правка минимальная, оба языка: «Описан
      А. Э. Уэйтом в 1911 году» → «На основе метода А. Э. Уэйта (1911)» / «Based on A. E. Waite's
      method (1911)». Позиции не тронуты (современная общепринятая версия — так и оставляем);
      пояснений к позициям не пишем (решение редактора 13.08). Кода нет, тестов 719 зелёные.
      Исходная постановка (заведено 15.08 при
      разборе первоисточника, см. задачу 32). В `content/spreads.json` описание `celtic-cross` говорит
      «Описан А. Э. Уэйтом в 1911 году» / «Described by A. E. Waite in 1911», но наши позиции — это
      общепринятый СОВРЕМЕННЫЙ вариант, отличающийся от книги (Part 3 §7 «An Ancient Celtic Method
      of Divination»):
      · у Уэйта 3-я позиция «This crowns him» — цель/лучшее достижимое, 4-я «This is beneath him» —
        основание, 5-я «This is behind him» — уходящее прошлое; у нас 3 = «Основание», 4 = «Недавнее
        прошлое», 5 = «Возможный венец», то есть три позиции переставлены;
      · у Уэйта на столе 11 карт: первой кладётся карта-сигнификатор (придворная, выбранная под
        кверента), и первые две ложатся ПОВЕРХ неё («covers him» / «crosses him»); у нас 10 карт,
        а первая названа «Суть вопроса»;
      · сторона, куда ложится 6-я карта, у Уэйта зависит от того, куда СМОТРИТ сигнификатор.
      Сама раскладка не ошибка — так расклад печатают везде последние сто лет; ошибка в том, что мы
      приписываем конкретно эту версию лично Уэйту. Минимальная правка (оба языка, только контент,
      кода не требует): «Описан А. Э. Уэйтом в 1911 году» → «На основе метода А. Э. Уэйта (1911)» /
      «Based on A. E. Waite's method (1911)». Заодно решить, не давать ли позициям пояснения словами
      Уэйта — но помнить решение редактора от 13.08: пояснения к позициям раскладов не пишем.

- [x] **32 · Уэйт (первоисточник) как справочный материал для заходов М5–М6 и блоков `symbolism`** —
      ЗАКРЫТА 15.08 (решение делегировано Claude Артёмом: «если улучшает и не противоречит
      правилам»). Решение — ДА, нарезка в репо: `docs/reference/waite-pictorial-key.md` (~90 КБ,
      public domain; собрана из PDF `pdftotext -layout -enc UTF-8` + скрипт склейки абзацев).
      Включено: Preface, Part I §1–4 (в т.ч. историко-сравнительные заметки по каждому старшему
      аркану и факт «младшие впервые с сюжетными рисунками»), Part III §1, §6–§9 (Кельтский метод,
      альтернативный метод, «Notes on the Practice of Divination», метод 35 карт). Исключено
      осознанно: Part II §1/§3 и эпилог (доктрина), Part II §2 + Part III §2–3 (уже в `cards.json`
      → `source`), Part III §4–5 (гадальный слой XIX века — против редполитики), библиография.
      В шапке файла — русский указатель «что брать / что нельзя» и шесть ловушек для авторов
      (главная: по младшим арканам курс расходится с Уэйтом; «Notes» писал не Уэйт; соответствия
      «масть = внешность кверента» не переносить). Пользы для пользователя напрямую нет — она
      косвенная: авторы М5–М6 и будущих правок `symbolism` работают с оригиналом, а не с пересказом,
      меньше выдуманных деталей рисунка (класс дефекта, найденный вычиткой М4). ⚠️ Хвост НЕ сделан
      осознанно: факт «первая иллюстрированная колода» в m1l2 не дописан — m1l2 в `reviewed`,
      правка вычитанного текста — отдельным решением (кандидат для волны правок курса).
      Исходная постановка (заведено 15.08: Артём прислал полный текст «The Pictorial Key to the Tarot» — PDF-издание
      Global Grey, плюс те же тексты на archive.org и sacred-texts).
      **Что уже есть в проекте:** у каждой из 78 карт поле `source` в `content/cards.json`
      (`waite_upright` ≈165 знаков, `waite_reversed` ≈75, `waite_description` ≈562) — пришло через
      tarot-api (MIT). Сверка с книгой: 77 описаний из 78 совпадают ДОСЛОВНО (у `s01` мешает артефакт
      текстового слоя). Видно и происхождение: у 22 старших описание — из Part 2 §2 «The Trumps Major
      and Their Inner Symbolism», у 56 младших — из Part 3 §2. То есть в бандле лежит примерно треть
      книги, и именно её получали агенты-авторы как «исходники карт» в заходах 25 и 26-М4.
      **Подлинность текста проверена программно** (вопрос Артёма: нет ли дописок современников).
      Издание Global Grey 2025 против независимой транскрипции sacred-texts (2002) — 95.0% совпадения
      слово в слово; ВСЕ расхождения — навигация сайта, оглавления и место вставки авторских сносок;
      фрагментов добавленного текста — ноль. Третья независимая линия (tarot-api → наш cards.json)
      сходится с обеими. Предисловий, вступлений и примечаний «от издателя» в книге нет: после
      колофона сразу Preface самого Уэйта.
      **Чего в проекте нет, а в книге есть — и стоит взять:**
      · **Part 1 §3 + Part 3 §1** — Уэйт своими словами фиксирует, что колода RWS первая, где все
        числовые младшие получили сюжетные картинки («the smaller cards, which—until now—have never
        been issued pictorially in these our modern days»). В `content/course.json` этого факта нет
        вообще: по всему курсу ноль упоминаний Памелы Колман Смит, 1909 и «впервые». Сильный материал
        для m1l2 и для вводного урока М4.
      · **Part 3 §8, блок «NOTES ON THE PRACTICE OF DIVINATION»** — четыре практических правила
        (сформулировать вопрос точно и произнести вслух; освободить голову при перемешивании; отложить
        предубеждения; для постороннего читать легче, чем для себя или друга). Ложится прямо в М6
        «Практика чтения» и в этику m1l4. ⚠️ Уэйт прямо пишет, что эти правила дописал НЕ он, а
        «тот, кто имеет больше прав говорить» — цитировать как «правила Уэйта» нельзя.
      · **Part 1 §2** — вторая, историко-сравнительная заметка по каждому старшему аркану (какие
        варианты карты бывали, что писали предшественники). У нас нет ни строки; сырьё для блока
        `symbolism` и для истории в теории.
      · **Part 3 §9** — метод «35 карт»: шесть строк-значений (дом · сам кверент · внешние события ·
        неожиданность · утешение · строка-разъяснение). Кандидат в новый расклад для v2.
      **Что брать НЕ надо:** Part 3 §4 «Some Additional Meanings of the Lesser Arcana» и §5
      «The Recurrence of Cards in Dealing» — гадальный слой XIX века («Семёрка Жезлов — тёмный
      ребёнок», «4 Пажа = опасная болезнь», «для мужчины хороший брак»). Прямо против редполитики
      и против дисклеймера экрана «О приложении»; сам Уэйт тут же оговаривается, что эти значения
      «часто расходятся» с основными и с рисунками карт связаны слабо. Part 2 §1 и §3 — каббала
      и «тайная доктрина», тон не наш.
      ⚠️ **Главная ловушка: по младшим арканам мы с Уэйтом РАСХОДИМСЯ.** В Part 1 §3 и Part 3 §1 он
      прямо пишет, что у младших арканов глубокого символизма нет и они относятся «к гаданию», тогда
      как весь наш М4 построен на числовой оси и стихиях. Современная традиция ушла от Уэйта, и это
      нормально — но ссылаться на него как на авторитет там, где мы его же и опровергаем, нельзя.
      ⚠️ **Границы источника — чего в книге нет вообще:** системы «стихия в стихии» для придворных
      карт (наш М5 — у Уэйта они описаны бегло), сочетаний и связок пар (m6l2), методики чтения
      перевёрнутых (m6l1 — есть только перечни значений), астрологии и деканов младших (это уже
      Золотая Заря, «Book T»). Для М5–М6 Уэйт закроет меньше половины материала.
      **Что сделать:** решить, класть ли нарезку нужных разделов в репо (`docs/reference/`, ~230 КБ
      текста, public domain) как исходник для агентов-авторов заходов М5–М6 — по образцу того, как
      выжимка `cards.json` давалась авторам в заходах 25 и 26-М4. Текст извлекается из PDF командой
      `pdftotext -layout`. Дата в атрибуции менять не надо: книга вышла в 1910-м, но датирована
      1911-м (William Rider & Son) — наша «1911» и есть стандартная ссылка.

- [x] **34 · Заголовок урока m4l2 обещает не то, чем урок кончается** — ЗАКРЫТА 15.08 (решение
      делегировано Claude Артёмом): перепроверено — теория m4l2 заканчивается ношей Десятки и итожит
      урок как «историю после победы», новый заголовок «Жезлы: триумф и его цена (6–10)» / «Wands:
      triumph and its price (6–10)» этому соответствует, укладывается в схему названий М4
      («Мечи: тьма перед рассветом», «Пентакли: труд и наследие»), рамки предсказания не несёт,
      короче прежнего на два знака (подпись узла на пути). Названия уроков нигде больше не
      цитируются (grep по content/ и docs/ — только сама спека 26-М4 как история). Правка в
      `course.json` и в массиве уроков макета `design-reference.html` (макет зеркалит course.json —
      правило «названия = course.json»). Тестов 719 зелёные.
      Исходная постановка (заведено 15.08 вычиткой М4;
      номер сменён с 31 на 34 — 31 заняла спека повторной вычитки канона из параллельной сессии).
      «Жезлы: от борьбы к триумфу (6–10)» / «Wands: from struggle to triumph (6–10)»: триумф здесь
      Шестёрка, то есть середина дуги, а урок заканчивается ношей Десятки. Новичок, дочитав,
      получает не то, что обещал заголовок. Предложено «Жезлы: триумф и его цена (6–10)» /
      «Wands: triumph and its price (6–10)» — заодно ложится на ступень оси «цена пройденного пути».
      В заходе М4 НЕ применено осознанно: спека 26 прямо запрещает трогать названия уроков
      (они часть структуры курса, согласованной раньше). Мелкая правка `content/course.json`,
      затрагивает подпись узла на пути курса. Решение — Артёма.

- [x] **31 · Повторная вычитка Claude корпуса, вычитанного редактором** — ЗАКРЫТА 15.08:
      оба захода сделаны, **лайв-проверка Артёма на iPhone пройдена ✓** (замечаний нет).
      Осталось у редактора: перечитать 50 изменённых карт точечно по списку
      `docs/specs/31-changed-addresses.md` — статус `reviewed` на этих блоках теперь относится
      к тексту после правок Claude, а не к тому, что человек читал 13.08.
      ЗАХОД А ЗАКРЫТ 15.08
      (спека и отчёт: `docs/specs/31-canon-reread.md`, список правок: `31-changed-addresses.md`).
      Решения Артёма 15.08: правки вношу сам; род читателя — БЕЗЛИЧНЫЙ во всём контенте (титулы
      уровней остаются женскими — другой жанр); зачин-указатель сферы («В любви…») — конвенция,
      а не однообразие; нормы длин приведены к факту корпуса и разведены по языкам.
      Сделано: `scripts/check_canon.py` (10 корпусных проверок, постоянный — пригодится волнам
      ES/PT и М5–М6), 113 правок в `cards.json` и `phrases.json`, задача 30 поглощена целиком.
      Метод: механический слой + десять sonnet-агентов, каждому карта ЦЕЛИКОМ вместе с оригиналом
      Уэйта. Тестов 574 в 23 сьютах, `npx tsc --noEmit` чистый.
      ⚠️ Главное: «карта обещает» оказалась шаблоном блоков `love` у восьми карт в обоих языках —
      дефект уровня корпуса, невидимый при чтении текстов по одному. Английский канон не читал
      никто (статус один на оба языка) — 44 правки из 110 пришлись на него, включая неканоничные
      имена карт (`Strength` вместо `Fortitude`, `Judgement` вместо `The Last Judgment`).
      ⚠️ Проверка рода писалась по образцу шести известных находок и потому пропустила семь других
      форм («задолжали самой себе», «станьте той», «понял(а)», «мамой команды», «мыслите как
      хозяин», «обнимите первой», «я рада») — их принесли агенты, после чего регулярка усилена.
      И наоборот: усиленный скрипт нашёл три места, которые пропустили все десять агентов.
      ЗАХОД Б ЗАКРЫТ 15.08 (коммит `5d189c1`): 21 текстовая правка курса (13 теории,
      8 формулировок вопросов и пояснений) + 51 выровненный вопрос, задача 29 закрыта.
      ⚠️ Урок захода Б: агент переставил варианты местами, НЕ ТРОНУВ поле `correct`, — верным
      ответом стал дистрактор (`m3l2` в5). Индекс остался валидным, схема цела, контракт-тест
      молчит. Ловится только сверкой ТЕКСТА верного варианта до и после: доля общих слов близка
      к нулю. Прогон по всем 51 вопросу дал ровно два сигнала — этот и легитимное расширение.
      ⚠️ Второй урок: вывод собственного аппликатора печатал «верный было → стало» по каждому
      вопросу, то есть сбой был виден сразу — я прочитал первые четыре строки из 22.
      Осталось от задачи 31 (не дефекты, отдельные волны): 49 блоков карт беднее нормы длины,
      29 повторов зачинов и 38 оборотов-тиков в корпусе карт, 23 повтора зачинов вопросов
      в курсе («What does…» открывает 10 вопросов из 24), вынесение порога длин вариантов
      в контракт-тест курса, чтобы М5–М6 не повторили.

- [x] **30 · Канон карт: рамка предсказания и женский род читателя в шести блоках** — ЗАКРЫТА
      15.08 в составе задачи 31 (коммит `61af5a4`): рамка предсказания убрана из `c09`/`c08`,
      витринное «заветное желание» → «исполненность» / «fulfilment», шесть родовых мест приведены
      к безличной форме. ⚠️ Родовых мест в корпусе оказалось не шесть, а 21 — остальные нашла
      корпусная проверка и агенты-редакторы. Исходная постановка: (заведено
      15.08 вычиткой М4 — редакторы курса читали уроки ВМЕСТЕ с исходниками карт и нашли дефекты
      не в уроке, а в справочнике). Все блоки в статусе `reviewed`, то есть прошли вычитку
      и лайв-проверку — правка меняет вычитанный корпус, поэтому отдельной задачей, а не хвостом 26.
      **Рамка предсказания (запрещена content-guide и logic-spec §3):**
      · `c09 general` — «знаменитая „карта исполнения желаний“… то, о чём вы мечтали, **сбывается**
        или уже совсем близко», «Она **обещает** период удовольствия». Это ровно тот остаток
        задачи 20, что был записан «за редактором»: grep по «сбыл» его не ловил, потому что форма
        другая. ⚠️ Урок m4l4 теперь прямо учит обратному («не обещание чуда извне»), и пользователь
        читает оба текста рядом — расхождение стало видимым.
      · `c09 keywords[0]` — «заветное желание» / «heartfelt wish»: это ВИТРИННОЕ слово, золотой чип
        под названием карты (тот же класс, что и в задаче 20, где вхождение оказалось в keywords,
        а не в search) → «довольство» / «contentment».
      · `c08 general` — «именно там **вас ждёт** настоящее» / «the real thing waits there».
      **Женский род читателя** (правило должно быть одно на канон и приписки; два случая уже
      записаны в `editor-questions.md` — Дьявол love, Восьмёрка Мечей health, здесь ещё три):
      · `c03 reversed ru` — «можно быть **настоящей**, а не **удобной**» (en нейтрален);
      · `s07 general ru` — «в прятки с **самой собой**»;
      · `p05 general ru` — «остались на морозе **одна**»; `p07 general ru` — «что я **посадила**».
      Готовые формулировки замен — в отчёте вычитки М4 (`docs/specs/26-course-content-m4.md`).
      После правки: `npm test` (контракт-сьюты карт), проверка, что статус блоков не откатывается.

- [x] **29 · Викторины М1–М3: верный ответ угадывается по длине варианта** — ЗАКРЫТА 15.08
      в составе захода Б задачи 31 (коммит `5d189c1`). Выровнен 51 вопрос в обоих языках:
      неверным вариантам дописана конкретика (заодно стали правдоподобнее), из верных убраны
      хвосты-объяснения. Было — верный самый длинный в 14 уроках из 16, у `m1l1` и `m1l4`
      во всех пяти вопросах; стало — ни один урок не превышает порога «не больше 2 из 5»,
      корпусная проверка упала со 101 находки до 9 (все внутри нормы по уроку либо `card`).
      ⚠️ Вопросы типа `card` выведены из правила осознанно, к одному выводу пришли все три
      агента: их варианты — имена карт («Мир» — 3 знака, «Колесо Фортуны» — 14), дописать им
      конкретики нельзя без порчи формата, и эвристики «длинное название = верное» не существует.
      Порог замеряет `python scripts/check_canon.py --scope course --only 11`; вынесение его
      в контракт-тест курса осталось хвостом (записан в задаче 31).
      Исходная постановка: (заведено 15.08 при
      заходе М4 задачи 26). Замер по собранному `content/course.json`: верный вариант оказывается
      САМЫМ ДЛИННЫМ из трёх в **56 вопросах из 80 (70%)**, одинаково в ru и en; случайное
      распределение дало бы 33%. Ученик, не знающий материала, выбирает самый развёрнутый
      вариант и попадает — вопрос перестаёт проверять знание, а XP за урок обесценивается.
      Причина механическая, а не редакторская: верный вариант авторы пишут точной формулировкой
      с уточнением («Тренировочное столкновение мнений, которое проверяет вас на прочность»),
      а неверные — короткими отговорками («Знак, что нужно уступить»). Перемешивание вариантов
      кодом (`shuffleOptions`) от этого не спасает: перемешивается порядок, а не длина.
      ⚠️ Дефект не виден ни при написании вопроса по одному, ни при вычитке подряд — только
      счётом по всей пачке; вычитку редактором М1–М3 он прошёл незамеченным (тот же класс, что
      однообразие зачинов в задаче 25).
      Что делать: выровнять длины трёх вариантов внутри каждого вопроса (разброс между самым
      коротким и самым длинным — не больше трети), дописав неверным столько же конкретики,
      сколько есть у верного (заодно они станут правдоподобнее), и убрав из верного
      хвост-объяснение — объяснение живёт в `explain`. Цель: не больше 2 вопросов из 5 в уроке.
      Правка контентная, кода не требует; заодно стоит поставить проверку в контракт-тест курса
      (порог по модулю), чтобы М5–М6 не повторили. Скрипт замера — в конвейере задачи 26.
      В М4 дефект исправлен сразу при написании (15.08), М1–М3 остались как есть.

- [x] **24 · Формула аркана рождения — РЕШЕНО 13.08 разговором с редактором: метод остаётся**
      (вариант (а)): сумма всех цифр даты со свёрткой, как в logic-spec §5 и `birthArcana.ts`.
      Маг и Верховная Жрица осознанно «в стол»; их фразы `birth_path` сохраняем (решение
      делегировано Claude, см. задачу 23). Идея пары «основная + теневая» не принята —
      возвращаться к ней, если вообще, отдельным решением в v2. Код и спека не меняются.
      Исходная постановка (заведено 13.08 по вопросу Артёма при лайв-проверке 09) — задача
      на РЕШЕНИЕ, не на код.
      Формула из logic-spec §5 (сумма всех цифр даты, свёртка до ≤22, 22 = Дурак) реализована и
      протестирована, вопрос не в правильности кода, а в выборе метода. Замер по всем датам
      рождения 1950–2010 (22 280 дней):
      · **Маг (I) и Верховная Жрица (II) не выпадают НИКОМУ** — минимальная сумма цифр для этих
        лет около 17, а свёртка двузначных чисел даёт только 4–12. Два текста «ваш путь» из 22
        не увидит ни один живой пользователь;
      · семь арканов (Иерофант…Правосудие, V–XI) забирают **68%** аудитории, примерно по 9.7% каждый;
      · Смерть, Дьявол, Башня и прочие XIII–XXI — по 1.3–1.7%, то есть в 7 раз реже.
      Это свойство самого метода, а не ошибка: суммы цифр дат тесно сгруппированы. Но решить надо:
      (а) оставить как есть и предупредить редактора, что тексты I и II пишутся «в стол»
      (для дат до 1950 они появляются, но такой аудитории у нас нет);
      (б) взять другую распространённую школу расчёта — их несколько, и распределение у них другое;
      (в) считать пару карт (частая практика: основная + «теневая»), тогда охват арканов шире.
      Выбор за женой как за экспертом; после решения — обновить logic-spec §5 и, если метод
      меняется, тест-кейсы и `src/lib/birthArcana.ts`.

- [x] **23 · Контент: вычитать `birth_path` — 22 фразы «ваш путь»** — ЗАКРЫТА 14.08: ответ
      на три вопроса делегирован Claude решением Артёма («по 23 ответь сам»); решение — «да,
      так и задумано»: жанр «характер пути» читает все 22 карты через сильную сторону, тень
      карты остаётся в значениях на странице карты (один тап от чипа аркана рождения); у Дьявола
      привязанности в тексте названы, у Башни «отпускать шаткое» — достаточный намёк, у Луны
      «только темнота» признана. Тексты не менялись. Подробно: `docs/editor-questions.md`,
      раздел «Решения (14 августа)».
      ВЫЧИТКА СДЕЛАНА 14.08:
      нарушений тона нет ни в одной из 22 фраз (ни предсказаний, ни жаргона; трудные карты, кроме
      Луны, называют трудность и дают выход), длина ровная (10–19 слов), дублей нет, обращение
      везде гендерно-нейтральное (глаголы 2-го лица мн. числа рода не различают). Две правки,
      обе в en: Маг «manifestation» → «bringing ideas to life» (лексикон law-of-attraction —
      та же рамка, что запрещённое «сбылось»), Луна калька «half-tones» → «subtle shades»
      (в английском half-tone — термин полиграфии). Статусы: 22 блока draft → **reviewed**
      (`set_status.py reviewed --only cards`, задело ровно 22 — других черновиков у карт не было),
      корпус карт теперь 646/646 reviewed. 362 теста зелёные (контракт `birthPathContent` в т.ч.).
      Спорные ТРАКТОВКИ не правил — три вопроса редактору записаны в `docs/editor-questions.md`
      (раздел «Вычитка "ваш путь" сделана»): Дьявол (несвобода перевёрнута в «самочестность =
      свобода»), Башня (внезапность обвала не названа), Луна (трудность не названа вовсе) +
      общий вопрос «весь блок трактует карты только через сильную сторону — так и задумано?».
      Ответ получен 14.08 (делегирован Claude, см. выше) — все три закрыты одним «да».
      Замечание не в тексты, а в стиль корпуса (править не стал, для UX не проблема): «вы умеете»
      открывает 6 фраз из 22 — заметно только при чтении списком, пользователь видит одну.
      Исходная постановка (заведено 13.08 при задаче 09) —
      РЕШЕНИЕ 13.08: вычитка делегирована Claude («оставляю задачу на тебя, прими подходящее
      решение»); спорные трактовки — показать редактору списком, а не молча править. Второе
      решение, тоже за Claude: фразы Мага и Верховной Жрицы СОХРАНЯЕМ, хотя при текущем методе
      (задача 24) они не выпадают никому — тексты уже написаны, вычитка 22 фраз не дороже 20,
      при смене метода или теневой паре в v2 пригодятся, а даты до 1950 дают их и сейчас.
      Черновики написаны кодом (`scripts/add_birth_path.py`, статус `draft`) и уже показываются
      на шаге 3 онбординга: вау-момент не ждёт вычитки. Тон — по content-guide: про характер
      пути по жизни, НЕ предсказание; «сбылось» и обещания запрещены.
      После круга правок — `python scripts/set_status.py reviewed --only cards` (⚠️ двинет ВСЕ
      draft-блоки карт; если к тому моменту появятся другие черновики — править точечно).
      Контракт-тест `birthPathContent.test.ts` следит, что блок есть у всех 22 старших, нет
      у младших и все 22 фразы РАЗЛИЧНЫ (защита от копипасты одной фразы всем — без неё сьют
      оставался зелёным, проверено экспериментом).

- [x] **22 · Обсудить: зачем нужен опыт (XP) и что он должен давать** — ЗАКРЫТА 15.08 решением
      Артёма: **в v1 ничего не меняем**, вопрос выносится в следующую версию. Титулов как смысла
      XP для v1 достаточно; сток, добор после конца курса (расклады +5, ачивки, флеш-карты
      этапа 3+) и общий дневной потолок повторов — обсуждение при планировании v2 (сверить
      с master-plan). Кода задача не требовала и не потребовала.
      Исходная постановка (заведено 13.08 по вопросу
      Артёма после лайв-проверки задачи 08) — задача на РАЗГОВОР и решение, не на код.
      ЧАСТИЧНО РЕШЕНО 13.08 разговором с редактором: пункт (г) закрыт — титулы ОСТАЮТСЯ
      женскими в v1, осознанное решение по аудитории; нейтральные формулировки — кандидат в v2.
      Пункты (а)–(в) открыты: Артём запросил рекомендацию Claude — дана 13.08 (титулов для v1
      достаточно, сток не делать, после курса добирать практикой: расклады +5, ачивки
      и флеш-карты этапа 3+), решение Артёма впереди. Сейчас XP
      реализован и работает, но остаётся числом, которое только растёт.
      **Как есть сегодня:** источники — карта дня +5, первый ответ рефлексии дня +3, урок
      10 − 2×ошибки (мин 4), повтор урока +2 раз в день на урок; расклад +5 ждёт своего этапа
      (logic-spec §4). Уровни накопительные: 50 / 150 / 300 / 500, дальше каждый +250, титулы
      1–6 (Любопытная → Мастерица). Обычный день без учёбы даёт 8 XP, день с уроком — 16–18,
      то есть 2-й уровень через 3–6 дней, 3-й через пару недель, 5-й через два месяца.
      Показывается ровно в одном месте — пилюля на «Сегодня» (карточка уровня в профиле —
      задача 16, не сделана). XP ничего не открывает и не блокирует: путь по курсу гейтится
      только пройденными уроками, серию дней XP не двигает.
      **Что обсудить:**
      (а) **Тратить не на что.** У опыта нет стока: он копится, но ни на что не обменивается.
      Смысл ему придают только титулы — раз в несколько недель. Нужен ли сток вообще, или
      достаточно титулов?
      (б) **Курс конечен.** После 32 уроков основной источник иссякает, остаётся 8 XP в день
      с ритуала, и кривая уровней растягивается непропорционально долго. Чем добирать —
      раскладами (+5), ачивками, флеш-картами SM-2?
      (в) **Повторы уроков не ограничены сверху.** Потолок «+2» стоит на каждый урок отдельно,
      а не на день целиком: пройдя все 32 урока заново, можно получить 64 XP за день. Сегодня
      безобидно (нужен реальный час времени, тратить опыт всё равно некуда), но если XP начнёт
      что-то открывать или появится рейтинг — понадобится общий дневной потолок.
      (г) **Титулы только женского рода** (Любопытная, Ученица, Толковательница…). Если среди
      пользователей окажутся мужчины — титул будет читаться неверно. Осознанное ли это решение
      по аудитории, или нужны нейтральные формулировки?
      ⚠️ Перед реализацией чего-либо из этого — сверить с `docs/master-plan.md` (там XP заявлен
      вместе с сериями и ачивками) и решить, не дублирует ли это задачу 16.

- [x] **Хелпер `useLang()` вместо повторяющегося каста языка** — ЗАКРЫТО 15.08 В СОСТАВЕ 27 (задача 5
      плана `docs/plans/27-lang-infra.md`; хук читает зеркало i18n, как весь существующий код, —
      третий источник правды не заводится). Исходная постановка (заведено 13.08, финальное
      ревью 07) — выражение `(i18n.language.startsWith('ru') ? 'ru' : 'en') as 'ru' | 'en'`
      повторяется в 10 файлах (`app/(tabs)/course.tsx`, `app/settings.tsx`, `app/lesson/[id].tsx`,
      `app/(tabs)/index.tsx`, `app/(tabs)/profile.tsx`, `app/card/[id].tsx`, `app/(tabs)/cards.tsx`,
      `app/note/[date].tsx`, `app/(tabs)/spreads.tsx`, `src/components/StreakPill.tsx`), и каждый
      новый экран добавляет свою копию. Вынести в хук `useLang()` в `src/lib/i18n.ts`, заодно
      убрать `as`-каст. ⚠️ Обдумать при выносе: язык сейчас живёт в ДВУХ местах (`i18n.language`
      и `useApp().lang`) — хук должен читать то же место, что и весь существующий код, а не
      завести третий источник правды.

- [x] **27 · Инфраструктура 4 языков (CC-L1 из docs/localization-plan.md)** — ЗАКРЫТА 15.08:
      веб-проверка 6а/6б и **лайв-проверка Артёма на iPhone пройдены ✓** (замечаний нет), ветка
      `feat/27-lang-infra` влита в main. Итог: 719 тестов в 26 сьютах (было 574 в 23), `tsc` чист;
      финальное ревью ветки нашло расхождение кода и документов про язык при импорте бэкапа —
      закрыто волной фиксов (`resolveImportedLang` с ограничителем по `AVAILABLE_LANGS`).
      Исходная постановка и решения ниже.
      СПЕКА И ПЛАН ГОТОВЫ 15.08 (согласованы Артёмом): `docs/specs/27-lang-infra.md`,
      `docs/plans/27-lang-infra.md` (9 задач, tsc чист после каждого коммита); реализация — Opus 5
      по плану в ветке `feat/27-lang-infra`. Решения: один модуль `src/lib/lang.ts` (тип на 4 значения,
      эндонимы, локали `es-MX`/`pt-BR`, детекция по всему списку предпочтений устройства,
      `Localized<T>`/`inLang` с фолбэком на en); `useLang()` читает зеркало i18n; пикер языка —
      модальный список (общий `OptionPicker` из `TimePicker.web`); язык первой установки — снимок
      с устройства в `onRehydrateStorage`; es/pt в пикере и детекции появляются только с их
      UI-строками (`AVAILABLE_LANGS`, в dev — все 4); persist v8; 9 хардкод-тернаров `lang === 'ru'`
      уходят в i18n. Пакет `expo-localization` → npm install. Заодно поглощает пункт `useLang()` ниже.
      Перенумерована из «15» — номер конфликтовал с макетными дорисовками.
      Объединяет заведённый хелпер `useLang()` (тот же файл, те же 10 мест — делать одним заходом):
      (а) тип Lang = 'ru'|'en'|'es'|'pt' везде; (б) хук useLang() в src/lib/i18n.ts — единый источник
      правды языка (см. предупреждение про два места), без as-кастов, готовый к 4 значениям;
      (в) переключатель языка в настройках — 4 позиции (es/pt до заливки контента фолбэк на en —
      это штатный дев-режим); (г) определение языка устройства при первом запуске: es-* → es,
      pt-* → pt, ru-* → ru, иначе en; (д) плюрализация es/pt через уже стоящий intl-pluralrules
      (hf-02) + тесты по образцу i18nPlurals.test.ts. Чем раньше — тем дешевле: каждый новый экран
      после этой задачи автоматически четырёхъязычный.
      ГОТОВО В КОДЕ 15.08: девять коммитов реализации, ревью пройдены, 719 тестов в 26 сьютах
      зелёные, `npx tsc --noEmit` чист, веб-проверка 6а/6б пройдена. Менялись `package.json`
      (пакет `expo-localization`) и `app.json` (плагин дописал себя в `plugins` сам) → нужен
      `npm install` и перезапуск `npx expo start --tunnel`. Переводов в задаче нет ни одного —
      es/pt пока падают на английский, это штатное поведение (волна переводов — задача 28).
      Ждёт лайв-проверки Артёма на iPhone. Спека `docs/specs/27-lang-infra.md`, план
      `docs/plans/27-lang-infra.md`, ветка `feat/27-lang-infra`.

- [x] **36 · Расклады: интерактив «разложить» (этап 5 плана)** — ЗАКРЫТА 17.08 (лайв-проверка
      Артёма на iPhone пройдена, ветка влита в main). — заведено 15.08 (номер был
      зарезервирован спекой 26, записи в бэклоге не было). Последняя большая фича v1: сегодня таб
      «Расклады» — только список из 8 панелей, тап никуда не ведёт. Поведение целиком описано
      в product-spec §4 (настрой с вопросом → тасование по logic-spec §1а → открытие тапом,
      перевёрнутые вверх ногами → значения из cards.json → «Состав расклада» по офлайн-правилам
      §1б без ИИ → «Сохранить в дневник» / «Уйти без сохранения?» → «Сохранено ✓» + «Разложить
      заново»; несохранённый расклад НЕ перетасовать — решение 09.08), макет ЕСТЬ: малый «Три
      карты» полным циклом и большой «Кельтский крест» (лента 10 позиций). Дневник получает
      второй тип записи (строка с иконкой-веером, режим просмотра сохранённого расклада), схема
      стора меняется → **persist version 9**. Расклад +5 XP (logic-spec §4). Крупная задача:
      ветка `feat/36-spreads`, брейншторм → спека → детальный план. Заодно она — условие
      заморозки строк UI для задачи 28.
      **Спека и план готовы 15.08 (брейншторм с Артёмом):** `docs/specs/36-spreads.md`,
      `docs/plans/36-spreads.md` (15 задач, реализация — сессия на Opus 5). Решения: лента 7–10 —
      абзац значения сразу под строкой; шаг настроя с CTA «Разложить карты» по product-spec (вопрос
      фиксируется до карт); экран расклада — вложенный стек таба (таб-бар виден), просмотр из
      дневника — корневой `app/spread/[ts]`; +5 XP при сохранении; `SpreadDraw` с `ts` без
      `position`; «Разложить заново» = чистый лист; просмотр — только чтение. Найдено при разборе:
      фраза courts в `composition.json` ломается на 5+ картах (числительные, класс hf-02) —
      переписана в плане; таб-бар определяет активную вкладку по точному `pathname`, а
      `useTabScrollToTop` слушает `tabPress` только на навигации экрана — оба лечатся в плане.
      ГОТОВО В КОДЕ 16.08: задачи плана 1–13 (данные, чистые модули, стор/бэкап, экраны, дневник)
      реализованы на ветке `feat/36-spreads`, документы (задача 14: logic-spec/product-spec/
      design-system/backlog) синхронизированы с кодом; 840 тестов в 30 сьютах зелёные,
      `npx tsc --noEmit` чист. Ждёт веб-проверки 6а/6б и лайв-проверки Артёма на iPhone (задача 15
      плана) — только после них merge в main и статус `[x]`.

- [x] **43 · Импорт бэкапа с сегодняшней картой дня оставляет на «Сегодня» рубашку** — ЗАКРЫТА
      19.08: лайв-проверка Артёма на iPhone пройдена ✓ («всё работает окей» — весь сценарий,
      включая резкость лица после импорта и полноэкранный просмотр по тапу), веб-проверка 6а/6б
      пройдена, в main без ветки. Заведена 18.08 при спеке 42.
      Спека, план и отчёт: `docs/specs/43-import-today-card.md`.
      Сделано: эффект состояния карты дня переписан на три ветки (закрыто → рубашка,
      открыто тапом → анимацию ведёт `onDraw`, открыто без тапа → сразу конечное состояние) —
      та же тройка, что у `SpreadCard` с задачи 36. Веб-проверка 6а/6б пройдена: сценарий прогнан
      СНАЧАЛА на сломанном коде (красный — лицо на `matrix3d` с `rotateY(180deg)`, текст
      `opacity: 0`), потом на исправленном (зелёный). `npx tsc --noEmit` чист, 919 тестов зелёные.
      Исходная постановка: `flip` карты дня поднимается только в `onDraw`; эффект по `drawn`
      обрабатывал лишь сброс (`!drawn`). Импортируй бэкап, в котором есть запись за СЕГОДНЯ,
      при смонтированном табе «Сегодня» — `drawn` станет истинным, но лицо не повернётся: на
      экране рубашка, а тап по ней открывает лайтбокс (ветка `if (drawn)`). Проявляется только
      при импорте того же дня; до перезапуска приложения.

- [x] **44 · Блик карты дня перезапускается после ответа рефлексии и заметки** — ЗАКРЫТА 19.08:
      лайв-проверка Артёма на iPhone пройдена ✓ (блик проходит штатно, моргания в момент ответа
      рефлексии нет), веб-проверка пройдена. Заведена 19.08 при задаче 43 (найдена при разборе
      эффектов; чинить в 43 не стали — вне постановки).
      Спека и отчёт `docs/specs/44-glare-restart.md`: зависимость эффекта
      блика переведена с объекта `drawn` на булев `isDrawn`. Веб-замер сделан по правилу «сначала
      красный»: до правки блик после тапа по кнопке рефлексии прыгал в точное `-305.76`
      (крайнее левое положение) при том, что за 50 мс до тапа двигался; после правки доезжает
      до правого края своим ходом (`303.8 → 305.76`). `tsc` чист, 919 тестов зелёные.
      ⚠️ Урок процесса: в первый заход лайв-проверки эта правка НЕ попала — сценарий проверки
      ушёл Артёму раньше, чем правка легла в рабочее дерево. Сценарий нельзя отправлять раньше
      кода, который он проверяет: проверка выглядит пройденной, не будучи ею.
      В `app/(tabs)/index.tsx` эффект блика висит на ОБЪЕКТЕ `drawn`, а `setOutcome`/`setNote`
      кладут в `history` новую запись — идентичность меняется, эффект перезапускается, блик гаснет
      и через 500 мс проходит заново. Выглядит как случайное мерцание карты в ответ на действие,
      к ней не относящееся. Лечение — та же зависимость `isDrawn` (булев вместо объекта), которую
      задача 43 уже ввела в этот файл. Косметика: ничего не ломает, кроме впечатления.

- [x] **42 · Карта дня замылена после переворота — ДВЕ независимые причины** — ЗАКРЫТА 18.08:
      веб-проверка 6а/6б и лайв-проверка Артёма на iPhone пройдены ✓ (все шесть пунктов, замечаний
      нет), ветка `feat/42-day-card-sharpness` влита в main. Спека и отчёт —
      `docs/specs/42-day-card-sharpness.md`, план — `docs/plans/42-day-card-sharpness.md`.
      Исходная постановка: заведено 17.08
      решением Артёма отдельной задачей: правка касается эталонного экрана, а не задачи 36.
      ⚠️ Первая версия этой записи называла только причину №1 и была НЕПОЛНОЙ — исправлено 17.08
      после замеров и показаний с устройства.
      **Причина 1 — осиротевшие нативные свойства слоя.** В `app/(tabs)/index.tsx` лицевая грань
      получает через `useAnimatedStyle` трансформацию `perspective` + `rotateY` и остаётся с ней
      после переворота. Слой продолжает жить в 3D-контексте и рисуется через offscreen-текстуру.
      ⚠️ Важно: просто ПЕРЕСТАТЬ передавать анимированный стиль недостаточно — reanimated
      накладывает свойства императивно со стороны UI-потока, и без явной перезаписи (или
      пересоздания представления) они остаются на слое. Именно на этом споткнулась первая правка
      в раскладах: флаг «переворот доехал» подняли, стиль убрали — мыло осталось до следующего
      рендера. Признак этой причины со слов Артёма: мягкой выглядит ПОСЛЕДНЯЯ перевёрнутая карта,
      а любой следующий рендер (открытие соседней карты, возврат на экран) делает все чёткими.
      Рабочее лечение — в `src/components/SpreadCard.tsx` ветки 36, разбор —
      `.superpowers/sdd/36-spreads/blur-fix-report.md`.
      **Причина 2 — растяжение исходника, и для карты дня она главная.** Изображения в
      `assets/cards/` имеют размер **300×537 px**. Карта дня рисуется в 218pt, то есть **655px**
      при плотности 3x — картинку растягивают в **2.2 раза**. Никакая правка слоя этого не лечит.
      Замеры по остальным поверхностям (3x): герой страницы карты 128pt → 384px (растяжение 1.3),
      сетка справочника 107pt → 320px (1.07, Артём подтвердил 17.08, что там чётко),
      карта расклада 88pt → 264px (запас есть), мини-карта ленты 34pt → 102px (запас втрое).
      То есть терпимо примерно до 1.3, а 2.2 у карты дня — уже видно.
      Варианты по причине 2 (решение за Артёмом): заменить все 78 исходников на версии большего
      размера (public domain Райдер–Уэйт, сканы большего разрешения доступны) — ⚠️ вырастет вес
      бандла: сейчас ~29 КБ на карту, при ширине ~900px будет в разы больше; либо уменьшить
      отрисовку карты дня; либо принять как есть.
      ⚠️ Экран проверен на устройстве и является эталоном — правку сверять с `docs/motion-spec.md`
      и обязательно прогонять пункт 6в: веб и jest этот класс дефектов не воспроизводят В ПРИНЦИПЕ.

- [x] **45 · Флеш-карты: повторение изученных карт (SM-2)** — **ЗАКРЫТА 19.08 целиком (45а + 45б)**, — заведена 19.08, первая задача
      этапа 4 (master-plan §3.1 п. 4 и п. 12), спека `docs/specs/45-flashcards.md` согласована
      с Артёмом 19.08 (брейншторм: флип + самооценка, вход с таба «Курс», колода = карты пройденных
      уроков, +1 XP за вспомненную карту, ТРИ кнопки «Не помню / Помню / Легко», интервалы на
      кнопках не показываем, сессии по 10 — этим закрыты вопросы 1–4 Cowork ниже). Ветка
      `feat/45-flashcards`, две части: **45а — логика и данные** (план `docs/plans/45-flashcards.md`;
      `srs.ts` переписан на даты, `review.ts` — сборка сессии/сводка, стор `srs` + `reviewDay`,
      persist **10**, бэкап, три DEV-строки; лайв-проверки экранов нет по существу — мерж после
      ревью, прецедент 28а); **45б — экраны** (план `docs/plans/45b-review-screens.md`, ветка
      `feat/45b-review-screens`). Статус: **45а ЗАКРЫТА 19.08** — ветка влита в main (merge a98dd7c)
      и запушена, лайв-проверка Артёма на iPhone пройдена ✓, 1273 теста в 35 сьютах зелёные (отчёт
      `docs/specs/45-flashcards.md`, раздел «Отчёт 45а»). **45б СДЕЛАНА 19.08**: карточка
      «Повторение» в шапке курса (4 состояния), экран `/review` (два направления, переворот
      `toCard`, панель «ЗНАЧЕНИЕ», три кнопки оценки, счётчик очереди, результат с «Ещё N», пустые
      состояния), веб-проверка 6а/6б ✓ (46/46 проверок, скриншоты `docs/screenshots/45/`), **1295
      тестов в 35 сьютах** зелёные (отчёт `docs/specs/45-flashcards.md`, раздел «Отчёт 45б», там же
      сценарий лайв-проверки). Ветка `feat/45b-review-screens` влита в main (merge e091e7f)
      решением Артёма 19.08 — до лайв-проверки, а не после (обычный порядок обратный): финальное
      ревью ветки прошло, находки закрыты, слитый результат зелёный. **Лайв-проверка на iPhone
      пройдена 19.08 ✓** — «все пункты работают», замечаний нет; задача закрыта.
      Флаг Cowork про `promptSentence`/имя карты в toCard (см. закрытую задачу «Макет: правки
      дорисовок» ниже и абзац выше) — **закрыт 45б**: `maskCardName` + корпусный контракт 78 × ru/en.
      После лайв-проверки Артёма — задача целиком `[x]` и статус «ЗАКРЫТА».
      ⚠️ Два хвоста, найденные финальным ревью ветки 45б (19.08), — **ОБА ЗАКРЫТЫ 21.08**:
      1. **Третья копия механики 3D-переворота** — РЕШЕНО: копий остаётся две, параметризацию
         `FlipCard` отклоняем. `app/(tabs)/index.tsx` держит свою реализацию, потому что там
         переворот сцеплен с покачиванием ±6px внутри тех же worklet-стилей (2D-сдвиг обязан идти
         ПЕРЕД `perspective`), длится 850 мс вместо `FLIP_MS` 500, держит обе грани смонтированными
         (`FlipCard` убирает рубашку из дерева) и ведёт соседние анимации — подпись, панель значения,
         цикл блика. Параметризация потребовала бы 5–6 пропов, половина — проброс shared values,
         а трёхветочный эффект состояния (спека 43) всё равно остался бы снаружи. Общего там ~15 строк,
         и это не абстракция, а ПРАВИЛО 3D-контекста — оно записано в design-system §5 («Переворотов
         в приложении ДВА») и перекрёстно указано в шапках обоих файлов, чтобы третий переворот
         начинался с правила, а не с копипасты.
      2. **«Всё повторено ✓ · завтра: 0»** — СДЕЛАНО: хвост «завтра: N» печатается только при N ≥ 1,
         иначе строка — просто «Всё повторено ✓» (новый ключ `review.allDoneNoTomorrow` на четырёх
         языках, выбор ключа в `ReviewPanel`). Макет нулевой случай не рисует вовсе (там демо
         «завтра: 4»), конфликта с эталоном нет. Контракт совпадения наборов ключей (`i18nPlurals`)
         проверен мутацией: удаление ключа из pt валит тест поимённо.
      3. **⚠️ ПРЕДУСЛОВИЕ ЗАДАЧИ 28 (ES/PT): зеркальный промах маски имени.** Найдено ре-ревью
         волны правок 19.08, оставлено осознанно (сегодня недостижимо, править на пустом корпусе
         нечем и нечего тестировать). Экран тренажёра маскирует имя карты в подсказке на рубашке
         языком ПОКАЗАННОГО текста: `inLang(card.name, presentLang(card.content.general, lang))`.
         Это закрывает случай «`name.es` есть, `general.es` нет». Обратный случай не закрыт:
         если `general.es` ЕСТЬ, а `name.es` нет, `inLang` внутри падает на английский, маска ищет
         английское имя в испанском тексте и не находит — рубашка-вопрос печатает ответ. Причина
         архитектурная: `name`/`keywords`/`search` переводятся атомарно (`wordsStatus`), блоки
         значений — своим статусом (спека 28а), а порядок между этими двумя единицами нигде
         не задан. Сегодня выстрелить не может: у канона ru/en оба поля всегда полны (контракт-тест).
         Волна ES/PT обязана либо перевести `name` раньше блоков, либо перед заливкой доработать
         маску (маскировать всеми вариантами имени) и добавить зеркальный тест — сейчас и корпусный
         `it.each(LANGS)`, и синтетический тест проверяют только одно направление.
         **ЗАКРЫТО 20.08 на старте задачи 28:** корпусный контракт в `cardsContent.test.ts`
         («у неканоничного языка нет блоков без названия карты») делает запретное состояние
         недостижимым в закоммиченном контенте — блок `es`/`pt` без `name` того же языка валит
         `npm test`. Красный прогон подтверждён мутацией (`general.es` + статус у fool без
         `name.es` → падает ровно этот тест, 1 из 469). Дорабатывать маску не нужно; правило
         заливки остаётся прежним — карта атомарно, name+keywords+search+блоки одним куском.
         ⚠️ Там же второй хвост того же места: вычисление имени для маски живёт в `app/review.tsx`,
         а тесты держат его независимую КОПИЮ — откат строки в экране ни один тест не поймает.
         **ЗАКРЫТ 23.08 задачей 55** — и обоими способами сразу: связка вынесена в `src/lib/review.ts`
         как `reviewPrompt(card, lang)` (экран зовёт одну функцию, тесты считают через неё же)
         И закрыта контракт-тестом по исходнику (`promptSentence(`/`maskCardName(`/`presentLang(`
         в `app/review.tsx` запрещены, `reviewPrompt(` обязателен). Заодно добавлена положительная
         пара к отрицательному контракту: «подсказка непустая у всех 78 карт на четырёх языках» —
         без неё проверка «не содержит имени» проходила бы и на функции, возвращающей пустую строку.

- [x] **48 · Контраст светлой темы: `muted` + бейдж «ИЗУЧЕНО ✓»** — **ЗАКРЫТА 21.08 решением
      Артёма («оставляем так»)** — принята как есть, без отдельной лайв-проверки; код в main
      с 20.08. Сделано 20.08, обе
      развилки решены Артёмом (#6f664b; золото не трогаем), веб-проверка 6а/6б пройдена
      (145 muted-узлов на 5 экранах ≥4.93:1, красный прогон на старом токене — 145/145 ниже
      нормы; бейдж светлая = тёмная пиксель в пиксель).
      Спека и отчёт: `docs/specs/48-light-muted-contrast.md`.
      Замеры: `muted` `#a29878` на светлой — 2.48:1 плоский / 1.84:1 по пикселям при норме 4.5:1;
      бейдж «ИЗУЧЕНО ✓» на светлой — 1.35–1.46:1 (тёмный `accent2` на чёрном скриме, почти невидим).
      Решение: `lightTheme.muted` → `#6f664b` (4.93:1; вариант с запасом `#665d43` — 5.64:1,
      выбирает Артём) — один токен чинит все места разом; текст бейджа — фиксированное светлое
      золото `gold.onScrim = '#f0d9a4'` в обеих темах (правило «цвет на тёмном скриме не следует
      теме» — скрим одинаково тёмный всюду). Тёмную тему не трогаем (5.20:1). Золотые мелкие
      подписи (`accent`, 3.13:1) — осознанно вне задачи, открытый вопрос Артёму (вариант: на
      светлой красить `accent2`, 4.35:1). Хвост про макет ЗАКРЫТ 20.08 после приёмки
      Cowork-дорисовок (коммит 5815ef0): `--muted` светлой темы → #6f664b, текст `.st2` →
      фиксированный #f0d9a4. Остаётся только лайв-проверка Артёма.

- [x] **49 · Уровни мастерства карты** — **ЗАКРЫТА 21.08**: веб-проверка 6а/6б (32/33) и
      лайв-проверка Артёма на iPhone пройдены ✓ («всё отлично»), ветка `feat/49-mastery`
      влита в main. Единственный провал веб-проверки — контраст — вынесен Артёму и решён
      им же: «оставить как есть, вроде норм смотрится» (задача 50, см. ниже).
      Спека и подробный отчёт — `docs/specs/49-mastery.md`, план —
      `docs/plans/49-mastery.md`, скриншоты — `docs/screenshots/49/`, ветка `feat/49-mastery`.
      Закрывает хвост (а) задачи 46 (master-plan п. 13). У ИЗУЧЕННОЙ карты появилась ступень,
      выведенная из SRS: чип на странице карты между overline и названием, 4 полоски в ячейке
      справочника. Пороги: нет записи → НОВАЯ, `intervalDays` 0–5 → ЗНАКОМАЯ, 6–20 → УВЕРЕННАЯ,
      21+ → МАСТЕР; ступень N подсвечивает ровно N сегментов из 4 (решение Артёма 21.08 —
      демо макета светило 0/1/3/4 и приведено к этому правилу). Неизученная карта индикатора
      не имеет вовсе. Ничего не хранится, persist version НЕ менялась (остаётся **10**).
      Новое: `src/lib/mastery.ts` (чистый, 6 тестов), `MasteryChip`, проп `mastery` у `CardCell`,
      токен `successBg`, DEV-строка «Рассадить ступени мастерства». Тестов **1475 в 38 сьютах**.
      **Контраст ярлыка чипа — РЕШЕНО Артёмом 21.08: оставить как есть** («вроде норм
      смотрится», после просмотра всех четырёх ступеней на устройстве). Цвета не менялись.
      Замеры сделаны и зафиксированы, чинятся задачей 50 — НЕ заводить снова как находку.
      Хвосты (не блокируют): (а) вычитка ярлыков es/pt носителем — NUEVA/CONOCIDA/SEGURA/MAESTRA
      и NOVA/CONHECIDA/SEGURA/MESTRA написаны сессией, прецедент `push.freeze_saved` задачи 10;
      (б) **флаг 6а-0** — в макете `--done` тёмной темы #3d7a5c против `success` #5aa07e
      в design-system и коде, из-за чего `.mastery.m4` красит текст одним зелёным, а фон задаёт
      литералом другого; расхождение СТАРШЕ задачи 49 и затрагивает ещё четыре места макета
      (узел пройденного урока, кнопки оценки, карточка повторения, шаг викторины), а в строке 996
      кто-то уже перекрыл его вручную на #5aa07e — приложение самосогласовано и не правилось.

- [x] **50 · Контраст мелких подписей — одним заходом во всех местах** — **ЗАКРЫТА 21.08**:
      лайв-проверка Артёма на iPhone пройдена ✓ («всё норм, контраст»). Замечаний нет, в том числе
      к побочным сдвигам, о которых предупреждал отчёт: золото светлой темы стало глубже во всех
      акцентах, а градиент полос прогресса на светлой теме теперь идёт от тёмного к светлому.
      Сделано в коде 21.08:
      (решение Артёма «приводим к норме целиком»), веб-проверка 6а/6б пройдена: красный прогон
      7 провалов на старых токенах, зелёный 14/14 ≥4.5 (худший 4.73), 1488 тестов зелёные.
      Ждёт лайв-проверки Артёма. Спека и отчёт: `docs/specs/50-small-label-contrast.md`.
      ⚠️ Кандидаты ниже не пережили пиксельного суда (4.05/4.25/4.41 в худших точках) — итоговые
      значения темнее/светлее: светлая `accent` #7c5e20, светлая `success` #386b51, тёмная
      `success` #63ab89, плюс НОВАЯ четвёртая пара — тёмный `muted` #848cb2 («ВАШ ПУТЬ» на верхе
      градиента давал 4.38, плоский расчёт 47 видел 5.20). Хвост `--done` макета закрыт.
      Заведена 21.08,
      собирает вместе три провала нормы WCAG 4.5:1, копившиеся с задач 25, 47, 48 и 49.
      Все три ЗАМЕРЕНЫ точно (формула WCAG по реальным пикселям скриншота, а не на глаз),
      подбирать заново не нужно — задача про решение и свип, а не про измерение:
      1. **Overline + `muted` на светлой теме — 1.84:1** (подписи дней недели лунного календаря,
         «ВАШ ПУТЬ», заголовки блоков). Найдено задачей 47; макет даёт те же 2.18 — свойство
         пары целиком, а не дефект одного экрана.
      2. **`accent` / `chipBg` на светлой — 3.17:1** (ступени ЗНАКОМАЯ и УВЕРЕННАЯ чипа
         мастерства, чип «✦ ВАШ АРКАН РОЖДЕНИЯ» с задачи 09 — 2.97). Кандидат `#a8802e →
         #896825` (4.51). ⚠️ Вариант из задачи 48 «на светлой красить `accent2`» НЕ работает:
         #8a6a24 даёт **4.40**, а не заявленные тогда 4.35, и норму не берёт.
      3. **`success` / `successBg` — 3.00 светлая, 4.44 тёмная** (ступень МАСТЕР, задача 49).
         Кандидаты: тёмная `#5aa07e → #5ba280` (4.55, светлее на 0.6 % L — глазом неразличимо),
         светлая `#4d9370 → #3c7358` (4.53, темнее заметно).
      ⚠️ Решение Артёма 21.08 по пункту 3 в рамках задачи 49 — «оставить как есть, вроде норм
      смотрится»: на устройстве читается. То есть задача 50 НЕ про баг-репорт, а про вопрос
      «приводим ли мы приложение к норме доступности целиком» — и ответ на него общий для всех
      трёх пунктов. Если да — чинить ОДНИМ заходом: точечная правка одного места делает
      интерфейс разнобойным (урок задачи 48), а после смены токена нужен свип грепом по всем
      потребителям + повторный замер на 5–6 экранах в обеих темах.
      ⚠️ Метод замера (выведен задачей 49, годится всем будущим): полупрозрачный фон
      раскладывают на то, что под ним В ЭТОЙ ТОЧКЕ экрана, а не на «фон экрана вообще» —
      чип мастерства стоит на y≈93 из 844, то есть в светлой части градиента `ScreenBg`,
      и композит на плоский `theme.bg` завысил замер с 4.44 до 5.14. Судья — пиксель скриншота.
      Хвост-сосед, чинится тем же заходом: в макете `--done` тёмной темы #3d7a5c против
      `success` #5aa07e в design-system и коде (расхождение старше 49, затрагивает узел
      пройденного урока, кнопки оценки, карточку повторения, шаг викторины; в строке 996
      макета оно уже перекрыто вручную на #5aa07e).

- [x] **51 · Лунные расклады** — **ЗАКРЫТА 22.08**: лайв-проверка Артёма на iPhone ✓ («всё отлично»),
      ветка `feat/51-moon-spreads` влита в main; флаг 6а-0 принят как есть. Веб-проверка
      6а/6б **39/39** с честным красным прогоном, консоль чистая, 20 скриншотов
      в `docs/screenshots/51/`; 1501 тест в 40 сьютах, `tsc` чист; финальное ревью ветки прошло,
      его волна из 9 правок закрыта и переревьюирована. Отчёт — в конце спеки.
      **22.08: добавлена DEV-строка настроек «Лунный расклад: открыть окно»** (решение Артёма —
      иначе открытое окно до 27.08 не увидеть): подменяет «сейчас» моментом ближайшего события
      (`useDevMoonNow`), веб-проверка 9/9, описание — в отчёте спеки.
      ⚠️ **Вопрос Артёму (флаг 6а-0):** панель на экране луны пушит `/spreads/[id]` — маршрут внутри
      табов, а `/moon` живёт в корневом стеке. Из-за этого «назад» подписан «Расклады» и вернуться
      в лунный календарь нельзя, тогда как макет обещает «ЛУНА». Кодом не чинили: честная починка
      архитектурная, а подмена подписи сделала бы её ложью. Решить: принять и поправить макет
      либо завести отдельную задачу.
      ⚠️ Записанные решения, которые НЕ надо «чинить» как баги: 3–4 дня в году окно открыто,
      а панели на `/moon` нет (окно заезжает в соседний месяц, а строки события того месяца
      на экране нет по построению); приглушение строки события считается отдельно от окна панели
      (согласовано с приглушением клетки календаря того же дня).
      Спека `docs/specs/51-moon-spreads.md`, план `docs/plans/51-moon-spreads.md`.
      Исходная постановка: заведена 21.08 решением Артёма («берём следующей»). Вторая половина master-plan п. 11 (первая —
      экран календаря, задача 47). Два расклада по 4 карты: новолуние (`free`) и полнолуние
      (premium-бейдж), доступны в окно события; вне окна карточка показывается заблокированной
      с датой ближайшего события (решение 20). Опирается на готовое: цикл расклада из задачи 36,
      точные моменты луны из 47, `localMidnight` из 47б, макет `v-moonspread` из дорисовок 20.08.
      Решения 17–20 приняты Артёмом 20.08; роль редактора (названия позиций) делегирована
      Claude Code 21.08. Новое: чистый модуль `src/lib/moonSpread.ts` (правило окна), поле
      `moon` у расклада, панель на экране луны, бейдж события в списке. Persist НЕ меняется (10).
      ⚠️ В спеке один пункт ждёт подтверждения Артёма: окно считается КАЛЕНДАРНЫМИ сутками
      (день события ±1 день по локальной полуночи), а не ±24 часами от точного момента.
      ⚠️ es/pt нового контента заливает Cowork после мержа — задача **28м** (решение Артёма 21.08:
      «мержим, Cowork догоняет»); до неё es/pt показывают английский штатным фолбэком.

- [x] **52 · Реструктуризация CLAUDE.md (без потери содержимого)** — **ЗАКРЫТА 22.08**: CLAUDE.md
      грузился в контекст каждой сессии целиком (~168 тыс. знаков ≈ 55–70 тыс. токенов), из них 95 % —
      хроника задач 01–50 с 211 ⚠️-уроками, а не инструкции. Сделано: хроника перенесена ДОСЛОВНО
      в `docs/changelog.md` (архив + раздел новых записей), 196 уроков разложены по 13 темам
      в `docs/lessons.md` (тексты дословно, повторяющиеся правила — под одним заголовком со всеми
      подтверждениями), CLAUDE.md сжат до ~18 тыс. знаков: процесс (+шаг 0 «прочитай уроки по теме»,
      шаг 7 — куда писать хронику и уроки), правила, таблица доков (+2 строки), структура, 13 сквозных
      правил, короткий «Статус», указатель общих модулей. Проверено скриптом: вся история в changelog
      единым блоком, 196/196 уроков в lessons ровно по разу, 210/210 ⚠️ в архиве. Спека
      `docs/specs/52-claude-md-restructure.md`. ⚠️ Сделано в worktree на main, пока ветка 51 в работе:
      при мерже 51 конфликт в CLAUDE.md решать в пользу новой структуры, хронику 51 — в changelog.md
      и одной строкой в «Статус».

- [ ] **53 · Монетизация: Arcanum Premium (пейвол + гейты + RevenueCat)** — заведена 22.08 решением
      Артёма («давай начнём»). master-plan §3.4. Номер 52 занят реструктуризацией CLAUDE.md.
      **Спека `docs/specs/53-premium.md` — логическая часть согласована 22.08** (лимит тренажёра
      10 карт/день; истечение ничего не отбирает; ссылки условий — в 53б обязаны стать web-URL по
      Apple 3.1.2). **Решение Артёма 22.08: подписка живая с ПЕРВОЙ версии** — master-plan «запуск
      бесплатно, подписка в v1.1» отменён; платежи из РФ — отдельно потом.
      **Деление free/premium — решение Артёма 22.08 (по рекомендации сессии):** бесплатно — карта дня,
      рефлексия, справочник 78 карт ЦЕЛИКОМ (сферы и символика тоже: справочник — главная причина
      открыть приложение, решение 46б), дневник без лимита, курс М1–М2, расклады «Три карты» и
      «Новолуние», тренажёр одна порция в день; premium — курс М3–М6, остальные 7 раскладов (включая
      полнолуние, решение 19), тренажёр без лимита («Ещё N»). Цены — плейсхолдеры master-plan
      (год 2 890 ₽ ≈ −40 %, месяц 399 ₽), окончательные назначит Артём; пробный период не решён.
      ⚠️ **Ключевое ограничение:** `react-native-purchases` (RevenueCat) — нативный модуль, в Expo Go
      не работает; реальные покупки проверяются только на EAS dev-сборке/TestFlight (этап 6). Поэтому
      два захода: **53а** — право `premium` в сторе (persist → 11), гейты (курс/расклады/тренажёр),
      экран пейвола по макету, «Восстановить покупки», DEV-тумблер Premium, веб- и лайв-проверка
      в Expo Go; **53б** (этап EAS) — RevenueCat к готовому праву (узнать/купить/восстановить),
      аккаунты и продукты подписки — Артём.
      **Порядок:** (1) дорисовка макета Cowork — промт `docs/prompts/53-paywall-mockup.md` (экран
      `v-paywall` в двух состояниях, запертые состояния курса/раскладов/тренажёра, строка настроек);
      (2) брейншторм/спека `docs/specs/53-premium.md` + план; (3) ветка `feat/53-premium`.
      Кто что: Cowork — макет и es/pt строк пейвола; сессия — спека, код, проверки; Артём — цены,
      аккаунты RevenueCat/сторов на этапе 53б.

      **МАКЕТ ДОРИСОВАН 22.08 (Cowork).** `docs/design-reference.html`: экран `v-paywall` в двух
      состояниях, четыре точки входа и кнопка «Premium» в демобаре (три шага, как `__revState`
      у тренажёра: пейвол А → пейвол Б → обратно в приложение). Скриншоты — `docs/screenshots/53-paywall/`
      (пейвол А/Б в обеих темах, низ пейвола с юридической подписью, курс, расклады, тренажёр,
      настройки, плюс курс и тренажёр с активной подпиской). Смоук jsdom: 17 вью открываются,
      консоль чистая, проверки состояний зелёные. Дифф 228 строк, существующие экраны не тронуты.
      **Что пришлось решать самому, вопросы Артёму:**
      1. **Чип «✦ ПРЕМИУМ» у первого узла модуля не помещался над кружком** — налезал на шапку
         модуля (замер: низ чипа 667px против низа шапки 682px). Добавлен вариант `.below`:
         у первого узла чип уходит ПОД подпись, стрелка переворачивается. Тем же классом починен
         и «НАЧАТЬ УРОК» — он попадал бы в ту же ловушку, когда первый урок модуля становится
         текущим. Если вариант «под узлом» не нравится — альтернатива сдвинуть первую точку пути
         ниже, но это тронет геометрию всех модулей.
      2. **Второй тариф без подписи выглядел беднее выбранного** — добавил `small` «списывается
         ежемесячно» (в промте текста для месячной карточки не было). Если лишнее — снимается одной
         строкой.
      3. **Состояние «активна» показывает конкретную дату продления** («Продлится 22 сентября 2027»)
         — дата выдумана как плейсхолдер под годовой тариф; в коде это придёт из стора.
      4. ⚠️ **Экран длиннее одного экрана телефона**: «Восстановить покупки» и юридическая подпись
         видны только после прокрутки (скриншот `paywall-A-bottom.png`). Это следствие требований
         промта — четыре строки преимуществ, две карточки тарифов и полный юридический абзац.
         Если важно уместить CTA и подпись без скролла, надо резать список преимуществ до трёх строк
         или убирать эмблему-компас. Решение за Артёмом.
      5. `.lk` вынесен из `.sp` в общий селектор (в `.sp` остался только `margin-left:auto`) —
         как и требовал промт, стиль не дублируется. Текст в раскладах заменён с «PREMIUM» на
         «ПРЕМИУМ», как в приложении с hf-02.
      6. Генерация пути курса вынесена в функцию `renderCourse()` — демо-кнопка перерисовывает
         курс при переключении состояния. Раньше путь строился один раз на загрузке.
      **ПРИЁМКА МАКЕТА 22.08 (сессия): ПРИНЯТ**, смоук 16/16 по существу (две ошибки консоли
      `ERR_INVALID_URL` есть и на HEAD до дорисовки — не её), обе темы, демобар «Premium» ходит
      по трём шагам, все четыре точки входа ведут на пейвол с верной подписью «назад». Ответы на
      вопросы Cowork: **1** — в приложении чип остаётся НАД узлом: путь модуля начинается с отступа
      под чип (`TOP` в `CoursePath`, спека 07), вариант `.below` — обход геометрии самого макета,
      в код не переносится (пометка оставлена в CSS макета); **2** — подпись «списывается ежемесячно»
      у месячного тарифа снята (ничего не сообщает, лишняя строка i18n); **3** — ок, дата из стора;
      **4** — принято: CTA в первом экране (в приложении у пейвола нет таб-бара — места больше),
      «Восстановить покупки» и юридическая подпись под прокруткой — норма. ⚠️ Поправлено при приёмке:
      панель полнолуния на луне и карточка полнолуния в списке ВНЕ ОКНА были кликабельны → пейвол,
      а спека 51 требует «вне окна не нажимается — причина написана датой»; premium-гейт срабатывает
      только В ОКНЕ (лунный гейт первый). Клик снят, подпись «ПРЕМИУМ» оставлена. Замечено для плана 53а:
      «Ещё 3» в тренажёре макет рисует текстом + бейдж «✦ ПРЕМИУМ» (а не одной строкой) — план
      переведён на `PremiumBadge`; карточка полнолуния в списке несёт ОБА бейджа («○ СОБЫТИЕ» + «ПРЕМИУМ»).

      **53а ЗАКРЫТА 22.08 (все 10 задач детального плана `docs/plans/53-premium.md`).** Право
      `premium` в сторе (persist → 11, миграция под тестом через `useApp.persist.rehydrate()`);
      `src/lib/premium.ts` под тестами (модуль/урок/расклад заперты ⟺ `!free && !active`, лимит
      тренажёра 10 карт/день по `doneCount`); гейты курса/раскладов/тренажёра/лунной панели; экран
      `app/paywall.tsx` по принятому макету (оба состояния, диалог «появится в сборке» вместо
      покупки); DEV-тумблер Premium в настройках; контракт-тест `premiumSources.test.ts` (никто,
      кроме пейвола и настроек, не читает `premium.active`; прямое сравнение `.free` — только
      с маркером `// показ флага`), проверен экспериментом на все три ветки. Документы обновлены:
      logic-spec §7/§14, product-spec (§2/§4/§5 + новый §5а «Пейвол»), design-system §5. **1521 тест
      в 43 сьютах**, `tsc` чист. **Веб-проверка 6а/6б: 31 из 31**, красный прогон подтверждён
      (снятый гейт маршрута урока роняет ровно одну проверку); сценарий — `scripts/check_53_web.js`,
      отчёт и сценарий лайва — в конце спеки. **Лайв-проверка Артёма на iPhone ✓ 22.08**, ветка
      влита в main. Правки по лайву: бейдж «−40 %» сделан непрозрачным (сквозь `chipBg` 10 %
      просвечивала рамка карточки) ✓; место плашки «ПРЕМИУМ» в шапке модуля (рядом с замком,
      а не у правого края, как в макете) принято как есть.
      **Чекбокс 53 остаётся снят: ждёт 53б** — RevenueCat, продукты и цены на этапе EAS-сборки.

- [x] **54 · Юридические страницы (GitHub Pages), web-ссылки пейвола и «О приложении», дисклеймер
      онбординга** — заведена 22.08 по вопросу Артёма «разве нужна внешняя ссылка и лендинг?».
      Ответ (проверено по гайдлайнам 22.08): лендинг НЕ нужен; нужны публичные web-страницы —
      политика (обязательное поле App Store Connect и Play Console, Apple 5.1.1(i) «в метаданных
      И в приложении»), условия подписки (Apple: «app and App Store metadata must include links
      to Terms of Use and Privacy Policy»), страница поддержки (Support URL). Репо публичный →
      GitHub Pages бесплатный. Делаем: `site/` (privacy/terms/support/index, 4 языка, обе темы,
      без внешних ресурсов) + workflow Pages; URL-константы в `appInfo.ts`; `LinkTxt`
      (expo-web-browser, уже в зависимостях); ссылки пейвола → web-URL (снимает блокер 53б);
      три ссылки на «О приложении»; мелкий дисклеймер на шаге 1 онбординга (`ob.disclaimer`,
      пункт release-checklist); политика и `about.dataText` переписаны под релиз с RevenueCat
      (решение Д4); контракт-тест `site.test.ts` «текст страницы = текст приложения».
      Спека — `docs/specs/54-legal-pages.md` (решения Д1–Д5 ждут «ок»; Д2 — e-mail поддержки —
      требует ответа Артёма). Артёму после: Settings → Pages → Source: GitHub Actions.
      ⚠️ 6а-0: в макете `v-onboarding` дисклеймера нет — хвост дорисовки (Cowork).
      Вычитка es/pt новых строк и страниц носителем — следующая волна Cowork (промт после
      реализации). **Аналитика: решение Артёма 22.08 — в v1 НЕ ставим** (master-plan §4 и §6,
      анкеты сторов), политика прямо утверждает «аналитики и рекламы нет».
      **СДЕЛАНО 22.08 (ждёт лайв-проверки 6в):** `site/` — 4 страницы × 4 языка × 2 темы,
      палитра из `theme.ts`, ноль внешних запросов; `.github/workflows/pages.yml`;
      `appInfo.ts` (SITE/PRIVACY/TERMS/SUPPORT_URL), `LinkTxt` (expo-web-browser), ссылки
      пейвола и три ссылки «О приложении», дисклеймер онбординга ×4 языка;
      `about.dataText` переписан под релиз с RevenueCat. **1551 тест в 44 сьютах**, `tsc` чист.
      Контракт-тест `site.test.ts` 30/30 — красный прогон подтверждён (одно слово в
      `privacy.html` роняет ровно одну проверку). Веб-проверка: страницы 197/197
      (сломанный `lang.js` → 32 падения), приложение `scripts/check_54_web.js` **50/50**
      (подмена ссылки «Условия» на политику → 2 падения). Скриншоты — `docs/screenshots/54/`.
      Правка при приёмке: с `index.html` снята дублирующая строка «Ссылки:» и дата
      «Обновлено» (оглавление не обновляется, обновляются страницы).
      **ЗАКРЫТА 23.08: лайв-проверка Артёма ✓** («всё открывается и отображается»), Pages
      включены, сайт живой — приёмка живого сайта 122/122 (5 страниц × 4 языка × 2 темы,
      ноль внешних запросов, 404 работает и на вложенном адресе). Первый прогон деплоя упал
      («Configure Pages» до включения Pages) — перезапущен пушем страницы `site/404.html`.
      **URL для сабмита:** `https://sivyankof.github.io/arcanum/privacy.html` · `/terms.html`
      · `/support.html`. E-mail поддержки пока личный gmail — Артём заведёт ящик позже
      (решение 22.08), менять сразу в `feedback.ts` и `site/` (держит `site.test.ts`).

- [x] **55 · Предрелизная гигиена кода: патчи SDK 54, `reviewPrompt`, иконка пуша Android** —
      заведена 23.08 решением Артёма «берём то, что решается кодом, пока аккаунты и EAS откладываются»
      (пункты плана E + C + D). Аудит `npx expo-doctor` выполнен 23.08: 17/18, единственная находка —
      патч-версии внутри SDK 54 (`expo` 54.0.36→~54.0.37, `expo-constants`, `expo-file-system`,
      `jest-expo`); мажоров нет, правило «SDK не обновлять» не задето. Три пункта одним заходом:
      (1) патчи через `npx expo install …` (смотреть дифф `app.json` — урок задачи 12); (2) хвост 45б —
      формула подсказки тренажёра из `app/review.tsx` переезжает в `src/lib/review.ts` как
      `reviewPrompt(card, lang)`, тесты зовут её же + контракт по исходнику; (3) иконка пуша Android —
      монохромный силуэт эмблемы 96×96 скриптом `scripts/gen_notification_icon.py`, плагин
      `expo-notifications` в `app.json` (`icon`, `color #caa45a`, `defaultChannel daily`), контракт-тест
      на PNG. Спека — `docs/specs/55-prerelease-hygiene.md`. После мерджа Артёму: `npm install` +
      перезапуск `--tunnel`.
      **ЗАКРЫТА 23.08** — лайв-проверка Артёма ✓ (четыре коммита в main; отдельно подтверждён на устройстве тренажёр: у рубашек toCard в подсказке стоит «···», имя карты не проглядывает — то есть перенос формулы в `reviewPrompt` поведение не изменил): `expo-doctor` 17/18 → **18/18**,
      тестов 1551 → **1561 в 45 сьютах**, `tsc` чист, веб-проверка `scripts/check_55_web.js` — 7 из 7
      с подтверждённым красным прогоном. Отчёт и сценарий лайва — в конце спеки.
      ⚠️ Находка сверх плана: `expo install` продублировал `jest-expo` (новая версия в `dependencies`,
      старая осталась в `devDependencies`) — дубль снят руками, урок дописан в lessons.
      ⚠️ Хвост 45б закрыт этой задачей (формула подсказки + контракт по исходнику).

- [x] **28р · Второй заход по списку 28е: раздел C + 13 адресов механики (48 вопросов)** —
      заведена 23.08, последний хвост 28е. Ядро (26) и «только механика» (7) исправлены 21.08 и
      перезалиты 28п; раздел C — пометки решателей без механической опоры, часть — рационализация
      (у агентов ноль промахов на 61 пометке). Правило: **вердикт по каждому вопросу, правка списком
      запрещена**; правятся только дистракторы ru/en синхронно, es/pt — Cowork (задача 28с по промту
      `docs/prompts/28r-es-pt.md`). Механика 21.08 оформляется скриптом `scripts/check_quiz_guess.py`
      (абсолютные маркеры только в дистракторах, длины, страж «верный/correct/q/explain не тронуты»
      против HEAD) — калибровка на HEAD до правок обязана дать 0. Спека —
      `docs/specs/28r-guessable-second-pass.md`.
      **ЗАКРЫТА 23.08** — лайв-проверка Артёма ✓ (три коммита в main): периметр вырос до 48 вопросов
      (калибровка нашла 13 адресов сверх раздела C), исправлен 41 дефект, 7 оставлено с обоснованием.
      Метрики: категоричность 13 → 0, длины 5 → 0, `check_canon --scope course` 233 → 225, страж 0,
      1561 тест. Слепой прогон изменённых: 42/42 верно, 13 пометок, из них 3 настоящие — исправлены
      вторым проходом. ⚠️ Правка трижды внесла дефект того же класса, все три поймали проверки
      (подробности в отчёте спеки). Хвост — задача **28с** (es/pt, Cowork).

- [x] **56 · Сверка приложения с макетом после задач 50–54 (плановый аудит)** — заведена 23.08 по
      правилу «раз в ~5 задач»; за проект ни разу не делалась. 16 пар «экран ↔ вью макета» в обеих
      темах скриптом `scripts/shoot_56.js` → `docs/screenshots/56/`, чтение по `ui-verification.md`
      §6а, вердикт по каждой паре; порог «чиним здесь» ≤30 строк без спорных решений, остальное —
      задачи 58+ и промт дорисовок Cowork; один полный 6б-проход. Известные принятые отличия
      (CLAUDE.md «Решения») находками не считать. Спека — `docs/specs/56-design-audit.md`.
      **ЗАКРЫТА 23.08** — лайв-проверка Артёма ✓ (разделитель «Раскладов» и подписи у кнопки «назад» на iPhone прошли): снято 16 пар × 2 темы (64 скриншота), прочитаны
      сессией и тремя агентами-сверщиками. Итог: **1 расхождение в коде исправлено** (разделитель
      шапки «Расклады»), **1 функциональный пробел** заведён задачей 59 (строка «Имя и дата
      рождения»), **8 дорисовок макета** — задача 60. Проход 6б — 23 из 23, консоль чистая
      (13 warnings, все `props.pointerEvents is deprecated`). ⚠️ Аудит поймал ТРИ дефекта своего
      сида (карточка аркана, дневник, карта месяца не попадали в кадр) и ЧЕТЫРЕ дефекта своих
      проверок (неуникальный маркер, плейсхолдер вместо текста, тумблер вместо пикера,
      регистронезависимый поиск) — подробности в отчёте спеки.

- [x] **60 · Дорисовки макета по итогам аудита 56 — ВЫПОЛНЕНО 23.08 (Cowork)** — все восемь мест
      закрыты, приёмка `scripts/check_60_mock.js` 8 из 8, каждая проверка подтверждена своей
      мутацией; отчёт — `docs/specs/60-mockup-tails.md`. **Приёмка 23.08: 8/8 подтверждены,
      плюс добавлена девятая дорисовка**: задача 59 разбила строку «Имя и дата рождения»
      на две, и макет снова отстал от приложения — тот самый класс, ради которого заводилась 60.
      Строка разбита, в `check_60_mock.js` проверка 9 (мутация `--mutate 9` красная), итог **9/9**. Отступление от промта одно и намеренное:
      список экранов без таб-бара сделан полным по дереву `app/` (десять вью вместо названных
      аудитом четырёх), иначе таб-бар остался бы на настройках и пейволе. Промт
      `docs/prompts/56-mockup-tails.md`. Восемь мест, где `design-reference.html` отстал от
      приложения или противоречит спеке: таб-бар на четырёх экранах-стеках, активный таб не
      переключается, у `v-lesson` нет «назад», у `v-spread3` потерян счётчик карт, на `v-profile`
      нет шестерёнки, в `v-settings` порядок строк против product-spec, «Вкл» покрашено accent
      против design-system, «Экспорт дневника» вместо двух строк экспорта и импорта.
      НЕ блокирует релиз, но чем дольше макет расходится, тем выше риск «починить» приложение
      по устаревшему эталону.

- [x] **62 · Пейвол без покупок: честное состояние вместо плейсхолдерных цен** — найдено 25.08
      при разборе «что дальше» после верификации Google. `src/lib/purchases.ts` — заглушка 53а:
      `getOffers` ВСЕГДА возвращает `PLACEHOLDER_OFFERS` («2 890 ₽ / 399 ₽», бейдж «−40 %»), а
      `purchase`/`restore` отвечают `unavailable`, и экран показывает диалог «появится в сборке
      из стора». В Expo Go это честно, но в сборке из магазина получается витрина с ценами
      в рублях (аккаунт продавца — Беларусь, валюта USD) и кнопкой, которая ничего не делает.
      **Блокер закрытого теста и любого ревью**: закрытые треки Google ревьюит, а тестеры увидят
      мёртвую покупку. Что делать: при `PURCHASES_AVAILABLE === false` (и при пустом ответе
      магазина — это уже 53б) экран показывает список возможностей Premium и нейтральную строку
      «подписка появится в следующем обновлении», без цифр и без CTA; плейсхолдеры остаются
      только для веб-проверки и макета. Заодно закрывается класс «магазин не ответил → показали
      выдуманную цену», который иначе приедет вместе с 53б. Спека нужна, ~30 строк кода.
      **26.08: спека сверена с кодом, план готов** — `docs/plans/62-paywall-no-store.md` (раздел
      «Вердикт»: Д1′ тристейт предложений, Д3′ «Восстановить» внутри состояний, Д4′ без
      `purchases.web.ts` — веб-проверка спеки противоречила сама себе). **«Ок» Артёма 26.08**:
      текст панели нейтральный («Оформить подписку пока нельзя / Попробуйте позже…»), порядок
      работ **61 → 53б → закрытый тест** — закрытый тест идёт уже с настоящими покупками, 62 —
      страховка внутреннего теста и постоянное состояние «магазин не ответил» после 53б.
      **ЗАКРЫТА 26.08** (лайв-проверка Артёма на iPhone ✓ — «всё работает»): восемь коммитов, `getOffers()` → `[]` на всех
      платформах, плейсхолдеры удалены, панель «скоро» вместо тарифов, «Восстановить» внутри
      состояний, диалог без Expo Go, макет получил блок `#pwSoon` и хук `mockSetup` в аудите.
      Сценарий 6б `scripts/check_62_web.js`: красный 31 из 58 → зелёный 58 из 58, мутация
      роняет 24. Тестов 1704 в 47 сьютах. Отчёт — в спеке.

- [x] **63 · Материалы витрины Google Play (снимаются без телефона)** — найдено 25.08: чтобы
      заполнить Store listing, нужны три вещи, которых в репозитории нет и которые не значились
      в release-checklist: **иконка 512×512 PNG** (делается из `assets/images/icon.png` скриптом),
      **баннер 1024×500** («Feature graphic» — обязателен, без него витрина не сохраняется) и
      **минимум 2 скриншота телефона**. Скриншоты можно снять БЕЗ устройства: Playwright с
      вьюпортом 430×932 и `deviceScaleFactor: 3` даёт ровно 1290×2796 — это же принимает
      App Store как 6.7". Для Google соотношение сторон скриншота не должно превышать 2:1,
      поэтому наш обычный 390×844 (2.16) не подходит — снимать 390×780 либо добавлять поля.
      Баннер рисуем в HTML по «Небесному золоту» (`design-system.md`) и снимаем тем же
      Playwright. Спека нужна; порядок: после 62, до закрытого теста.
      **26.08: спека и план написаны** — `docs/specs/63-store-assets.md`, `docs/plans/63-store-assets.md`:
      кадры компонуются на холст 1080×1920 (Google) / 1290×2796 (Apple) с подписями, 7 кадров ×
      4 языка, тёмная тема + один светлый кадр, карты без наготы, контракт-тест `storeAssets.test.ts`.
      Семь вопросов Артёму — в конце спеки. Ждёт «ок».
      **26.08: конвейер и материалы РЕАЛИЗОВАНЫ, задача ЕЩЁ НЕ ЗАКРЫТА.** `python scripts/store_assets.py
      icon` пишет иконку 512×512, `node scripts/shoot_63.js` снимает и компонует 28 кадров
      (7 экранов × 4 языка, 1080×1920 JPEG) и 4 баннера 1024×500, `store_assets.py feature`/`verify`
      снимают альфу и проверяют набор целиком; все материалы закоммичены в `docs/store/`. Контракт
      `storeAssets.test.ts` (10 проверок), общий парсер заголовков `src/lib/imageHeaders.ts`.
      Тестов **1860 в 48 сьютах**, `tsc` чист. Полный прогон на финальном коде: 28 снято / 0 не
      снято, `verify` зелёный. Остаются два человеческих критерия: Артём смотрит 7 кадров ru +
      баннер ru на ноутбуке — **сделано 26.08, Артём принял без замечаний** («кадры нравятся»).
      **26.08: материалы ЗАГРУЖЕНЫ в Play Console — задача ЗАКРЫТА.** Витрина заполнена на всех
      четырёх языках (ru-RU по умолчанию + переводы en-US / es-419 / pt-BR): название, краткое
      и полное описания копией из `docs/store-listing.md`, значок 512×512, свой баннер 1024×500
      на каждый язык и свои 7 скриншотов телефона в порядке 01→07. Сохранено кнопкой «Сохранить»
      (решение Артёма 26.08) — статус страницы «Готово к отправке на проверку», на проверку
      НЕ отправляли. Проверено после перезагрузки страницы: тексты во всех четырёх языках
      побайтово равны `store-listing.md`. Как это делалось автоматизацией и на что там ловятся —
      в `docs/release-checklist.md`, раздел «Заливка витрины: как прошла 26.08».
      Apple-набор (`--store apple`, 1290×2796) снимается тем же скриптом, но
      `docs/store/apple/` — в `.gitignore` до аккаунта Apple; вопрос про `supportsTablet` в
      `app.json` — отдельная задача **63б**. Отчёт с числами и мутациями — в конце спеки.

- [~] **61 · EAS Build: проект, профили сборки, первая сборка** — заведена 25.08, первый пункт
      после исчерпания очереди «решается кодом». Expo-аккаунт `art9`, проект `@art9/arcanum`
      (`eas init` → `owner` + `extra.eas.projectId` в `app.json`), `eas.json` с профилями
      `preview` (internal, Android APK / iOS ad hoc) и `production` (AAB, `autoIncrement`,
      `appVersionSource: remote`). Без `development`-профиля (нужен `expo-dev-client` — придёт
      с RevenueCat в 53б) и без EAS Update. **Сделано сессией 25.08**: проект, `eas.json`, спека.
      **26.08: ПЕРВАЯ СБОРКА ПРОШЛА** — Артём запустил интерактивно, keystore сгенерирован
      в облаке Expo, `versionCode` 1, статус `finished` за 41 минуту (большая часть — очередь).
      APK 118 МБ, состав проверен распаковкой: 78 JPEG карт на месте, бандл 6.07 МБ, шрифты.
      ⚠️ Команда в промте была неверной: `npx eas` падает с `could not determine executable
      to run`, пакет называется `eas-cli` — правильно `npx eas-cli@latest build …`.
      ⚠️ Сборка идёт под все 4 архитектуры, включая `x86_64`, — значит **APK ставится
      на обычный эмулятор Android**, и часть пункта 6в (иконка приложения, иконка пуша,
      русские числительные на том же Hermes с урезанным ICU) закрывается на компьютере,
      не дожидаясь телефона. Хаптика и резкость — по-прежнему только на устройстве.
      **Ждёт**: установка APK и лайв-проверка Артёма; затем `production` (AAB) для внутреннего
      теста; iOS — после одобрения Apple. **26.08 добавлен `.easignore`** (архив сборки
      305 МБ → 31.7 МБ; страж `python scripts/check_easignore.py`) — подтвердится на следующей
      сборке. Спека — `docs/specs/61-eas-build.md`.

- [x] **57 · Тексты для сторов на четырёх языках** — **ЗАКРЫТА 25.08** (приёмка = чтение Артёмом) — заведена 23.08; единственная часть
      «Магазины: тексты и материалы» release-checklist, которую можно сделать без аккаунтов. Один файл
      `docs/store-listing.md`: название/подзаголовок/промо/описание/ключевые слова iOS, короткое и
      полное Google, RuStore = Google ru, заметки ревьюеру; лимиты держит контракт-тест
      `storeListing.test.ts` (как `site.test.ts` держит «страница = приложение»); запрещённые слова
      (гарантии, предсказания, лечение) — тестом. Вычитка редактором и нейтивами — после, волной Cowork.
      Скриншоты сторов — не здесь (девайс/симулятор). Спека — `docs/specs/57-store-listing.md`.
      **НАЧАТА 23.08**: `docs/store-listing.md` написан — название/подзаголовок/промо/полное
      описание/ключевые слова/«что нового» на четырёх языках, плюс категории, рейтинг, ответы
      анкет приватности и заметки для ревьюера (en). Факты о приложении сверены по коду и вынесены
      таблицей в шапку файла (78 карт, 6 модулей, 32 урока, 160 вопросов, 10 раскладов из них
      3 бесплатных, 13 блоков значения на карту). es/pt помечены как черновик без носителя.
      **СДЕЛАНО 23.08 (ждёт чтения Артёмом и выбора названия)**: контракт-тест
      `src/lib/__tests__/storeListing.test.ts` — 122 проверки (длина в символах Unicode, правила
      ключевых слов, запрещённые корни с учётом отрицания, «бесплатно» не в заголовке,
      периметр языков и полей, числа из кода); красный прогон — 5 мутаций документа,
      каждая роняет свою проверку и называет поле. Ключевые слова переписаны: черновик
      повторял слова названия и подзаголовка (ru теряла 4 слова из 12, en — 4, es — 3, pt — 2).
      release-checklist: три пункта текстов и два пункта решений (рейтинг, анкеты) → `[~]`.
      Промт вычитки носителем — `docs/prompts/57-store-native.md` (задача 57н ниже).
      Отчёт — `docs/changelog.md`. **Название утверждено Артёмом 23.08** — рамка
      «обучение таро» («курс таро» и «таро для начинающих» отклонены).
      **ЗАКРЫТА 25.08**: Артём прочитал `docs/store-listing.md` («всё окей») — приёмка этой
      задачи это чтение, а не телефон. Пункты release-checklist остаются `[~]` до вставки
      текстов в формы магазинов при сабмите.

- [x] **57н · Вычитка носителем текстов магазина (es/pt)** — **ЗАКРЫТА 23.08 (Cowork)** — работа Cowork, промт
      `docs/prompts/57-store-native.md`. es и pt в `docs/store-listing.md` написаны сессией
      и носителем не читаны (28н закрыта 21.08, дописывать было некуда). Отличие от
      прежних волн: текст маркетинговый, его читают вслух и калька слышна сразу.
      ⚠️ Португальский подзаголовок — ровно 30 из 30 символов, запаса нет. Термины обязаны
      совпадать с интерфейсом (`tirada`/`tiragem`, `Entrenador`/`Treinador`, `racha`/`sequência`).
      Заодно той же волной удобно взять висящую вычитку строк и страниц задачи 54.
      **Приёмка 23.08 зелёная**: пять полей на язык переписаны, ru/en и таблица фактов не тронуты,
      `storeListing.test.ts` 122/122, длины сверены независимо. ⚠️ **испанские ключевые — 100 из 100**,
      pt-подзаголовок — 30 из 30: правка этих двух полей только с пересчётом (кандидат на выброс
      в es — `mazo`, дублирует `baraja`). Отчёт — конец `docs/localization-plan.md`.
      **Решения Артёма 24.08 применены**: название pt — `Arcanum — Aprenda a Ler Tarô` (28/30,
      рекомендация носителя), es оставлено `Aprende Tarot` (рамка одна с ru/en); термины —
      pt `Treinador→Treino`, `cartões→flashcards`, `sequência→ofensiva`; es `manual→guía`
      (`Entrenador` оставлен). Переименование прошло по ВСЕМ местам термина, а не только по тексту
      магазина: `i18n.ts` (5+2), `content/phrases.json` (7 вариантов пушей и заморозки),
      `site/privacy.html` (зеркало политики — контракт `site.test.ts`, мутация подтверждена),
      тексты магазина. ⚠️ Согласование вокруг нового слова проверялось глазами (оба слова
      женского рода, артикли и прилагательные не меняются) — строка для следующей волны
      Cowork: перечитать семь фраз `phrases.json` с новым термином целиком.

- [x] **59 · Настройки: строки «Имя» и «Дата рождения» (пробел против product-spec)** — **ЗАКРЫТА 23.08** — НАЙДЕНО
      аудитом 56 (23.08). `docs/product-spec.md` §5 перечисляет строки настроек и прямо требует:
      «**Имя и дата рождения** (редактирование; если дата не указана — кнопка „рассчитать аркан")».
      В приложении этой строки нет, а в шапке `app/settings.tsx` до сих пор висит комментарий
      «имя и „о приложении" остаются на очереди — задачи 12 и 13» — обе задачи давно закрыты,
      «о приложении» сделано, а строка имени потерялась вместе с этим комментарием.
      **Что реально доступно сейчас:** дату рождения можно указать ОДИН раз — пунктирной плашкой
      «Указать дату рождения» на профиле (`BirthArcanaCard` → `DatePicker` → `setBirthDate`), и то
      только пока она пустая: с непустой датой карточка показывает аркан и нажатие ничего не делает.
      **Имя не редактируется нигде** — экшена `setName` в сторе нет вовсе, имя задаётся один раз
      в онбординге. Опечатался при первом запуске — живёшь с ней.
      **Объём (почему не «чиним здесь»):** экшен `setName` в сторе, строка настроек с диалогом ввода
      (общий `ModalPanel` + поле), перевод строки в четыре языка, возможность СМЕНИТЬ уже указанную
      дату (перевести карточку аркана в нажимаемое состояние или дать строку в настройках),
      пересчёт `birthArcanaId` при смене даты (`setBirthDate` это уже умеет), тесты.
      persist не трогается — поля `name`/`birthDate` в схеме уже есть.
      ⚠️ Решить при спеке: менять ли дату из профиля (нажимаемая карточка аркана) ИЛИ только
      из настроек — два входа к одному действию делать не стоит.
      **СДЕЛАНО 23.08 (ждёт лайв-проверки 6в)**: две строки настроек (решение Артёма
      23.08 — две, а не одна; product-spec §5 переписан по 6а-0), экшен `setName`
      (пустое имя УДАЛЯЕТ ключ), общий `TextPrompt`, `NAME_MAX`+`normalizeName` через
      существующий `normalizeText` (их же начал звать онбординг), дата — существующим
      `DatePicker` с `value`. Второго входа из профиля НЕ делали (тап карточки занят).
      persist не поднимался (11). **1695 тестов в 46 сьютах**, красные прогоны: 3 мутации
      юнитов + 3 мутации веб-сценария. 6б — `scripts/check_59_web.js` **20/20**, 6а —
      `scripts/shoot_59.js`, 8 кадров в `docs/screenshots/59/`. ⚠️ es/pt новых строк написаны
      сессией — долг вычитки носителем дописан в периметр волны 57н.
      **Лайв-проверка Артёма 23.08 ✓** («всё отработало корректно») — задача закрыта.

- [x] **28с · Перезаливка es/pt у дистракторов, изменённых задачей 28р** — **ЗАКРЫТА 23.08 (Cowork)** — работа Cowork
      (переводы Claude Code не пишет), промт `docs/prompts/28s-es-pt.md`. Периметр: 116 адресов,
      42 вопроса в `content/quiz-*.json` (список — `docs/specs/28r-changed-addresses.md`); правились
      только ru/en, переводы отстали от канона. Прецедент — 28п (закрыта 23.08), правила те же.
      Статусы остаются `reviewed`: меняется формулировка дистрактора, не смысл вопроса.
      Ориентир приёмки: `check_canon --scope course` ≤ 225, `check_quiz_guess --against` = 0.
      ⚠️ Девять адресов правились только в английском (русский уже был чист) — переводить их
      всё равно с пары ru+en. ⚠️ Если задача 58 будет сделана раньше, часть вопросов может быть
      переписана — сверяться с актуальным JSON, а не со списком из промта. НЕ блокирует релиз.
      **Приёмка 23.08 зелёная** (прогнана самостоятельно, не со слов Cowork): 126 адресов
      (63 варианта × два языка), `merge_quiz` идемпотентен (160 вопросов, дифф пустой),
      `check_translation --scope course --status reviewed` — 0, `check_canon --scope course` — **221**
      при ориентире 225, `check_quiz_guess --against c02e4ec` — **0** (слепота снята
      `--mutate correct` → 3 находки), `npm test` 1695/1695.
      ⚠️ Отчёт Cowork говорит «`check_edits` все классы по нулю, кроме 6» — на самом деле
      класс 9 даёт **8 находок**. Разобраны поимённо: все восемь — ложные, повторяющийся
      фрагмент есть грамматический каркас параллельных вариантов (`Que o resultado…`,
      `uma das cartas`, `Ele trata do…`), и РУССКИЙ ОРИГИНАЛ держит ту же параллель дословно
      («Итог…/Итог…», «Она про…» × 3) — правило «прежде чем винить перевод, проверь оригинал».

- [x] **58 · Картинка выдаёт ответ в карточных вопросах — ОСТАВЛЕНО КАК ЕСТЬ (решение Артёма 23.08)** —
      **Решение принято после разбора: не чиним ни группу A, ни группу B.** Возврат — только новым
      явным решением, не «переоткрыть как идею» (правило раздела решений).
      Что это значит на практике: 25 карточных вопросов из 160 (16 % экзамена) проверяют узнавание
      картинки, а не знание карты; в английской версии подпись на скане совпадает с текстом верного
      варианта дословно. Курс от этого не ломается — карточные вопросы стоят по одному в уроке
      и идут ПОСЛЕ теории и разбора, то есть работают как закрепление, а не как проверка.
      Если после релиза появится сигнал (жалобы «тест слишком лёгкий», аномально высокий процент
      верных ответов в телеметрии v1.1) — вернуться к записанному ниже разбору, он готов.
      НАЙДЕНО 23.08 в ходе задачи 28р (там правятся формулировки дистракторов,
      здесь вопрос не работает вовсе). У вопроса `type: card` экран урока показывает изображение
      карты `cardId` НАД текстом вопроса (`app/lesson/[id].tsx`, строки ~240–248). На сканах
      Райдер–Уэйта внизу напечатано имя карты («THE EMPRESS.», «THE TOWER», «JUSTICE») — и в
      25 вопросах из 28 верный ответ РАВЕН имени показанной карты. Ответ буквально нарисован.
      ⚠️ Почему не нашли раньше: дефект живёт в СВЯЗКЕ «картинка + варианты», а не в тексте.
      Ни одна текстовая проверка его не видит, а слепые решатели задачи 28е получали только
      текст вопроса и варианты — без картинки (так был устроен метод), поэтому по построению
      не могли его заметить. Родня правила «дефект уровня корпуса не виден автору отдельного текста».
      **Два разных класса, чинятся по-разному:**
      - **A — 14 вопросов «какая это карта?»** (`m3l2-q3`, `m3l3-q2`, `m3l4-q3`, `m3l5-q2`, `m3l6-q3`,
        `m4l1-q5`, `m4l2-q5`, `m4l3-q5`, `m4l4-q5`, `m4l7-q5`, `m4l8-q5`, `m5l1-q5`, `m5l2-q5`,
        `m5l3-q5`). Картинка нужна по жанру — значит убрать с неё ПОДПИСЬ: показывать в викторине
        кадр без нижней полосы с именем. Инструмент уже есть — `scripts/fetch_card_scans.py` умеет
        кадрировать по рамке (задача 42); нужен второй вывод (например `assets/cards/quiz/`) или
        обрезка на лету. ⚠️ Точную границу подписи мерить отдельно: грубый замер по яркости ловит
        тёмные места рисунка (трава, кусты), а не рамку, — нужна приёмка контакт-листом, как в 42.
      - **B — 11 вопросов «какая карта означает X?»** (`m2l1-q4`, `m2l2-q3`, `m2l3-q2`, `m2l4-q4`,
        `m2l5-q2`, `m2l5-q3`, `m2l6-q4`, `m3l1-q2`, `m4l5-q3`, `m4l6-q3`, `m5l4-q5`). Здесь картинка
        ЛИШНЯЯ в принципе: вопрос самодостаточен текстом, а показ карты и есть ответ — даже без
        подписи её узнаёт любой, кто только что прошёл урок про неё. Чинится данными: снять
        `cardId`/сменить `type` на `single`. Дешёвая половина задачи.
      **Эталон правильного жанра — 3 вопроса М6** (`m6l2-q5`, `m6l3-q5`, `m6l4-q5`): показана карта,
      а варианты — трактовки чтения, имя карты в ответах не участвует. Так должны выглядеть все
      карточные вопросы, где картинка остаётся.
      **Решение Артёма нужно по A:** кадрировать подпись (сохраняет жанр «узнай карту») ИЛИ
      переписать эти 14 вопросов в жанр М6 (дороже, но снимает зависимость от картинок).
      Контракт-тест после починки: «у вопроса type card верный вариант не равен имени карты
      cardId ни на одном языке» — он же не даст дефекту вернуться.

- [ ] **41 · Дневник при большом числе раскладов за день + вытеснение старых записей** — **ОТЛОЖЕНА В v2 (решение Артёма 23.08)** — заведено
      16.08 по вопросу Артёма на лайв-проверке задачи 36. Сейчас каждый сохранённый расклад — своя
      строка ленты, потолок истории `SPREADS_MAX = 100` (logic-spec §7), при переполнении молча
      отрезаются самые старые. Технически ничего не ломается: лента виртуализована (`FlatList`),
      месяц показывается по одному. Но три следствия, которые проявятся у активного пользователя:
      1. **День превращается в стену одинаковых строк.** 50 сохранений за день — 50 строк
         «Три карты · …» подряд под одной датой; читать невозможно. Возможное решение: сворачивать
         расклады одного дня в одну строку-счётчик («5 раскладов»), раскрывать по тапу. ⚠️ Затронет
         `journalOfMonth`/`journalKey`/`filterJournal` и счётчики чипов — то есть чистую логику,
         которая сейчас покрыта тестами; менять вместе с ними.
      2. **Вытеснение при 100 записях происходит без предупреждения.** Расклад просто пропадает
         из дневника, и восстановить его нечем (удаления и редактирования записей в v1 нет).
         Решить: предупреждать заранее, поднять потолок, либо явно сказать про лимит в интерфейсе.
      3. **XP: 50 сохранений = 250 XP за день.** Это частный случай вопроса, закрытого решением
         задачи 22 («XP в v1 не меняем, сток/добор/дневной потолок — обсуждение в v2»); здесь он
         всплывает конкретным сценарием. Единственный нынешний тормоз — что для сохранения надо
         честно открыть все карты расклада.
      **Решение 23.08: всё три следствия решаем в v2**, в v1 не трогаем вовсе — подтверждение
      оценки 16.08 после повторного разбора.
      Оценка Артёма и моя на 16.08: для v1 терпимо, потому что 50 сохранений подряд — поведение
      неестественное, а не путь наименьшего сопротивления. Задача заведена, чтобы не всплыло
      внезапно на живых пользователях.

- [x] **28 · Локализация контента ES + PT-BR** — **ЗАКРЫТА 21.08, весь трек L-0…L-5 пройден
      и принят** (отчёты сессий и приёмок — `docs/localization-plan.md`). Итог: испанский
      и португальский готовы к релизу по Definition of Done — 100% контента (3103 записи
      на язык: 958 блоков карт, 78 наборов слов, 32 теории, 160 вопросов, 8 раскладов),
      100% UI-строк (267, полноту стережёт контракт `i18nPlurals`), дисклеймер и политика
      переведены, статусы `reviewed` подняты только `--lang es`/`--lang pt` (1105 единиц
      на язык), канон ru/en не тронут. Корпусные проверки `check_canon.py` перенесены
      на новые языки целиком — включая те, что план числил непереносимыми: словари
      `scripts/dict_es.py` и `scripts/dict_pt.py` откалиброваны на живом корпусе, перенос
      проверен приёмкой мутациями (предсказание, медицина, род читателя ловятся поимённо).
      Вне трека остаётся только ASO: тексты стора на es/pt — задача Артёма.
      Хвост без срока: нейтивы предложили добавить в реестр указателей es «en la pareja»
      и pt «na relação» для блока `love` — это правка `content-guide.md`, решать с редактором.
      История трека ниже. — L-0 и L-1 СДЕЛАНЫ И ПРИНЯТЫ (20.08, отчёты
      сессий и приёмок в docs/localization-plan.md): UI-строки 267×2, phrases/composition,
      Старшие арканы 572 блока залиты атомарно со статусом draft; приёмка L-1 — 1454 теста
      зелёные (два устаревших контракта переписаны на синтетическую фикстуру + обратный
      контракт «переведённая карта ищется своими словами»), UI-прогон es/pt 12/12, скриншоты
      `docs/screenshots/28-l1/`. Оба флага отчёта L-1 закрыты решениями 20.08: (1) предсказания
      и медицинская рамка в русском КАНОНЕ (5 адресов) — вопрос редактору, editor-questions.md
      шестая волна; (2) зачины — РЕШЕНО «единообразные указатели, как в каноне», исполняет
      шаг 0 промта L-2 (`docs/prompts/L-2-wands-cups.md`). Осталось: L-2/L-3 (масти),
      L-4 (курс), L-5 (приёмка + статусы reviewed). — В РАБОТЕ с 20.08: сессия **L-0 принята**
      (отчёт Cowork — в конце `docs/localization-plan.md`; приёмка CC-L2: 1453 теста зелёные,
      сьют i18nLangs перевёрнут с «языка без ресурсов» на контракт «es/pt отдают СВОИ строки»
      (красный прогон на HEAD без L-0 подтверждён) + новый контракт паритета {{плейсхолдеров}};
      UI-прогон es/pt — 5 экранов без переполнений и ошибок консоли, пикер 4 языка,
      скриншоты `docs/screenshots/28-l0/`). Правка приёмки: в `phrases.json` у
      `push.streak_save[0]`/`push.freeze_saved[0]` es/pt переведены с `{n}` на `{days}` —
      «racha de 1 días» ломалась ровно классом «из 1 дней» (hf-02), а у freeze-пуша n=1 реален;
      en с `{n}` законен («3-day streak» не склоняется). Флаг из отчёта Cowork этим закрыт.
      Дальше: «запускай L-1» (Старшие арканы). Исходная постановка: (решение 12.08: v1 на 4 языках; ответственность —
      Cowork целиком, БЕЗ внешних переводчиков — решение 14.08, план и конвейер качества:
      docs/localization-plan.md, сессии L-0…L-5). СТАРТ: после редактуры канона RU женой
      (минимум Старшие арканы) И заморозки строк (все экраны v1 готовы).
      **Предусловие — задача 28а — ЗАКРЫТА 19.08** (спека и отчёт
      `docs/specs/28a-per-language-status.md`, план `docs/plans/28a-per-language-status.md`,
      ветка `feat/28a-per-language-status` влита в main, merge 5bbb5c1): статус контента стал словарём по языкам, заливать
      переводы теперь есть куда. Все четыре развилки решены Артёмом по рекомендациям, периметр
      максимальный (staging-викторины + `wordsStatus` на name/keywords/search). Мигрировано
      958 блоков карт + 78 wordsStatus + 32 теории + 32 quizStatus + 5 staging-файлов, все
      в `{ru: reviewed, en: reviewed}`; `content_stats.py` считает готовность по языкам.
      Заливать перевод карты АТОМАРНО (см. п. 5 ниже) — под это и заведён общий `wordsStatus`.
      Исходная формулировка: пер-языковый статус
      в схеме контента** — сейчас `status` у блока ОДИН на все языки (`{ru, en, status}`),
      «русский вычитан, испанский черновик» записать негде; решить схему (например `status`
      → объект по языкам с миграцией `build_cards.py`/`set_status.py`/`content_stats.py`
      и контракт-тестов) ДО заливки первого перевода, пока дёшево. Claude Code в этой
      задаче только принимает файлы (CC-L2 плана): валидация схемы, content:stats по языкам,
      тест «нет блока без 4 языков», UI-прогон скриншотов es/pt (испанский ~25% длиннее русского).
      ⚠️ **Проверять КАЖДЫЙ язык отдельно — уроки задачи 25 (15.08), где EN разошёлся с RU тихо:**
      1. **Дописанное переводчиком.** Два текста Мечей начинались с «At work», хотя в русском
         «на работе» не было вовсе — переводчик добавил рамку от себя. Проверка: сверять зачин
         и наличие «лишних» обстоятельств места/времени против оригинала; на 312 текстах глазами
         это не видно — ловится сравнением первых слов пары ru/en.
      2. **Однообразие в переводе не наследует однообразие оригинала.** У русских текстов зачины
         были разнообразны, а в английском «Your body…» открывало 11 текстов из 78 — переводчик
         сводит разные обороты к одной удобной конструкции. Считать повторы первых двух слов
         ОТДЕЛЬНО по каждому языку (скрипт задачи 25 — в отчёте спеки), порог «3+ повтора».
      3. Пункты 1–2 — не про качество отдельной строки, а про корпус: обе находки прошли
         построчную проверку и всплыли только на сводке по всем 78 картам.
      4. Объём вырос: с 15.08 у карты 12–13 блоков вместо 8–9 (добавились 4 `*_reversed`,
         спека 25) — это 312 коротких текстов на язык сверх прежней оценки.
      5. **Перевод карты заливать АТОМАРНО — название, слова витрины и поисковые синонимы одного
         языка одним куском, не по частям.** Источник поиска по справочнику выбирается по языку,
         на котором у карты реально есть НАЗВАНИЕ (`presentLang` в `cardSearch.ts`): появись
         `name.es` без `keywords.es`/`search.es`, источником станет испанское название с
         английскими ключевыми и поисковыми словами (они по-прежнему падают на фолбэк `en`) —
         а стеммер к тому моменту режет текст испанскими окончаниями (`ENDINGS['es']`), то есть
         английские слова незаметно порежутся по чужим правилам. Источник поиска станет смешанным
         языком (найдено 15.08, волна фиксов финального ревью спеки 27).

      **ОБНОВЛЕНИЕ 20.08 (Cowork, отчёт в localization-plan.md):** ШАГ 0 ПРОМТА L-2 ВЫПОЛНЕН —
      закрывать заново не нужно. (1) Зачины: реестр указателей для 4 языков сведён по фактическому
      канону и записан в content-guide.md; 280 блоков es/pt Старших переписаны нейтивами
      (перестройка фразы, не подстановка префикса), попутно снято ~33 романские формы, выдававшие
      пол читателя. Проверки: указатели 100%, повторов после указателя 0, приписки сфер различны
      22/22, слова карт и правки приёмки CC-L2 сохранены, канон и Младшие не тронуты. Заодно
      исправлена МЕТОДИКА: порог разнообразия считается ПОСЛЕ указателя, иначе проверка сама
      требует ломать конвенцию. (2) Канон: вместо пяти адресов проведён сплошной аудит 78 карт
      (1014 блоков × 2 языка) с независимой проверкой каждой находки на опровержение — 171
      пометка, 53 отклонены, осталось 62 места.

      **L-2 СДЕЛАНА 20.08** (отчёт в localization-plan.md): Жезлы и Кубки, 28 карт × 12 блоков × 2 языка
      = 672 текста + слова карт, залито атомарно, статус draft, ошибок слияния 0. В базе 50 карт
      из 78 на четырёх языках, 622 блока на язык. Корпусные проверки прошли с первого прогона
      (в L-1 было 32 правки), обратный перевод 68 блоков — 0 блокирующих. Главная находка захода:
      **вычитка нейтивом поправила 283 блока из 672**, и самое серьёзное было не грамматикой —
      испанская партия 1 пришла пиренейской нормой вместо латиноамериканской, вплоть до vosotros
      и фразы, говорящей обратное канону. Плюс сплошной скрининг нашёл 32 места, где в каноне
      вопрос читателю, а в переводе вопросительного знака не было, — восстановлены все.
      Осталось: **L-3** (Мечи + Пентакли, 28 карт), L-4 (курс и викторины), L-5 (приёмка + reviewed).

      **L-3 СДЕЛАНА 20.08 — КОЛОДА ПЕРЕВЕДЕНА ЦЕЛИКОМ** (отчёт в localization-plan.md): Мечи
      и Пентакли, 28 карт × 12 блоков × 2 языка = 672 текста, ошибок слияния 0. **Все 78 карт
      существуют на четырёх языках: 958 блоков ru/en/es/pt**, статусы draft, wordsStatus 78/78.
      Требование нормы языка, вынесенное в начало промта со списком конкретных запретов, сняло
      проблему L-2 полностью: 0 пиренейских и 0 европейских форм, вычитка поправила 184 блока
      против 283 в прошлый раз. Обратный перевод 68 блоков: 2 блокирующих, оба одного класса —
      русская назывная конструкция требует в романском языке глагола, и переводчик выбирал
      предсказательный («тепло возвращается ЕСЛИ» → «как только»; описание партнёра → «появится
      партнёр»). Исправлены. Типографика выровнена: es « », pt “ ”.
      Осталось: **L-4** (курс и викторины), **L-5** (сквозная приёмка + подъём статусов до reviewed).

      **L-4 СДЕЛАНА 20.08** (отчёт в localization-plan.md): курс и викторины переведены —
      6 названий модулей, 32 урока теории (~60 тыс. знаков), 160 вопросов × (вопрос + 3 варианта +
      пояснение) на два языка, всего 1740 единиц, ошибок слияния 0. Залито и в `course.json`,
      и в staging `quiz-m*.json` (файлы синхронны). Статусы `theory`/`quizStatus` es/pt — draft.
      **Главная проверка — слепое решение экзамена:** агент без ключа прошёл 160 вопросов на каждом
      языке; es 157/160, pt 158/160 — перевод сохраняет однозначность правильного ответа.
      Осталось: **L-5** (сквозная приёмка + подъём статусов до reviewed).

      **L-5 СДЕЛАНА 20.08 — ТРЕК ЗАКРЫТ (отчёт в localization-plan.md).** Статусы es/pt подняты
      до `reviewed`: 1105 единиц на язык, `content_stats.py` печатает 100% по всем разделам и всем
      четырём языкам, `todo` и `draft` — 0. Закрыты долги 28д (вопросы, зачины, указатели, prima).
      Правок за сессию: 215 блоков колоды + 5 мест курса. **Единственный незакрытый пункт DoD —
      UI-прогон карт на es/pt после этих правок** (см. задачу 28и).

- [x] **28з · Корпусные проверки распространены на es/pt — СДЕЛАНО 20.08.** `check_canon.py`
      больше не ограничен `LANGS = ("ru","en")`: перенесены все 11 проверок. Для пяти «непереносимых»
      (предсказание, жаргон, медицина, род читателя, глоссарий) носители составили и откалибровали
      на живом корпусе словари — `scripts/dict_es.py`, `scripts/dict_pt.py`, с историей калибровки
      в комментариях (голое `sol[oa]` давало 134 срабатывания при 2 настоящих, `energía` 52 при 0).
      Нормы длины сняты с корпуса квантилями, строгость сравнялась с каноном (ru/en 2.2% блоков
      вне нормы против 1.9% у es/pt). Побочно починен английский канон: указатели сфер не были
      в списке структурных триграмм, из-за чего «at work a» ×29 шло в тики — после правки en упал
      с 9 находок до 3. Не применено сознательно: `check_4_medicine` на курс (там медицина —
      предмет урока этики) и подъём порога тиков (он языкозависим, см. отчёт).

- [x] **28в · Редактор перечитывает 67 изменённых мест канона** — ЗАКРЫТА 21.08. Роль редактора
      передана Claude Code решением Артёма («жена читать не сможет»). Прочитаны все 65 правок
      с русской парой — четырьмя независимыми редакторами, каждому срез ВМЕСТЕ со всеми блоками
      той же карты. Найдено 19 мест, исправлен 21 адрес (17 ru + 4 en); отчёт — в конце
      `docs/canon-fixes-applied.md`, список — `docs/specs/28v-changed-addresses.md`.
      Преобладал один класс: на месте снятого обещания появлялась фраза, уже сказанная в соседнем
      блоке ТОЙ ЖЕ карты (7 случаев). Английский правился только там, где повтор есть и в нём
      (3 пары из 8 по общим 3-граммам). ⚠️ Прогон `check_edits.py` по собственным правкам поймал
      три вылета длины и две внесённые мной тавтологии — включая добор длины фразой из того
      самого соседнего блока, повтор с которым я и чинил. Исправлено вторым проходом.
      `check_canon --scope cards` 199 → 198, рассинхрон ru↔en 2 → 1, тесты 1469 зелёные.

- [x] **28и · UI-прогон карт на es/pt после правок L-5** — ЗАКРЫТА 21.08: прогон по самым
      длинным текстам корпуса (`world.symbolism.es` 571 знак, `sun`, `star`) и самому длинному
      названию («El Caballero de Espadas», «La Rueda de la Fortuna») — 21/21, переполнений нет,
      консоль чистая, скриншоты `docs/screenshots/28-l5-cards/`. Definition of Done закрыт
      полностью. — был единственный незакрытый пункт
      Definition of Done. В L-5 переписано 215 блоков колоды, из них 58 — с возвратом телесной
      лексики в блоки здоровья, то есть тексты стали ДЛИННЕЕ именно там. Экраны курса, урока,
      таба «Расклады» и двух карт уже сняты прошлыми приёмками; нужен прогон по картам на
      обновлённых текстах. Метрики в пользу того, что переполнений не будет: после подрезки вне
      нормы длины остались es 4 и pt 6 блоков против ru 15 и en 27. Самое рискованное место —
      испанское «Situación — Acción — Resultado» в списке раскладов (30 знаков против ru 26).

- [x] **28п · Перезалить es/pt у 32 вопросов викторины после правки дистракторов** — заведена
      21.08 задачей 28е. Работа Cowork (переводы Claude Code не пишет). Периметр: 121 адрес
      в `content/quiz-*.json` (списком — `docs/specs/28e-changed-addresses.md`), правились только
      НЕВЕРНЫЕ варианты в ru и en; испанский и португальский у них остались от старой формулировки.
      ⚠️ Смысл правки, который перевод обязан сохранить: дистрактор больше не должен выдавать себя
      ФОРМОЙ (категоричность, нелепость, короткая отговорка против развёрнутого верного). Он должен
      быть правдоподобным заблуждением, которое отвергают по содержанию. Перевод, вернувший
      «siempre»/«nunca»/«completamente» там, где в русском их сняли, вернёт и дефект.
      ⚠️ Верные варианты НЕ трогать: они не менялись, и их переводы верны.
      После заливки: `check_translation.py --scope course --lang es,pt` и `npm test`.
      **ПОСТАВКА СДАНА 22.08, ПРИЁМКА 23.08 — с двумя хвостами, чекбокс пока снят.**
      Промт — `docs/prompts/28p-quiz-distractors.md`, список правок — `docs/specs/28p-changed-addresses.md`
      (122 адреса = 61 дистрактор × 2 языка в 32 вопросах), коммит `d5cbe59`.
      Зелёное: `check_translation --scope course --lang es,pt --status reviewed` — **0 ошибок**
      (полнота, `correct`, порядок вариантов, канон ru/en, статусы); `merge_quiz.py` прогнан,
      дифф `course.json` ровно 122/122 строки, формат файлов сохранён побайтно; 1551 тест, `tsc` чист.
      **Цель достигнута:** `check_canon --scope course` 246 → 235 находок, из них ключевое —
      «верный вариант самый длинный» **30 → 19**.
      **Хвост 1 — две родовые формы, внесённые волной** (проверка 5 «род читателя» 6 → 8):
      `m3l4-q4[0].es` («a la vida a la que estás acostumbrado» — ru/en/pt нейтральны, дефект
      чистого перевода) и `m3l1-q4[1].pt` («Ficar dura demais» вместо прежнего нейтрального
      «Endurecer demais» — здесь корень в русском каноне «Стать слишком жёсткой»). Работа Cowork,
      промт — `docs/prompts/28p-gender-tail.md` (отправлен 23.08); канон «жёсткой» **решением
      Артёма 23.08 ОСТАВЛЕН как есть** (седьмая волна `docs/editor-questions.md`) — значит
      правится только перевод, и правило теперь постоянное: где русский задаёт род, es/pt
      обходят его своими средствами, а не копируют. Ровно правило проекта: правка, снимающая дефект, вносит дефект
      того же класса.
      **Хвост 2 — `check_edits.py` не понимал адреса викторины** (`m1l1-q2[1].es` — две части
      вместо трёх, записи молча пропускались): прогон печатал «Записей 0 / находок 0» — ложно-зелёный.
      Тот же результат уже был засчитан за успех в задаче 28е («0 находок по всем восьми классам»),
      то есть 121 правка дистракторов в ru/en этой проверкой не проверялась вовсе. Инструмент
      чинится сессией разработки, после починки прогнать ОБА списка — 28е и 28п.
      **ЗАКРЫТА 23.08 — оба хвоста сняты в тот же день.**
      Хвост 1: Cowork по промту `docs/prompts/28p-gender-tail.md` сняла обе родовые формы
      (`Endurecerse demasiado` / `Endurecer demais`; `a la vida que conoces desde siempre`),
      проверка «род читателя» 8 → **6** — остальные шесть волне не принадлежат; коммит `780ab7c`.
      Хвост 2: `check_edits.py` научен адресам викторины и откалиброван (коммит `74628e4`) —
      подробности ниже в записи инструмента. Итог по контенту: `check_canon --scope course`
      246 → **233**, «верный самый длинный» **30 → 19**, `check_translation --scope course` 0 ошибок,
      1551 тест зелёный. Прогон `check_edits` по 28п и 28е: настоящих дефектов 0.
      ⚠️ Остаточный шум, который НЕ дефект и в чинке не нуждается: проверка 6 «сменилась мысль»
      даёт 86 (28п) и 73 (28е) строк — на переводческой волне это by design, перевод пишется
      ЗАНОВО с пары ru+en, поэтому общих слов со старым текстом мало. Это материал для глаз,
      а не находки; на карточных списках (31) проверка даёт 4 и остаётся информативной.
      ⚠️ Известное свойство: при нескольких правках одного адреса проверки 5 и 8 берут последнюю
      (словарь по языку перезаписывается) — не двойной счёт, но и не разбор истории; историю
      целиком стережёт проверка 11.

- [x] **28н · Вычитка носителем строк es/pt, написанных сессиями** — **ЗАКРЫТА 21.08** (Cowork,
      промт `docs/prompts/28n-native-review.md`, коммит 96c4acd, отчёт — в конце
      `docs/localization-plan.md`, список правок — `docs/specs/28n-changed-addresses.md`).
      Закрывала долг: весь контент карт, курса и раскладов переводили и читали носители, а часть
      строк интерфейса и вариативных фраз писали сами сессии разработки — их не видел ни один
      носитель. Периметр: ярлыки ступеней мастерства (49), заголовки лунных пушей (47б) и пуша
      заморозки (10), одиннадцать фраз `push.freeze_saved`/`push.moon_new`/`push.moon_full`/
      `freeze.saved`, плюс два хвоста приёмки 28л.
      Приёмка зелёная: `check_translation --wave all --lang es,pt --status reviewed` — 0 ошибок;
      `check_canon --scope cards` **200 → 198** (оба хвоста 28л сняты, новых не появилось);
      `check_edits` — 3 находки, все ожидаемые разведения зачинов; 1488 тестов и `tsc` зелёные.
      ⚠️ Содержательная находка носителя: в pt `mastery.confident` было `SEGURA` — то же слово,
      что в es у той же ступени, но в португальском оно читается иначе; заменено на `SÓLIDA`.

- [x] **28к · Хвосты инструментов, найденные L-5** — **ЗАКРЫТА 21.08**, все три пункта:
      (1) СДЕЛАНО 21.08 — `set_status.py` писал с `newline="\n"` и нормализовал CRLF в LF, из-за чего
      `course.json` и пять staging-файлов переписывались целиком (6935 строк в diff вместо одной).
      Теперь `detect_newline()` читает фактический стиль КОНКРЕТНОГО файла с диска и пишет им же
      (на `core.autocrlf` не опираемся — git может держать дерево не в том стиле, что блоб).
      Замер до/после на копиях реальных файлов: `course.json` 6935 → **256** строк (ровно 128 theory
      + 128 quiz × 4 языка), `quiz-*.json` — по 4 строки, `cards.json` 4145 из 19254. Стиль каждого
      файла после записи подтверждён побайтово: `cards.json` остался LF, `course.json` и `quiz-*` — CRLF.
      (2) СДЕЛАНО 21.08 — добавлен ключ `--status draft|reviewed|final`, приёмка L-5 гоняется
      с `--status reviewed` и зелёная; подписи разделов берут значение из ключа;
      (3) РЕШЕНО 21.08 — предложение носителей добавить в реестр указателей формы `love` →
      «en la pareja» (es) / «na relação» (pt) **отклонено**, обоснование записано в content-guide
      под реестром. Коротко: реестр — таблица соответствий, а не список синонимов; в корпусе этих
      форм ноль («En el amor» у 76 блоков из 78, «No amor» — та же картина); и главное — они
      предполагают, что у читателя ЕСТЬ пара, тогда как блок `love` обращается и к одиноким
      (46 блоков из 78 в русском каноне; переводы передают это внутри текста — «Sin pareja…»,
      «Para quem está só…»), то есть блок открывался бы предположением, которому сам же
      противоречит через два предложения. Нужный смысл в реестре уже есть: «В отношениях…» →
      «En las relaciones…» / «Nos relacionamentos…».
- [x] **28е · Фактическая ошибка курса ИСПРАВЛЕНА + карикатурные дистракторы** — **ЗАКРЫТА 21.08**
      целиком: фактическая ошибка про происхождение таро исправлена ещё 20.08 в L-4, дистракторы
      переписаны 21.08 (коммит 5c1107b, подробности ниже). Остаток — только перезаливка es/pt,
      она вынесена отдельной задачей 28п. Исходная постановка: слепое
      решение викторины поймало в `m1l2` вопрос, где «правильный» ответ исторически неверен: курс
      учил, что игральная колода произошла от таро. По источникам верно обратное — игральные карты
      пришли в Европу около 1370-х уже с четырьмя мастями, а таро появилось в 1420–1440-х как
      надстройка (Britannica: «invented in Italy in the 1430s by adding to the existing four-suited
      pack a fifth suit of 21 … trionfi»). **Исправлено 20.08 синхронно на 4 языках**: теория m1l2,
      `correct` 2→0, пояснение. Тексты вариантов не тронуты — миф остался дистрактором.
      ⚠️ ОСТАЁТСЯ: решатели отметили 9 вопросов es и 11 pt как угадываемые без знания таро —
      два дистрактора карикатурны (абсолютные формулировки, очевидные нелепости) либо правильный
      ответ заметно длиннее прочих. Это дефект ИСХОДНЫХ вопросов, размноженный на 4 языка;
      чинится только в каноне. Не блокирует релиз, но снижает ценность экзамена.
      **ЗАМЕР ПЕРЕДЕЛАН 21.08 — список есть: `docs/specs/28e-guessable-questions.md`.** Прошлый
      перечень нигде не сохранился (были только числа 9/11), поэтому замер сделан заново и по
      РУССКОМУ канону — дефект живёт в исходных вопросах. Два независимых слоя: шесть агентов
      решали викторину вслепую (без ключа и без доступа к контенту) + механический счётчик
      абсолютных маркеров и длины вариантов. Итог по 160 вопросам: решатели ответили верно
      155/160 (97%), пометили «решается вслепую» 61, механика независимо — 33.
      **Доказанное ядро (совпали оба слоя) — 26 вопросов**, только механика — 7, только суждение
      решателя — 35 (перечитать вторым заходом, там опоры нет).
      **ПРАВКА ВЫПОЛНЕНА 21.08** (коммит 5c1107b): исправлено 32 вопроса, 121 адрес в ru и en
      одновременно; правились ТОЛЬКО неверные варианты, верный ответ и текст вопроса не тронуты
      нигде (проверено стражем). Механика 33 сигнала → 0, `check_edits.py` — 0 находок, 1488 тестов
      зелёные, `check_canon.py --scope course` вернулась к исходным 247. Отчёт — в конце
      `docs/specs/28e-guessable-questions.md`, список адресов — `docs/specs/28e-changed-addresses.md`.
      ⚠️ Три дефекта внесла сама правка и все три пойманы: ложная цель (карточный вопрос попал
      в список по длине, хотя задача 29 вывела такие из правила), новый абсолют «без единого
      просвета» вместо снятых, и превышение лимита «верный самый длинный» в двух уроках — разница
      была 2–7 знаков, порог «в 1.3 раза» её не видел, а контракт-тест считает без запаса.
      ⚠️ Хвост: es/pt этих вопросов отстали от канона — задача **28п**.
      ⚠️ Методическая находка, важная для будущих замеров: у решателей НОЛЬ промахов среди 61
      пометки — то есть агент, знающий таро, не может честно «забыть» его и помечает задним
      числом. Разброс между агентами это подтверждает (М6 — 13 из 20, М4 — 12 из 40). Слепое
      решение без второго, механического слоя даёт завышенный список.
      Дальше: править ДИСТРАКТОРЫ (не верный вариант) синхронно ru+en, затем перезаливка es/pt
      силами Cowork; после правок обязателен повторный прогон обоих слоёв — правка склонна вносить
      дефект того же класса (в задачах 29 и 26 это случалось трижды).

- [x] **28б · Правки канона по `docs/canon-fixes.md`** — ЗАКРЫТА 20.08 Cowork по прямому поручению
      Артёма («жена не может, беру под твою ответственность»). Решены все 62 места без редактора-
      человека: 11 обязательных (медицина и гарантии — риск ревью сторов), 44 по редполитике,
      7 спорных, 5 про род читателя. Изменено 65 текстов ru, 62 en; 23 затронутых блока Старших
      перезалиты на es/pt с указателями. Пять проходов с правом отмены: сведение пары ru/en →
      ревизия (8 правок из 62 признаны ухудшением и переписаны) → расшаблонивание (31 правка
      независимо выбрала один ход «обратите внимание, как…» — 61% против 16% в каноне; 21 текст
      переписан другими ходами) → приёмка (4 отклонены: обещание переехало в мягкую форму) →
      автопроверки. Отчёт — localization-plan.md, журнал изменений — `docs/canon-fixes-applied.md`.

- [x] **28д · Потерянная вопросительная интонация в es/pt Старших** — ЗАКРЫТА сессией L-5
      (21.08): 8 блоков восстановлены, сплошной скрининг даёт 0 в обоих языках; сведённые
      зачины разведены, «prima» → «bono». Проверено приёмкой. — заведена приёмкой L-2 20.08,
      НЕ блокирует L-3. В русском каноне вопрос читателю, в переводе — утверждение: «…а что
      отнимает силы?» → «…y cuáles te agotan.» Меняется жанр — вместо повода подумать приложение
      сообщает читателю, как у него дела. Сессия L-2 нашла и починила 32 таких места, но только
      в своей волне (масти); в Старших осталось 8 блоков — es: `hierophant.health`,
      `wheel-of-fortune.day_card`, `hanged-man.health`, `death.career_reversed`, `sun.health`,
      `judgement.career_reversed`; pt: `hanged-man.health`, `sun.health`.
      Проверка одной строкой: блок, где в `ru` есть «?», а в переводе нет. Отдать любой L-сессии.
      **Расширена приёмкой L-3 (20.08) — сюда же волна вычитки перед L-5:**
      · **Сведённые зачины, 4 группы × 3 блока.** Русский использует три разных оборота, перевод —
        один: es «hace falta» (`justice.love`, `c13.health`, `s04.love`), es «hay riesgo`
        (`devil.finances`, `s05.finances`, `s11.finances_reversed`), pt «esta carta»
        (`emperor.career`, `c01.health`, `p09.health`), pt «olhe para» (`w03.day_card`,
        `s11.health`, `p07.health` — «смотрите на горизонт», «понаблюдайте за собой»,
        «оглянитесь на привычки» стали одинаковыми). Обеднение стиля, не ошибка смысла.
      · **Два «prima»** в значении премии (`c03.finances`, `c11.finances`, es) — волна L-2
        внесла их в свой же список запретов, но эти блоки скрининг L-3 не покрывал.
        В мексиканской норме слово законно; менять на «bono» — по вкусу нейтива.
      · Указатели приписок сфер (`*_reversed`) местами разошлись с каноном в обе стороны —
        список печатает `check_translation.py --wave all` в разделе [ОТЧЁТ].

- [x] **28ж · Расклады на es/pt — ЗАКРЫТА 20.08** — заведена приёмкой 20.08,
      **БЛОКИРУЕТ L-5**. `content/spreads.json` не тронут: 8 раскладов × (`name`, `description`,
      все `positions`) = 108 единиц на два языка. Промт L-4 требовал их наравне с курсом,
      в отчёте сессии они не упомянуты вовсе — значит пункт выпал, а не перенесён осознанно.
      Пока не сделано, Definition of Done «100% блоков контента и строк UI» недостижим,
      а на табе «Расклады» испанец и бразилец видят английские названия и описания.
      Проверка: `python scripts/check_translation.py --scope spreads --lang es,pt` (сейчас 108
      ошибок, после заливки обязан быть 0). У раскладов НЕТ статусов — есть перевод или нет.

      **СДЕЛАНО 20.08 (Cowork, довесок к L-4):** 108 единиц залиты, проверка 108 ошибок → 0,
      приёмка зелёная. Канон ru/en не тронут побайтно, порядок языков ru→en→es→pt, CRLF сохранены,
      норма языка 0 нарушений. Термины сверены с курсом (tirada/tiragem, Pasado/Presente/Futuro),
      атрибуция Уэйта переведена дословно. Метод: перевод → вычитка нейтивом → слепой обратный
      перевод; последний дал 19 замечаний, редакторы приняли 6, отклонили 13 как свойства русского
      исходника. Не проверено инструментально: UI-прогон (приложение в сессии не собиралось) —
      посмотреть при приёмке испанское «Situación — Acción — Resultado» (30 знаков против ru 26)
      и десятикарточный крест на узком экране. Подробности в localization-plan.md.

- [x] **28з · `check_translation.py` не запускается на Python < 3.12** — ЗАКРЫТА 21.08:
      f-строка переписана без вложенных кавычек одного вида, ВСЕ 12 скриптов проекта проверены
      разбором с `feature_version=(3, 10)`. Урок: инструмент, уходящий в чужую среду,
      проверяется на нижней поддерживаемой версии. — найдено 20.08.
      Строка 372 использует одинарные кавычки внутри f-строки с одинарными кавычками (PEP 701,
      только 3.12+). На локальной машине Python 3.10, поэтому `python scripts/check_translation.py`
      падает с `SyntaxError: f-string: unterminated string` ЕЩЁ ДО разбора аргументов — то есть
      любая приёмка молча не выполняется, а не «даёт ошибки». Прогоны довеска L-4 сделаны на 3.12.
      Починка на минуту: сменить внутренние кавычки на двойные. Заодно зафиксировать минимальную
      версию Python в README и в CI, чтобы это не повторилось на чужой машине.
- [x] **28м · Перевод лунных раскладов на es/pt** — **ЗАКРЫТА 22.08**: Cowork сдала по промту
      `docs/prompts/28m-moon-spreads.md`, приёмка пройдена — `check_translation --scope spreads --lang es,pt`
      0 ошибок (было 24), формат файла цел (отступ 2, CRLF), канон ru/en и остальные 8 раскладов не тронуты,
      1501 тест зелёный; шесть строк `moonSpread.*` носители оставили без правок. Отчёт — в конце
      `docs/localization-plan.md`. Единственное осознанное отличие: позиции лунных раскладов развёрнуты
      в многострочный вид, как у остальных восьми (в одну строку четыре языка не читались).
      Периметр маленький и жёстко очерченный: в `content/spreads.json` два расклада `new-moon`
      и `full-moon` — название, описание и 4 позиции у каждого, всего 20 единиц на язык. Плюс
      вычитка носителем шести строк интерфейса `moonSpread.*` в `src/lib/i18n.ts` (`event`, `open`,
      `opensOn`, `position`, `hintNew`, `hintFull`), написанных сессией.
      ⚠️ Ключи `es`/`pt` у этих раскладов сейчас ОТСУТСТВУЮТ намеренно — не пустые строки: пустая
      строка значит «язык есть и он пустой» и показала бы пустое место вместо готового английского
      (штатный фолбэк `inLang` срабатывает только на отсутствующем ключе). Заливать сразу текстом.
      ⚠️ До заливки `check_translation.py --scope spreads --lang es,pt` показывает ровно эти два
      расклада как непереведённые — ожидаемое состояние, а не регрессия.
      Позиции — короткими именами, без пояснений (решение редактора 10). Русские: Семя · Почва ·
      Помеха · Первый шаг / Урожай · Что стало ясно · Тень · Что отпустить.

- [x] **28л · Перезалить es/pt блоков, изменённых правками 28г и 28в** — **ЗАКРЫТА 21.08**:
      Cowork сдала, приёмка пройдена (отчёт — в конце `docs/localization-plan.md`). 42 адреса
      в 18 картах (21 блок × 2 языка), список — `docs/specs/28l-changed-addresses.md`; переводы
      написаны заново с пары ru + en. `check_translation --wave all --lang es,pt --status reviewed`
      — **0 ошибок по всем 12 классам**; `check_edits.py` — 33 находки, все разобраны, дефектов нет
      (ожидаемая подпись волны + служебные романские 3-граммы + повторы, унаследованные из русского);
      1488 тестов зелёные, `tsc` чистый; `check_canon --scope cards` 198 → 200, дельта разобрана
      поимённо. Промт, по которому работала Cowork: `docs/prompts/28l-resync.md`.
      Два хвоста в следующую волну Cowork (вместе с 28м, не блокируют): `lovers` pt потерял
      вариацию «надо/положено», которая есть в русском, и зачин `health` сошёлся на трёх картах
      в pt и четырёх в es — подробности в отчёте приёмки.
      Правки 28г («ок» Артёма) изменили 4 блока, вычитка 28в — ещё 18 (17 русских, 4 английских,
      с пересечением). Переводы этих блоков остались со старой формулировкой и разошлись
      с каноном. Нужен перевод по актуальному ru/en; списки «было → стало» —
      `docs/specs/28g-changed-addresses.md` и `docs/specs/28v-changed-addresses.md`
      (вместе 22 адреса, из них с текстом на es/pt — все).
      ⚠️ Правило плана локализации: переводы делает Cowork, Claude Code их не пишет.
      Проверка после заливки: `python scripts/check_translation.py --wave all --lang es,pt
      --status reviewed` (обязан остаться зелёным) и `check_edits.py` по списку правок.
      Статус этих блоков в es/pt остаётся `reviewed` — на релиз не влияет, расхождение
      стилистическое, а не смысловое: правки снимали повторы, а не меняли мысль.

- [x] **28г · Повторы, внесённые правками канона** — ЗАКРЫТА 21.08 («ок» Артёма). Применены
      все четыре: `temperance.day_card` (снят дословный повтор `symbolism`), `emperor.day_card`
      (финал больше не пересказывает начало), `justice.general` («решения»/«честный» по разу),
      `w09.love` («усталость» вместо трёх раз — дважды, как риторический приём; английский
      не тронут осознанно — там уже стояли разные слова, tired и exhaustion).
      Прогон `check_edits.py` по собственным правкам: 0 тавтологий, 0 повторов с соседним
      блоком, 0 смен мысли. Хвост — задача 28и (перезалить es/pt).

- [x] **46 · Коллекция-альбом → 46б «альбом внутри справочника»** — ЗАКРЫТА 19.08: лайв-проверка Артёма на iPhone
      пройдена ✓ («всё чётко работает»), ветка `feat/46-collection` влита в main. (Этап 4, master-plan п. 13) — заведена 19.08,
      спека `docs/specs/46-collection.md` (раздел «46б»), план `docs/plans/46b-collection-in-reference.md`.
      **Решение Артёма 19.08 на лайв-проверке 46 («всё чётко работает»):** отдельный альбом дублирует
      справочник (те же карты, половина «заперта» — читается странно) → объединить: справочник и есть альбом.
      Панель «Изучено N из 78» с полосой под заголовком «78 карт» (следует за активным чипом: «Старшие ·
      изучено 8 из 22»), неизученные карты приглушены (картинка opacity .55, без бейджа), изученные — полный
      цвет + «ИЗУЧЕНО ✓», новый чип-фильтр «Изучено». Экран `/collection`, вход «Коллекция» и строки мастей
      удаляются; ничего не запирается (справочник — причина №1 открыть приложение; карта дня и расклады
      постоянно ведут на неизученные карты; freemium закрыл бы 70 карт). Ссылка в урок со страницы карты —
      НЕ делаем (решение Артёма: перепрыгнуть урок нельзя, строка лишняя). Переиспользуется из 46:
      `collection.ts` (секции/прогресс), `CardCell`, `CardGridRow`, `StarBack`; `CountRow` удаляется.
      Первая редакция 46 (отдельный экран, ветка до коммита 58e23d4): спека `docs/specs/46-collection.md`,
      план `docs/plans/46-collection.md`, ветка `feat/46-collection`. Вопросы 5–7 ниже решены 19.08 по
      рекомендациям Claude (решение Артёма): вход со вкладки «Карты» отдельным экраном; карту открывает
      ТОЛЬКО пройденный урок (`learnedCardIds` — то же множество, что бейдж «ИЗУЧЕНО ✓» и колода
      тренажёра); нетронутые масти — строками «0 ИЗ 14», секция с ≥1 открытой картой — сеткой с заголовком.
      Решения сверх вопросов: глифы мастей 🜂🜄🜁🜃 из макета НЕ используем (системный шрифт iOS их не
      гарантирует) — слева в строке мини-рубашка `StarBack`; `grayscale(.4)` закрытой ячейки не
      воспроизводим (только opacity .42); вход в макете не нарисован — дорисовывается задачей. Ничего
      не хранится — всё из `lessonsProgress`; persist version 10 НЕ меняется. Выносы по правилу «2+ раза»
      ДО нового кода: `StarBack` (из `SpreadCard`/`SpreadRow`), `CardCell` (из справочника), новый `CountRow`.
      Хвосты (не блокируют): (а) **уровни мастерства карты** из master-plan п. 13 — ✅ ЗАКРЫТ
      задачей **49** (21.08, ветка `feat/49-mastery`, ждёт лайв-проверки): макет был дорисован
      Cowork-сессией 20.08, спека и план написаны, чип и полоски сделаны, вывод — чистая функция
      `masteryLevel` над `srs[cardId].intervalDays`, как и предполагалось этой записью;
      (б) «закрыть коллекцию» 78/78 как момент для просьбы оценить приложение (master-plan п. 19) —
      вместе с механикой оценки; (г) `BAR_DEPTH = 140` (`src/lib/scrollAware.ts`) калибровался под
      прежнюю высоту шапки «Карт»; с панелью прогресса (≈70 px) низ поля поиска в потоке ушёл на
      ~200 px, и на глубине 140–200 парящая панель поднимается поверх ещё видимого поля —
      пересмотреть по наблюдению на устройстве (поднять до ~210 или оставить); (д) Overline
      9.5/ls3.5 живёт в четырёх файлах (`cards.tsx`, `index.tsx`, `profile.tsx`, `review.tsx`) —
      кандидат на общий `Overline`/`ScreenTitle` по правилу «2+ раза»; (е) **46в · момент
      «переворота»** — ✅ ЗАКРЫТ 21.08 (спека `docs/specs/46v-flip-moment.md`, motion-spec
      §17, ветка `feat/46v-flip-moment` влита в main; веб-проверка 13/13 с честным красным
      прогоном, лайв-проверка Артёма ✓). Механизм — очередь от `completeLesson` (`revealQueue.ts`),
      а не «диф learned между фокусами» из исходной постановки: ленивые табы теряли бы волну
      на холодном старте (флаг в спеке); играет на фокусе «Карт», сетка скроллится к первой
      новой карте (решение Артёма 21.08), потолок 8, повторно не играет, без персиста; (ж) бейдж «ИЗУЧЕНО ✓»
      (`CornerBadge`, задача 08) на СВЕТЛОЙ теме почти не читается: `accent2` на скриме
      rgba(0,0,0,.4) поверх яркого арта — дефект старше ветки, но с 46б бейдж стал единственным
      положительным маркером «изучено»; измерить контраст программно (правило задачи 25),
      решить цвет/скрим. ⚠️ **Объединить с находкой задачи 47 (20.08):** мелкий Overline цветом
      `muted` на светлой теме даёт **1.84:1** при норме 4.5:1 (замер по пикселям скриншота,
      формула WCAG; подписи дней недели лунного календаря). Макет даёт те же 2.18:1 — то есть это
      свойство пары Overline + `muted` на светлой теме целиком, а не дефект конкретного блока
      (вывод задачи 25 подтверждён замером). Чинить одним токеном во ВСЕХ местах сразу — карта дня,
      подписи недели календаря, страница карты, бейдж — а не точечно; сначала подобрать токен,
      потом свип грепом по Overline.

- [x] **47 · Лунный календарь** — ЗАКРЫТА 20.08: веб-проверка 6а/6б (35/35, консоль чистая, два
      честных красных прогона) и лайв-проверка Артёма на iPhone пройдены ✓ («все ок»), ветка
      `feat/47-moon-calendar` влита в main. Отчёт — в конце спеки. (этап 4, master-plan §3.1-Р п. 11) — заведена 19.08, спека
      `docs/specs/47-moon-calendar.md` (все развилки решены Артёмом 19.08), план — `docs/plans/47-moon-calendar.md`,
      ветка `feat/47-moon-calendar`. Экран `/moon` по макету `v-moon`: шапка месяца, строка луны, сетка
      месяца (прошедшие дни приглушены, сегодня обведён, глифы ●/○ на днях событий), строки событий
      «НОВОЛУНИЕ · 12 АВГУСТА · 20:37» с подписью; вход — нажимаемая строка луны на «Сегодня» (вынос
      `MoonRow`). Ядро `moon.ts` переписывается на точные моменты новолуния/полнолуния (Meeus, гл. 49,
      без зависимостей; тесты против опубликованных эфемерид, ±10 мин) — строка «Сегодня», календарь
      и пуши 47б считают от одной правды. Начало недели по языку (ru — ПН, en/es/pt — ВС). Persist не
      меняется. НЕ делаем: пуши (47б), тексты на лунные дни, листание месяцев, «расклады лунного цикла».
      **47б · пуши новолуния/полнолуния — ЗАКРЫТА 20.08**: веб-проверка 6б (12/12 с подменой
      часов браузера, два честных красных прогона) и лайв-проверка Артёма на iPhone пройдены ✓
      (спека и отчёт `docs/specs/47b-moon-pushes.md`, план `docs/plans/47b-moon-pushes.md`, ветка
      `feat/47b-moon-pushes`): в локальный день новолуния/полнолуния утренний пуш заменяется
      лунным (`pushPlan.ts`, вид `moon`, приоритет обрезки `streak > evening > freeze > moon >
      morning > comeback`). Отложенное подтверждение: утром 28.08.2026 (полнолуние) баннер обязан
      сказать «Полнолуние ✦», если карта к 09:00 не открыта. Хвосты (не блокируют): фразы
      `push.moon_new`/`push.moon_full` ждут вычитки редактором (прецедент `push.freeze_saved`
      задачи 10); ✅ хвост про `TITLE_KEY` ЗАКРЫТ 20.08 (коммит 9763bf8): константа вынесена
      из `pushes.ts` в чистый `pushPlan.ts` рядом с `MOON_TITLE`, контракт-сьют 47б расширен
      на все шесть ключей заголовков (плюральные семейства вида `titleStreak_one/_few/_many`
      считаются существующими по любой форме — полноту семейств стережёт `i18nPlurals`, не
      дублируем); красный прогон подтверждён мутацией ключа: 8 упавших, оба направления,
      все языки из `resources`. «Расклады лунного цикла»
      из master-plan — макета нет, сначала дорисовать; листание месяцев — только если попросят
      на лайве (v1.1).

### Вопросы Артёму по дорисовкам 20.08 (шаринг, мастерство, лунные расклады, события) — решить ДО спек

Экраны нарисованы простейшими вариантами (демобар: «Событие», «Виджеты»; шаринг — ссылкой под
картой дня; лунный расклад — с экрана луны и из списка раскладов). Где пришлось придумывать:

**Шаринг-карточка (п.10):** 11) вход — текстовая ссылка под CTA (нарисовано), иконка-стрелка в углу
карты или пункт в лайтбоксе полноэкранного просмотра?; 12) состав карточки — показывать ли
ключевые слова (нарисованы) и ставить ли имя пользователя («карта Анны»)?; 13) даёт ли шаринг XP
(риск: люди начнут «фармить» репостами — я бы не давал).

**Уровни мастерства (п.13):** 14) пороги ступеней — предложены по интервалу SM-2: НОВАЯ (нет записи) ·
ЗНАКОМАЯ (1–5 дней) · УВЕРЕННАЯ (6–20) · МАСТЕР (21+); согласны или считать по числу успешных
повторений?; 15) место показа — чип на странице карты + 4 полоски в ячейке справочника (нарисовано)
или только страница карты (сетка и так плотная)?; 16) показывать ли ступень в тренажёре после оценки.

**Лунные расклады (п.11):** 17) окно доступности — сутки до и после события (нарисовано) или весь
лунный день?; 18) сколько карт в раскладах новолуния/полнолуния и КАКИЕ позиции — это контент
редактора, в макете плейсхолдеры «ПОЗИЦИЯ N»; 19) расклад события — бесплатный или premium?;
20) что показывать вне окна события — прятать карточку совсем (нарисовано так) или показывать
с датой ближайшего события?

**Сезонные события (п.15) — ПЕРЕНЕСЕНЫ В v2 решением Артёма 21.08.** Ответы 21–25 ниже остаются
в силе и ждут v2 вместе с дорисованными макетами (баннер на «Сегодня» и экран квеста) — стартовать
придётся не с нуля. Обоснование переноса — в master-plan п. 15; коротко: потребность в «празднике»
закрыли лунные события (2 в месяц против 4–8 в год у сезонных), это единственная фича v1
с ПОТОЧНЫМ контентом, и она тянет механику ачивок, которой больше никто не пользуется.
Исходные вопросы: 21) кто заводит событие — фиксированный календарь в JSON (ретроградный
Меркурий, солнцестояния) или ручной список от редактора?; 22) шаги квеста — из существующих действий
(урок/серия/тренажёр/расклад/заметка — нарисовано так) или отдельные задания с текстами?;
23) награда — XP + ачивка (нарисовано +50 XP), только ачивка или ещё заморозка серии?;
24) что если событие не завершено вовремя — прогресс сгорает или остаётся до следующего раза?;
25) баннер на «Сегодня» — постоянно в окно события (нарисовано) или прячется после первого захода?

**Момент переворота (46в):** 26) параметры в макете — 520ms cubic-bezier(.2,.9,.3,1.05), stagger 70ms,
первые 8 ячеек; подтвердить как источник для motion-spec §4 (его запрет каскада >8 соблюдён).

**РЕШЕНИЯ Артёма по всем 16 вопросам (20.08):**
- **11** вход шаринга — текстовая ссылка под CTA, как нарисовано. **12** ключевые слова — да,
  имя пользователя — НЕ ставить. **13** XP за шаринг не давать.
  ⚠️ **Перекрыто 21.08: шаринг-карточку НЕ делаем вовсе** — решение Артёма («расшарить свою
  карточку кому-то — это не нужно»). Ответы 11–13 остаются записью истории; макет `v-share` —
  архив. Отражено в master-plan п. 10. Не переоткрывать как новую идею.
- **14** ступени по интервалу SM-2, пороги как в макете (нет записи / 1–5 / 6–20 / 21+).
  **15** показывать в обоих местах (чип на странице карты + полоски в ячейке справочника);
  полоски можно снять на лайве, если сетка окажется перегруженной. **16** в тренажёре после
  оценки НЕ показывать в v1 (кандидат на потом: только при росте ступени).
- **17** окно лунного расклада — сутки до и после события. **18** карт РОВНО 4 (зафиксировано);
  названия позиций и описания — контент редактора, вопрос записан в editor-questions.md
  (пятая волна). **19** новолуние free, полнолуние premium. **20** вне окна карточку ПОКАЗЫВАТЬ
  заблокированной с датой ближайшего события («Откроется 28 августа») — это расхождение
  с макетом (там спрятана): хвост дорисовки записан ниже.
- **21** (+уточнение) солнцестояния/равноденствия считает КОД (Меес гл. 27, сверка с USNO —
  как у луны); окна ретро-Меркурия — таблицей в JSON на 2026–2028 (точность «день» достаточна);
  тексты всех событий пишет редактор. **22** шаги квеста — из существующих действий (словарь
  типов: урок/серия/тренажёр/расклад/заметка). **23** награда «+50 XP и ачивка»; ачивки в v1 —
  минимальная механика: список полученных значков в профиле, выдаёт только квест, отдельной
  системы условий нет. **24** прогресс незавершённого квеста сгорает молча, без упрёков.
  **25** баннер на «Сегодня» — всё окно события. **26** параметры 46в подтверждены как источник
  для motion-spec §4.

Хвост дорисовки по решению 20 (не блокирует): состояние лунного расклада «вне окна» —
карточка в списке раскладов и панель на экране луны заблокированы, с датой ближайшего
события — дорисовать в design-reference.html до спеки задачи.

### Вопросы Артёму по нарисованным экранам этапа 4 (Cowork, 15.08 — решить ДО спек)

Экраны дорисованы в design-reference.html простейшими вариантами (кнопки демобара «Тренажёр /
Коллекция / Луна»); где приходилось придумывать поведение — вопросы здесь, решений в макете нет:

**Тренажёр (SM-2) — ВСЕ ЧЕТЫРЕ РЕШЕНЫ 19.08 (задача 45, спека `docs/specs/45-flashcards.md`):**
1) вход — с «Курса» ✓; 2) интервал на кнопках — НЕ показываем; 3) сессия — порциями по 10;
4) XP — +1 за вспомненную карту (logic-spec §4/§12 дописываются в 45а). Плюс решение сверх
вопросов: кнопок оценки три, не четыре, подписи безличные — макету хвост дорисовки (п. 10).

**Коллекция — ВСЕ ТРИ РЕШЕНЫ 19.08 (задача 46, спека `docs/specs/46-collection.md`):**
5) отдельный экран, вход из «Карт» ✓; 6) открывает пройденный урок ✓; 7) нетронутые масти — строками,
с первой открытой картой секция становится сеткой ✓ (применяется и к старшим арканам).

**Лунный календарь — ВСЕ ТРИ РЕШЕНЫ 19.08 (задача 47, спека `docs/specs/47-moon-calendar.md`):**
8) вход — только со строки луны на «Сегодня» ✓ (строка становится нажимаемой, шеврон дорисовывается
в макет); 9) минимум — фазы + новолуние/полнолуние, без текстов на лунные дни ✓; 10) пуши —
отдельной задачей **47б** после экрана ✓ (прецедент 06а→06б). Плюс решение сверх вопросов: ядро
`moon.ts` переписывается на точные моменты (алгоритм Меюса) — средняя луна давала до ±14 ч
к реальному моменту, для календаря с датами это не годится.

## Этап 3+: см. docs/master-plan.md (флеш-карты SM-2, коллекция, лунный календарь, расклады, виджет)

**Перенесено в v2 решениями Артёма (задачами не заводились):** сезонные события (п. 15, 21.08);
«карта года» (п. 14), квест первой недели (п. 17), момент оценки в сторе (п. 19) — 22.08. Обоснования
записаны в master-plan у самих пунктов, чтобы не вернулись идеей. Из блока удержания в v1 осталось:
виджеты (ждут EAS) и монетизация (§3.4) — задача **53**, заведена 22.08 (номер 52 занят реструктуризацией CLAUDE.md).

⚠️ Правило (обновлено 19.08): тренажёр (спека 45), коллекция (спека 46) и лунный календарь
(спека 47) в макете ОТРИСОВАНЫ и поставлены. Остальные
экраны этапа 3+ НЕ отрисованы — перед их стартом сначала дорисовать
в design-reference.html (задача для Cowork-сессии), потом спека, потом код. Без эталона UI не делаем.

## Правила синхронизации доков

- Закончил задачу → отметь здесь [x], обнови короткий «Статус» в CLAUDE.md, запись о задаче —
  в docs/changelog.md, уроки ⚠️ — в docs/lessons.md по теме (в CLAUDE.md хронику и уроки не писать — задача 52).
- Изменил визуальное решение против эталона → сначала правка в design-system.md, потом код.
- Раз в ~5 задач: сверь design-reference.html с реальным приложением, расхождения — сюда как задачи.
