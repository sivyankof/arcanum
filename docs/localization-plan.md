# Arcanum · план локализации ES + PT-BR (без внешних переводчиков)

**Ответственный: Cowork (Клод).** Артём — запускает сессии и передаёт файлы. Жена — ТОЛЬКО русский канон
(ES/PT она не проверяет). Claude Code — код i18n и приёмка, переводов не делает.

## Почему качество будет без нейтива

Конвейер имитирует студию локализации четырьмя независимыми проходами:
1. **Перевод с двух источников** — переводчик-агент видит И английский, И русский текст (два языка
   снижают ошибки интерпретации вдвое: где EN двусмыслен, RU уточняет).
2. **Вычитка чужого текста** — отдельный агент-«редактор-нейтив» (испанец/бразилец по промпту) правит
   перевод, которого не делал. Никогда не вычитываем свой же текст.
3. **Обратный перевод выборки** — 10% блоков переводятся обратно на русский третьим агентом и
   сверяются с каноном: расхождение смысла = красный флаг на всю партию.
4. **Автопроверки скриптом**: плейсхолдеры {name}/{card}/{n} на месте, длины строк UI (пере полнение
   кнопок!), глоссарий соблюдён, запрещённые слова отсутствуют (предсказания/медицина — на всех языках).
5. **Проверки по КОРПУСУ, а не по строке** — добавлено 15.08 после задачи 25, где английский
   разошёлся с русским так, что первые четыре прохода этого не поймали бы:
   - *однообразие зачинов, отдельно на каждом языке.* Русские тексты были разнообразны,
     а в английском «Your body…» открывало 11 приписок из 78: переводчик сводит разные обороты
     к одной удобной конструкции. Скрипт считает первые два слова каждого текста и печатает
     повторы от трёх. Испанский и португальский к этому склонны не меньше английского.
   - *дописанное переводчиком.* Два английских текста начинались с «At work», хотя в русском
     «на работе» не было вовсе. Сверять зачины пары «оригинал → перевод» и лишние обстоятельства
     места и времени.
   Обе находки прошли построчную проверку незамеченными и всплыли только на сводке по всем
   78 картам — поэтому это отдельный проход, а не пункт внутри вычитки.

## Модели (что чем переводим)

- **Старшая модель (Opus / максимальный режим)**: глоссарий, строки UI, онбординг, пуши, фразы
  composition/phrases, Старшие арканы, ВСЯ вычитка и обратный перевод. Малый объём × максимальная цена ошибки.
- **Средняя модель (Sonnet)**: массовые блоки Младших арканов, теория уроков, вопросы викторин —
  с эталонами стиля в промпте. Большой объём × стиль уже задан.
- В Cowork это мои субагенты (модель указываю при запуске); в Claude Code переводы НЕ делаются вообще.

## Особенности языков (зашиты в промпты)

**ES**: нейтральная латиноамериканская норма (не кастильская «vosotros»); обращение tú (тепло,
не usted); Arcanos Mayores/Menores, Bastos/Copas/Espadas/Oros. **PT-BR**: бразильская норма (не
европейская); você; Arcanos Maiores/Menores, Paus/Copas/Espadas/Ouros. Оба: род прилагательных
согласуется с «картой» (la carta/a carta — она), «отозвалась» = resonó / ressoou.

## Разбивка по сессиям

**Предусловие всего трека: жена вычитала русский канон Старших арканов** (Младшие могут догонять).

### Cowork-сессии (мои, Артём просто пишет мне «запускай L-N»)
- **L-0 · Фундамент (½ вечера):** глоссарий ES/PT (расширение content-guide) → UI-строки i18n
  (es/pt) → phrases.json → composition.json. Всё старшей моделью. Выход: файлы + отчёт автопроверок.
- **L-1 · Старшие арканы (вечер):** 22 карты × 8 блоков × 2 языка, старшей моделью, партиями по 5.
  Сразу вычитка-проход и обратный перевод выборки. Выход: cards.json с es/pt Старших.
- **L-2 · Младшие, Жезлы+Кубки (вечер):** 28 карт × 2 языка, средней моделью + вычитка старшей.
- **L-3 · Младшие, Мечи+Пентакли (вечер):** то же, 28 карт.
- **L-4 · Курс (вечер):** теория 10 уроков + 50 вопросов викторин + расклады (spreads.json) × 2 языка.
- **L-5 · Финальная приёмка (½ вечера):** сквозные автопроверки всей базы, отчёт покрытия
  (0 блоков без es/pt), back-translation сводка, статусы залитого языка → reviewed
  (`python scripts/set_status.py reviewed --lang es`). ⚠️ Статус двигается ТОЛЬКО у залитого
  языка: с 19.08 (задача 28а) он живёт по языкам, и канон `ru`/`en` трогать нельзя — иначе
  вернётся ровно та дыра, из-за которой английский не читал никто.
Итого: ~4 вечера моей работы. Сессии L-1…L-3 можно запускать в любом порядке и параллельно с разработкой.

### Claude Code-сессии (код; запускать ПОСЛЕ L-0)
- **CC-L1 · Инфраструктура 4 языков** (готово 15.08, спека `docs/specs/27-lang-infra.md`, ветка
  `feat/27-lang-infra`): тип Lang = 'ru'|'en'|'es'|'pt' везде; i18n.ts принимает
  es/pt строки из L-0; переключатель в настройках — 4 языка; плюрализация под 4 языка (включая хотфикс
  русских числительных — сразу правильной архитектурой: Intl.PluralRules); определение языка
  устройства при первом запуске (es-MX → es, pt-BR → pt, иначе en, ru → ru). Главное следствие для
  сессий L-0…L-4: язык «включается» появлением его UI-строк в `resources` внутри `src/lib/i18n.ts` —
  как только L-0 положит строки для испанского, он сам появится и в списке выбора, и в определении
  языка устройства (`AVAILABLE_LANGS`); отдельной правки кода для этого не потребуется.
- **CC-L2 · Приёмка контента:** после каждой моей L-сессии — валидация схемы,
  `python scripts/content_stats.py` (с 19.08 считает ПО ЯЗЫКАМ и видит курс), tsc,
  юнит-тест «нет блока без 4 языков», UI-прогон 6а на es и pt (скриншоты: тексты
  не обрезаются, кнопки не разъезжаются — испанский на ~25% длиннее русского!).
  ⚠️ Предусловие CC-L2 закрыто задачей 28а (спека `docs/specs/28a-per-language-status.md`):
  схема статуса пер-языковая, заливать переводы есть куда. Заливать карту атомарно —
  `name` + `keywords` + `search` одним куском (у них общий `wordsStatus` ровно поэтому).

### Роль Артёма (пошагово)
1. Дождаться вычитки Старших арканов женой → сказать мне: «канон готов, запускай L-0».
2. После каждой моей L-сессии: сказать Claude Code «прими контент L-N по плану локализации».
3. Раз в сессию: открыть приложение, переключить язык на ES и PT, полистать 2–3 экрана глазами
   (даже без знания языка видно обрезанные строки и разъехавшиеся кнопки).

## Definition of Done (язык готов к релизу)
- [ ] 100% блоков контента и строк UI имеют es/pt · [ ] автопроверки зелёные · [ ] вычитка-проход
  пройден · [ ] back-translation без красных флагов · [ ] UI-скриншоты es/pt без переполнений ·
- [ ] тексты стора на языке (моя задача, этап ASO) · [ ] дисклеймер и политика переведены.

## Риск и честная оговорка
Машинный конвейер даёт качество «очень хороший неродной редактор» — выше среднего перевода в сторах,
но идиому уровня живого нейтива не гарантирует. Страховка: кнопка «Обратная связь» работает на всех
языках; первые отзывы ES/PT читаем внимательно; грубые жалобы на язык → точечные правки за минуты.

---

# ОТЧЁТ L-0 (20.08, Cowork) — ФУНДАМЕНТ ГОТОВ

**Сделано:** глоссарии ES/PT + конвейер качества → `docs/content-guide.md`; UI-строки es/pt (267×2) →
`src/lib/i18n.ts` (блоки после en, порядок ru → en → es → pt); `content/phrases.json` (44 фразы ×2);
`content/composition.json` (28 фраз ×2). Итого **630 переведённых значений**.

**Конвейер отработал полностью:** перевод с двух источников (Opus) → вычитка чужого текста
редактором-нейтивом → контрольный обратный перевод 26-строчной выборки на русский (переводчик
не видел оригинала) → автопроверки скриптом. Обратный перевод показал совпадение смысла с каноном
по всем 26 строкам и вскрыл 18 дефектов формы, все исправлены редакторами. Самое ценное из
найденного: `lesson.passedOf` в ES согласовывал причастие с `{{count}}`, а не с `{{done}}` (при 0
фраза ломалась); в PT `push.titleFreeze` смысловая ошибка — «заморозка спасла серию» вместо
«наготове»; в PT терминологический разнобой pausa/congelamento между UI и контентом.

**Автопроверки (финальные, обе партии):** ключей 267 = 267 · пропущенных 0 · лишних 0 ·
расхождений плейсхолдеров 0 · пустых значений 0 · остатков кириллицы 0 · расхождений `\n` 0 ·
нарушений UPPERCASE 0 · плюрал-семейств 13/13 полных (_one+_other, ни одного _few/_many) ·
дублей внутри вариативных массивов 0 · `_comment`-поля не тронуты (остались служебными).

**Чего в L-0 НЕТ (по плану — дальше):** тексты карт (L-1…L-3), теория и викторины (L-4),
финальная сквозная приёмка (L-5). Статусы контента не двигались: `set_status.py --lang es/pt`
запускается на партиях карт, а UI-строки статусов не имеют.

**Флаг для приёмки CC-L2 (не блокирует, но проверить):** в `content/phrases.json` у
`push.streak_save[0]` и `push.freeze_saved[0]` расходятся плейсхолдеры МЕЖДУ ЯЗЫКАМИ в самом
источнике: `ru` = `{days}` (подставляется уже склонённая строка из `push.streakDays_*`),
`en/es/pt` = `{n}` (голое число). Это существовало до L-0; испанский и португальский следуют
английскому. Если рантайм кормит только `{days}`, у трёх языков плейсхолдер не подставится —
решить на приёмке (или выровнять источник на `{n}`, или добавить `streakDays_*` в es/pt).

**Что дальше:** «запускай L-1» — Старшие арканы (22 карты × 8 блоков × 2 языка), с атомарной
заливкой карты (name + keywords + search + блоки одного языка одним куском — правило хвоста 45б)
и корпусными проверками по каждому языку отдельно (повторы первых двух слов, дописанные
обстоятельства — уроки задачи 25).

---

# ОТЧЁТ L-1 (20.08, Cowork) — СТАРШИЕ АРКАНЫ ЗАЛИТЫ

**Объём:** 22 карты × 13 блоков × 2 языка = **572 текста** + name/keywords/search на 22 карты × 2 языка.
Залито АТОМАРНО (правило хвоста 45б): у каждой карты name + keywords + search + wordsStatus и все 13
блоков одного языка ставились одним куском, после предварительной валидации всего пакета — карта
с любым дефектом не заливалась бы вовсе. Асимметрий `general.es` без `name.es` — 0.
**Статусы:** `draft` для es/pt (пер-языковая схема 28а). Канон ru/en не тронут: статусы ru/en остались
`reviewed`, Младшие арканы не задеты (проверено — 0 карт). Пустых заглушек `""` во всей базе — 0.
До `reviewed` статусы поднимет L-5 после сквозной приёмки.

**Конвейер (пять проходов):** 8 партий перевода (Opus, по 4–6 карт, каждая с двумя источниками) →
корпусная проверка зачинов → правка повторов (8 текстов ES, 24 PT) → контрольный обратный перевод
30 блоков на русский вслепую → финальная вычитка нейтивами (29 блоков ES, **134 PT**).

**Автопроверки после слияния:** 22/22 карты обоих языков · 286 блоков на язык · пустых 0 · кириллицы 0 ·
длины всех блоков в пределах ±20% от английского · keywords ровно 4 · search 8–12 · зачинов с 3+
повторами **0** в обоих языках · дописанных обстоятельств сверх оригинала 0 (4 срабатывания оказались
ложными — обстоятельство есть и в английском).

**Главное, что вскрыл обратный перевод — и это НЕ дефект перевода:**
предсказательные формулировки и медицинская рамка пришли ИЗ РУССКОГО КАНОНА, перевод их точен
(сверено построчно с `ru`). Флаг редактору (правка канона, не перевода — иначе 4 языка разойдутся):
· `magician.day_card` — «Вечером заметите: получилось больше обычного» (обещание результата);
· `tower.day_card` — «Сегодня будьте готовы к неожиданностям», «Правда, которая сегодня вскроется»
  (события подаются как данность) — оба места это прямые предсказания, а `content-guide` их запрещает
  и они же — риск при ревью сторов;
· `temperance.health` — «восстановление через ритм», `sun.health_reversed` — «Тело чувствует себя лучше,
  чем вам кажется», `world.health_reversed` — «целостность почти вернулась»: рамка выздоровления
  и утверждения о состоянии тела читателя вместо рефлексии.
Пока канон не поправлен, es/pt повторяют его дословно — это осознанно.

**Второй флаг — стилистический (решает редактор):** в ru и en зачины блоков единообразны и служат
навигацией («В любви…» ×21, «В работе…» ×19, «Ваш путь…» ×22, «For wellbeing…» ×15), а в es/pt по
требованию корпусной проверки все зачины уникальны (0 повторов). То есть испанская и португальская
версии стилистически разнообразнее канона. Вопрос: вернуть единообразие блоков (читатель сразу видит,
о чём абзац) или оставить как есть? От ответа зависят L-2…L-4 — там 56 карт, и переделывать потом дорого.

**Из починенного, что стоит знать:** в португальском была массовая калька русской тире-пунктуации
(«X — Y» вместо глагола): 276 тире → 160, все 22 блока `symbolism` перестроены с безглагольных
перечислений на нормальные предложения; в испанском та же болезнь была в 15 блоках symbolism.
Идиомы-ловушки: «на коротком поводке» в PT стало «na coleira curta» (собачий ошейник) → `de rédea curta`;
«истончилась» → буквальная калька в обоих языках → выправлено.

**Дальше:** L-2 (Жезлы+Кубки, 28 карт) и L-3 (Мечи+Пентакли, 28 карт) — по тем же правилам; L-4 — курс
и викторины; L-5 — сквозная приёмка и подъём статусов. Перед L-2 желательно решение по второму флагу.

---

# ПРИЁМКА CC-L2 сессии L-1 (20.08, Claude Code) — ПРИНЯТА

**Структура (свой скрипт + красный прогон на 4 подсаженных дефектах):** 22/22 старших атомарно
целые (name + keywords ровно 4 + search 8–12 + wordsStatus draft + 13 блоков draft × es/pt);
канон ru/en побайтно не тронут; 56 младших не тронуты; порядок ключей ru→en→es→pt; пустых
строк и кириллицы в es/pt — 0. **Тесты:** 1454 в 37 сьютах зелёные, `tsc` чистый. Два теста
устарели с приходом переводов (фиксировали «у fool нет es/pt» литералами) и переписаны:
контракт «стемминг по языку текста» переведён на синтетическую ru/en-карту (не протухнет
с волнами L-2/L-3), добавлен обратный контракт «переведённая карта ищется своими словами»
(`loco`/`riesgo`/`risco`, английское `risks` больше не находит fool на es).
**Корпусные проверки (свои, по каждому языку):** зачины 3+ — 0; английских имён карт
в текстах — 0; general каждой карты зовёт карту её же именем `name.es/pt` — 22/22.
**UI-прогон** (страница карты fool, 390×844, скриншоты `docs/screenshots/28-l1/`): названия,
чипы и тексты на языке страницы, английский фолбэк НЕ показан, заглушек нет, вкладки влезают,
консоль чистая — 12/12.
**Минорная находка:** 6 блоков чуть за коридором длины ±20% от en (макс. 1.28 —
`tower/star.birth_path`, отчёт L-1 заявлял «все в пределах»; вероятно, считали слова, я считаю
знаки). Не блокирует, статус draft, добьёт вычитка L-5.
**Решение Артёма по «второму флагу» (зачины):** единообразные зачины-указатели, как в каноне —
конвенция распространяется на es/pt, разнообразие применяется к тексту ПОСЛЕ указателя.
Исполнение — шаг 0 промта L-2 (`docs/prompts/L-2-wands-cups.md`): выбрать формулы указателей,
записать в глоссарий, привести 22 старших L-1, новые масти писать сразу с указателями.
**Первый флаг** (предсказания/медицина в каноне ru, 5 адресов) — записан редактору
в `editor-questions.md`, шестая волна.

**Довесок приёмки (20.08, после сдачи отчёта): поиск швов в контрактах.** Приёмка L-1 сама
показала класс «тест ограничен литералами `['ru','en']`, и волна переводов проходит мимо него
молча». Перебраны ВСЕ такие места в тестах — нашлось четыре контракта, три из них с реальным
дефектом внутри:
· подсказка тренажёра (`review.test.ts`): длина первого предложения `general` проверялась
  только на каноне — развёрнута на LANGS; красные прогоны честные (блок-огрызок короче 20
  знаков и первое предложение длиннее 160 — обе ветки роняют тест). ⚠️ Первая мутация
  («короткое первое предложение») оставила тест зелёным: `promptSentence` при коротком первом
  предложении сама берёт до второго, то есть вход не попадал в опасную ветку — ровно урок 28а
  «мало написать проверку на тот самый случай, надо убедиться, что её вход туда доходит».
· правила слов карты (`cardSearch.test.ts`, три контракта: 4 витринных, 8–12 поисковых,
  «поиск не дублирует витрину») — тоже только канон. Развёрнуты на все языки с обязательной
  половиной «если язык присутствует» (язык без перевода законно не имеет слов, его полноту
  стережёт атомарность в `cardsContent`). **Контракт покраснел сразу**: в L-1 приехали
  6 мёртвых дублей — `moon.es`, `sun.es`, `judgement.es`, `judgement.pt` повторяли витринные
  слова в поиске. Дубль не ломает выдачу (витрина индексируется и так), но тратит слот из
  8–12 впустую. Починено удалением (осталось 9/10/8/9 — в коридоре), ничего не выдумано.
Тестов стало **1466** (было 1454). Инструмент приёмки вынесен из scratchpad в
`scripts/check_translation.py` — 12 классов проверок, каждый подтверждён мутацией.
**Правило для L-2 и дальше:** search не должен повторять keywords НИ НА ОДНОМ языке; теперь
это контракт, а не договорённость.
