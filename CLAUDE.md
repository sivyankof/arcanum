# Arcanum — обучающее приложение по таро

@AGENTS.md

## Контекст проекта

Приложение для изучения таро (курс как в Duolingo + карта дня + справочник 78 карт + расклады).
Делают Артём (новичок в разработке — объясняй просто, по-русски) и его жена (эксперт-таролог,
главный редактор контента). Полный план: `docs/master-plan.md` — **прочитай его перед крупными задачами**.

## ОБЯЗАТЕЛЬНЫЙ ПРОЦЕСС РАБОТЫ (нарушение = переделка)

Никакого кода «с ходу». Для КАЖДОЙ задачи из docs/backlog.md строго по циклу:

0. **Уроки** — открой `docs/lessons.md` и прочитай разделы по теме задачи (оглавление в шапке файла:
   стор/persist, веб ≠ натив, анимации, вёрстка, навигация, пуши, i18n, тесты, веб-проверка,
   контент-конвейер, редактура, процесс, алгоритмы). Сквозные правила — раздел ниже в этом файле.
1. **Брейншторм** — используй skill brainstorming из плагина superpowers (если установлен — он
   триггерится сам; если нет — проведи структурный брейншторм вручную): уточни требования,
   рассмотри 2–3 варианта реализации, выбери и обоснуй.
2. **Спека** — запиши в `docs/specs/NN-<имя-задачи>.md`: цель, что именно делаем, что НЕ делаем,
   точные значения из design-system.md, критерии приёмки (чек-лист «готово, когда…»).
   Эталон формата: `docs/specs/01-today-screen.md`.
3. **План** — в той же спеке: шаги по файлам (какой файл, что меняется), порядок, где нужен
   `npm install`. Мелкие шаги: один шаг = одна проверяемая правка.
   Для КРУПНЫХ задач (07+, те же, что идут в ветках) — дополнительно отдельный детальный план
   через skill writing-plans (superpowers) в `docs/plans/NN-<имя>.md`: микрошаги с точным кодом
   и проверкой каждого. Мелким задачам отдельный план не нужен — хватает плана в спеке.
4. **Согласование** — покажи спеку Артёму, дождись «ок» (для мелких задач <30 строк кода можно пропустить).
5. **Реализация** — по шагам плана, после каждого шага `npx tsc --noEmit`. Новая формула/алгоритм —
   юнит-тест в том же коммите (кейсы — из logic-spec.md; правила — docs/testing-strategy.md);
   `npm test` зелёный перед каждым push. Git: мелкие задачи — в main; задачи 07+ (крупные) —
   ветка `feat/NN-имя`, merge после лайв-проверки.
6. **Проверка** — трёхуровневая, первые два уровня делаешь САМ, не привлекая Артёма:
   - **6а-0. Макет ≠ истина в последней инстанции.** При сверке проверяй ДВЕ пары: «приложение = макет»
     И «макет = спеки» (product-spec / logic-spec / design-system). Заметил конфликт макета со спекой —
     НЕ копируй макет слепо: подними флаг Артёму («макет говорит X, спека говорит Y»), реализуй по спеке,
     а расхождение макета запиши в backlog как задачу дорисовки.
   - **6а. Визуальная сверка с макетом (обязательно, каждая UI-задача).** Запусти `npx expo start --web`
     (приложение откроется на http://localhost:8081) и через браузерный MCP (Playwright: если не подключён —
     `claude mcp add playwright -- npx "@playwright/mcp@latest"`) открой ДВА окна: приложение и
     file:///.../docs/design-reference.html. Сделай скриншоты каждого изменённого экрана в размере 390×844
     в ОБЕИХ темах, положи в docs/screenshots/<spec>/, сравни с макетом по чек-листу
     docs/ui-verification.md. Каждое расхождение — либо исправь, либо явно перечисли в отчёте с причиной.
   - **6б. Прокликивание (обязательно).** Пройди по каждому интерактивному элементу изменённых экранов:
     каждая кнопка, таб, фильтр, поле — клик → результат совпадает с ожиданием из спеки; проверь консоль
     браузера на ошибки/warning. Сломанное — чини до зелёного прохода.
   - **6в. Лайв-проверка Артёмом на iPhone (финал).** Только после зелёных 6а+6б. Хаптика, шрифты и
     нативные тени видны только тут — веб им не судья.
   Известные отличия веба от нативного (НЕ считать багами): нет вибрации, системные тени слабее,
   шрифт может грузиться с задержкой; перелёт картинки из сетки на страницу карты в вебе не
   отрабатывает — изображение появляется сразу на месте (замерено 10.08: промежуточных кадров нет),
   проверять только на устройстве. Ошибки вида «X is not defined» в консоли во время правки файлов —
   артефакт Fast Refresh на полусохранённом модуле: перезагрузить страницу и проверить заново.
   ⚠️ **Чего веб-проверка не видит в принципе** (найдено 13.08 на лайв-проверке задачи 08):
   всё, что считает САМ движок JS, а не наш код — числительные, даты, форматы чисел. Браузер
   несёт полный ICU, Hermes на телефоне — урезанный, поэтому русские формы («6 УРОКОВ») в вебе
   правильные, а на устройстве падают в английский фолбэк. Юнит-тесты это тоже не ловят: jest
   гоняется на Node, у которого ICU полный. Вывод: веб достоверен для композиции, логики и
   поведения, но по числительным и датам судья только пункт 6в. Разбор: `docs/specs/hf-02-plurals-research.md`.
7. **Синхронизация** — отметь задачу в backlog.md; обнови короткий «Статус» в CLAUDE.md (только
   текущее состояние: ветки, persist version, тесты, хвосты); запись о задаче — в `docs/changelog.md`
   (что сделано, решения, новое общее, ссылка на спеку); новые уроки ⚠️ — в `docs/lessons.md` под
   свою тему. В CLAUDE.md хронику и уроки НЕ писать — он грузится в каждую сессию целиком.
   Если менялся дизайн — обнови design-system.md. Коммит: `feat: <задача> (spec 0N)`.
8. **Пуш** — после коммита `git push` (если remote ещё не настроен — предложи Артёму создать
   приватный репозиторий на GitHub и настрой origin; без бэкапа в облаке проект живёт в одном
   экземпляре на диске). Задача закрыта только после: лайв-проверка Артёмом на телефоне ✓ + push ✓.

Если Артём просит что-то вне backlog — сначала добавь это в backlog как задачу со спекой, потом делай.

## Жёсткие правила

1. **SDK НЕ обновлять.** Проект на Expo SDK 54, потому что Expo Go в App Store поддерживает только SDK 54
   (политика Expo с мая 2026). Не запускать `expo upgrade`, не менять мажорные версии пакетов.
2. **Язык общения — русский.** Комментарии в коде — тоже русские.
3. После каждой правки код должен проходить `npx tsc --noEmit` без ошибок.
4. Если меняешь `package.json` или `app.json` — явно скажи Артёму: «нужен npm install и перезапуск
   `npx expo start --tunnel`». Обычные правки кода подхватываются Fast Refresh автоматически.
5. **Бэкенда нет и в v1 не будет** — всё офлайн: контент в JSON-бандле, прогресс в AsyncStorage/SQLite.
6. Тексты значений карт **не выдумывать**: их пишет редактор (жена). В коде показывать блоки со
   статусом `todo` как «Текст готовится» (см. `content/cards.json`, статусы: todo → draft → reviewed → final).
7. Git: коммить осмысленными шагами с русскими сообщениями.

## Дизайн — какой файл для чего (ЧИТАТЬ ПЕРЕД ЛЮБОЙ РАБОТОЙ НАД UI)

Направление — **«Небесное золото»**: глубокая ночная синева + шампанское золото, БЕЗ фиолетового.

| Файл | Что это | Когда обращаться |
|---|---|---|
| `docs/product-spec.md` | **Как всё работает**: каждый раздел приложения — элементы, поведение, состояния, граничные случаи | ПЕРВЫМ ДЕЛОМ при любой задаче: сначала пойми поведение здесь, потом смотри вид |
| `docs/logic-spec.md` | **Алгоритмы и данные**: формулы (карта дня, серия, XP, аркан рождения, луна), схема хранения, правила пушей, использование собранной статистики. С тест-кейсами | Любая задача с логикой/данными. Тест-кейсы из него — проверять в коде |
| `docs/design-system.md` | **Все численные значения**: цвета, типографика, отступы, радиусы, тени, спецификации компонентов, состояния, тон текстов | Всегда при создании/правке любого экрана или компонента. При конфликте с кодом — прав этот файл |
| `docs/design-reference.html` | **Визуальный эталон**: интерактивный макет всех экранов (композиция, анимации в CSS с точными параметрами). Читай его код: значения в CSS — источник правды для композиции | Когда делаешь конкретный экран — найди его в макете и сверь композицию; параметры анимаций бери из CSS |
| `docs/motion-spec.md` | **14 анимаций** по приоритетам с параметрами (длительности, кривые, пакеты) + анти-чеклист | Любая работа с анимациями; делать по одному пункту за коммит |
| `src/theme/theme.ts` | **Токены в коде** (обе темы) | Цвета ТОЛЬКО отсюда, хардкод цветов запрещён |
| `docs/master-plan.md` | План проекта: этапы, фичи удержания, монетизация | Перед крупными задачами и при вопросах «зачем/что дальше» |
| `docs/testing-strategy.md` | Уровни тестирования, какие модули покрывать юнитами | При добавлении логики и перед push |
| `docs/content-guide.md` | Редполитика: тон, глоссарий RU/EN, workflow правок редактора, правила переводов | Любая работа с текстами (content/*.json, i18n.ts) |
| `docs/reference/waite-pictorial-key.md` | **Первоисточник колоды** — нарезка книги Уэйта (1911, public domain) с русским указателем «что брать / что нельзя» и ловушками для авторов | Заходы курса М5–М6, правки `symbolism`, расклады. Часть книги уже в `cards.json` → `source` |
| `docs/release-checklist.md` | Всё для выхода в сторы: аккаунты, материалы, юридическое, прогон | Этапы 5–6; сроки аккаунтов — смотреть заранее |
| `docs/store-listing.md` | **Тексты для магазинов** на 4 языках (название, описания, ключевые слова, заметки ревьюеру) + таблица фактов с источниками; лимиты держит `storeListing.test.ts` | Сабмит в сторы; любая правка текстов — сюда, а не в форму магазина |
| `docs/lessons.md` | **Уроки проекта по темам** — ~200 ⚠️-находок задач 01–50 дословно, сгруппированы (стор, веб ≠ натив, тесты, контент, веб-проверка…) | Шаг 0 каждой задачи: разделы по теме; новый урок — туда |
| `docs/changelog.md` | **Хроника задач** — что сделано, когда, решения, новое общее (архив бывшего «Статуса» CLAUDE.md + новые записи) | Вопросы «как это делалось / почему так»; закрыл задачу — запись туда |

Порядок работы над UI-задачей: design-system.md (значения) → design-reference.html (композиция экрана)
→ motion-spec.md (если есть движение) → код с токенами из theme.ts → `npx tsc --noEmit`.

## Структура

- `app/` — экраны (expo-router): (tabs)/ = Сегодня, Курс, Карты, Расклады, Профиль; card/[id] — страница карты
- `src/theme/` — токены и useTheme; `src/store/useApp.ts` — Zustand-состояние (тема, язык, серия, история)
- `src/lib/content.ts` — доступ к контенту; `src/lib/cardImages.ts` — АВТОГЕНЕРИРУЕТСЯ (`scripts/gen_card_images.py`), не править руками
- `src/lib/srs.ts` — алгоритм интервальных повторений SM-2 (для флеш-карт, этап 4)
- `content/*.json` — 78 карт (cards) / 10 раскладов (spreads, из них 2 лунных — задача 51) / курс 6 модулей (course) /
  фразы «Состава расклада» (composition) / вариативные системные фразы (phrases) — все на четырёх языках
  ru/en/es/pt (статусы готовности — по языкам, спека 28а).
  Правило вариативности: повторяющийся текст = минимум 2–3 формулировки одного смысла (content-guide)
- `assets/cards/` — 78 изображений Райдер–Уэйт (public domain)

## Сквозные правила (выжимка из docs/lessons.md — то, что повторялось чаще всего)

1. **Правка, снимающая дефект, вносит дефект того же класса** (подтверждено 7 раз): после любой волны
   правок — повторный прогон тем же инструментом (`check_canon.py`, `check_edits.py`, контракт-тесты);
   свою правку гонять тем же, чем проверял чужую. Диагноз агента-редактора принимать, его готовый
   текст — проверять по карте целиком и по корпусу.
2. **Тест или проверка обязаны краснеть на сломанном коде** — мутация, вход в опасную ветку, допуск по
   замеру (не «с запасом»); зелёный с первого раза — искать ошибку в проверке. У пары «проверить /
   применить» и у контракта «текст ⟺ статус» закрывать ОБЕ половины; периметр тестов сверять списком.
3. **DRY проверяется грепом по ВСЕМУ проекту**, не по периметру задачи; перед новым компонентом или
   хелпером — список «Общие модули» ниже и `ls src/components src/lib`.
4. **Веб-проверка не видит**: всё, что считает движок JS (числительные, даты, форматы — Hermes без ICU),
   worklets, нативные модалки и жесты, сенсоры, резкость картинок, иконку приложения (Expo Go). Судья —
   только устройство (6в). Jest на Node этого тоже не видит — механизм сносить руками (`i18nPlurals.test.ts`).
5. **Persist**: `zustand/persist` сливает состояние только по верхнему уровню ключей — новое поле внутри
   `settings`/`profile`/`reviewDay` = version+1 и слияние руками; `SCHEMA_VERSION` в `backup.ts` —
   единственный источник версии. Состояние, взятое из стора при монтировании, обязано иметь следящий эффект.
6. **Системным компонентам тему и язык передавать явно** (`themeVariant`, `locale`); цвет на тёмном скриме
   не следует теме (`gold.onScrim`). Контраст меряется по пикселям скриншота в худшей точке фона.
7. **Контент**: тексты не выдумывать; статус по языкам — только `statusIn`/`blockText`, текст — только
   `inLang`; скрипты правят JSON, сохраняя формат (indent 1, стиль переводов строк); контент-волна
   заканчивается корпусными проверками по КАЖДОМУ языку отдельно; редактору (агенту) давать карту целиком
   с исходниками. Прежде чем винить перевод — проверить, соблюдает ли правило оригинал.
8. **Веб-проверка**: Playwright-скрипт 390×844; dev-сервер заново с `--clear` на каждое состояние кода,
   доставку правки проверять грепом по бандлу; сид `goto → evaluate → reload`; `click({ force: true })`
   на качающихся элементах; сценарий сначала на сломанном коде.
9. **Макет ≠ истина**: сверять «приложение = макет» И «макет = спека»; эмодзи в макете — смысл, а не
   способ рисования; расхождение из-за отсутствующих данных лучше вранья числом.
10. **Всё, что зависит от текущего времени, слушает `AppState`** (`useAppActive`), а не только фокус
    экрана; границы дня — локальные (`dates.ts`); тест дат ждёт литерал, а не вызов проверяемой функции.
11. **i18n**: текст переносится побайтово; плюрализация через `count`; функция с параметром `lang` берёт
    `i18n.getFixedT(lang)`; ключи закрыты контрактом по ресурсам языка, не через `getFixedT`; никаких
    тернаров «если русский — одно, иначе другое». Расширяя тип-объединение или число языков — перечислять
    ЛИТЕРАЛЫ и молчаливые допущения о числе.
12. **Окружение**: стейджить поимённо (`git add <путь>`), не `-A` — сессии параллельны; PowerShell не
    трогает UTF-8-файлы (мутации — python/Edit; `.ps1` с BOM); инструмент для чужой среды проверять на
    нижней версии (Python 3.10).
13. **Процесс**: сценарий лайв-проверки отправлять, только когда правка уже в рабочем дереве; «упало N»
    писать вместе с «из M»; рецепт из спеки выполнить хотя бы раз; исключение «файл не трогай» — с парой
    «а трогает его кто»; свой вывод дочитывать до конца.

## Статус (на 25.08.2026)

- **Сделано**: этапы 1–2 плана закрыты (задачи 01–50 — экран «Сегодня», карта дня, справочник, страница
  карты, курс 32 урока с контентом, дневник, рефлексия, пуши, онбординг, профиль, заморозка, бэкап,
  расклады, лайтбокс, лунный календарь и лунные пуши); этап 4 частично: флеш-карты SM-2 (45), прогресс
  «изучено» в справочнике (46/46б/46в), мастерство карты (49). **Локализация ES/PT закрыта (28)**: контент
  100 % на четырёх языках, UI — 4 языка, статусы es/pt `reviewed`; корпус карт 958/958 `reviewed`
  (ru/en после правок 28б/28в — условно, журнал `docs/canon-fixes-applied.md`).
- **Цифры**: тестов 1931 в 52 сьютах (28.08, задача 67), `tsc` чистый, `expo-doctor` 18/18; **persist version 12** (`SCHEMA_VERSION`) —
  следующая задача, меняющая схему стора, поднимает до 13; схема контента — статус по языкам (28а).
- **Задача 51 (лунные расклады) ЗАКРЫТА 22.08**: лайв-проверка Артёма на iPhone ✓ («всё отлично»), ветка
  `feat/51-moon-spreads` влита в main. Два лунных расклада (новолуние free / полнолуние premium) открыты
  сутки до и после события (календарными днями); DEV-строка настроек «Лунный расклад: открыть окно»
  подменяет «сейчас» ближайшим событием. Флаг 6а-0 («назад» → Расклады, не ЛУНА) принят как есть.
  Спека/отчёт — `docs/specs/51-moon-spreads.md`. **28м (es/pt лунных раскладов) ЗАКРЫТА 22.08** —
  приёмка зелёная.
- **Задача 53а (пейвол + гейты Premium) ЗАКРЫТА 22.08**: лайв-проверка Артёма на iPhone ✓
  («работает»), ветка `feat/53-premium` влита в main. Право `premium` в сторе (вне бэкапа),
  `src/lib/premium.ts` — единственный источник решения о доступе (стережёт контракт-тест
  `premiumSources`), гейты курса/раскладов/тренажёра/лунной панели, экран `app/paywall.tsx`,
  DEV-тумблер настроек. Веб-проверка 6а/6б: 31 из 31, красный прогон подтверждён (снятый гейт
  маршрута урока роняет ровно одну проверку); сценарий — `scripts/check_53_web.js`, отчёт и
  сценарий лайва — в конце `docs/specs/53-premium.md`. Правки по лайв-проверке: бейдж «−40 %»
  сделан непрозрачным (сквозь заливку `chipBg` 10 % просвечивала рамка карточки — плотный слой
  `bg` под ту же заливку, проп `solid` у `PremiumBadge`) ✓ Артёмом. Место плашки «ПРЕМИУМ»
  в шапке модуля (рядом с замком, а не у правого края, как в макете) принято как есть.
  Деление free/premium — решение Артёма 22.08 (бэклог 53). **53б (RevenueCat, продукты в сторах,
  цены) — отдельный заход на этапе EAS-сборки**; правило цен на четырёх языках (валюту выбирает
  страна аккаунта магазина, строку отдаёт магазин, «−40 %» считается из фактической пары)
  записано в спеке 53 и в release-checklist.
- **Задача 28п ЗАКРЫТА 23.08** (перезаливка es/pt дистракторов + два хвоста приёмки): контент
  чист (`check_canon --scope course` 246 → 233, «верный самый длинный» 30 → 19), род читателя
  8 → 6. Инструмент `check_edits.py` научен адресам викторины — до 23.08 он их МОЛЧА пропускал,
  и правки задач 28е/28п им не проверялись вовсе; добавлены проверки 9 (повтор с другим
  вариантом вопроса), 10 (дистрактор сблизился с верным), 11 (цепочка правок), семьи языков
  в 5 и 8, калибровка записана числами ложных срабатываний в докстроках.
- **Задача 54 (юридические страницы) ЗАКРЫТА 23.08** (лайв-проверка Артёма ✓, сайт живой): `site/` — четыре
  страницы на четырёх языках (`index/privacy/terms/support`) на GitHub Pages этого репо,
  деплой `.github/workflows/pages.yml`; лендинг и домен не нужны. Ссылки пейвола и три ссылки
  «О приложении» ведут на web-URL из `appInfo.ts` через общий `LinkTxt` (`expo-web-browser`);
  дисклеймер на первом шаге онбординга. Контракт `site.test.ts` держит равенство «текст страницы
  = строка приложения» (правишь политику — правь оба места). **Аналитики в v1 нет** (решение
  22.08): политика утверждает это прямым текстом, события воронки — v1.1. Спека —
  `docs/specs/54-legal-pages.md`. URL для сабмита: `https://sivyankof.github.io/arcanum/`
  + `privacy.html` / `terms.html` / `support.html`; правка `site/**` деплоится сама.
- **Заход 23.08 «то, что решается кодом» (аккаунты/EAS отложены)**: спеки готовы и ждут «ок» —
  порядок **55** (патчи SDK 54 по `expo-doctor` 17/18, `reviewPrompt`, иконка пуша Android) → **28р**
  (второй заход по 35 вопросам раздела C списка 28е — вердикт по каждому, без правки списком) →
  **56** (плановая сверка приложения с макетом, 16 пар) → **57** (тексты сторов на 4 языках + тест
  лимитов). Промт сессии реализации — `docs/prompts/55-57-implementation-session.md`.
  **55 ВЫПОЛНЕНА 23.08** (ждёт лайв-проверки): `expo-doctor` 18/18, хвост 45б закрыт
  (`reviewPrompt` + контракт по исходнику), иконка пуша Android нарисована скриптом и подключена
  плагином. Артёму нужен `npm install` и перезапуск `--tunnel` (менялись `package.json`/`app.json`).
  **55, 28р и 56 ЗАКРЫТЫ 23.08** (лайв-проверка Артёма ✓ по каждой): 55 — патчи SDK 54,
  `reviewPrompt` (хвост 45б), иконка пуша Android; 28р — 41 дефект викторины исправлен,
  категоричность и длины в нуле, инструменты `scripts/check_quiz_guess.py` (замер + страж) и
  `scripts/apply_quiz_edits.py`; 56 — плановый аудит макета: 16 пар × 2 темы, одно расхождение
  в коде исправлено (разделитель шапки «Расклады»), проход 6б 23/23, сценарии
  `scripts/shoot_56.js` и `scripts/check_56_web.js`.
  **57 ЗАКРЫТА 25.08** (Артём прочитал тексты — «всё окей»): `docs/store-listing.md` — тексты сторов
  на 4 языках (факты сверены по коду); контракт-тест `storeListing.test.ts` (122 проверки:
  лимиты в символах Unicode, ключевые слова, запрещённые корни с учётом отрицания, периметр
  языков/полей, числа из кода), красный прогон — 5 мутаций; release-checklist — пять пунктов
  в `[~]`; промт вычитки носителем — `docs/prompts/57-store-native.md` (задача 57н).
  Название утверждено Артёмом 23.08 — рамка «обучение таро» на всех четырёх языках.
  **Очередь «решается кодом» исчерпана 25.08**: следующий шаг за аккаунтами Артёма
  (Apple Developer, Google Play Console, Expo для EAS, RevenueCat, ящик поддержки) →
  EAS dev-сборка → 53б, виджеты, скриншоты сторов, технический прогон release-checklist.
  **Аккаунты 25.08**: Apple — **АКТИВИРОВАН 27.08 20:36** (веб-заявка + PDF документов, регион
  Грузия; сканер приложения отклонял паспорт Беларуси по несовпадению страны с регионом), доступ
  в App Store Connect есть; **27.08 вечером закрыто целиком**: Team ID `52QV87WW73`, **Paid Apps
  Agreement Active** (банк Bank of Georgia USD Active, W-8BEN + Certificate of Foreign Status
  Active — вводил Артём), Bundle ID `app.arcanum.tarot` зарегистрирован, приложение «Arcanum —
  обучение таро» создано (короткое `Arcanum` в App Store занято), DSA «трейдер» подан — In Review.
  **28.08: iPhone зарегистрирован в EAS, первая iOS `preview`-сборка прошла** (`23daec5d`,
  1.0.0 (1), ad hoc; лайв на iPhone — за Артёмом); **63б: Apple-набор 28 кадров 1290×2796
  снят и закоммичен** (`docs/store/apple/`, стражи обязательны). Дальше: заливка витрины
  в App Store Connect (тексты `store-listing.md`, кадры, URL политики/условий/поддержки,
  возрастной рейтинг) → продукты подписки + API key RevenueCat (release-checklist п. 1);
  Google Play — **аккаунт полностью верифицирован 25.08** (личность, Android-устройство, телефон),
  «Создать приложение» открыто; Expo — `art9`. **Задача 61 (EAS Build): ПЕРВАЯ СБОРКА ПРОШЛА 26.08**:
  проект `@art9/arcanum`, `eas.json` (`preview` APK/ad hoc, `production` AAB,
  `appVersionSource: remote`), без dev-client и EAS Update. Android `preview` собран за 41 минуту
  (keystore сгенерирован в облаке Expo), APK 118 МБ, версия 1.0.0 / `versionCode` 1; состав
  проверен распаковкой — 78 JPEG карт на месте. ⚠️ **Команда — `npx eas-cli@latest build …`**:
  `npx eas` падает, пакет называется `eas-cli`. ⚠️ Сборка идёт под все 4 архитектуры, включая
  `x86_64`, — **APK ставится на обычный эмулятор Android**, и часть пункта 6в (иконка приложения,
  иконка пуша, русские числительные на том же Hermes с урезанным ICU) закрывается без телефона;
  хаптика и резкость — только устройство. **26.08 добавлен `.easignore`**: архив сборки
  305 МБ → 31.7 МБ, файл ОТМЕНЯЕТ `.gitignore` для EAS целиком (поэтому секретные шаблоны
  продублированы в нём), страж — `python scripts/check_easignore.py`. ⚠️ 27.08 на первой
  `production`-сборке архив был 180 МБ: `.git` shallow-клона (152 МБ) EAS удаляет только по
  явной строке `.git` без слэша — добавлена, страж проверяет, воспроизведение кодом eas-cli
  28.7 МиБ; **подтверждено логом сборки №3: 28.7 MB**. **27.08 `production` (AAB) собрана**
  без интерактива: id `62152db3`, 13 мин, `versionCode` 2, 86.6 МиБ — файл для внутреннего
  теста — **залита во внутренний тест 27.08** (выпуск 2 (1.0.0), список тестировщиков — аккаунт
  Артёма, ссылка `https://play.google.com/apps/internaltest/4701646545825132337`; AAB кладёт Артём руками —
  артефакт EAS без CORS, загрузка из сессии ≤10 МБ). Контакты в консоли вписаны — задачи
  настройки закрыты все, кроме «Здоровье»: его консоль держит из-за `ACTIVITY_RECOGNITION` в манифесте
  (`expo-sensors`, шагомер; нам нужен только `DeviceMotion`) — **задача 64**: `android.blockedPermissions`
  в `app.json` + страж `androidPermissions.test.ts`, сборка `versionCode` 3, перезаливка во внутренний
  тест (AAB кладёт Артём руками) — **сделано 27.08**: сборка `versionCode` 3 (id `f7ee427c`), манифест
  без строки (проверено zipfile до заливки), выпуск 3 (1.0.0) активен во внутреннем треке, «Контент
  приложения» → «Все декларации заполнены». **Магазинная сборка прогнана на эмуляторе 27.08**
  (через внутренний трек, installer `com.android.vending`, сплиты; онбординг, числительные, карта дня —
  ок). За телефоном — только хаптика и резкость.
  Спека `docs/specs/61-eas-build.md`.
  Свой Android-телефон Артём ещё покупает (проверка устройства прошла на чужом) — для консоли
  и для сборки он не нужен, только для установки APK. RevenueCat не заведён; ящик поддержки — `arcanum.tarot@icloud.com` (27.08, псевдоним
  iCloud Mail; Google/Outlook регистрацию блокировали).
  **Задача 62 ЗАКРЫТА 26.08** (лайв-проверка Артёма на iPhone ✓): пейвол больше не показывает цену,
  которой не назвал магазин. `getOffers()` → `[]` на всех платформах (плейсхолдеры «2 890 ₽»
  удалены), экран различает ТРИ состояния предложений (`Offer[] | null`: не запрошены / нет /
  есть) и при пустом рисует панель «Оформить подписку пока нельзя»; «Восстановить покупки»
  переехало внутрь состояний (в «скоро» — под `PURCHASES_AVAILABLE`, появится само в 53б),
  ссылки условий вынесены наружу (Apple 3.1.2); диалог больше не поминает Expo Go. Новое общее:
  контракт `purchases.test.ts` «цен в коде нет» (проверяет и комментарии, и строки `paywall.*`
  четырёх языков), поле `mockSetup` у пары в `scripts/shoot_56.js` (снимать состояния, которых
  нет в демобаре), блок `#pwSoon` в макете. Сценарий 6б `scripts/check_62_web.js`: красный 31
  из 58 → зелёный 58 из 58, мутация роняет 24. Спека и отчёт — `docs/specs/62-paywall-no-store.md`.
  **Задача 63 (материалы витрины Google Play) ЗАКРЫТА 26.08**:
  конвейер из двух команд без телефона — `python scripts/store_assets.py icon` (иконка
  512×512 RGBA) и `node scripts/shoot_63.js` (28 скриншотов 1080×1920 на 4 языках + 4 баннера
  1024×500, затем `store_assets.py feature`/`verify`) — материалы закоммичены в `docs/store/`.
  Приложение снимается как есть (390×844 @3×) и КОМПОНУЕТСЯ на холст магазина шаблоном
  `docs/store/frame.html` (не режется — у вьюпорта 2.16:1, Google требует ≤2:1). Семь кадров
  (карта дня → курс → страница карты → расклад → тренажёр → лунный календарь → светлая тема),
  карты сида — вне списка наготы под сценарным стражем, часы браузера пришпилены к 18.09.2026.
  Контракт `storeAssets.test.ts` (10 проверок), общий парсер заголовков `src/lib/imageHeaders.ts`
  (на нём же теперь `cardAssets.test.ts`/`notificationIcon.test.ts`). Тестов **1860 в 48 сьютах**,
  `tsc` чист. Артём посмотрел 7 кадров ru + баннер ru 26.08 — «кадры нравятся», замечаний нет.
  **Витрина ЗАЛИТА в Play Console 26.08** — на всех четырёх языках (ru-RU + переводы en-US /
  es-419 / pt-BR): тексты копией из `store-listing.md`, значок, свой баннер и свои 7 скриншотов
  на язык в порядке 01→07; сохранено кнопкой «Сохранить» (решение Артёма), статус страницы
  «Готово к отправке на проверку», на проверку НЕ отправляли. После перезагрузки все четыре
  языка сверены с `store-listing.md` побайтово. Графика заливается автоматизацией: `input[type=file]`
  появляется ТОЛЬКО после клика «Добавить объекты», файлы берутся `fetch`-ем с raw.githubusercontent
  (локальный 127.0.0.1 из https-страницы висит вечно), порядок кадров = порядок ВЫДЕЛЕНИЯ.
  Грабли целиком — `docs/release-checklist.md`, раздел «Заливка витрины: как прошла 26.08».
  Apple-набор (`--store apple`) снимается тем же скриптом, но не коммитится до **63б**
  (аккаунт Apple есть с 27.08; **`supportsTablet: false` — решение Артёма 27.08**, iPad-кадры
  не нужны). Спека и отчёт — `docs/specs/63-store-assets.md`.
  **Задача 59 ЗАКРЫТА 23.08** (лайв-проверка Артёма ✓): в настройках две строки — «Имя»
  и «Дата рождения» (решение Артёма: две, а не одна; product-spec §5 переписан по 6а-0).
  Новое общее: экшен `setName` (пустое имя УДАЛЯЕТ ключ, а не пишет `''`), компонент
  `TextPrompt` (диалог ввода одной строки), `NAME_MAX`/`normalizeName` в `birthArcana.ts` через
  существующий `normalizeText`. Дата меняется только из настроек (тап карточки аркана
  занят переходом). Веб-проверка 6б 20/20 (`scripts/check_59_web.js`), 6а — 8 кадров
  (`scripts/shoot_59.js`); красные прогоны — 3 мутации юнитов и 3 мутации сценария.
  Спека — `docs/specs/59-name-birthdate.md`.
  **Новые задачи из аудитов**: 59 (в настройках нет строки «Имя и дата рождения» — пробел против
  product-spec §5, имя не редактируется нигде), 60 (8 дорисовок макета, промт готов),
  28с (перезаливка es/pt после 28р, Cowork), 57н (вычитка носителем текстов сторов es/pt).
- **Поставка Cowork 23.08 ПРИНЯТА** (три патча, `git am -3` без конфликтов): **28с**
  (126 адресов es/pt дистракторов; `check_canon --scope course` 221 при ориентире 225,
  страж `check_quiz_guess --against` 0), **57н** (вычитка текстов магазина es/pt,
  `storeListing.test.ts` 122/122), **60** (дорисовки макета, `check_60_mock.js` теперь **9/9**).
  ⚠️ Девятая дорисовка сделана при приёмке: задача 59 разбила строку настроек на две,
  а макет нёс одну. **Правило: задача, меняющая экран, правит макет в том же коммите.**
  **Решения 24.08 применены**: название pt — `Arcanum — Aprenda a Ler Tarô`, es оставлено
  `Aprende Tarot`; термины pt `Treinador→Treino`, `cartões→flashcards`, `sequência→ofensiva`
  и es `manual→guía` переименованы во ВСЕХ местах (`i18n.ts`, `phrases.json`,
  `site/privacy.html`, тексты магазина) — `Entrenador` оставлен как есть.
  ⚠️ Без запаса по лимиту: испанские ключевые 100/100 и pt-подзаголовок 30/30.
- **Задача 53б (RevenueCat) — КОД СДЕЛАН И ВЛИТ В MAIN 27.08, ждёт консоли и лайва 6в**: ветка `feat/53b-revenuecat`,
  persist **12** (`plan`/`willRenew` внутри `premium`, `mergePremium`). `src/lib/purchases.ts`
  переписан на `react-native-purchases` (единственный импорт SDK), `purchases.web.ts` — заглушка,
  чистые преобразования в `purchasesMap.ts`, `usePremiumSync` держит право в синхроне с магазином
  (старт/возврат из фона/push SDK). Пейвол называет тариф и различает продление/истечение из магазина,
  бейдж скидки считается из цен, четыре исхода покупки/восстановления — свои диалоги. Тестов
  **1917 в 52 сьютах**, `tsc` чист. Веб-регресс 62 переснят на новом коде — 58 из 58 без правок
  сценария, SDK в веб-бандле — 0 вхождений (веб и Expo Go остаются в состоянии «скоро»). Спека
  и отчёт — `docs/specs/53b-revenuecat.md`. **Консоль 28.08 — Apple-сторона ГОТОВА**: цены
  утверждены ($5.99/мес, $34.99/год, все страны, без триала), в App Store Connect группа
  `Premium` + `premium.year`/`premium.month` (США переставлены вручную — Apple уравнивает цены
  после налога от витрины Грузии), In-App Purchase Key `67VRFKU59T`, Sandbox-тестер (Gmail
  `+sandbox`, данные в `Documents\keys`); RevenueCat: entitlement `premium`, App Store-приложение,
  продукты, offering `default` (`$rc_annual`/`$rc_monthly`), ключ `appl_…` в EAS (production +
  preview) и `.env`; iOS `preview`-сборка с ключом `a320c6dd` — пейвол показывает цены магазина
  **Лайв iOS в Sandbox ПРОЙДЕН 28.08** (покупка, «✦ АКТИВНА», настройки, модуль 3, авиарежим,
  восстановление после переустановки; App Store Server Notifications → RevenueCat подключены;
  ветка «Действует до …» — на следующей покупке). **Google — задача 66, «доводим» (решение
  Артёма 28.08 вечером)**: продавцом Google пускает только Беларусь (Грузия ✘), выплаты — только
  на белорусский USD-счёт; профиль продавца «Беларусь / физлицо» создан, Bank of Georgia
  отвергнут формой, карта Сбер Банка РБ не годится (SDN США + ЕС) — Артём открывает карту в
  несанкционном банке. Сделано: лицензионный тестер, GCP `arcanum-play` + сервисный аккаунт
  `revenuecat@…` (ключ в `Documents\keys`) в Play Console, RevenueCat Play Store-приложение +
  продукты `premium:year`/`premium:month` в entitlement и offering, `goog_…` в EAS и `.env`.
  **Play Console закрыт 28.08 вечером**: AAB №2 `6cf48d53` (versionCode 5, с ключом; №1
  `f12b6183` без ключа — не заливать) — выпуск 5 во внутреннем треке; подписка `premium` с
  планами `year` ($34.99) и `month` ($5.99) — оба АКТИВНЫ, 174 страны; сведения + переводы
  en-US/es-419/pt-BR (тексты — `store-listing.md`, «Подписки»). **Лайв Android на эмуляторе
  ПРОЙДЕН 28.08 вечером** (выпуск 5 разошёлся через ~час; покупка → «✦ АКТИВНА» → модуль 3 →
  отмена → «Действует до …» → истечение → тарифы → переустановка → право вернулось → авиарежим);
  найденный дефект «план подписки не распознаётся на Android» (`productPlanIdentifier`) закрыт
  коммитом `5a16379` — **в AAB versionCode 5 его нет, нужна сборка 6**. Data Safety Play Console
  переписана (История покупок + Идентификаторы, не передаются; удаление — «Да» с URL
  `privacy.html#deletion` с 28.08 поздно вечером, задача 67). Ждёт белорусский счёт для выплат (66). **Apple до сабмита ЗАКРЫТО
  28.08 вечером**: витрина ASC на 4 языках (кадры 1290×2796 в слот 6,9″), название/подзаголовок,
  категории, рейтинг 13+, контакт и заметки ревью, кадр + заметка у обеих подписок, App Privacy
  опубликована, ASC API key `V55RK5Z8R9` (submit-профиль в `eas.json`), заявка Small Business
  подана; **iOS `production`-сборка `9d27418f` (buildNumber 3) и Android `ca680d94`
  (versionCode 6) запущены 28.08 поздно вечером** (iOS — интерактивно Артёмом: сертификат
  `98PR42H89R`, App Store-профиль `Y3B4UW7278`, push key). Промт —
  `docs/prompts/next-session-29-08-submit.md`.
- **Открытые хвосты**: **28е ЗАКРЫТ ЦЕЛИКОМ 23.08** задачей 28р (раздел C разобран, 41 дефект
  исправлен); её хвосты — 28с (es/pt, Cowork) и задача 58 (картинка выдаёт ответ в 25 карточных
  вопросах, ждёт решения Артёма);
  41 отложена в v2 (23.08); вопросы редактору — `docs/editor-questions.md`; фразы `push.freeze_saved`/`freeze.saved`/
  `push.moon_*` ждут вычитки; отложенное подтверждение лунного пуша (47б) **получено 28.08** —
  пуш полнолуния пришёл на iPhone Артёма; вычитка носителем строк и страниц задачи 54 — следующая волна Cowork,
  туда же строки пейвола задачи 62 (`paywall.soonTitle`/`soonSub`/`unavailableText` на es/pt).
- **Решения, чтобы не возвращались**: **выплаты Google — банк РБ отложен (решение Артёма 28.08
  поздно вечером)**: продажи идут без способа выплаты, выручка копится на балансе платёжного профиля
  Google (выплаты ~15-го числа при балансе ≥ 1 $ / 100 $ для USD), счёт в несанкционном банке РБ
  открывать по факту спроса — может и не понадобиться; Apple выплаты уже настроены (Bank of Georgia
  USD Active); **картинка в карточных вопросах викторины оставлена как есть
  (23.08, задача 58)** — у 25 из 28 таких вопросов над текстом висит скан карты с напечатанным
  именем, равным верному ответу; решено не чинить, потому что карточный вопрос идёт после теории
  и работает закреплением, а не проверкой; вернуться только по сигналу телеметрии v1.1;
  **задача 41 (дневник при большом числе раскладов и вытеснение старых записей) — в v2
  (решение Артёма 23.08, подтверждает откладывание 16.08)**; сезонные события → v2; шаринг-карточка снята; XP в v1 не меняем
  (22); волну появления сетки не делаем (18); «изучи карту, чтобы открыть» отклонено; виджеты ждут
  EAS-сборки; контраст чипа мастерства оставлен как есть (49), контраст мелких подписей — задача 50 ✓.
- **ЧТО ДАЛЬШЕ (на 28.08.2026, поздно вечером, после сессии 67)** — промт следующей сессии:
  `docs/prompts/next-session-29-08-ios-submit.md`. Сделано сессией 28.08 поздно вечером: обе
  `production`-сборки `finished` (iOS `9d27418f` buildNumber 3, Android `ca680d94` versionCode 6,
  флаг шифрования `c115b6a` уже в них); **67 ЗАКРЫТА** (инструкция удаления данных RevenueCat в
  политике на 4 языках + `dataText`, якорь `privacy.html#deletion`, контракт по абзацам; тестов
  1931 в 52 сьютах); Play Console: Data Safety «удаление — Да» с URL, **AAB 6 залит
  автоматизацией** — внутренний трек «Активно 6 (1.0.0)», закрытый Alpha + Data Safety
  «на проверке». IPA залит Артёмом (`eas submit` из сессии блокирует классификатор; submission `900f377e`,
  22:38), сборка **3 (1.0.0)** выбрана в версии 1.0 ASC (~22:55), «Готово к отправке». Осталось:
  (1) сабмит iOS 1.0 с группой `Premium` («Добавить для проверки») — только по «ок»; (3) пейвол versionCode 6 на эмуляторе через внутренний трек
  («Месячная/Годовая подписка»); (4) письма Apple: Small Business, DSA (банк РБ — отложен).
  Промт `next-session-29-08-submit.md` — история (выполнен 28.08 поздно вечером, кроме сабмита).
  Промт `next-session-29-08.md` — история
  (выполнен 28.08 вечером):
  `docs/prompts/next-session-29-08.md` (Apple до сабмита: витрина ASC, App Privacy, скриншоты
  подписок для ревью, ASC API key + `eas submit`, релизная сборка, Small Business Program; Google —
  добить, если не закрыто: AAB №2 во внутреннем треке, подписка `premium`, лайв Android, карта
  в несанкционном банке для выплат). Промты `next-session-28-08.md` и `next-session-27-08.md` —
  история; консольная часть 53б по Apple выполнена 28.08 (см. бюллет 53б выше).
  Коротко: (1) **утром 28.08 спросить Артёма про баннер лунного пуша — там обязано быть
  «Полнолуние ✦»**, отложенное подтверждение 47б, другого случая не будет до следующего цикла;
  (2) **сделано 27.08**: ящик `arcanum.tarot@icloud.com` в коде/сайте/консоли (задачи настройки
  11 из 11), AAB `versionCode` 2 опубликован во внутреннем треке; `.easignore` разобран (архив был
  180 МБ из-за `.git` shallow-клона, строка добавлена, лог сборки №3 подтвердил 28.7 MB); **64 закрыта**:
  сборка `versionCode` 3 без `ACTIVITY_RECOGNITION` во внутреннем треке, все декларации консоли заполнены;
  (3) **закрытый тест настроен и отправлен на проверку 27.08**: трек Alpha, 177 стран,
  тестировщики — Google Group `arcanum-testers@googlegroups.com` (вступление `https://groups.google.com/g/arcanum-testers`),
  выпуск 3 (1.0.0), 18 изменений (витрина, декларации, выпуск) **одобрены Google за ~1.5 часа —
  закрытый тест активен с 27.08 15:50**;
  задача Артёма — 12+ человек в группе на 14 дней подряд (аудитория жены, r/AndroidClosedTesting),
  opt-in `https://play.google.com/apps/testing/app.arcanum.tarot`; лайв-проверка на устройстве через внутренний трек
  — **61 ЗАКРЫТА 27.08** (лайв-проверка Артёма ✓; iOS-сборка — хвост 63б); (4) **53б: код сделан
  27.08** (ветка `feat/53b-revenuecat`, детали — бюллет «Задача 53б» выше) → **теперь консоль**:
  аккаунт RevenueCat, продукты и цены, лицензионный тестер, `eas env:set` → `production`-сборка →
  лайв 6в → слияние ветки → затем закрытый тест 12 × 14 дней (список email тестеров
  собирать заранее; после RevenueCat переписать анкету «Безопасность данных» — чек-лист готов
  в release-checklist). Apple: **активирован 27.08 вечером** — открыты 63б и iOS-сборка
  (соглашения и Paid Applications Agreement первыми, порядок — release-checklist п. 1).
  Эмулятор Android на машине Артёма уже настроен — рецепт в AGENTS.md.
- Подробности по каждой задаче — `docs/changelog.md` и `docs/specs/`; уроки — `docs/lessons.md`.

## Общие модули и компоненты (проверь ЗДЕСЬ, прежде чем писать новое)

- **`src/components/`**: каркас и текст — `Txt`, `Block` (+`paragraphStyle`), `Rule`, `Pill`, `ScreenBg`,
  `BlurSurface`, `GlassPanel`, `Skeleton`, `EmptyState`, `SettingsRow`, `Emblem`, `FadeUp`, `PressableScale`,
  `Sparks`, `LinkTxt` (текст-ссылка на web через `expo-web-browser`); кнопки и диалоги — `CtaButton`, `ConfirmDialog`, `ModalPanel`, `OptionPicker`, `TimePicker`(.web),
  `DatePicker`(.web); карты — `CardBack`, `CardBackSurface`, `CardCorners`, `CornerBadge`, `CardCell`,
  `CardGridRow`, `CardLightbox`, `FlipCard`, `StarBack`, `KeywordChips`, `MasteryChip`, `MeaningPanel`;
  списки и поиск — `SearchField`, `FilterChips`, `JournalRow`, `MonthNav`, `MonthCard`, `NotePlate`, `MoonRow`;
  прогресс и итоги — `ProgressBar` (+`PROGRESS_EASE`), `XpPill`, `StreakPill`, `LevelCard`, `StatBox`,
  `BirthArcanaCard`, `ResultPanel`, `LessonResult`, `ReviewResult`, `ReviewPanel`, `ReviewFlashcard`;
  курс — `CoursePath`, `PathNode`, `ModuleHeader`; расклады — `SpreadBoard/Card/Cells/Diagram/Fields/
  Meaning/Row/Screen`; `Reflection`, `TabIcons`.
- **`src/lib/`** (чистые модули под тестами): `content` (`statusIn`, `blockText`), `lang` (`Lang`, `inLang`,
  `localeTag`), `i18n` (`useLang`, `AVAILABLE_LANGS`), `dates` (границы дня, `localMidnight`, `plusDaysISO`,
  `daysInMonth`, `formatTime`), `phrases` (`pickPhrase`/`pickVariant`), `shuffle`, `xp`, `streak`, `journal`,
  `reflection`, `settings`, `backup`/`backupIo`(.web), `pushPlan`/`pushBody`/`pushes`(.web), `moon`/
  `moonCalendar`, `srs`/`review` (+`reviewPrompt` — тексты флеш-карты, экран своей копии формулы держать не вправе)/`mastery`, `courseProgress`/`lesson`/`collection`, `cardSearch`, `spread`/
  `spreadLayout`/`composition`, `lightbox`, `birthArcana`, `feedback`, `appInfo` (версия + `SITE/PRIVACY/TERMS/SUPPORT_URL`), `revealQueue`/
  `cardTransition`, `premium` (гейты `moduleLocked`/`lessonLocked`/`spreadLocked`, лимит тренажёра),
  `purchases`/`purchases.web` (адаптер покупок — единственный файл с импортом `react-native-purchases`,
  веб-заглушка без SDK), `purchasesMap` (чистые преобразования ответов магазина в типы приложения),
  `purchasesEnv` (ключ SDK по платформе, признак Expo Go); хуки — `useAppActive`, `useScrollAwareBar`,
  `useTabScrollToTop`, `useLeaveGuard`, `usePushScheduler`, `useDeviceTilt`, `useBackHaptic`,
  `usePremiumSync` (право Premium в синхроне с магазином: старт/возврат из фона/push SDK).
- **`src/theme/`**: `theme.ts` (токены), `glow.ts` (`glowShadow`, `textGlow`), `navHeader.ts`
  (`transparentHeader`), `webInput.ts` (`noOutline`).

Полный план этапов, фичи удержания, монетизация и метрики — в `docs/master-plan.md`.
